# 浙西南革命精神 - 红色文化交互展示项目文档

基于 `pygame-ce` 与好搭AI派硬件（ESP32 扩展板 + 语音 AI 云服务）开发的红色文化主题交互展示程序。固定窗口尺寸 1920×1080，集多页面导航、人体感应欢迎词、红色地标介绍、大模型语音问答、WS2812 灯效于一体。

## 一、功能特性

| 模块 | 功能 |
| --- | --- |
| 首页 | 背景 `jingshen.png`，中间两个按钮「浙南红地标」「浙红智助手」 |
| 人体感应欢迎词 | P0 口接人体红外传感器，感应到人时自动播放 `huanying.wav`；每次停留在首页只播 1 次，离开再返回时重置 |
| 浙南红地标 | 背景 `hongdibiao.png`，3 个按钮「王村口」「安岱后」「小吉村」 |
| 地标详情 | 点击对应地标后切换为该地标图片，同时播放对应介绍音频 |
| 浙红智助手 | 背景 `wenda.png`，按住圆形按钮录音 → 语音识别 → 大模型对话 → 语音合成 → 播报，对话记录实时显示 |
| 导航 | 每个页面均有「返回」按钮（回到上一级，一级一级返回）和「退出程序」按钮 |
| WS2812 灯效 | P1 口接 4 颗 WS2812，根据屏幕内容与播音状态变换颜色与效果 |
| 串口防冲突 | PIR 读取与 LED 写入通过共享锁串行化，PIR 慢速后台轮询，避免"读取引脚超时" |

## 二、资源文件命名

程序使用拼音命名的资源文件，需放在对应目录：

```markdown
images/
  jingshen.png          首页背景（浙西南革命精神）
  hongdibiao.png        地标列表页背景（浙南红地标）
  wenda.png             AI 问答页背景（红军故事智能问答）
  wangcunkou.jpg        王村口
  andaihou.jpg          安岱后
  xiaojicun.jpg         小吉村

recordings/
  huanying.wav          欢迎词
  wangcunkou.wav        王村口介绍
  andaihou.wav          安岱后介绍
  xiaojicun.wav         小吉村介绍
  ai_voice_chat.wav     AI 对话用户录音（自动生成）
  ai_answer.wav         AI 回答合成音频（自动生成）
```

## 三、项目结构

```markdown
haoda_AIPad/
├── red_revolution_app.py          # 主程序
├── 红色文化交互展示项目文档.md      # 本文档
├── images/                        # 背景与地标图片
└── recordings/                    # 音频文件目录（自动创建）
```

## 四、环境要求

- Python 3.8 及以上
- 依赖：
  - `pygame-ce`（提供 `pygame` 模块）
  - 好搭AI派 SDK：`ESP32`、`voice_api`、`audio_player`、`audio_recorder`
  - ESP32 扩展板（P0 接人体红外传感器，P1 接 WS2812 灯带）
  - 麦克风（AI 语音问答录音）

> 硬件模块缺失时程序会自动降级，界面仍可运行调试。

## 五、安装与运行

```bash
# 安装依赖
pip install pygame-ce

# 运行程序
python3 red_revolution_app.py
```

> 运行前请修改 `red_revolution_app.py` 顶部的 `USERNAME` 和 `PASSWORD` 为你的好搭AI派账号。

## 六、硬件接线

| 端口 | GPIO | 连接设备 | 用途 |
| --- | --- | --- | --- |
| P0 | `GPIO_IO_01` | 人体红外传感器（PIR） | 检测有人 → 播放欢迎词 |
| P1 | `GPIO_IO_02` | WS2812 灯带（4 颗） | 根据页面/状态变换灯效 |
| — | — | 麦克风 | AI 语音问答录音（系统音频） |

## 七、界面与页面流程

### 页面层级

```markdown
首页（jingshen.png）
  ├─ 浙南红地标 ──> 地标列表（hongdibiao.png）
  │                  ├─ 王村口 ──> 王村口详情（wangcunkou.jpg + wangcunkou.wav）
  │                  ├─ 安岱后 ──> 安岱后详情（andaihou.jpg + andaihou.wav）
  │                  └─ 小吉村 ──> 小吉村详情（xiaojicun.jpg + xiaojicun.wav）
  └─ 浙红智助手 ──> AI 问答（wenda.png）
```

### 导航栈（一级一级返回）

每次进入子页面时把当前页面压入 `page_stack`，点「返回」弹出栈顶页面回到上一级：

```markdown
首页 ──浙南红地标──> 地标列表 ──王村口──> 王村口详情
       stack=[HOME]      stack=[HOME,地标列表]
                                        │ 点返回
                                        ▼
                              地标列表  stack=[HOME]
                                        │ 点返回
                                        ▼
                              首页      stack=[]
```

- 返回按钮在首页隐藏
- 导航加 350ms 防抖，避免连点触发多次跳转

## 八、WS2812 灯效设计

灯带共 4 颗 LED，由后台线程驱动（约 30 FPS），根据当前页面与 AI 状态变换效果：

| 场景 | 灯效 | 颜色 |
| --- | --- | --- |
| 首页 | 红色呼吸（革命主题） | 暖红 |
| 地标列表 | 金色流光跑马灯 | 金黄 |
| 地标详情播音 | 蓝色波浪流动 | 蓝 |
| AI 空闲 | 青色轻柔呼吸 | 青 |
| AI 录音中 | 红色快速脉冲 | 红 |
| AI 思考中 | 橙色跑马灯 | 橙 |
| AI 播报中 | 绿色流动波纹 | 绿 |
| 退出 | 关闭所有灯 | 黑 |

## 九、AI 语音对话流程

```markdown
按住圆形按钮 ──> recorder.start_recording()
   │
松开按钮 ──> recorder.stop_recording() + save_audio()
   │
   ├─ 录音为空或 < 0.3s ──> 回到空闲，等待下次
   │
   └─ 录音有效 ──> 启动子线程处理：
           │
           ├─ 1. 语音识别 voice_recognition()
           │     失败/空 ──> 进入错误状态
           │
           ├─ 2. 大模型对话 llm_chat(prompt)
           │     prompt 已注入「浙红智助手」红军故事主题提示词
           │     失败/空 ──> 进入错误状态
           │
           ├─ 3. 语音合成 tts_synthesize()（检查返回值与文件存在性）
           │     失败 ──> 进入错误状态
           │
           └─ 4. 播报 AudioPlayer.play_file()（阻塞式播放）
                 完成 ──> 回到空闲
```

### 关键设计

- **子线程处理**：语音识别/大模型/合成为网络请求，放在 daemon 子线程避免界面卡死
- **会话隔离**：`ai_session` 计数器，进入 AI 页或开始新录音时递增，旧线程通过 session 校验自动作废，避免串扰
- **AudioPlayer 播放**：用 SDK 的 `AudioPlayer`（与 `AudioRecorder` 共存），避免录音后 `pygame.mixer.music` 无法发声
- **错误可重试**：ERROR 状态下按钮显示"点击重试"，可重新开始录音

## 十、串口防冲突设计

### 问题

主循环 60 FPS 每帧调用 `board.digitalRead()`（60次/秒）+ LED 线程 30次/秒 `ws2812Write()`，两个线程并发访问同一扩展板串口，导致"读取引脚1超时"。

### 解决方案

| 措施 | 说明 |
| --- | --- |
| 共享串口锁 `board_lock` | LED 写入与 PIR 读取通过同一把锁串行化，避免并发冲突 |
| PIR 慢速后台轮询 | `PIRSensor` 类独立线程，每 0.3 秒读一次并加锁；主线程只读缓存值，不直接访问串口 |
| 异常容忍 | 串口偶发超时被 `try/except` 捕获，不影响功能 |

### 效果

- PIR 读取频率：60次/秒 → 3.3次/秒
- 串口并发：通过锁串行化，不再冲突
- 响应延迟：最多 0.3 秒，人体感应场景完全够用

## 十一、状态定义（AI 页面）

| 状态 | 含义 | 录音按钮显示 | LED 灯效 |
| --- | --- | --- | --- |
| `STATE_IDLE` | 空闲，可开始录音 | 按住说话 | 青色呼吸 |
| `STATE_RECORDING` | 录音中 | 录音中 / 松开结束 | 红色脉冲 |
| `STATE_RECOGNIZING` | 语音识别中 | 处理中 | 橙色跑马灯 |
| `STATE_THINKING` | 大模型思考中 | 处理中 | 橙色跑马灯 |
| `STATE_SPEAKING` | 播报回答中 | 处理中 | 绿色波纹 |
| `STATE_ERROR` | 出错，可重试 | 点击重试 | 青色呼吸 |

## 十二、可配置项

在 `red_revolution_app.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `IMAGE_DIR` / `AUDIO_DIR` | 图片/音频目录 | `images` / `recordings` |
| `PIR_PIN` | 人体红外传感器引脚 | `GPIO_IO_01`（P0） |
| `LED_PIN` | WS2812 灯带引脚 | `GPIO_IO_02`（P1） |
| `LED_COUNT` | LED 数量 | 4 |
| `USERNAME` / `PASSWORD` | 好搭AI派账号 | 需手动修改 |
| `VOICE_API_URL` | 语音 API 地址 | `http://www.haohaodada.com/project/voiceAI/ApiZNBW.php` |
| `NAV_DEBOUNCE_MS` | 导航防抖间隔 | 350 ms |

## 十三、快捷键

| 按键 | 功能 |
| --- | --- |
| `ESC` | 退出程序 |

> 主要交互为鼠标点击按钮与按住/松开录音按钮，无需键盘操作。
