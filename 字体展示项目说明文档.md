# 字体展示项目文档

基于 `pygame-ce` 开发的字体浏览展示程序，固定窗口尺寸 1920×1080，用于浏览和预览好搭AI派上的所有字体文件。

## 一、功能特性

| 模块 | 功能 |
| --- | --- |
| 字体扫描 | 自动扫描 `/home/cxdz/jupyter/assets/` 目录下的字体文件（支持 ttf / ttc / otf / fon） |
| 字体列表 | 左侧显示所有字体列表，支持滚动浏览，选中项高亮 |
| 滚动条 | 加宽滚动条（28px），支持触摸拖拽和点击轨道跳转 |
| 三行预览 | 中文、英文、数字三行固定预览文本，全面展示字体效果 |
| 中文检测 | 自动检测字体是否支持中文，不支持时显示占位提示 |
| 中文状态 | 右上角显示中文支持状态标识（✓ 支持中文 / ✗ 不含中文） |
| 字体大小 | 可调节预览字体大小（12px–120px），步进 4px |
| 字体信息 | 显示字体名称、类型（TrueType/OpenType等）、当前大小 |
| 背景 | 使用 `images/1.jpg` 作为全屏背景图 |
| 退出 | 右上角「退出程序」按钮 |

## 二、快捷键

| 按键 | 功能 |
| --- | --- |
| `↑` | 上一个字体 |
| `↓` | 下一个字体 |
| `←` | 字体大小 - |
| `→` | 字体大小 + |
| `ESC` | 退出程序 |

## 三、项目结构

```
haoda_AIPad/
├── font_showcase.py           # 主程序
├── 字体展示项目说明文档.md      # 本文档
├── images/
│   └── 1.jpg                  # 背景图（1920×1080）
└── 好搭AI派字体文件名称列表.txt  # 字体文件名称参考列表
```

## 四、环境要求

- Python 3.8 及以上
- 依赖：`pygame-ce`（提供 `pygame` 模块）

## 五、安装与运行

```bash
# 安装依赖
pip install pygame-ce

# 运行字体展示程序
python3 font_showcase.py
```

> 字体文件存放在好搭AI派的 `/home/cxdz/jupyter/assets/` 目录下，程序会自动扫描并显示。

## 六、界面布局

界面分为左右两栏（1920×1080）：

```
┌─────────────────────────────────────────────────────────────────┐
│  好搭AI派 字体展示                     共 45 个字体              │
├─────────────────────────────┬───────────────────────────────────┤
│  字体列表                   │  字体预览                        │
│  ┌───────────────────────┐  │  ✓ 支持中文                      │
│  │ ARIALN.TTF      TTF   │  │  字体名称：simhei.ttf           │
│  │ ARIALNB.TTF     TTF   │  │  字体类型：TrueType             │
│  │ Deng.ttf        TTF   │  │  字体大小：48px                 │
│  │ simhei.ttf      TTF ◄│  │  ┌─────────────────────────┐   │
│  │ simkai.ttf      TTF   │  │  │ 连接已建立，等待数据传输。│   │
│  │ ...               ▼   │  │  │Algorithm is thought...  │   │
│  │                     │  │  │      149,600,000         │   │
│  └───────────────────────┘  │  └─────────────────────────┘   │
│                             │  [A-] 字体大小 [A+]            │
│                             │      ███████████░░░░░░ 48px    │
└─────────────────────────────┴───────────────────────────────────┘
   左侧：字体列表面板                          右侧：预览区域
```

### 左侧：字体列表面板

- **标题**：字体列表（32pt 加粗）
- **列表项**：字体名称（尝试用该字体渲染）+ 字体类型（小字）
- **选中状态**：高亮背景 + 金色边框
- **滚动条**：28px 宽，支持触摸拖拽和点击轨道跳转

### 右侧：预览区域

- **标题**：字体预览（28pt 加粗）
- **中文状态**：右上角显示「✓ 支持中文」或「✗ 不含中文」
- **字体信息**：名称、类型、大小
- **预览文本**：三行固定内容（中文 / 英文 / 数字）
- **大小控制**：A- / A+ 按钮 + 滑动条
- **底部提示**：快捷键说明

## 七、预览文本

三行固定预览文本，全面展示字体效果：

| 行号 | 内容 | 说明 |
| --- | --- | --- |
| 第一行 | `连接已建立，等待数据传输。` | 中文字符测试 |
| 第二行 | `Algorithm is thought, code is poetry.` | 英文字符测试（含标点） |
| 第三行 | `149,600,000` | 数字测试（含千位分隔符） |

**不含中文字形的字体**：第一行显示灰色提示 `[ 该字体不含中文字形 ]`

## 八、技术说明

### 1. 字体扫描优先级

程序优先扫描好搭AI派主目录 `/home/cxdz/jupyter/assets/`，只有当主目录不存在或无字体时才回退到系统字体目录（`/usr/share/fonts/truetype` 等）。这样用户在 assets 目录中的任何增删改都会直接反映到列表中。

### 2. 中文支持检测

`font_supports_chinese(font)` 函数通过以下方式检测字体是否支持中文：

1. 渲染汉字「中」，检查渲染宽度是否 > 3px（宽度为0或极小说明不支持）
2. 检测结果缓存到 `chinese_support_cache`，避免重复计算

### 3. 滚动条触摸适配

滚动条宽度从 4px 加大到 28px，滑块最小高度 48px，方便手指触摸操作：

- **拖拽滑块**：按住滑块上下拖动
- **点击轨道**：点击滑块上方或下方区域，列表跳转到对应位置
- **滚轮支持**：鼠标滚轮上下滚动列表

### 4. 预览文本渲染

预览文本带黑色阴影描边（8方向偏移1px），确保在任何背景下都清晰可读。

## 九、可配置项

在 `font_showcase.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `MAIN_FONT_DIR` | 好搭AI派字体主目录 | `/home/cxdz/jupyter/assets` |
| `FALLBACK_FONT_DIRS` | 备用系统字体目录 | `/usr/share/fonts/truetype` 等 |
| `BG_IMAGE` | 背景图路径 | `images/1.jpg` |
| `SUPPORTED_FORMATS` | 支持的字体格式 | ttf, ttc, otf, fon |
| `PREVIEW_LINE_CN` | 中文预览文本 | `连接已建立，等待数据传输。` |
| `PREVIEW_LINE_EN` | 英文预览文本 | `Algorithm is thought, code is poetry.` |
| `PREVIEW_LINE_NUM` | 数字预览文本 | `149,600,000` |

## 十、字体文件管理

用户可以通过以下方式管理字体文件：

1. **添加字体**：将 `.ttf` / `.ttc` / `.otf` / `.fon` 文件放入 `/home/cxdz/jupyter/assets/` 目录
2. **删除字体**：从 `/home/cxdz/jupyter/assets/` 目录删除不需要的字体文件
3. **更新字体**：替换同名字体文件

修改字体文件后，重新运行程序即可看到更新后的字体列表。
