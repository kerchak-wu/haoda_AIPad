# 人数实时统计项目说明文档

基于好搭AI派（ESP32 + pygame）开发的人数实时统计程序：通过视觉系统目标检测算法（object_detection）实时统计摄像头画面内的现有人数，无论人员进出始终反映画面中当前真实人数，并记录历史峰值和人数变化日志。

> 本项目开发过程中解决了关键问题：人流计数 API（people_counter）只能提供累计进入/离开值，无法直接得到现有人数；最终改用目标检测算法直接统计画面中类别为"person/人"的目标数量，实现准确的实时人数统计。完整调试过程见第八章。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 目标检测 | `camera_vision_system_v3` 的 `open_camera` + `start_background_detection` + `result_accessor`（严格参照范例 5.17） |
| 摄像头采集 | 视觉系统统一管理（`capture_frame`），不使用 cv2 `VideoCapture`，避免设备冲突 |
| 界面显示 | `pygame`，1920×1080，背景图 `images/1.jpg` + 半透明面板 |
| 多线程 | 后台采集线程（固定 0.15s 间隔）+ 主循环线程（人数检查 + 界面绘制） |
| 调试日志 | `logs/` 目录下日志文件，程序名+日期命名，追加模式，块缓冲 |

> 关键设计：**使用目标检测算法直接数画面里的人数**，而非用"进入-离开"累计计算。目标检测属于"连续检测"，必须使用全托管模式（V3 管理摄像头），禁用 cv2 `VideoCapture`。

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 实时人数统计 | 目标检测后台检测，每 15 帧轮询一次结果（约 2 次/秒），统计画面中"person/人"的数量 |
| 历史峰值记录 | 运行期间自动记录最大人数，支持一键重置 |
| 人数变化记录 | 最近 10 条人数变化事件（增加/减少，带时间戳） |
| 运行时长显示 | 标题栏实时显示运行时长（HH:MM:SS） |
| 摄像头状态 | 显示摄像头连接状态（已连接/未连接） |
| 退出方式 | 界面右上角「退出程序」按钮 / ESC 键 / 窗口关闭按钮 |
| 资源清理 | 退出时关闭摄像头、清理视觉系统资源 |
| 调试日志 | 所有 print 输出同步写入 `logs/人数实时统计_YYYYMMDD.log`，方便导出排查 |

## 三、核心算法选择

### 为什么用目标检测而非人流计数

本项目开发初期尝试使用**人流计数**（people_counter）算法，但发现其无法准确统计现有人数：

| 算法 | 提供数据 | 能否直接得到现有人数 | 局限性 |
| --- | --- | --- | --- |
| 人流计数（people_counter） | `get_people_counter_in()` 累计进入、`get_people_counter_out()` 累计离开 | 否，只能用"进入-离开"近似净流量 | 程序启动时区域非空会变成负数；不反映真实在场人数 |
| 目标检测（object_detection） | `get_object_detection_count()` 目标数量、`get_object_detection_class_name(i)` 类别名 | 是，直接统计画面中"人"的数量 | 不知道"谁是谁"（无身份识别），只统计总数 |

### 学习手册原文（人流计数限制）

> 人流计数一共可以提供两个参数：进入人数 `get_people_counter_in`、离开人数 `get_people_counter_out`。
> 通过处理这两个参数，可以获得：总通过人数（进入+离开）、净流量（进入-离开）。

人流计数**没有**直接获取"现有人数"的接口，"进入-离开"只是净流量，无法满足"实时统计画面内人数"的需求。

### 最终方案

采用**目标检测**算法，遍历所有检测到的目标，筛选类别为 `person` 或 `人` 的目标，数量即现有人数。该方案直接反映画面中真实人数，无论人员进出均准确。

## 四、界面布局

界面按左摄像头右统计面板布局（1920×1080）：

```markdown
y=  0   ┌──────────── 顶部标题区 (高130) ────────────┐
y= 18   │           人数实时统计（标题 64pt）  [退出程序]│
y= 90   │  目标检测实时统计 · 画面内现有人数 · 运行时长 00:00:05 │
y=130   └────────────────────────────────────────────┘
y=150   ┌──────────────────┐  ┌─────────────────────────┐
        │ 摄像头画面  [重置峰值]│
        │ ● 已连接          │  │  实时统计                │
        │                   │  │  当前现有人数            │
        │   实时摄像头画面   │  │       5                 │
        │   (880 × 660)     │  │       人                │
        │                   │  │  历史峰值：8 人          │
        │           640 × 480│  │  ───────────────────    │
        │ 提示：目标检测...  │  │  正在统计中...           │
y=970   └──────────────────┘  │  ───────────────────    │
y=970   ┌──────────── 底部栏 (高110) ────────────────┐  │  人数变化记录           │
y=1025  │ ESC 退出 · 画面内自动统计人数 · 点击"重置峰值"│  │  14:23:05 ↑ +1 → 5 人   │
y=1080  └────────────────────────────────────────────┘  │  14:22:58 ↓ -2 → 4 人   │
                                                       │  14:22:40 ↑ +3 → 6 人   │
                                                       └─────────────────────────┘
```

> 退出按钮固定置于标题栏右上角，不设在右下角。

### 界面元素说明

| 区域 | 元素 | 说明 |
| --- | --- | --- |
| 标题栏 | 标题 + 运行时长 + 退出按钮 | 运行时长实时更新 |
| 摄像头面板 | 摄像头画面 + 连接状态 + 分辨率 + 提示 | 880×660 显示区 |
| 统计面板 - 当前人数 | 超大橙色数字（120pt）+ "人"单位 | 突出显示，一目了然 |
| 统计面板 - 历史峰值 | 小字灰色 | 运行期间最大人数 |
| 统计面板 - 状态消息 | 状态文字 | 正在统计中/峰值已重置等 |
| 统计面板 - 变化记录 | 最近 10 条变化 | ↑绿色表示增加，↓红色表示减少，↻橙色表示重置 |
| 底部栏 | 操作提示 | ESC 退出、R 键重置峰值 |

## 五、项目结构

```markdown
haoda_AIPad/
├── 人数实时统计.py                  # 本程序主入口
├── 人数实时统计项目说明文档.md      # 本文档
├── logs/                            # 日志文件夹（自动创建）
│   └── 人数实时统计_YYYYMMDD.log     # 调试日志（按程序名+日期命名，追加模式）
├── images/
│   └── 1.jpg                        # 背景图（1920×1080，可选）
├── 好搭AI派学习手册.md               # 硬件 API 参考
├── 好搭AI派范例代码.md               # 范例代码参考
├── 视觉系统摄像头调用参考方案.md     # 视觉系统三种调用模式参考
└── 人脸识别灯效.py                  # UI 布局和采集线程参考项目
```

## 六、硬件接线

| 接口 | 设备 | 说明 |
| --- | --- | --- |
| USB | 外接摄像头 | 由视觉系统管理（不用 cv2 VideoCapture） |
| ESP32 | 好搭AI派扩展板 | `board.start()` 初始化（本程序不控制外设，但按规范初始化） |

> 注意：好搭AI派右下角开关需拨到左侧以启用外设接口。本程序不依赖外设，扩展板初始化失败时程序仍可继续运行。

## 七、环境要求

- 好搭AI派平台（ESP32 + Linux）
- 依赖（均平台自带，无需额外安装）：
  - `pygame` —— 界面显示
  - `opencv-python`（提供 `cv2`）—— 仅用于图像格式转换（BGR→RGB→Surface）
  - `numpy` —— 数组处理
  - `ESP32` —— 硬件控制（按规范初始化）
  - `camera_vision_system_v3` —— 目标检测算法（好搭AI派自带）
- 字体文件（位于 `/home/cxdz/jupyter/assets/`）：
  - `PingFang_Regular.ttf` —— 常规文字
  - `PingFang_Bold.ttf` —— 粗体（标题、按钮、大数字）
- 外接 USB 摄像头

## 八、运行

```bash
python3 人数实时统计.py
```

启动后：

1. 程序自动初始化 ESP32 扩展板（可选）和视觉系统
2. 视觉系统启用目标检测算法，打开摄像头并启动后台检测
3. 对准区域，程序自动统计画面中的人数
4. 界面实时显示当前人数、历史峰值、人数变化记录
5. 点击「重置峰值」按钮或按 R 键可重置历史峰值和变化记录
6. 点击界面右上角「退出程序」按钮或按 ESC 退出

> 无需前置条件，不依赖外部数据文件。摄像头对准目标区域即可开始统计。

## 九、技术说明

### 1. 视觉系统初始化（严格参照范例 5.17 目标检测）

```python
self.vision_system = create_vision_system_v3(
    camera_id=-1, width=1280, height=720,
    enable_basic=False, enable_advanced=False
)
self.vision_system.detection_config.enable_object_detection = True
self.vision_system._init_detectors()
self.vision_system.open_camera()
self.vision_system.threaded_system.start_background_detection(show_preview=False)
```

**关键点**：
- `show_preview=False`：不弹出 OpenCV 窗口，画面由 pygame 界面显示
- 摄像头完全由视觉系统管理，不使用 cv2 `VideoCapture`
- 初始化顺序：视觉系统初始化 → 打开摄像头 → 启动后台检测

### 2. 目标检测结果轮询

在主循环中每 15 帧调用一次 `check_counter()`：

```python
self.vision_system.result_accessor.refresh_results()
det_count = self.vision_system.result_accessor.get_object_detection_count()
person_count = 0
for i in range(det_count):
    cls = self.vision_system.result_accessor.get_object_detection_class_name(i)
    cls_str = str(cls).lower()
    if 'person' in cls_str or cls == '人':
        person_count += 1
self.disp_current = person_count
```

**`refresh_results()` 只读取后台检测线程的缓存结果**，不直接访问 V4L2 设备，因此不会与采集线程冲突。

### 3. 类别名兼容处理

目标检测模型可能用英文 `person` 或中文 `人` 作为人的类别名，程序同时兼容两者：

```python
cls_str = str(cls).lower()
if 'person' in cls_str or cls == '人':
    person_count += 1
```

首次检测到目标时，程序会打印实际类别名到日志，便于确认模型使用的标签：

```python
if not self._det_class_logged:
    print('[调试] 目标检测类别示例: %s' % cls)
    self._det_class_logged = True
```

### 4. 多线程架构

| 线程 | 职责 | 阻塞点 |
| --- | --- | --- |
| 主循环 | 事件处理 + 人数检查 + 界面绘制 | 无阻塞，30 FPS |
| 采集线程 | `vision_system.capture_frame()` 固定 0.15s 间隔 | V4L2 阻塞读帧 |

**关键设计**：
- 采集线程固定 0.15s 间隔（约 6-7 FPS），给 V4L2 后台检测线程留出缓冲区访问时间
- 采集线程只负责界面显示用的帧，目标检测由后台检测线程独立完成
- `threading.Lock` 保护帧数据的读写
- 帧有效性验证：`hasattr(frame, 'shape') and len(frame.shape) == 3`

### 5. 人数变化检测

通过对比上次检测到的人数，记录变化事件：

```python
if self.disp_current != self.last_count:
    delta = self.disp_current - self.last_count
    if self.last_count >= 0 and not self.status_msg.startswith('正在启动'):
        now_str = _datetime.datetime.now().strftime('%H:%M:%S')
        self.change_log.insert(0, (now_str, delta, self.disp_current))
```

- `delta > 0`：绿色 ↑ 标记，表示人数增加
- `delta < 0`：红色 ↓ 标记，表示人数减少
- `delta == 0`：橙色 ↻ 标记，表示重置事件
- 仅保留最近 10 条记录

### 6. 重置峰值机制

点击「重置峰值」按钮或按 R 键触发：

```python
def reset_peak(self):
    self.peak_current = self.disp_current  # 以当前人数为新峰值起点
    self.change_log = []
    now_str = _datetime.datetime.now().strftime('%H:%M:%S')
    self.change_log.insert(0, (now_str, 0, self.disp_current))
```

重置操作只影响应用层显示，不调用视觉系统内部接口，避免破坏检测模型。

### 7. 调试日志

程序启动时在 `logs/` 目录下创建日志文件（文件名含程序名+日期，如 `人数实时统计_20260806.log`），通过 `_TeeStdout` 类将所有 `print` 输出同时写入控制台和文件：

```python
_LOG_DIR = 'logs'
_LOG_FILE = _os.path.join(_LOG_DIR, '人数实时统计_%s.log' % _datetime.datetime.now().strftime('%Y%m%d'))
_debug_log_fp = open(_LOG_FILE, 'a', encoding='utf-8', buffering=-1)
```

日志输出特点（参考人脸识别灯效项目）：
1. 日志统一存到 `logs/` 文件夹，避免散落在项目根目录
2. 文件名含程序名+日期，不会覆盖上次的日志
3. 用追加模式 `'a'`，同一程序多次运行追加到当天日志
4. 用块缓冲 `buffering=-1`，避免后台检测线程高频写日志阻塞主循环
5. 每次运行写入分隔标记 `======== 时间 运行开始 ========`，区分不同次运行

调试日志包含：
- 视觉系统初始化信息
- 首次检测到目标的类别名（确认 person/人 标签）
- 人数变化记录（增加/减少的数值和当前人数）
- 异常信息（采集失败、检查异常等）

### 8. 帧缩放方式

使用 `pygame.transform.scale`（非 `smoothscale`）进行帧缩放，降低 CPU 开销（项目经验：smoothscale 对摄像头帧缩放有显著 CPU 开销）。

### 9. 界面半透明面板

```python
panel = pygame.Surface(self.cam_rect.size, pygame.SRCALPHA)
pygame.draw.rect(panel, (*PANEL_COLOR, 170), panel.get_rect(), border_radius=18)
```

- 使用 `pygame.SRCALPHA` 创建带 alpha 通道的 Surface
- 面板颜色 alpha 设为 170（0-255），背景图片可透过面板显示

## 十、调试经验总结

### 1. 人流计数无法得到现有人数

**问题**：用户需求是"实时统计摄像头区域内的人数"，包括现有人数。最初使用人流计数（people_counter）算法，试图用"进入-离开"计算现有人数。

**分析**：
- 人流计数 API 只提供 `get_people_counter_in()` 和 `get_people_counter_out()` 两个累计值
- 没有直接获取现有人数的接口
- "进入-离开"只是净流量，程序启动时若区域已有人，离开时会被计入"离开"但之前未计入"进入"，导致现有人数变负数

**解决**：改用目标检测算法（object_detection），直接统计画面中类别为 `person/人` 的目标数量，结果准确。

**教训**：选择算法前先确认 API 实际能提供的数据，不要用近似计算代替直接测量。

### 2. 双算法方案优化为单算法

**中间过程**：曾尝试同时启用 people_counter + object_detection 双算法，用人流计数提供进出统计，用目标检测提供现有人数。

**优化**：用户明确需求是"摄像头范围内有多少个人"，不需要进出累计统计。最终简化为仅使用目标检测，删除人流计数相关代码，界面也更简洁。

**教训**：功能开发前明确用户需求，避免过度设计。

### 3. 类别名兼容处理

**问题**：不确定目标检测模型使用 `person` 还是 `人` 作为人的类别名。

**解决**：
- 程序同时兼容两种类别名：`'person' in cls_str or cls == '人'`
- 首次检测到目标时打印实际类别名到日志，便于确认

**教训**：对不确定的 API 返回值，先打印日志确认，再做兼容处理。

### 4. 调试提示词经验

| 轮次 | 提示词要点 | 效果 |
| --- | --- | --- |
| 初始开发 | 「统计摄像头区域内的人数，包括现有人数和进出人数，实时统计人数并显示」 | AI 最初用人流计数 + 净流量近似现有人数 |
| 质疑准确性 | 「这个是检测现有人数加减进出人数从而统计总人数吗？」 | AI 解释了两个算法独立的逻辑，确认净流量不准 |
| 明确需求 | 「我就想知道摄像头范围内有多少个人，就算有人进出，也能统计出实时人数」 | AI 改用目标检测直接数人数，删除进出统计 |

**通用经验**：
1. 开发前明确需求边界，区分"实时人数"和"进出累计"是不同需求
2. 对 AI 给出的方案主动质疑，确认逻辑是否符合预期
3. 涉及算法选择时，先查 API 文档确认实际能力，不要假设

## 十一、可配置项

在 `人数实时统计.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `CAMERA_W` / `CAMERA_H` | 摄像头采集分辨率 | 640 × 480 |
| `CAM_DISP_W` / `CAM_DISP_H` | 摄像头画面显示尺寸 | 880 × 660 |
| `_LOG_DIR` | 日志文件夹 | `logs` |
| `_LOG_FILE` | 日志文件名（含程序名+日期） | `logs/人数实时统计_YYYYMMDD.log` |
| `FONT_PATH` | 常规字体路径 | `/home/cxdz/jupyter/assets/PingFang_Regular.ttf` |
| `FONT_BOLD_PATH` | 粗体字体路径 | `/home/cxdz/jupyter/assets/PingFang_Bold.ttf` |
| `BG_TOP` / `BG_BOTTOM` | 背景渐变色 | 天空蓝 → 浅蓝白 |
| `CURRENT_COLOR` | 当前人数颜色 | 橙色 (255, 140, 0) |
| `SUCCESS_COLOR` | 人数增加颜色 | 绿色 (60, 180, 80) |
| `ERROR_COLOR` | 人数减少颜色 | 红色 (220, 80, 80) |

## 十二、常见问题与故障排查

### 1. 一直显示「正在启动人数统计」

| 可能原因 | 排查与解决 |
| --- | --- |
| 摄像头未打开 | 查看 `logs/人数实时统计_YYYYMMDD.log` 中 `视觉系统摄像头已打开` 是否输出 |
| 后台检测未启动 | 查看 `logs/人数实时统计_YYYYMMDD.log` 中 `目标检测后台检测已启动` 是否输出 |
| 目标检测未启用 | 确认 `enable_object_detection = True` 已设置 |
| 摄像头被占用 | 确保没有其他程序正在使用摄像头 |

### 2. 人数统计不准确

| 可能原因 | 排查与解决 |
| --- | --- |
| 类别名不匹配 | 查看 `logs/人数实时统计_YYYYMMDD.log` 中 `目标检测类别示例` 输出，确认 person/人 标签 |
| 检测频率过低 | 检查主循环 `check_counter` 每 15 帧调用一次，可调整为更小值 |
| 画面遮挡 | 确保人员完整出现在画面中，避免严重遮挡 |
| 光线不足 | 增加环境光线，避免逆光 |

### 3. 摄像头掉线

| 可能原因 | 排查与解决 |
| --- | --- |
| V4L2 缓冲区冲突 | 采集线程间隔已设为 0.15s，若仍掉线可增大间隔 |
| 摄像头被占用 | 确保没有其他程序正在使用摄像头 |
| USB 连接松动 | 检查 USB 摄像头连接 |

### 4. 画面卡顿

| 可能原因 | 排查与解决 |
| --- | --- |
| CPU 负载过高 | 确认使用 `pygame.transform.scale` 而非 `smoothscale` |
| 后台检测占用 | 目标检测算法本身有 CPU 开销，画面降帧属正常现象 |
| 日志写入频繁 | 确认日志使用块缓冲 `buffering=-1` |

## 十三、版本历史

| 版本 | 日期 | 主要变更 |
| --- | --- | --- |
| 1.0 | 2026-08-06 | 初始版本：人流计数 + 净流量近似现有人数，双算法方案 |
| 2.0 | 2026-08-06 | 重写为单一目标检测方案：直接统计画面中"person/人"数量，删除进出累计逻辑，简化界面 |

## 十四、扩展方向

- 增加人数阈值报警（超过设定值触发声音或灯效提示）
- 增加人数变化趋势图表（折线图显示历史人数）
- 支持 MQTT 上报人数数据，接入物联网平台
- 增加人数统计报表导出（CSV/JSON 格式）
- 结合人脸识别，统计不同人员出现频次
- 增加区域划分功能，分别统计画面不同区域的人数
- 支持历史数据持久化，断电后可查询历史统计
