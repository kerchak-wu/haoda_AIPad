# 好搭AI派项目开发规范

> 文档版本：v1.1  
> 生成日期：2026-08-14  
> 适用范围：好搭AI派设备（Rockchip RK3566 / Ubuntu 20.04.6 LTS / Python 3.8.10）上所有新项目开发  
> 配套文档（均在当前文件夹内）：project_memory.md（所有硬约束与经验事实的单一信源）、系统环境与非视觉官方库探测报告_v1.md、视觉系统摄像头调用参考方案.md、camera_vision_system_v3_API分析报告.md、好搭AI派范例代码.md + 好搭AI派范例代码补充说明.md

---

## 第一章 参考文件分级

开发任何新项目前，先按本章分级阅读参考文件，**🔴必选文件的约束优先级最高，任何代码不得与之冲突**。所有文件均与本规范位于同一文件夹，直接打开文件名即可。

---

### 1.1 🔴 必选文件（5 份，任何项目都要先通读对应章节）

| # | 文件 | 作用 | 必看章节 / 内容 |
|---|---|---|---|
| 1 | project_memory.md | **项目级铁律总表**，所有硬约束、工程约定、经验事实的单一信源 | Hard Constraints 全部（Python 3.8.10 锁版、cv2 5.0.0 锁版、pygame-ce 2.5.2 锁版、**人脸/物体数据与 V3 数据库同目录、2 层存储模型、单删/全删流程**）；Engineering Conventions 全部（**V3 7 步初始化、日志必须 logs/、摄像头探测顺序 40→41→42、ESP32 串口 /dev/ttyS9** 等）；Lessons Learned 全部（删 face_database/object_database 无破坏性、`object_data/` 是历史遗留、Tier-2 JSON 丢失补录策略 等） |
| 2 | 系统环境与非视觉官方库探测报告_v1.md | 系统环境全景 + 7 个非视觉官方库完整反射枚举 API + 三项关键版本决策 + 15 条风险表 | 第 2 章 Python/库版本约束；**2.5 节三项关键版本决策（Python 不升 / pygame-ce 不换 / cv2 不装 contrib）**；第 3-8 章 ESP32 / Line_Sensor / voice_api / audio_recorder / audio_player / text_recognition 的完整方法签名；第 9 章 15 条风险表（其中 13/14/15 为三条版本红线高优警告） |
| 3 | 视觉系统摄像头调用参考方案.md | 摄像头三种调用模式总览 + Rockchip 平台兼容性补丁 + V3 全托管实测约束 + 剩余盲点记录 | 第 2 章三大模式总览（决定项目采用哪种摄像头模式）；第 5 章黄金法则（按需求选择模式的决策树）；第 7 章兼容性补丁（LIBGL_ALWAYS_SOFTWARE、pygame 分段 init、cv2 必须在 pygame 之后 import）；第 16 章 V3 全托管模式的 5 条实测约束；第 17 章剩余未探测盲点（项目设计时尽量避开） |
| 4 | camera_vision_system_v3_API分析报告.md | V3 视觉系统完整 API + 14 类算法返回结构 + 已知易错点 | 第 2 章实例成员（摄像头生命周期、算法开关、人脸管理、自定义物体管理）；第 3 章 DetectionConfig + CameraConfig 完整默认值（含 `face_db_path='face_database'`、`object_db_path='object_database'`、`backup_camera_ids=[40,41,42,43]`）；第 5 章 14 类算法结果访问器的**字段级返回结构**；第 8 章已知易错点 + 8.8 节系统实测补漏（engagement/emotion 优先级、callback 参数、已知 V3 bug） |
| 5 | 好搭AI派范例代码.md + 好搭AI派范例代码补充说明.md | 65 个官方范例代码集 + 补充说明（原文件已删除的关键差异提醒原文、15 个问题复核、6 项最新探测发现、各范例适用性评估表） | 范例代码按需查阅对应章节；补充说明 1.4 节（原文件已删的 8 月 14 日补充更新 + 关键差异提醒原文，含 USB 摄像头 40→41→42、LIBGL_ALWAYS_SOFTWARE、GPIO_BUTTON=0 忽略、ESP32 隐藏能力、dt-apriltags 已装）；补充说明第四章（49 个范例按 P0/P1/P2/P3 分优先级的适用性评估表，推荐直接复用的标★） |

---

### 1.2 🟡 可选文件（按项目类型选择，同类项目代码优先于范例代码）

所有项目代码、说明文档、资源目录均与本规范位于同一文件夹。

#### A. 视觉类项目参考代码

| 功能需求 | 优先参考的项目代码（已验证可运行 + 符合 project_memory 全量约定） | 配套项目说明文档 | 对应范例章节 |
|---|---|---|---|
| 人脸学习（人脸录入 + 姓名标签管理） | 人脸学习.py — FaceLearner 类封装完整 2 层存储 | 人脸学习项目说明文档.md | 范例 5.9 |
| 人脸识别 + 硬件联动（灯带灯效） | 人脸识别灯效.py — V3 全托管模式 + face_id → `face_database/face_records.json` 映射姓名 | 人脸识别灯效项目说明文档.md | 范例 5.10 |
| 人脸表情识别（本地 V3，走 NPU 加速） | 人脸表情识别器（自带算法）.py — 注意 expression 8 分类优先于 engagement | 人脸表情识别器项目说明文档.md | — |
| 人脸表情识别（云端百度云 API） | 人脸表情识别（云算法）.py — 160×120 帧 / JPEG 质量 60 / 5 秒节流约定 | 人脸表情识别（云算法）项目说明文档.md | — |
| 手势识别 + RGB 灯带 | 手势控制RGB灯带.py — MediaPipe Hands + ESP32 ws2812Write | 手势控制RGB灯带项目说明文档.md | 范例 2.4 RGB 灯 |
| 人体姿态（MediaPipe Pose） | 人体姿态识别器（MediaPipe）.py — 纯 MediaPipe，不依赖 V3 | 人体姿态识别器（MediaPipe）项目说明文档.md | — |
| 姿态检测（V3 自带，YOLOv8-Pose） | 姿态检测（自带算法）.py — 17 个关键点返回结构 | 姿态检测（自带算法）项目说明文档.md | — |
| 物体学习（类别 + 多样本增量学习） | 物体学习.py — ObjectLearner 类封装完整 2 层存储，`object_database/object_records.json` + V3 特征库 | 物体学习项目说明文档.md | 范例 5.11 |
| 物体识别 + 语音播报 | 物体识别播报.py — VoiceAPI TTS + 英拼→中映射表 OBJECT_NAME_CN_MAP + recordings/ 音频缓存 | 物体识别播报项目说明文档.md | 范例 5.12、4.1、3.2 |
| 人流计数 + 追踪线方向计数 | 人数实时统计.py — V3 people_counter + 虚拟追踪线 | 人数实时统计项目说明文档.md | — |
| 文字识别（本地 PPOCR）+ 播报 | 文字识别播报器.py — TextRecognizer（仅 `recognize_text(image, confidence_threshold=0.5)`） + TTS | 文字识别播报器项目说明文档.md | — |
| 文字识别视频播放器（字幕叠加） | 文字识别视频播放器.py | 文字识别视频播放器项目说明文档.md | — |

#### B. 硬件控制类项目参考代码

| 功能需求 | 项目代码 | 配套项目说明文档 |
|---|---|---|
| 风扇 / 电机控制（继电器数字 IO） | fan_control.py | 风扇控制项目说明文档.md |
| WS2812 RGB 灯带 + 10 种预设灯效 | 手势控制RGB灯带.py — wheel() + 10 种灯效函数封装 | 手势控制RGB灯带项目说明文档.md |
| ESP32 传感器全库（DHT11 / DS18B20 / 超声波 / BMP280 / 数字 IO / ADC / I²C / UART） | 系统环境与非视觉官方库探测报告_v1.md 第 3 章（ESP32 50+ 方法完整签名 + 8 个异步 Callback API 优先级） | — |

#### C. 语音 AI 类项目参考代码

| 功能需求 | 项目代码 | 配套项目说明文档 |
|---|---|---|
| 语音 LLM 对话（ASR + LLM + TTS 完整链路） | voice_llm_chat.py — AudioRecorder（16k float32，与 VoiceAPI 规格精准匹配） + VoiceAPI.voice_recognition() + llm_chat_znbw_2025() + tts_synthesize() + AudioPlayer.play() | 语音大模型对话项目说明文档.md |
| TTS 朗读（唐诗宋词库） + 播放控制 | 唐诗宋词朗读器.py — VoiceAPI TTS + AudioPlayer（⚠️ 官方 AudioPlayer 无 pause/resume/stop/音量控制，如需控制改用 pygame.mixer） | 唐诗宋词朗读器项目说明文档.md |
| 本地音乐播放器 + 完整播放控制 | music_player.py — pygame.mixer（具备完整 play/pause/stop/进度控制） | 音乐播放器项目说明文档.md |

#### D. 物联网 / 综合类项目参考代码

| 功能需求 | 项目代码 | 配套项目说明文档 |
|---|---|---|
| HTTP REST API 调用（城市天气）+ 展示 | weather_app.py | 城市天气展示项目说明文档.md |
| MQTT 订阅 + 展示（城市天气） | weather_mqtt.py | MQTT城市天气订阅展示项目说明文档.md |
| 综合 UI 框架（智慧阅读角：摄像头 + ESP32 传感器 + 语音 + 界面） | 智慧阅读角.py — 综合类项目骨架 | 智慧阅读角项目说明文档.md |
| 大型交互展示（红色文化：语音 + 视频 + 翻页 + TTS） | red_revolution_app.py — 大型交互 UI 骨架 | 红色文化交互展示项目说明文档.md |
| 字体预览工具（24 种中文字体对比） | font_showcase.py — 选定字体时先跑此工具做预览 | 字体展示项目说明文档.md |

#### E. 故障恢复 / 运维工具

| 工具 | 文件 | 用途 |
|---|---|---|
| 清空人脸数据库（彻底重置，破坏 tier-1 + tier-2） | 清空人脸数据库.py — 严格按 6 步顺序：清库 API → 删 JSON → rmtree(`face_database/`) → **必须重启 Python 进程**后再学习 |
| 清空物体数据库（彻底重置，破坏 tier-1 + tier-2） | 清空物体数据库.py — 严格按 7 步顺序：逐个删类 → 删 JSON → rmtree(`object_database/`) → **必须重启 Python 进程**后再学习 |
| 环境探测脚本集（7 份脚本 + 3 份干净日志） | 探测工具/ 目录 — 验证环境/库/算法状态时复跑对比基准日志 |

#### F. 资源目录（直接引用）

| 目录 | 内容 | 典型引用方式 |
|---|---|---|
| 字体文件/ | 24 种中文字体（PingFang_Regular / msyh / simhei / PingFang_Bold 等） | `pygame.font.Font('/home/cxdz/jupyter/assets/PingFang_Regular.ttf', size)` |
| images/ | 1.jpg（1920×1080 背景图） | `pygame.image.load('images/1.jpg')`，失败回退渐变 |
| icons/ | 96 个 PNG 图标 | UI 按钮 / 状态小图标 |
| `face_database/`（自动生成） | V3 人脸特征库（tier-1 二进制） + `face_records.json`（tier-2 应用层姓名↔face_id 映射） | 学习程序 / 调用程序用 `os.makedirs('face_database', exist_ok=True)` 自动创建 |
| `object_database/`（自动生成） | V3 物体特征库（tier-1 二进制） + `object_records.json`（tier-2 应用层名称 / 样本数 / 时间戳） | 同上，`os.makedirs('object_database', exist_ok=True)` |
| `logs/`（自动生成） | 全部程序日志，**追加模式**，命名格式 `<程序名>_YYYYMMDD.log` | 所有项目代码的标准模式：先建 logs/，再 open 追加写 + 块缓冲 |
| `recordings/`（按需生成） | TTS 语音 WAV 缓存（按文字内容哈希命名，重复播报不重复合成） | 物体识别播报.py / 唐诗宋词朗读器.py 等共用 |

> ⚠️ **`object_data/object_db.json` 是历史遗留空文件（内容 `{"objects":{}}`），不被任何当前代码或文档引用，与 V3 SDK 的 `object_database/` 完全无关。安全删除，新项目绝对不要写入此目录。**

---

## 第二章 新项目需提供的 8 类信息清单

开发新项目时，除参考文件外，请按以下 8 类信息提供完整需求（不涉及的类别写"无"即可，不要省略）。

---

### 2.1 项目功能需求（必填）

- **一句话功能描述**：核心做什么  
  例：「USB 摄像头实时识别已学习物体，命中「水杯」后驱动风扇 10 秒并语音播报「检测到水杯」，同时 WS2812 灯带显示彩虹效果。」
- **输入清单**：摄像头画面 / 麦克风语音 / ESP32 传感器值 / HTTP 或 MQTT 网络数据 / 用户键盘或触屏按钮
- **输出清单**：屏幕 UI 显示 / TTS 语音播报 / WS2812 灯带灯效 / 电机·风扇·继电器动作 / HTTP 请求或 MQTT 发布
- **交互流程（按时间轴）**：  
  例：「①开机显示待机界面 → ②按屏幕「开始」或物理按键 → ③摄像头实时采集 + 后台识别 → ④命中 → 动作 + 播报 → 3 秒冷却 → ⑤再次等待触发 → ⑥长按 ESC 退出」
- **触发模式**：定时轮询？按钮单次触发？事件回调？无限循环持续运行？

---

### 2.2 涉及的硬件外设（涉及硬件时必填）

- **ESP32 传感器清单**：每个传感器的型号（DHT11 / DS18B20 / 超声波 HC-SR04 / BMP280 / 巡线 Line_Sensor / 其他 I²C 或 UART 自定义设备）
- **ESP32 执行器清单**：舵机 / 直流电机 / WS2812 灯带（几颗灯珠，接 GPIO_IO_xx？）/ RGB 灯 / 风扇（继电器接哪个 IO？）/ 蜂鸣器 / OLED / 其他
- **引脚分配表**：把每个外设连接的 GPIO / ADC 口列出来（好搭AI派扩展板丝印：GPIO_IO_01 ~ GPIO_IO_16），**避免冲突**
- **非原厂外设**：如果使用好搭AI派原厂之外的硬件，提供型号 + 通信协议（I²C 地址 / SPI / UART 波特率 / PWM 频率等）

---

### 2.3 涉及的视觉算法（涉及摄像头时必填）

- **算法类型勾选项**（可多选，写名称即可）：
  - 颜色识别（目标颜色：红 / 黄 / 绿 / 蓝？多区域吗？）
  - 色块检测（目标颜色 + 最小尺寸 + 相似度阈值）
  - 二维码识别 / AprilTag（标签家族 tag36h11？需要 3D 位姿吗？→ 用 dt-apriltags 而不是 V3）
  - 人脸学习 / 人脸识别（必须先运行人脸学习.py，Tier-2 映射在 `face_database/face_records.json`）
  - 人脸表情识别（本地 V3 NPU？还是云 API？）
  - 物体学习 / 物体识别（必须先运行物体学习.py，Tier-2 映射在 `object_database/object_records.json`）
  - 人流计数（需要虚拟追踪线与方向判定吗？）
  - YOLOv8 目标检测（s/m/l/x 哪种规格？需要筛选类别吗？）
  - 姿态检测（MediaPipe Pose？还是 V3 YOLOv8-Pose？需要 Euler 角吗？）
  - 文字识别 OCR（本地 PPOCR？还是云 API？置信度阈值多少？）
  - 黑线巡线（最多需要几条返回？）
  - 车牌识别
  - 图像分类 ResNet
  - MediaPipe Hands 手势 / MediaPipe Face Mesh（不依赖 V3 的独立管线）
- **摄像头模式**（三选一，直接引用视觉系统摄像头调用参考方案.md 第 2 章）：
  - 模式 A：纯 cv2 独占（第三方算法：MediaPipe / dt-apriltags / 百度云）
  - 模式 B：混合模式（V3 官方算法 + 离线录入，⭐ 推荐绝大多数场景）
  - 模式 C：V3 视觉系统全托管（实时追踪 + 硬件联动，最高帧率）
- **是否复用现有映射机制**：人脸识别灯效.py 的人脸→灯效映射？物体识别播报.py 的英拼→中 OBJECT_NAME_CN_MAP？（有的话直接复用，不重写）

---

### 2.4 UI 设计要求（涉及界面时必填）

- **分辨率**：全屏 1920×1080（推荐）还是窗口模式（如 640×620 / 1280×720）
- **配色风格**：用户偏好——浅色背景 / 多色文字 / 不要黑色暗色 / 天空蓝背景优先；是否加载 `images/1.jpg` 作为背景图（失败回退渐变）
- **布局结构**：画面区 + 标题栏 + 状态栏 + 按钮区 + 侧边栏？相对位置画一个文字草图最好
- **字体方案**：从「字体文件/」目录中选名称，例：PingFang_Regular / msyh / simhei；并给出各区域字号（标题 px / 正文 px / 按钮 px / 状态提示 px）
- **图标**：需要 UI 图标吗？从「icons/」目录选或描述需求
- **交互控件清单**：按钮数量 + 名称 / 输入框（支持中英文？）/ 列表（可点击？可删除？）/ 进度条 / 下拉选择 / 开关

---

### 2.5 联网需求（涉及网络时必填）

- **运行形态**：离线（完全不联网）/ 需要外网
- **协议与接口细节**：
  - HTTP REST：完整 URL + 请求方法（GET/POST）+ 请求参数（Query/Body/Headers）+ 鉴权方式（Token/Basic/签名）+ 响应 JSON 字段示例
  - MQTT：Broker 地址 + 端口 + Client ID + 用户名密码 + 是否 TLS + 订阅 Topic 列表 + 发布 Topic 列表 + QoS
  - WebSocket / TCP / UDP / Modbus 等其他协议：给出协议完整细节
- **VoiceAPI 使用说明**（TTS / ASR / LLM）：
  - 是否使用？→ 如用，请提供已认证的好好搭搭账号（username / password）或确认使用已缓存 token
  - 选哪个 LLM 后端？→ 默认 `llm_chat_znbw_2025()`（更新），需要旧兼容时才用 `llm_chat()`
  - TTS 是否启用音频缓存（`recordings/` 目录）→ 默认启用，减少 API 调用量

---

### 2.6 数据持久化（涉及数据存储时必填）

- **存储方式与位置**：
  - JSON 文件：存到哪个目录？文件名？字段结构？（⚠️ 人脸/物体学习类项目**必须**使用项目约定的 2 层存储模型，不要自创路径：`face_database/face_records.json` / `object_database/object_records.json`）
  - SQLite / CSV：字段结构 + 文件名 + 路径
  - 纯内存：不需要持久化
- **数据生命周期**：每次运行重置？启动加载 + 修改后立即保存？定时 flush？有没有数据上限或过期清理策略？
- **是否与 V3 学习数据库联动**？（人脸/物体识别项目必定联动 → 直接用人脸学习.py / 物体学习.py 作为前置程序，新项目不要重复造学习 UI）

---

### 2.7 性能与约束（有特殊要求时填写，未写按默认）

- **帧率目标**：
  - 实时交互类（画面 + 追踪 + 联动）：≥ 15 FPS
  - 检测展示类（按一下识别一次）：5-10 FPS 可接受
  - 纯展示类（无摄像头）：30 FPS UI 刷新率
- **响应时延上限**：语音播报延迟？从识别到硬件动作的延迟？
- **资源预算**：允许同时加载几个 V3 算法？几个 MediaPipe 管线？（RK3566 8 核 7.7GB 内存余量很大，但多算法叠加会降低帧率）
- **日志要求**：
  - 默认：所有程序日志按标准模式输出到 `logs/<程序名>_YYYYMMDD.log`（追加模式 + 块缓冲 + stdout 分路）
  - 如需更详细的调试日志 / 关闭日志 / 改为 daily rotation，请明确说明
- **异常处理策略**：摄像头打开失败 / API 超时 / 传感器无响应 / 网络断连 —— 是重试 N 次后退出？还是 UI 提示用户后降级运行？

---

### 2.8 交付物与验收标准（强烈建议提供，避免返工）

- **交付代码文件数**：只写 1 个主程序？还是要拆模块 + 说明文档 + 清库工具？（推荐标准写法：`<项目名>.py` + `<项目名>项目说明文档.md` 两个文件；可复用资源放到已存在的 `字体文件/`/`icons/`/`images/` 里，不要再新建资源目录）
- **验收场景（1-3 个可执行的测试流程）**：  
  例：  
  1. 插上 USB 摄像头 → 启动程序 → UI 显示待机画面，顶部显示标题「物体识别播报」  
  2. 点击「学习物体」按钮 → 输入名称「水杯」 → 对准水杯点「确认」→ 状态栏显示「学习成功：水杯（样本 1，累计 1 类）」  
  3. 对准水杯点击「播报」→ TTS 扬声器播放「这是水杯」→ 屏幕右下角弹出识别结果卡片，持续 3 秒
- **性能验收指标**：例「摄像头画面 ≥ 20 FPS，识别动作完成到 TTS 开始播报 < 1.5 秒」

---

## 第三章 项目启动标准流程（7 步走）

```
Step 1  读 project_memory.md
        → Hard Constraints 全读（8 条铁律）
        → Engineering Conventions 全读（12+ 条约定，含 V3 7 步初始化、日志路径、数据存储等）
        → 确认新项目不会与任何硬约束冲突

Step 2  读 视觉系统摄像头调用参考方案.md
        → 第 2 章（三大模式总览）
        → 第 5 章（黄金法则，按需求选模式的决策树）
        → 为新项目选定摄像头模式 A / B / C

Step 3  读 camera_vision_system_v3_API分析报告.md
        → 第 2 章（实例成员，摄像头生命周期与算法开关）
        → 第 3 章（DetectionConfig 默认值：face_db_path / object_db_path / backup_camera_ids）
        → 第 5 章（14 类算法的结果访问器，选定用到的子集，确认每个字段的返回结构）
        → 第 8 章（已知易错点 + 8.8 节系统实测补漏，写代码前必看）

Step 4  读 系统环境与非视觉官方库探测报告_v1.md
        → 第 2.5 节（三项版本决策：Python 不升 / pygame-ce 不换 / cv2 不装 contrib）
        → 涉及的非视觉官方库章节（ESP32 / Line_Sensor / voice_api / audio_recorder / audio_player / text_recognition），确认方法签名
        → 第 9 章 15 条风险表（特别是 13/14/15 三条版本红线警告）

Step 5  从本规范「1.2 🟡 可选参考代码」中挑选功能最接近的已验证项目
        → 优先作为骨架复制：UI 框架 / V3 初始化 / 日志模式 / 清库接口 / 数据持久化模式 全部 1:1 复用，不要从零写
        → 项目说明文档中的「已知限制 / 坑」章节先看，提前规避

Step 6  填写本规范「第二章 新项目需提供的 8 类信息清单」
        → 8 类信息提供齐全后再生成代码，尽量一次到位，避免来回修改

Step 7  代码生成完成并首次运行时
        → 涉及人脸/物体学习的项目：先确认 face_database/、object_database/ 目录权限正常（os.makedirs exist_ok=True 通常已处理）
        → 功能异常时：先查看 logs/<程序名>_YYYYMMDD.log，不要第一时间改代码
        → 需要彻底清库时：只使用「清空人脸数据库.py」/「清空物体数据库.py」工具，完成后必须重启 Python 进程再重新学习
```

---

**一句话总原则**：5 份必选文件管「不能做错的铁律 + 能用的 API 全集」，🟡 可选项目代码管「怎么写的现成骨架」，8 类信息管「你的项目到底要什么」。三项齐全，新项目一次性落地成功率最高。
