# 语音大模型对话项目文档

基于 `pygame-ce` 与好搭AI派云端语音 API 开发的桌面语音对话程序，固定窗口尺寸 1920×1080，支持语音输入、大模型对话、语音合成播报的完整闭环。

## 一、功能特性

| 模块 | 功能 |
| --- | --- |
| 语音录入 | 按住界面圆形按钮录音，松开自动结束，采样率 16kHz 单声道 |
| 语音识别 | 调用好搭AI派 `voice_recognition` 将录音转为文字 |
| 大模型对话 | 调用好搭AI派 `llm_chat` 获取大模型回答（自动追加"请尽量简短回答"） |
| 语音合成 | 调用好搭AI派 `tts_synthesize` 将回答合成为音频 |
| 语音播报 | 使用 `AudioPlayer` 播放合成的回答音频 |
| 对话记录 | 中部面板实时显示历史对话，用户蓝色、AI 绿色，自动换行 |
| 状态指示 | 顶部状态栏显示当前阶段（空闲/录音/识别/思考/播报/错误） |
| 异常处理 | 短按或空录音自动回到空闲；出错可点击重试 |
| 背景 | 使用 `images/1.jpg` 作为全屏背景图 |
| 退出 | 右上角「退出程序」按钮 |

## 二、快捷键

| 按键 | 功能 |
| --- | --- |
| `ESC` | 退出程序 |

> 主要交互为鼠标按住/松开录音按钮，无需键盘操作。

## 三、项目结构

```
haoda_AIPad/
├── voice_llm_chat.py          # 主程序
├── 语音大模型对话项目文档.md   # 本文档
├── images/
│   └── 1.jpg                  # 背景图（1920×1080）
└── recordings/                # 录音与合成音频目录（自动创建）
    ├── voice_chat.wav         # 用户录音文件
    └── answer.wav             # 大模型回答合成音频
```

## 四、环境要求

- Python 3.8 及以上
- 依赖：
  - `pygame-ce`（提供 `pygame` 模块）
  - 好搭AI派 SDK：`voice_api`、`audio_player`、`audio_recorder`
  - 录音设备（麦克风）

## 五、安装与运行

```bash
# 安装依赖
pip install pygame-ce

# 运行程序
python3 voice_llm_chat.py
```

> 运行前请修改 `voice_llm_chat.py` 顶部的 `USERNAME` 和 `PASSWORD` 为你的好搭AI派账号。

## 六、界面布局

界面按自上而下垂直布局（1920×1080）：

```
y= 70   语音大模型对话（标题 72pt 加粗）
y=215   空闲 - 按住下方按钮开始说话（状态栏 44pt）
y=285   ┌───────────────────────────────────┐
        │ 对话记录（36pt 加粗）              │
        │ 我：xxx（蓝色 34pt）               │
        │ AI：xxx（绿色 34pt）               │
y=865   └───────────────────────────────────┘
y=876   按住按钮说话，松开自动识别并对话 | ESC 退出（26pt）
y=965   ● 按住说话（圆形录音按钮，直径 140）
```

## 七、工作流程

完整对话流程如下：

```
按住按钮 ──> recorder.start_recording()
   │
松开按钮 ──> recorder.stop_recording() + save_audio()
   │
   ├─ 录音为空或 < 0.3s ──> 回到空闲，等待下次
   │
   └─ 录音有效 ──> 启动子线程处理：
           │
           ├─ 1. 语音识别 voice_recognition()
           │     失败/空 ──> 进入错误状态
           │
           ├─ 2. 大模型对话 llm_chat(question + "，请尽量简短回答")
           │     失败/空 ──> 进入错误状态
           │
           ├─ 3. 语音合成 tts_synthesize() -> answer.wav
           │     失败 ──> 进入错误状态
           │
           └─ 4. 播报 player.play_file(answer.wav)
                 完成 ──> 回到空闲
```

## 八、技术说明

### 1. 子线程处理避免界面卡死

语音识别、大模型调用、语音合成均为网络请求，耗时较长。若在主线程执行会导致 pygame 界面无响应。

**解决方案**：录音结束后启动 `daemon` 子线程执行 `process_conversation()`，主线程持续渲染界面。状态通过 `state_lock` 互斥锁保护，子线程通过 `set_state()` 更新状态，主线程通过 `get_state()` 读取并显示。

### 2. 短按与空录音处理

短按按钮会产生空录音或时长过短的录音，若直接送入语音识别会卡住或长时间"处理中"。

**解决方案**：
- 按下按钮时记录 `record_start_tick`
- 松开时计算时长，若 `audio_data is None` 或时长 `< 0.3 秒`，直接 `set_state(STATE_IDLE)` 回到空闲，不启动处理线程

### 3. 错误状态可重试

出错后按钮原本被归入"处理中"分支变灰，无法重新开始。

**解决方案**：`ERROR` 状态独立处理，按钮显示"点击重试"并允许重新按下开始录音。

### 4. 对话记录渲染

历史记录按时间顺序追加，界面从底部向上排布（最新在底部）。

**解决方案**：
- `wrap_text()` 按像素宽度对长文本换行（支持中英文）
- 倒序遍历历史，逐行计算 y 坐标向上递减
- 超出面板顶部时停止绘制（`top_limit` 限制）

### 5. 中文字体适配

`find_chinese_font()` 按优先级查找系统可用中文字体（SimHei / 微软雅黑 / PingFang / Noto Sans CJK / 文泉驿等），保证中文界面在 Windows / macOS / Linux 下均能正常显示。

## 九、可配置项

在 `voice_llm_chat.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `BG_IMAGE` | 背景图路径 | `images/1.jpg` |
| `RECORDINGS_DIR` | 录音与音频目录 | `recordings` |
| `RECORD_FILENAME` | 用户录音文件名 | `voice_chat.wav` |
| `ANSWER_FILENAME` | 回答音频文件名 | `answer.wav` |
| `USERNAME` / `PASSWORD` | 好搭AI派账号 | 需手动修改 |
| `VOICE_API_URL` | 语音 API 地址 | `http://www.haohaodada.com/project/voiceAI/ApiZNBW.php` |

## 十、状态定义

| 状态 | 含义 | 按钮显示 |
| --- | --- | --- |
| `STATE_IDLE` | 空闲，可开始录音 | 按住说话 |
| `STATE_RECORDING` | 录音中 | 录音中 / 松开结束 |
| `STATE_RECOGNIZING` | 语音识别中 | 处理中 |
| `STATE_THINKING` | 大模型思考中 | 处理中 |
| `STATE_SPEAKING` | 播报回答中 | 处理中 |
| `STATE_ERROR` | 出错，可重试 | 点击重试 |
