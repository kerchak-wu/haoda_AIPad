# 天气预报项目说明文档

基于 `pygame-ce` 开发的桌面天气查询程序，运行于好搭AI派，固定窗口尺寸 1920×1080。屏幕上可点击切换不同城市，实时显示该城市的当前天气与 7 天预报，所有天气信息框背景随天气情况动态渐变。

## 一、功能特性

| 模块 | 功能 |
| --- | --- |
| 城市选择 | 屏幕底部 8 个城市按钮：北京 / 上海 / 广州 / 深圳 / 成都 / 杭州 / 哈尔滨 / 三亚，鼠标点击切换 |
| 当前天气 | 大温度数字、天气描述、体感温度、自绘天气图标（晴/云/雨/雪/雷/雾/月） |
| 详情指标 | 湿度、风速、风向、气压、日出、日落 共 6 项，深色半透明卡片显示 |
| 7 天预报 | 7 张并排卡片，每张独立显示日期、天气图标、最高/最低温度 |
| 动态渐变 | 全屏背景、当前天气大卡、7 天预报卡片均随对应天气类型渐变变色 |
| 文字可读性 | 全屏叠加半透明黑色遮罩 + 智能文字色（亮色卡片自动用深色文字） |
| 退出 | 右上角独立的红色「退出程序」按钮 |

## 二、快捷键 / 操作

| 操作 | 功能 |
| --- | --- |
| 鼠标点击城市按钮 | 切换到该城市的天气 |
| 鼠标点击「退出程序」 | 退出程序 |
| `ESC` | 退出程序 |
| 关闭窗口 | 退出程序 |

## 三、项目结构

```
haoda_AIPad/
├── weather.py                    # 主程序
├── 天气预报项目说明文档.md       # 本文档
└── (无需额外资源文件，所有图标均为代码自绘)
```

## 四、环境要求

- 硬件：好搭AI派（或任何带屏 Python 桌面环境）
- Python 3.8 及以上
- 依赖：`pygame-ce`（提供 `pygame` 模块）
- 网络：需连接 WiFi（用于访问 Open-Meteo API）

## 五、安装与运行

### 1. 在好搭AI派上运行

1. 将 `weather.py` 通过好搭AI派软件的「文件管理」上传到设备
2. 确保设备已连接 WiFi
3. 点击运行即可

### 2. 在普通电脑上运行

```bash
# 安装依赖
pip install pygame-ce

# 运行程序
python3 weather.py
```

> 程序默认加载「北京」天气，启动后会自动联网获取数据。

## 六、界面布局

界面按自上而下垂直布局（1920×1080），各区域无重叠：

```
y= 25   天 气 预 报（标题 72pt 加粗）           [退出程序]
y=105   2026 年 7 月 27 日 星期一（36pt）
y=160   ┌─────────────────┐ ┌────┐┌────┐
        │ ● 北京          │ │湿度││风速│
        │  北京 · 中国    │ └────┘└────┘
        │  28° C         │ ┌────┐┌────┐
        │  晴间多云       │ │风向││气压│
y=590   │  体感 27°C     │ └────┘└────┘
        └─────────────────┘ ┌────┐┌────┐
                              │日出││日落│
                              └────┘└────┘
y=615   未 来 7 天 预 报（44pt 蓝色）
y=680   ┌──┐┌──┐┌──┐┌──┐┌──┐┌──┐┌──┐
        │今││明││7/││7/││7/││7/││7/│
y=860   └──┘└──┘└──┘└──┘└──┘└──┘└──┘
                ↕ 60px 间隔（无重叠）
y=920   [北京][上海][广州][深圳][成都][杭州][哈尔滨][三亚]
y=1020  点击下方城市按钮切换 · 按 ESC 或点击右上角退出按钮关闭
```

## 七、动态渐变色

### 1. 全屏背景渐变（`SCREEN_THEMES`）

整体背景按当前天气类型采用较深的渐变，确保白色文字清晰：

| 天气 | 上色 | 下色 |
| --- | --- | --- |
| 晴日 | 深蓝 `#1E5096` | 浅蓝 `#5A96DC` |
| 晴夜 | 深夜 `#0A0F28` | 蓝 `#1E2850` |
| 多云 | 灰蓝 `#37415A` | 浅灰 `#6E7D96` |
| 雨 | 深蓝 `#1E2D46` | 蓝灰 `#3C506E` |
| 雪 | 蓝灰 `#465A82` | 浅蓝 `#8CA5C8` |
| 雷 | 深紫 `#141628` | 紫 `#2D304B` |
| 雾 | 灰 `#3C4150` | 浅灰 `#6E7382` |

### 2. 天气信息卡渐变（`CARD_THEMES`）

当前天气大卡 + 7 张预报卡的背景按对应天气类型独立渐变，区分度强：

| 天气 | 上色 | 下色 | 视觉效果 |
| --- | --- | --- | --- |
| 晴日 | 金橙 `#FFB43C` | 红橙 `#FF7832` | 阳光金橙 |
| 晴夜 | 深蓝 `#28326E` | 夜空 `#14193C` | 夜空深蓝 |
| 多云 | 灰 `#A0AABE` | 深灰 `#6E7891` | 灰云 |
| 雨 | 蓝 `#466EB4` | 深蓝 `#284682` | 雨蓝 |
| 雪 | 浅青 `#C8E1FA` | 雪青 `#96B4DC` | 雪青 |
| 雷 | 紫 `#5A46A0` | 深紫 `#322864` | 雷紫 |
| 雾 | 浅灰 `#B4B9C8` | 灰 `#82879B` | 雾灰 |

### 3. 智能文字色

雪 / 雾 / 晴日 等背景较亮的卡片自动切换为深色文字（`#28283C`），其余天气保持白色文字，保证可读性。

## 八、技术说明

### 1. 数据源

使用 [Open-Meteo](https://open-meteo.com) 免费 API，**无需 API Key**：

- **地理编码**：`https://geocoding-api.open-meteo.com/v1/search?name={城市}&count=1&language=zh&format=json`
- **天气查询**：`https://api.open-meteo.com/v1/forecast?latitude={lat}&longitude={lon}&current=...&daily=...&timezone=auto&forecast_days=7`

返回 JSON 数据，包含当前温度、湿度、风速、气压、天气代码（WMO Code）及 7 天预报。

### 2. WMO 天气代码映射

程序内置 27 种 WMO 标准天气代码到中文描述与主题类型的映射表（`WMO` 字典），覆盖晴、多云、阴、雾、毛毛雨、雨、雪、阵雨、雷暴等所有类型。

### 3. SSL 证书处理

好搭AI派等嵌入式环境可能存在 SSL 证书问题，程序在 `fetch_json()` 中显式创建 SSL 上下文并关闭证书校验：

```python
ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
```

### 4. 天气图标自绘

所有天气图标均使用 `pygame.draw` 自绘，不依赖任何图片资源：

- **晴日**：圆 + 12 道光线
- **晴夜**：新月（圆 + 偏移阴影）+ 3 颗星
- **多云**：3 个白色圆叠加 + 小太阳
- **雨**：云 + 4 条蓝色斜线
- **雪**：云 + 3 个六角雪花
- **雷**：深色云 + 黄色闪电多边形
- **雾**：5 条横线

### 5. 渐变面板绘制

`draw_gradient_panel()` 函数生成带圆角的渐变面板：

1. 创建 `SRCALPHA` Surface
2. 逐行绘制渐变色（含 alpha 通道）
3. 用圆角白色矩形作 mask，通过 `BLEND_RGBA_MULT` 混合实现圆角效果
4. 绘制白色边框

### 6. 中文字体适配

`find_chinese_font()` 按优先级查找可用中文字体：

1. 好搭AI派内置：`/home/cxdz/jupyter/assets/simfang.ttf`
2. 系统字体候选：simhei / microsoftyahei / msyh / pingfang / notosanscjk / wqy-microhei 等
3. 常见字体路径：Linux / macOS / Windows 系统字体目录

### 7. UI 风格

参考仓库中 music_player.py 与 red_revolution_app.py 的设计：

- 自定义 `Button` 类支持 hover 高亮、selected 选中态
- 半透明深色遮罩（alpha=80）保证文字对比度
- 右上角红色「退出程序」按钮（与音乐播放器风格一致）
- 城市按钮选中态为金黄色填充

## 九、可配置项

在 `weather.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `FONT_PATH` | 好搭AI派内置字体路径 | `/home/cxdz/jupyter/assets/simfang.ttf` |
| `CITIES` | 城市列表（中文名 + 英文查询名） | 8 个中国主要城市 |
| `SCREEN_THEMES` | 全屏背景渐变配色 | 7 套 |
| `CARD_THEMES` | 天气卡片渐变配色 | 7 套 |

### 添加新城市

修改 `CITIES` 列表即可：

```python
CITIES = [
    {'name': '北京',   'q': 'Beijing'},
    {'name': '上海',   'q': 'Shanghai'},
    # 添加新城市：
    {'name': '武汉',   'q': 'Wuhan'},
    {'name': '西安',   'q': "Xi'an"},
    {'name': '伦敦',   'q': 'London'},
    {'name': '巴黎',   'q': 'Paris'},
]
```

按钮栏会自动居中排列所有城市。

## 十、错误处理

| 场景 | 表现 |
| --- | --- |
| 网络连接失败 | 屏幕中央红色显示「获取失败：xxx」，提示检查网络 |
| 城市名查询不到 | 屏幕中央显示「未找到该城市」 |
| 加载中 | 屏幕中央显示「正在加载天气数据...」 |
| SSL 证书问题 | 已通过关闭证书校验自动处理 |

## 十一、数据更新

- 程序启动时自动加载第一个城市（北京）的天气
- 每次点击新城市按钮即重新获取该城市最新数据
- 数据为实时获取，不缓存（每次切换都从 API 拉取最新值）
