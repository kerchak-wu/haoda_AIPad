# 智慧阅读角项目说明文档

## 一、项目概述

基于好搭AI派开发的智慧阅读角交互系统。通过人体红外传感器（PIR）自动检测读者到来并播放欢迎语，提供馆藏图书语音介绍、图书视频播放、智能导读（OCR 识别书名 + 语音对话）三大功能，全程配合 RGB 灯带营造沉浸式阅读氛围。

**程序文件**：`智慧阅读角.py`

**核心功能**：

- 欢迎屏幕：PIR 检测到人自动播放欢迎语并进入功能选择界面，30 秒无人自动返回
- 馆藏图书：8 本中外名著，点击书名语音+文字介绍
- 图书视频：西游记/三国演义/红楼梦对应视频播放
- 智能导读：USB 摄像头 OCR 识别书名 + 按住说话语音对话（含馆藏图书匹配 + LLM 智能问答）
- RGB 灯带：11 灯珠 WS2812，8 种灯效全程配合（欢迎/菜单/阅读/影院/导读/播报/识别/录音）

---

## 二、硬件接线

| 硬件 | 接口 | 说明 |
| ---- | ---- | ---- |
| 人体红外传感器（PIR） | IO1（GPIO_IO_01） | 检测读者到来 |
| WS2812 RGB 灯带（11 灯珠） | IO2（GPIO_IO_02） | 需接上拉扩展模块 |
| USB 摄像头 | ESP32 扩展板 USB 口 | 智能导读 OCR 识别 |

**依赖文件**：`videos/1.mp4`、`videos/2.mp4`、`videos/3.mp4`（图书视频）

---

## 三、关键技术经验（重要）

本项目开发过程中踩坑并解决了多个稳定性问题，以下经验对未来类似项目开发具有参考价值。

### 1. ESP32 串口访问必须串行化（board_lock 模式）

**问题现象**：PIR 检测延迟严重（实测启动后 10 秒才响应），有时完全不响应。

**根因**：`LightManager` 后台线程每 0.05 秒调用 `ws2812Write` 访问 ESP32 串口，主线程每帧调用 `digitalRead(PIR_PIN)` 也访问串口，两者无共享锁并发访问，导致 PIR 读取超时或返回错误值。

**解决方案**：所有 ESP32 串口访问共用一把 `board_lock`，并参考《红色文化交互展示》中已验证的 `PIRSensor` 后台轮询模式：

```python
# 共享串口锁
self._board_lock = threading.Lock()

# LightManager 接收并使用 board_lock
self.lights = LightManager(self.board, RGB_PIN, NUM_LEDS,
                           board_lock=self._board_lock)

# PIR 改为后台线程轮询，主线程只读缓存值
def _pir_loop(self):
    while self._pir_thread_running and self.running:
        try:
            if self.board is not None:
                with self._board_lock:          # 加锁串行化
                    val = self.board.digitalRead(PIR_PIN)
                with self._pir_lock:
                    self._pir_detected = (val == 1)
        except Exception:
            pass
        time.sleep(0.2)

# 主线程读取（非阻塞，不访问串口）
def check_pir(self):
    with self._pir_lock:
        return 1 if self._pir_detected else 0
```

**关键要点**：

- 任何访问 `board.digitalRead` / `board.ws2812Write` 的代码都必须持有 `board_lock`
- PIR 读取**绝不能放在主线程**，必须后台线程轮询 + 主线程读缓存
- 参考《红色文化交互展示》的 `PIRSensor` 类是已验证可用的标准模式

---

### 2. OCR 卡死的真正根因：GIL 饥饿（非死锁）

**问题现象**：点击"识别文字"按钮程序卡死，但语音识别功能正常。

**初步诊断（错误）**：以为是 `self._lock` 死锁，把 `ocr_recognize_async` 移到锁外仍未解决。

**真正根因**：PaddleOCR 的 `recognize_text` 是 CPU 密集型 C 扩展，推理期间长时间持有 GIL（全局解释器锁），导致主线程（pygame 循环）被饿死、界面卡死。语音识别不卡是因为 `voice_recognition`/`llm_chat`/`tts_synthesize` 都是网络 I/O 调用，会释放 GIL。

**加剧因素**：

- 摄像头配置为 1280×720，像素量是官方范例《文字识别播报器》（640×480）的 4 倍，推理时间数倍增长
- OCR 期间暂停摄像头采集（`_camera_paused = True`），使唯一能通过 I/O 释放 GIL 的摄像头线程休眠，PaddleOCR 更肆无忌惮独占 GIL

**解决方案（三处改动协同）**：

```python
def ocr_recognize_async(self):
    # ① 主线程提前抓帧快照（锁持有极短），OCR 线程与摄像头线程解耦
    with self._camera_lock:
        frame_snapshot = self._latest_frame.copy() if self._latest_frame is not None else None

    def worker(frame):
        def ocr_run():
            # ② 缩小送检帧尺寸到宽度 640（与官方范例一致），推理时间大幅缩短
            h, w = frame.shape[:2]
            if w > 640:
                frame_small = cv2.resize(frame, (640, int(h * 640 / w)))
            else:
                frame_small = frame
            res = self.ocr.recognize_text(frame_small, confidence_threshold=0.5)

        ocr_thread = threading.Thread(target=ocr_run, daemon=True)
        ocr_thread.start()
        ocr_thread.join(timeout=15.0)  # ③ 超时保护，避免永久阻塞

    # ④ 不暂停摄像头采集：capture_frame 是 I/O 会释放 GIL，让主线程有机会运行
    threading.Thread(target=worker, args=(frame_snapshot,), daemon=True).start()
```

**关键要点**：

- CPU 密集型操作（OCR/模型推理）会持有 GIL，必须**缩短单次占用时间**（缩帧）+ **保留 I/O 线程释放 GIL**（不暂停摄像头）
- 缩帧到 640 宽度对书名 OCR 识别效果无影响（书名字本身就大）
- 始终用 `ocr_thread.join(timeout=...)` 做超时保护

---

### 3. threading.Lock 不可重入导致的死锁

**问题现象**：在 `with self._lock` 块内调用会再次获取 `self._lock` 的函数，主线程永久阻塞。

**根因**：Python 的 `threading.Lock` 不可重入（同一线程二次获取会死锁），需用 `threading.RLock` 才可重入。但本项目统一用 `Lock`，因此必须避免在持锁时调用会再次加锁的函数。

**典型陷阱**：`speak_async`、`ocr_recognize_async`、`voice_converse_async` 内部都会获取 `self._lock`，不能在 `with self._lock` 块内调用。

**正确模式：锁内检查状态，锁外调用**

```python
# 错误：speak_async 内部获取 self._lock，在 with self._lock 内调用会死锁
with self._lock:
    if not self.welcome_played:
        self.welcome_played = True
        self.speak_async('欢迎语', restore_mode='menu')  # 死锁！

# 正确：锁内只检查并更新标志，锁外调用
with self._lock:
    need_play = not self.welcome_played
    if need_play:
        self.welcome_played = True
if need_play:
    self.speak_async('欢迎语', restore_mode='menu')  # 锁外调用，安全
```

**关键要点**：

- 凡是内部会获取 `self._lock` 的函数（`speak_async`/`ocr_recognize_async`/`voice_converse_async`/`set_light`），都不能在 `with self._lock` 块内调用
- 统一模式：锁内只做状态检查与布尔标志更新，锁外根据标志调用异步函数

---

### 4. 摄像头后台采集与 OCR 资源解耦

**问题**：OCR 线程和摄像头采集线程都访问 `vision_system` 和 `_latest_frame`，存在资源竞争。

**解决方案**：

- 用独立的 `_camera_lock`（与业务锁 `_lock` 分开）保护 `_latest_frame`
- 摄像头采集线程锁内只做帧赋值（`frame.copy()`），持锁时间极短
- OCR 启动前在主线程一次性抓帧快照传入 worker，OCR 线程不再访问 `_latest_frame`

```python
# 摄像头采集线程：锁内只赋值
with self._camera_lock:
    self._latest_frame = frame.copy()

# OCR 主线程提前抓帧（锁持有极短）
with self._camera_lock:
    frame_snapshot = self._latest_frame.copy() if self._latest_frame is not None else None
# frame_snapshot 作为参数传入 worker，与采集线程完全解耦
```

---

### 5. 模块导入顺序陷阱

`text_recognition`（PaddleOCR）必须在文件最顶部、所有其他库之前导入，否则 `ppocr_system` 报 `'utils' is not a package`。

```python
# !!! text_recognition 必须最先导入 !!!
from text_recognition import TextRecognizer

import pygame
import cv2
# ... 其他库
```

详见《文字识别播报器项目说明文档》第 1 条。

---

### 6. 摄像头初始化放后台线程，不阻塞主循环

摄像头 `open_camera()` 较慢（实测约 3 秒），放后台线程初始化，主线程可立即进入欢迎屏响应 PIR。

```python
self._camera_initializing = True
# 必须保存为成员变量，cleanup 时才能 join 等待其结束（避免清理后才打印"摄像头打开失败"）
self._camera_init_thread = threading.Thread(target=self._init_camera_background, daemon=True)
self._camera_init_thread.start()
# 主线程不等待，立即进入欢迎屏
self.state = self.S_WELCOME
```

**关键要点（见第三章第 13 条）**：
- `_init_camera_background` / `init_camera_open` 内部关键节点必须检查 `self.running`
- `cleanup` 中在释放 vision_system 前先 `join(timeout=5.0)` 等待初始化线程结束
- 用户退出期间的 `camera_ok=False` 日志应标记为"已中止"而非"打开失败"

---

### 7. 退出按钮重复日志修复

**问题**：点击退出按钮后日志打印两次。

**根因**：`handle_click` 设置 `self.running = False` 后未 `return`，同帧内后续事件继续处理；且 `handle_events` 循环未检测 `running` 状态。

**解决方案**：

```python
def handle_click(self, pos):
    if self.state == self.S_WELCOME:
        if self._exit_rect and self._exit_rect.collidepoint(pos):
            self.running = False
            return  # 立即返回，避免同帧重复处理

def handle_events(self):
    for event in pygame.event.get():
        if not self.running:
            break  # running 已关闭则不再处理后续事件
        ...
```

---

### 8. Rockchip 平台兼容性补丁（参照《视觉系统摄像头调用参考方案》第 7 章）

**问题现象**：在好搭AI派（RK3588 等 Rockchip SoC）上运行时，pygame+cv2+vision_system 同进程容易出现段错误、Mali GPU 驱动崩溃、mixer 死锁等问题。

**根因**：Rockchip 平台的 Mali GPU 驱动、音频驱动、V4L2 驱动在 pygame+cv2 同进程下存在资源冲突；`pygame.init()` 一次性初始化所有子模块（joystick/CDROM/mixer 等），部分子模块驱动缺失会导致段错误。

**解决方案（三段补丁必须同时执行）**：

```python
# 补丁1：LIBGL_ALWAYS_SOFTWARE 环境变量（必须在 import pygame 之前设置）
import os
os.environ.setdefault('LIBGL_ALWAYS_SOFTWARE', '1')
import pygame

# 补丁2：Pygame 分段初始化，不调用 pygame.init()
pygame.display.init()
pygame.font.init()
try:
    pygame.mixer.pre_init(44100, -16, 2, 512)
    pygame.mixer.init()
    self.mixer_ok = True
except Exception as e:
    self.logger.warning('pygame.mixer初始化失败(视频将无声音): %s', e)

# 补丁3：mixer 单次 init/quit（init_pygame 中初始化一次，cleanup 中统一 quit）
# 不在视频播放/停止时反复 init/quit mixer
```

**关键要点**：

- `LIBGL_ALWAYS_SOFTWARE=1` 强制 libGL 软件渲染，绕过 Mali GPU 驱动崩溃
- `pygame.init()` 会 init joystick 等子模块，Rockchip 上易段错误，改为 `display.init()` + `font.init()` 分段初始化
- mixer 用 `pre_init` 设置参数后再 `init`（pre_init 必须在 mixer.init 之前），失败时仅静音不影响画面
- mixer 在程序生命周期内只 init/quit 一次，不在视频播放/停止时反复 init/quit

---

### 9. 日志文件名规范与追加模式

**问题现象**：原实现日志文件名带时分秒（`智慧阅读角_MMDD_HHMMSS.log`），每次运行生成新文件，同一天多次运行日志分散在多个文件，不便排查。

**解决方案**：遵循项目工程约定，文件名改为 `智慧阅读角_YYYYMMDD.log`，追加模式（`'a'`），同一天多次运行累积保留，每次启动写分隔标记：

```python
def setup_logging():
    os.makedirs('logs', exist_ok=True)
    log_filename = os.path.join(
        'logs', '智慧阅读角_%s.log' % datetime.now().strftime('%Y%m%d'))
    logger = logging.getLogger('SmartReading')
    logger.handlers.clear()
    fh = logging.FileHandler(log_filename, mode='a', encoding='utf-8')  # 追加模式
    fh.setLevel(logging.DEBUG)
    logger.addHandler(fh)
    logger.info('======== %s 运行开始 ========', datetime.now().strftime('%Y-%m-%d %H:%M:%S'))
    return logger
```

**关键要点**：

- 文件名格式 `智慧阅读角_YYYYMMDD.log`，与仓库其他项目（人脸识别灯效、姿态检测、文字识别视频播放器）一致
- 追加模式（`'a'`）而非覆盖，同一天多次运行日志累积保留
- cleanup 中显式 `flush()` + `close()` 所有 logging handlers，确保日志写入磁盘

---

### 10. 摄像头帧校验与冗余 refresh_results 移除

**问题现象**：摄像头偶发全黑/全白脏帧被采集写入缓存，导致 OCR 识别到空内容；采集线程每帧调用 `result_accessor.refresh_results()` 但结果从未被读取。

**根因**：

1. 采集线程只判断 `frame is not None`，未校验帧内容有效性
2. 本项目用独立 `TextRecognizer` 做 OCR，不依赖 vision_system 的检测结果，`refresh_results()` 调用冗余，且增加 CPU 开销与内部锁竞争

**解决方案**：

```python
def _camera_capture_worker(self):
    while self._camera_thread_running and self.running:
        try:
            if self.vision_system and self.camera_ok:
                # 不再调用 result_accessor.refresh_results()（本项目用独立 OCR，不依赖检测结果）
                frame = self.vision_system.capture_frame()
                # 帧有效性校验：3维 shape + mean 过滤全黑/全白脏帧
                if (frame is not None and hasattr(frame, 'shape')
                        and len(frame.shape) == 3 and frame.size > 0):
                    try:
                        m = frame.mean()
                        valid = 5 <= m <= 250
                    except Exception:
                        valid = True
                    if valid:
                        with self._camera_lock:
                            self._latest_frame = frame.copy()
        except Exception as _e:
            if self.logger:
                self.logger.warning('摄像头采集线程异常: %s', _e)
        time.sleep(1.0 / 15)
```

**关键要点**：

- 用 `frame.mean()` 而非 `gray.std()` 检测全黑/全白帧，避免 ARM 上灰度转换+标准差计算的高开销
- mean 值在 `[5, 250]` 区间为有效帧，过滤 V4L2 偶发脏帧
- 移除冗余 `refresh_results()` 调用，减少 CPU 开销与 vision_system 内部锁竞争

---

### 11. 模块导入容错（不提前崩溃）

**问题现象**：`text_recognition` / `ESP32` / `camera_vision_system_v3` / `voice_api` 等模块任一导入失败，程序直接 ImportError 崩溃，无法进入界面调试。

**解决方案**：所有外部模块导入用 try/except 包裹，记录可用性标志，程序仍可启动（对应功能不可用）：

```python
try:
    from text_recognition import TextRecognizer as _TextRecognizer
    _TEXT_RECOGNITION_AVAILABLE = True
except Exception as _e:
    _TEXT_RECOGNITION_AVAILABLE = False
    _TEXT_RECOGNITION_ERROR = _e

# ESP32 / camera_vision_system_v3 / voice_api / audio_player / audio_recorder 同理
```

**关键要点**：

- `init_hardware` / `init_vision_system` / `init_voice` 内部检查对应 `_XXX_AVAILABLE` 标志，不可用时记录日志并跳过
- ESP32 导入失败时 `PIR_PIN` / `RGB_PIN` 回退到数字引脚号，避免 NameError
- `text_recognition` 仍必须最先导入（PaddleOCR 的 utils 包冲突问题不变），只是加了容错

---

### 12. 扩展板未连接时，cleanup 灯带关闭触发 AttributeError

**问题现象（来自 2026-08-12 08:28 运行日志）**：扩展板没插（`board.start()` 返回 False），程序仍能启动并进入降级模式；点击右上角退出后，cleanup 阶段出现警告：
```
[W] 警告：扩展板连接异常，硬件功能不可用
...（用户退出）
[W] 关闭RGB灯带异常: 'ESP32' object has no attribute 'device'
```

**根因**：`init_hardware()` 中，**即使** `ESP32()` 构造成功且 `board.start()` 返回 False（串口没响应），`self.board` 依然被赋值为 `ESP32()` 对象。但该对象是"半成品"——`device` 属性（串口连接句柄）只有在 `start()` 成功后才会被内部初始化。`cleanup()` 原来的代码只判断 `if self.board:` 就直接调用 `ws2812Write(RGB_PIN, 255, 0, 0, 0)`，触发 `AttributeError: 'ESP32' object has no attribute 'device'`。

**加剧因素**：`LightManager.stop()` 内部同样直接调用 `ws2812Write`，没做 `hasattr(board, 'device')` 检查；只是因为扩展板未连接时 `self.lights` 为 `None` 没被触发，但扩展板**部分初始化成功**的场景也会踩坑。

**解决方案（三层防护，全部生效）**：

**第 1 层：引入 `_board_connected` 状态标记（最可靠）**

```python
# __init__ 中
self.board = None
self._board_connected = False  # True 仅当 start() + ws2812Init 都成功
self.lights = None

# init_hardware 中：只有 board.start() 成功、ws2812Init 调用、
# LightManager 启动、PIR 线程启动全部 OK 后才翻转为 True
if not self.board.start():
    ...
    return False
self.board.ws2812Init(RGB_PIN, NUM_LEDS)
self.lights = LightManager(self.board, RGB_PIN, NUM_LEDS, board_lock=self._board_lock)
self.lights.start()
self._start_pir_thread()
self._board_connected = True  # ← 关键点
return True
```

**第 2 层：cleanup() 灯带关闭判断升级为三重检查**

```python
# 旧代码（只判断 board 是否非空，触发 Bug）
# if self.board:
#     self.board.ws2812Write(...)  # AttributeError!

# 新代码：三重保护
if (self._board_connected          # 1. 硬件连接真正成功
        and self.board is not None  # 2. board 对象存在
        and hasattr(self.board, 'device')):  # 3. device 属性已初始化
    try:
        self.board.ws2812Write(RGB_PIN, 255, 0, 0, 0)
    except Exception as _e:
        if self.logger:
            self.logger.warning('关闭RGB灯带异常: %s', _e)
```

**第 3 层：`LightManager.stop()` 内部增加 `hasattr` 保护**

```python
def stop(self):
    self.running = False
    try:
        if (self.board is not None
                and hasattr(self.board, 'ws2812Write')
                and hasattr(self.board, 'device')):  # ← 新增
            if self._board_lock is not None:
                with self._board_lock:
                    self.board.ws2812Write(self.pin, 255, 0, 0, 0)
            else:
                self.board.ws2812Write(self.pin, 255, 0, 0, 0)
    except Exception:
        pass  # 最后兜底
```

**修复后效果**：扩展板未连接场景下，cleanup 直接跳过 `ws2812Write` 调用，不再出现 `'ESP32' object has no attribute 'device'` 警告。

---

### 13. 摄像头初始化线程：用户退出后仍继续执行（日志顺序错乱 + 资源竞争）

**问题现象（来自 2026-08-12 08:29 第 2 次运行日志）**：用户在欢迎屏只停留了 2 秒（摄像头 `open_camera()` 仍在后台阻塞）就点退出，cleanup 完成后才打印"摄像头打开失败"，**日志时间顺序完全倒置**：
```
08:29:03 [I] 用户点击欢迎屏右上角【退出程序】按钮
08:29:03 [I] 正在清理资源...
08:29:03 [I] 摄像头后台采集线程已停止        ← 清理动作
08:29:03 [W] 警告：摄像头打开失败               ← 初始化线程的错误（在清理之后才出现！）
08:29:03 [I] 摄像头后台初始化流程结束 (camera_ok=False)
08:29:04 [I] 资源清理完成
```
而且从内容上看，`camera_ok=False` 是用户**主动打断**造成的，不是摄像头真的坏了，但日志措辞"打开失败"具有严重误导性。

**更严重的风险（Race Condition）**：`open_camera()` 阻塞 3 秒期间用户退出 → cleanup 调 `vision_system.cleanup()` 释放资源 → 紧接着 `open_camera()` 在另一线程里 return True → 正要 `_start_camera_thread()` 但 vision_system 已被释放 → 线程访问已释放的对象 → **段错误或死机**。

**根因**：
1. `run()` 中启动初始化线程时，把 `Thread` 对象只存为**局部变量** `cam_init_thread`，cleanup 无法引用，自然没法 join 等待它跑完
2. `_init_camera_background()` 从头到尾**没有任何 `self.running` 检查**——`self.running=False` （用户退出）后仍按正常流程打印"打开失败"日志并尝试启动采集线程
3. `init_camera_open()` 内部同理：`open_camera()` 前后完全不感知程序是否已退出

**解决方案（四层防御，全部叠加）**

**第 1 层：线程引用存为成员变量 + cleanup 时 join 等待（必做）**

```python
# __init__
self._camera_init_thread = None  # 存成员变量

# run()
self._camera_initializing = True
# 不再是 cam_init_thread（局部），存 self._camera_init_thread（成员）
self._camera_init_thread = threading.Thread(target=self._init_camera_background, daemon=True)
self._camera_init_thread.start()

# cleanup()：在停止采集线程 + 释放 vision_system 之前先 join init 线程
if self._camera_init_thread is not None:
    try:
        if self._camera_init_thread.is_alive():
            if self.logger:
                self.logger.info('等待摄像头初始化线程结束...')
            self._camera_init_thread.join(timeout=5.0)  # 最多 5 秒，永不卡死
    except Exception as _e:
        if self.logger:
            self.logger.warning('等待摄像头初始化线程异常: %s', _e)
    finally:
        self._camera_init_thread = None  # 避免重复 join
# ↑ 以上步骤完成后，再做 _stop_camera_thread()、vision_system.cleanup()
```

**第 2 层：cleanup 入口置 `self.running = False`（通知所有线程）**

```python
def cleanup(self):
    # 虽然主循环 break 时通常 running=False，但防御式显式置位确保所有线程都能感知
    self.running = False
    ...  # 再做视频停止、init 线程等待、采集线程停止、vision_system 释放
```

**第 3 层：`_init_camera_background()` 关键节点检查 `self.running` + 日志措辞区分**

```python
def _init_camera_background(self):
    try:
        if not self.running:      # 入口：用户已退出，直接跳过
            return
        self._init_stage = '正在启动摄像头...'
        if self.running:          # open_camera() 之前再次检查
            self.init_camera_open()
    except Exception as e:
        # 异常日志只在用户未退出时打印（退出期间异常属用户主动打断）
        if self.logger and self.running:
            self.logger.error('摄像头后台初始化异常: %s', e)
    finally:
        self._camera_initializing = False
        if self.logger:
            if self.running:
                # 正常跑完：打印"结束"
                self.logger.info('摄像头后台初始化流程结束 (camera_ok=%s)', self.camera_ok)
            else:
                # 用户打断：明确打印"已中止（用户退出）"，不用"打开失败"的误导表述
                self.logger.info('摄像头后台初始化流程已中止（用户退出）(camera_ok=%s)', self.camera_ok)
```

**第 4 层：`init_camera_open()` 内部三处 running 检查**

```python
def init_camera_open(self):
    try:
        # ① 在 open_camera() 之前：用户已退出就根本不打开（避免设备忙，下次启动更稳）
        if not self.running:
            return
        if self.vision_system.open_camera():
            # ② open_camera() 成功后再检查：如果阻塞期间用户已退出 → 立即关闭、不启动采集线程
            if not self.running:
                try:
                    self.vision_system.cleanup()  # 就地释放，不留活摄像头
                except Exception:
                    pass
                self.camera_ok = False
                return
            self.camera_ok = True
            self.logger.info('摄像头初始化成功')
            self._start_camera_thread()
        else:
            # ③ 打开失败的警告也只在用户没退出时打印（否则就是"主动打断"，不是设备故障）
            if self.running:
                self.logger.warning('警告：摄像头打开失败')
    except Exception as e:
        if self.logger and self.running:
            self.logger.error('摄像头初始化异常: %s', e)
```

**修复后预期日志顺序（欢迎屏快速退出场景）**：
```
正在清理资源...
等待摄像头初始化线程结束...          ← init 线程还在跑时才会有这条
摄像头后台初始化流程已中止（用户退出）(camera_ok=False)  ← 不再说"打开失败"
摄像头后台采集线程已停止
视觉系统资源已释放
资源清理完成
```
（符合资源生命周期顺序：先停 init 线程 → 再停采集线程 → 再释放 vision_system，无竞争风险，日志语义清晰）

---

## 四、线程架构总览

本项目共 6 个后台线程（含 1 个短生命周期），职责明确：

| 线程 | 生命周期 | 职责 | 关键锁 | 注意事项 |
| ---- | ---- | ---- | ---- | ---- |
| 主线程 | 长驻 | Pygame 事件循环 + 渲染 + 状态机 | `_lock`（业务状态） | 不访问串口，不执行 CPU 密集运算；cleanup 入口先置 `self.running=False` |
| LightManager | 长驻 | RGB 灯效渲染（每 0.05s） | `_board_lock`（串口） | 与 PIR 线程共享串口锁；`stop()` 前必须检查 `hasattr(board, 'device')` |
| PIR 轮询 | 长驻 | 读取 PIR 引脚（每 0.2s） | `_board_lock` + `_pir_lock` | 主线程只读 `_pir_detected` 缓存 |
| **摄像头初始化** | **短生命周期（~3s）** | **后台异步调 `open_camera()` + 启动采集线程** | 无（只读成员） | **必须保存为 `self._camera_init_thread` 成员变量；cleanup 时 `join(timeout=5.0)` 等待结束；内部多处检查 `self.running`** |
| 摄像头采集 | 长驻 | `capture_frame` 刷新帧（每 1/15s） | `_camera_lock`（帧数据） | OCR 期间不暂停，保留 GIL 释放点 |
| OCR/语音 worker | 短生命周期（按需） | 按需触发的短任务线程 | `_lock`（业务状态） | 启动后即结束，非长驻 |

**资源清理顺序（不可颠倒，否则有竞争风险）**：
1. `self.running = False`（先通知所有线程）
2. 停止视频播放（可能占用 mixer、cv2 资源）
3. **等待摄像头初始化线程结束**（`join(timeout=5.0)`）——若跳过此步，`vision_system.cleanup()` 可能和另一个线程中刚 return 的 `open_camera()` 同时操作同一对象，触发段错误
4. 停止摄像头采集线程
5. 停止 PIR 轮询线程
6. 调用 `lights.stop()`（依赖 board 对象仍可用）
7. 调用 `vision_system.cleanup()` 释放底层摄像头
8. 最后 `ws2812Write` 关灯（需要 `_board_connected=True`）、`pygame.quit()`

**锁的分层**（避免嵌套死锁）：

- `_board_lock`：ESP32 串口访问（LightManager + PIR 线程）
- `_camera_lock`：摄像头帧数据（采集线程 + 主线程抓帧）
- `_lock`：业务状态（guide_processing / is_speaking / welcome_played 等）
- `_pir_lock`：PIR 检测缓存值

**铁律**：锁内只做状态读写，锁外调用异步函数和网络/串口 I/O。

---

## 五、状态机与界面流转

```md
S_INIT（加载屏）
  │ 硬件/视觉/语音初始化完成
  ▼
S_WELCOME（欢迎屏）──PIR检测到人──▶ S_MENU（功能选择，30s超时返回）
  │                                  │
  │ 退出按钮                         ├─▶ S_LIBRARY（馆藏图书）
  ▼                                  ├─▶ S_VIDEOS（图书视频列表）─▶ S_VIDEO_PLAY（播放）
S_INIT                               └─▶ S_GUIDE（智能导读：OCR + 语音对话）
```

---

## 六、智能导读对话界面角色

对话记录中角色标记为 `[读者]`（用户语音输入）和 `[系统]`（OCR 识别/LLM 回答）。

---

## 七、开发提示词参考

本项目开发过程中使用的关键提示词（按实际开发顺序整理），供未来类似项目开发参考。每条提示词附带了实际效果与对应的文档章节，便于复现。

### 提示词 1：项目初始需求（整体功能定义）

> 参考云端仓库，做一个智慧阅读角案例。
> 首先，好搭AI派屏幕显示"欢迎来到智慧阅读角，享受阅读时光"
> 当人体红外传感器（接io1）检测到人，屏幕切换到功能选择界面，包括三个功能：馆藏图书、图书视频、智能导读；点击功能框切换到对应功能界面。
> 馆藏图书功能：界面上有一些图书的书名（按钮框），点击对应的书名，对该本图书进行简短的介绍（语音）；屏幕左侧显示书名，右侧显示"请点击书名，我来为你介绍"，当进行语音介绍时，显示介绍的文字内容。
> 图书视频功能：显示三个书名：西游记、三国演义、红楼梦，点击书名播放对应视屏，分别是videos文件夹下的1.mp4、2.mp4、3.mp4；
> 智能导读功能：USB摄像头识别文字（识别按钮点击一次识别一次），识别到图书书名，简短介绍图书；如果识别出来不是图书，说一下这不是图书请重新识别（说的内容你再组织）；放一个对话按钮，我按住按钮说话，你根据语音识别的结果，针对包含的书名、作者等信息，介绍图书或者作家，如果没有识别到图书相关有效信息，回复未检测到请重新说（说的内容你再组织）；
> 3个功能界面都要一个返回按钮，返回上一级；功能选择界面不要返回按钮，在这个界面30秒内没有检测到人，自动返回欢迎屏幕。欢迎屏幕放一个退出程序按钮。
> io2接RGB灯带，11灯珠，全程配合显示不同灯效，效果要好看、明显，与各种功能配合合理。
> 界面、背景设计好看，符合主题；根据以上功能要求补充合理内容，窗口、按钮布局合理，方便操作。

**实际效果**：完成了项目整体框架与全部基础功能，包括欢迎屏、功能选择、馆藏图书/图书视频/智能导读三大功能、RGB 灯效、状态机流转。

**对应文档**：第一章「项目概述」、第二章「硬件接线」、第五章「状态机与界面流转」

---

### 提示词 2：功能细化与稳定性要求

> 首页退出按钮放到右上角，
> 当人体红外传感器（接io1）检测到人，播放"欢迎来到智慧阅读角，开始享受你的阅读时光吧"，只有在欢迎屏幕时，检测到人才播放欢迎语，在其他界面不检测不播放。
> 灯带效果要动态的，
> 馆藏图书列表，作者字大一点，框大一点，不要重叠；显示文字介绍内容的时候要播语音，点击一次只播一遍，
> 图书视频很卡，返回后程序卡死，强制退出。
> 要生成日志文件，参考仓库中项目。

**实际效果**：调整了退出按钮位置、欢迎语播放逻辑（仅欢迎屏触发）、灯效改为动态、图书列表布局优化、语音播放单次触发；新增日志系统（参考仓库项目规范）；排查了图书视频播放卡死问题。

**对应文档**：第三章第 5 条「模块导入顺序陷阱」、第九章「配置参数」

---

### 提示词 3：OCR 识别卡死（初步排查 — 死锁方向）

> 语音识别好了，文字识别还是卡死。点击识别文字按钮卡死。
> ocr_recognize_async 内部会获取 self._lock，不能在 with self._lock 块内调用，
> 否则死锁（threading.Lock 不可重入）。请将状态检查放锁内，异步函数调用移到锁外。

**实际效果**：解决了 `threading.Lock` 不可重入导致的死锁，但 OCR 卡死问题仍未彻底解决（见提示词 4）。

**对应文档**：第三章第 3 条「threading.Lock 不可重入导致的死锁」

---

### 提示词 4：OCR 识别卡死（深入排查 — GIL 饥饿方向）

> 点击识别文字按钮，还是卡死。之前把 ocr_recognize_async 移到锁外了，但仍卡死。
> 注意：语音识别功能正常，只有 OCR 卡死。请深入分析 GIL 与 CPU 密集型操作的关系。
> 参考官方范例《文字识别播报器》用的 640x480，本项目用了 1280x720。

**实际效果**：定位到真正根因是 PaddleOCR 长时间持有 GIL 导致主线程饥饿。通过缩小送检帧到 640 宽 + 不暂停摄像头采集（保留 I/O 线程释放 GIL）+ 超时保护，彻底解决卡死。

**对应文档**：第三章第 2 条「OCR 卡死的真正根因：GIL 饥饿」、第三章第 4 条「摄像头后台采集与 OCR 资源解耦」

---

### 提示词 5：PIR 检测异常与启动慢

> 程序启动慢，启动后人体红外传感器检测好像不太正常。
> 日志显示：14:41:22 摄像头就绪，14:41:32 才 PIR 检测到人（延迟 10 秒）。
> 请参考《红色文化交互展示》中 PIRSensor 后台轮询 + board_lock 串行化串口访问的方案优化。

**实际效果**：定位到 ESP32 串口竞争（LightManager 的 ws2812Write 与主线程 digitalRead 并发）。通过 board_lock 串行化 + PIR 改后台线程轮询，PIR 响应恢复正常；同时修复了退出按钮重复日志问题。

**对应文档**：第三章第 1 条「ESP32 串口访问必须串行化」、第三章第 6 条「摄像头初始化放后台线程」、第三章第 7 条「退出按钮重复日志修复」

---

## 八、参考项目

| 参考项目 | 借鉴内容 |
| ---- | ---- |
| 《红色文化交互展示》（red_revolution_app.py） | PIRSensor 后台轮询 + board_lock 串行化模式 |
| 《文字识别播报器》 | OCR 初始化顺序、摄像头后台采集、text_recognition 导入陷阱 |
| 《唐诗宋词朗读器》 | 可中断语音播放、线程化播报模式 |
| 《视觉系统摄像头调用参考方案.md》 | 摄像头三种架构模式（本项目用模式3全托管）、Rockchip 兼容性补丁（LIBGL_ALWAYS_SOFTWARE、pygame 分段 init、mixer 单次 init/quit） |
| 《camera_vision_system_v3_API分析报告.md》 | V3 库 API 复核：本项目未启动 threaded_system.start_background_detection，只用 open_camera+capture_frame，cleanup 不需要 stop_background_detection |

---

## 九、配置参数

```python
WINDOW_W, WINDOW_H = 1920, 1080
PIR_PIN = GPIO_IO_01          # 人体红外传感器
RGB_PIN = GPIO_IO_02          # WS2812 RGB灯带
NUM_LEDS = 11                 # 灯珠数量
MENU_TIMEOUT = 30             # 功能选择界面无人操作超时(秒)
VOICE_USERNAME = '用户名'     # 好好搭搭账号（需替换）
VOICE_PASSWORD = '密码'       # 好好搭搭密码（需替换）
```

**摄像头配置**：`camera_id=-1`（自动检测），`width=1280, height=720`（预览），OCR 送检帧缩放到宽度 640。

---

## 十、常见问题排查

| 问题现象 | 可能原因 | 解决方法 |
| ---- | ---- | ---- |
| PIR 检测延迟/不响应 | 串口竞争（LED 与 PIR 并发访问） | 用 board_lock 串行化，PIR 改后台线程轮询 |
| 点击 OCR 按钮卡死 | GIL 饥饿（PaddleOCR 长时间持 GIL） | 缩小送检帧到 640 宽，不暂停摄像头采集 |
| 点击 OCR 按钮卡死 | threading.Lock 不可重入死锁 | 异步函数调用移到 with self._lock 块外 |
| 退出按钮日志重复 | handle_click 未 return，handle_events 未检测 running | 设置 running=False 后立即 return |
| OCR 报 'utils' is not a package | text_recognition 导入顺序错误 | text_recognition 必须在文件最顶部最先导入 |
| 摄像头启动慢阻塞界面 | open_camera 同步调用 | 放后台线程初始化，主线程立即进入欢迎屏 |
| 语音合成期间界面卡顿 | speak_async 在持锁时调用 | 锁内只更新状态，锁外调用 speak_async |
| pygame 启动段错误 / Mali 驱动崩溃 | Rockchip 平台 pygame.init() 一次性 init 子模块冲突 | LIBGL_ALWAYS_SOFTWARE=1 + pygame 分段 init + mixer 单次 init/quit（参照第 3 章第 8 条） |
| 视频第二次播放无声 / mixer 死锁 | mixer 反复 init/quit | mixer 生命周期内只 init/quit 一次，视频停止时仅 music.stop() |
| 摄像头画面偶发全黑/全白 | V4L2 偶发脏帧未过滤 | 采集线程用 frame.mean() 校验，mean∈[5,250] 为有效帧（参照第 3 章第 10 条） |
| 日志文件分散在多个文件 | 文件名带时分秒 | 改为 智慧阅读角_YYYYMMDD.log 追加模式（参照第 3 章第 9 条） |
| 模块导入失败直接崩溃 | 外部模块 try/except 缺失 | text_recognition/ESP32/vision_system 等导入用 try/except 容错（参照第 3 章第 11 条） |
| 扩展板未连接，退出时报 `'ESP32' object has no attribute 'device'` | `board.start()` 失败时 board 对象仍赋值但 `device` 未初始化；cleanup 只判断 `self.board` 非空就调 `ws2812Write` | 引入 `_board_connected` 标记 + `hasattr(self.board, 'device')` 三重检查（参照第 3 章第 12 条） |
| 欢迎屏快速退出后，日志才打印"摄像头打开失败"，顺序错乱；或 `camera_ok=False` 但设备其实正常 | 摄像头初始化线程引用没保存为成员变量，cleanup 不等待；内部无 `self.running` 检查 | 保存为 `self._camera_init_thread`；cleanup 中 `join(timeout=5.0)` 等待；`_init_camera_background` / `init_camera_open` 关键节点检查 `self.running`，日志措辞区分"已中止"与"打开失败"（参照第 3 章第 13 条） |

---

## 十一、参考文档对照

本次复核优化主要依据以下两份文档的约束：

| 文档 | 对应优化点 |
| --- | --- |
| 《视觉系统摄像头调用参考方案》第 7 章 Rockchip 兼容性补丁 | LIBGL_ALWAYS_SOFTWARE=1、pygame 分段 init、mixer 单次 init/quit |
| 《视觉系统摄像头调用参考方案》模式 3（全托管） | 使用 vision_system.open_camera + capture_frame，不混用 cv2 VideoCapture（原项目已符合，本次仅做核对） |
| 《camera_vision_system_v3_API分析报告》2.3.1 threaded_system 启停 | 本项目未启动 start_background_detection，cleanup 不需要 stop_background_detection（已核对） |
| 项目工程约定（logs/ 目录、YYYYMMDD 文件名、追加模式、帧校验 mean 代替 std） | 日志文件名规范、采集线程 mean 过滤全黑帧、frame.mean() 代替 gray.std() |

---

*文档版本：2.1*
*最后更新：2026年8月12日*
*适用环境：好搭AI派 / Ubuntu 20.04+ / Python 3.8+*

---

### 版本变更记录

| 版本 | 日期 | 变更内容 |
| ---- | ---- | ---- |
| v2.1 | 2026-08-12 | 修复 2 个稳定性问题并补充文档：① Bug #1 扩展板未连接时 cleanup 灯带关闭触发 AttributeError（引入 `_board_connected` 三重检查）；② 问题 #2 摄像头初始化线程用户退出后仍继续执行（成员变量保存引用 + cleanup join 等待 + 多层 `self.running` 检查 + 日志措辞区分"已中止/打开失败"）；同步更新第 3 章 §6/§12/§13、第 4 章线程表（新增第 6 个线程+清理顺序）、第 10 章 FAQ（新增 2 条） |
| v2.0 | 2026-08-11 | 整理项目工程约定：Rockchip 兼容补丁、YYYYMMDD.log 追加模式、帧 mean 校验、摄像头后台采集解耦、board_lock 串行化 PIR、模块导入容错等 11 条关键技术经验；新增线程架构、状态机、FAQ 等章节 |

