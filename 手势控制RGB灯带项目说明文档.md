# 手势控制RGB灯带项目说明文档

基于 USB 外接摄像头的手势识别程序：实时识别 7 种手势，根据不同手势驱动 IO1 WS2812 RGB 灯带（4 灯珠）显示不同灯效，1920×1080 界面同步显示摄像头画面、手势识别结果和灯效名称。
手势识别采用 Google Mediapipe Hands 方案（21 个手部关键点），经算法优化后各手势识别率均较高、基本无误。硬件控制严格遵循好搭AI派范例代码规范。

> 本项目经过三轮迭代优化：多线程架构解决画面延迟、归一化比值法解决大拇指误识别、判断顺序调整解决 thumb_up 失效。完整优化过程见第八章和第九章。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 手势识别 | Mediapipe Hands（21 关键点）+ 自定义手势判定规则（四指用距离法，大拇指用归一化比值法） |
| 摄像头采集 | OpenCV `VideoCapture`（设备节点路径方式打开，MJPG 编码） |
| 界面显示 | `pygame`，1920×1080，背景图 `images/1.jpg` + 半透明面板 |
| RGB 灯带控制 | `ESP32.ws2812Init` / `ws2812Write`（严格参照范例代码） |
| 多线程 | 采集线程 + 识别线程 + 主循环线程，三者分离避免阻塞 |

> 关键设计：采集线程只做 `cap.read()` 持续覆盖最新帧，识别线程取最新帧做 Mediapipe 处理。这样可避免 V4L2 内核缓冲区积压旧帧导致画面延迟。

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 实时手势识别 | 摄像头画面嵌入主窗口（1080×810），实时绘制手部 21 关键点和连接线 |
| 多手势支持 | 7 种手势 + 无手势状态，共 8 种状态切换 |
| 灯效驱动 | 7 种动态灯效（火焰/流光/呼吸/追逐/心跳/闪烁），由主循环按帧驱动 |
| 手势防抖 | 连续 3 帧识别到同一手势才切换灯效，避免误触 |
| 灯效对照表 | 右侧面板显示所有手势-灯效映射，当前项高亮显示 |
| 退出方式 | 界面右上角标题栏内「退出程序」按钮 / ESC 键 / 窗口关闭按钮 |
| 后台处理 | 采集线程与识别线程分离，主循环零阻塞 |
| 资源清理 | 退出时关闭摄像头、释放 Mediapipe 资源、熄灭灯带、关闭日志文件 |

## 三、手势-灯效映射

| 手势 | 灯效 | 说明 | 主题色 |
| --- | --- | --- | --- |
| 握拳 | 红色火焰 | 热情似火 | (255, 60, 30) |
| 张开手掌 | 彩虹流光 | 五彩缤纷 | (255, 100, 200) |
| 大拇指朝上 | 绿色呼吸 | 生机盎然 | (60, 200, 100) |
| 胜利V | 紫色流光 | 神秘优雅 | (160, 80, 220) |
| 食指指向 | 蓝色追逐 | 指引方向 | (60, 130, 255) |
| OK捏合 | 粉色心跳 | 爱心传递 | (255, 130, 180) |
| ILoveYou | 金色闪烁 | 闪耀光芒 | (255, 200, 50) |
| 无手势 | 灯光关闭 | 等待手势 | (150, 150, 150) |

映射表在 `手势控制RGB灯带.py` 顶部 `GESTURE_MAP` 中配置，可自由扩展。

## 四、项目结构

```markdown
haoda_AIPad/
├── 手势控制RGB灯带.py                # 本程序主入口
├── 手势控制RGB灯带项目说明文档.md    # 本文档
├── images/
│   └── 1.jpg                        # 背景图（1920×1080）
├── logs/                            # 日志目录（运行时自动创建）
│   └── 手势控制RGB灯带_YYYYMMDD.log # 按日期命名的运行日志（追加模式）
├── 字体文件/                         # 系统字体源文件（参考）
└── 好搭AI派学习手册.md               # 硬件 API 参考
```

### 资源文件说明

- `images/1.jpg`：背景图片，程序启动时加载。如缺失或加载失败，会自动生成浅蓝渐变背景，不影响运行。
- `logs/手势控制RGB灯带_YYYYMMDD.log`：运行日志，记录所有 `print` 输出（含异常堆栈），用于事后排查问题。每次运行追加写入，文件名按当天日期生成。

## 五、硬件接线

| 接口 | 设备 | 说明 |
| --- | --- | --- |
| IO1 (GPIO_IO_01) | WS2812 RGB 灯带 | 4 颗灯珠，需接上拉扩展模块 |
| ESP32 扩展板 USB 口 | 外接摄像头 | 系统识别为 `/dev/video40` 或 `/dev/video41` |

> 注意：好搭AI派右下角开关需拨到左侧以启用外设接口。

## 六、环境要求

- Python 3.8 及以上
- 依赖：
  - `pygame`（界面，好搭AI派自带）
  - `opencv-python`（提供 `cv2`，好搭AI派自带）
  - `numpy`（数组处理，好搭AI派自带）
  - `mediapipe`（手势识别，**需用户自行安装**：`pip install mediapipe`）
  - `ESP32`（硬件控制，好搭AI派自带）
- 外接 USB 摄像头
- WS2812 RGB 灯带 + 上拉扩展模块

## 七、运行

```bash
python3 手势控制RGB灯带.py
```

启动后：

1. 程序自动探测 `/dev/video41` 和 `/dev/video40`，打开第一个可用的摄像头
2. 将手对准摄像头做出手势，连续 3 帧确认后切换灯效
3. 点击界面右上角标题栏内「退出程序」按钮或按 ESC 退出

## 八、关键实现说明

### 1. 摄像头打开逻辑

复用 `人脸识别播放视频.py` 的探测方案，解决 USB 摄像头常见问题：

- **设备节点路径方式打开**：`cv2.VideoCapture('/dev/video41')`，避免整数索引越界
- **MJPG 编码**：`cap.set(cv2.CAP_PROP_FOURCC, cv2.VideoWriter_fourcc(*'MJPG'))`，解决默认格式导致的雪花/绿屏
- **SIGALRM 超时**：探测时设置 4 秒超时，打断卡在 `select()` 的 V4L2 设备
- **帧有效性检测**：通过 `gray.mean()` 检测全黑/全白帧（项目记忆：ARM 设备上 `std()` 计算开销过大，改用 `mean()`）

### 2. 手势识别算法

基于 Mediapipe Hands 输出的 21 个关键点，自定义判定规则。**大拇指与四指采用不同判定方法**，这是本项目识别率的关键。

#### 2.1 四指判定：距离法

食指/中指/无名指/小指用「指尖到手腕距离 > 指根到手腕距离」判断是否伸直：

```python
def is_finger_extended(landmarks, tip_idx, mcp_idx):
    """判断四指是否伸直：指尖到手腕距离 > 指根到手腕距离"""
    wrist = landmarks[0]
    tip = landmarks[tip_idx]
    mcp = landmarks[mcp_idx]
    tip_dist = math.hypot(tip.x - wrist.x, tip.y - wrist.y)
    mcp_dist = math.hypot(mcp.x - wrist.x, mcp.y - wrist.y)
    return tip_dist > mcp_dist
```

**优势**：不依赖手的朝向（无论手心朝向摄像头还是侧面都能判断），比单纯比较 y 坐标更稳健。

#### 2.2 大拇指判定：归一化比值法（关键优化）

大拇指骨骼特殊，横向活动范围大，**距离法对大拇指失效**：握拳时拇指横向搭在食指上，拇指尖(4)离手腕(0)的距离可能比拇指根(2)更远，于是被误判为"伸直"，导致握拳被识别成 thumb_up。

改用「拇指尖到食指根距离 / 手掌宽度」的归一化比值判断：

```python
def is_thumb_extended(landmarks):
    """大拇指是否伸直/张开。
    握拳时拇指搭在食指上，比值小（< 0.4）；
    拇指张开/朝上时，比值大（> 0.7）。"""
    palm_width = math.hypot(landmarks[5].x - landmarks[17].x,
                            landmarks[5].y - landmarks[17].y)
    tip_to_index = math.hypot(landmarks[4].x - landmarks[5].x,
                              landmarks[4].y - landmarks[5].y)
    ratio = tip_to_index / palm_width
    return ratio > 0.6
```

**为什么有效**：

- 手掌宽度（食指根5到小指根17）作为归一化基准，与手距摄像头远近、手的方向都无关
- 握拳时拇指搭在食指上，拇指尖离食指根很近 → 比值小 → 判为弯曲
- 拇指朝上时拇指张开，拇指尖离食指根远 → 比值大 → 判为伸直

#### 2.3 判断顺序：具体条件优先（关键优化）

`thumb_up`（四指全弯+拇指伸直）与 `fist`（四指全弯）的四指状态相同，**必须把更具体的 `thumb_up` 放在 `fist` 之前判断**，否则 thumb_up 永远被 fist 拦截：

```python
# 正确顺序：thumb_up 在 fist 之前
if thumb and not index and not middle and not ring and not pinky:
    return 'thumb_up'      # 四指全弯 + 拇指伸直（更具体，优先匹配）

if not index and not middle and not ring and not pinky:
    return 'fist'          # 四指全弯（走到这里说明拇指也是弯的）
```

**通用规则**：当两个手势的条件存在包含关系时，更具体（条件更多）的必须放前面优先匹配。

#### 2.4 OK 手势特殊处理

除判断三指伸直外，额外检测大拇指尖与食指尖的距离 `< 0.08`（归一化坐标），避免与 ILoveYou 混淆。

#### 2.5 完整判断顺序

```markdown
1. ok        拇指食指捏合 + 中指/无名指/小指伸直
2. thumb_up  四指全弯 + 拇指伸直      ← 必须在 fist 之前
3. fist      四指全弯                  ← 走到这里拇指必然也是弯的
4. open_palm 四指全伸直
5. victory   食指+中指伸直
6. point_up  只有食指伸直
7. iloveyou 拇指+食指+小指伸直
8. none      以上都不匹配
```

### 3. 多线程架构（解决画面延迟）

| 线程 | 职责 | 阻塞点 |
| --- | --- | --- |
| 主循环 | 事件处理 + 界面绘制 + 灯效驱动 | 无阻塞，30 FPS |
| 采集线程 | `cap.read()` 持续覆盖最新帧 | V4L2 阻塞读帧（本身等待下一帧） |
| 识别线程 | 取最新帧做 Mediapipe 处理 | Mediapipe 推理（约 50-100ms） |

**关键点**：采集线程总是覆盖 `self.raw_frame`，识别线程处理慢时自动跳过中间帧（降帧），从而保证显示的永远是最新画面，不会积压延迟。

### 4. RGB 灯效实现

参照范例代码 `2.扩展模块使用 4.RGB灯` 的 `wheel` 函数和 `ws2812Write` 调用方式：

- 灯效由主循环按帧驱动（`update_led_frame()`），单线程避免并发问题
- 每种灯效用 `led_frame` 全局计数器作为时间轴，通过 `math.sin` 产生呼吸/跳动效果
- 颜色计算后通过 `board.ws2812Write((LED_PIN), i, r, g, b)` 写入单颗灯珠

### 5. 界面半透明面板

```python
panel = pygame.Surface(self.cam_rect.size, pygame.SRCALPHA)
pygame.draw.rect(panel, (*PANEL_COLOR, 170), panel.get_rect(), border_radius=18)
```

- 使用 `pygame.SRCALPHA` 创建带 alpha 通道的 Surface
- 面板颜色 alpha 设为 170（0-255），背景图片可透过面板显示
- 同时保持文字清晰可读

### 6. Rockchip 平台兼容性补丁

为避免 Rockchip 平台（RK3566/RK3568）上 Pygame 与 OpenCV 的底层驱动冲突（GPU 驱动加载失败、音频驱动与 V4L2 死锁），程序开头加入三处补丁（参考《视觉系统摄像头调用参考方案》第 7 章）：

```python
import os
# 1. 强制 libGL 使用软件渲染，避免 rockchip 平台 GPU 驱动加载失败
os.environ.setdefault('LIBGL_ALWAYS_SOFTWARE', '1')

# 2. 导入顺序：先 pygame 再 cv2（pygame 先导入让 SDL 接管视频子系统）
import pygame
import cv2

# 3. 分段初始化（不调用 pygame.init()），避免 pygame.mixer 导致原生崩溃
pygame.display.init()
pygame.font.init()
```

**作用说明**：
- `LIBGL_ALWAYS_SOFTWARE=1`：强制 OpenGL 使用软件渲染，绕过 Mali GPU 驱动缺陷
- import 顺序：pygame 先导入让 SDL 接管视频子系统，cv2 不会再重复初始化 OpenGL
- 分段初始化：跳过音频模块，防止音频驱动与摄像头 V4L2 驱动产生死锁

### 7. 日志输出机制

程序启动时在 `logs/` 目录下创建按日期命名的日志文件，所有 `print` 输出（含异常堆栈）同时写入控制台和日志文件，便于事后排查问题。

```python
# 日志文件：logs/手势控制RGB灯带_YYYYMMDD.log，追加模式，块缓冲
_LOG_DIR = 'logs'
_LOG_FILE = os.path.join(_LOG_DIR, '手势控制RGB灯带_%s.log' %
                         _datetime.datetime.now().strftime('%Y%m%d'))
_debug_log_fp = open(_LOG_FILE, 'a', encoding='utf-8', buffering=-1)

# TeeStdout 包装 stdout/stderr，print 同时写入控制台和文件
sys.stdout = _TeeStdout(sys.stdout)
sys.stderr = _TeeStdout(sys.stderr)
```

**设计要点**：
- **目录与命名**：`logs/` 目录、`手势控制RGB灯带_YYYYMMDD.log`，符合项目日志规范
- **追加模式（`'a'`）**：同一天多次运行不会覆盖历史日志，每次运行写入 `======== 时间 运行开始 ========` 分隔
- **块缓冲（`buffering=-1`）**：避免高频 `print` 阻塞主循环
- **TeeStdout 包装**：无需修改业务代码中的 `print` 语句，自动双写
- **退出关闭**：`run()` 退出清理末尾关闭文件指针，防止日志丢失

### 8. 帧缩放方式

使用 `pygame.transform.scale`（非 `smoothscale`）进行摄像头帧缩放，降低 CPU 开销（项目记忆：smoothscale 对摄像头帧缩放有显著 CPU 开销）。

## 九、开发提示词（可复用经验）

以下是本项目开发时使用的提示词，可作为后续好搭AI派项目开发的模板参考：

### 原始提示词

```markdown
写一个好搭AI派程序，USB外接摄像头识别手势控制外接RGB灯带（接io1，4灯珠），
识别不同的手势RGB灯带显示不同效果；好搭AI派上要有界面1920*1080，
显示手势识别结果和灯带效果名称，界面上要有退出程序按钮。
程序涉及硬件部分严格参照本地文件夹中的好搭AI派学习手册和好搭AI派范例代码，
不涉及硬件的部分只需要符合python规范，可参考本地文件夹中的其他项目；
我已安装mediapipe库，如果需要安装其他库提示我。
```

### 提示词设计要点

1. **明确硬件接口**：指定 IO 口编号和外设规格（IO1、4灯珠），避免后续返工
2. **明确界面要求**：指定分辨率（1920×1080）和必备元素（手势结果、灯效名称、退出按钮）
3. **区分软硬件规范**：
   - 硬件部分「严格参照」学习手册和范例代码 → 保证 API 调用正确
   - 非硬件部分「符合 Python 规范」→ 给算法和界面留出自由发挥空间
4. **引用本地资源**：明确指向本地文件夹中的学习手册、范例代码和其他项目，让 AI 能检索到正确的 API 和风格
5. **主动声明已装库**：告知已安装 mediapipe，避免 AI 重复提示安装；同时要求「需要安装其他库提示我」，让依赖管理可控

### 优化迭代提示词

本项目经过三轮优化迭代，每轮提示词都体现了不同的调试技巧。

#### 第一轮：性能与界面优化

```md
功能基本正常。画面有延迟；背景改成images/1.jpg，背景框半透明。
```

**优化要点**：

- **先肯定再提问题**：「功能基本正常」让 AI 知道整体方向正确，只需局部优化
- **问题定位精准**：「画面有延迟」直接指向性能问题，引导 AI 分析根因（V4L2 缓冲区积压）
- **需求具体可执行**：「背景改成 images/1.jpg」给出确切文件名；「背景框半透明」明确效果
- **一条提示词多个改动**：合理批量提出相关优化，减少往返次数

#### 第二轮：手势误识别优化（提问式）

```md
存在误识别手势的问题，比如拳头会被识别成拇指向上，
是固有问题，还是可以通过程序优化？
```

**优化要点**：

- **用提问代替指令**：「是固有问题，还是可以通过程序优化？」引导 AI 分析根因而非盲目修改
- **给出具体误识别案例**：「拳头会被识别成拇指向上」让 AI 能定位到具体的手势冲突
- **区分问题性质**：先确认是算法问题还是框架限制，再决定优化方向
- AI 分析出大拇指骨骼特殊、距离法失效的根因，改用归一化比值法解决

#### 第三轮：回归 Bug 修复（精准描述）

```md
修改后的程序各手势的识别率大幅提高，基本无误，
唯一的问题是大拇指朝上完全无法识别，全部识别成握拳。
```

**优化要点**：

- **先肯定改进效果**：「识别率大幅提高，基本无误」确认上一轮优化方向正确
- **精确定位回归问题**：「大拇指朝上完全无法识别，全部识别成握拳」明确指出哪个手势失效、被误判为什么
- **"完全无法"+"全部识别成"**：用绝对语气强调是 100% 失效而非偶发，提示是逻辑错误而非阈值问题
- AI 立刻定位到判断顺序问题（fist 拦截了 thumb_up），调整顺序后修复

#### 三轮迭代的经验总结

1. **性能问题先描述现象**：「画面有延迟」→ AI 自行分析根因
2. **算法问题用提问式**：「是固有还是可优化？」→ 引导深度分析
3. **回归 Bug 精准描述**：哪个手势失效 + 被误判为什么 → 快速定位
4. **每轮都先肯定效果**：让 AI 知道大方向正确，只需局部调整，避免推倒重来
5. **一次只解决一类问题**：性能/算法/逻辑分开迭代，便于定位每轮改动的效果

### 后续项目可复用的提示词模板

```md
写一个好搭AI派程序，[功能描述]。
硬件接线：[IO口编号]接[外设规格]；
好搭AI派上要有界面1920*1080，显示[界面元素]，界面上要有退出程序按钮。
程序涉及硬件部分严格参照本地文件夹中的好搭AI派学习手册和好搭AI派范例代码，
不涉及硬件的部分只需要符合python规范，可参考本地文件夹中的其他项目；
我已安装[已装库]库，如果需要安装其他库提示我。
```

## 十、常见问题

### Q1：程序启动后提示"摄像头打开失败"

**A**：检查以下几点：

1. USB 摄像头是否插入 ESP32 扩展板的 USB 口
2. 右下角开关是否拨到左侧（USB 摄像头模式）
3. 终端执行 `ls /dev/video*` 确认设备节点是否存在
4. 确认没有其他程序占用摄像头

### Q2：程序启动后立即崩溃（段错误）

**A**：通常是 Rockchip 平台兼容性问题，确认以下几点：

1. 程序开头已设置 `LIBGL_ALWAYS_SOFTWARE=1` 环境变量
2. 未调用 `pygame.init()`，改用 `pygame.display.init()` + `pygame.font.init()` 分段初始化
3. import 顺序为先 `pygame` 再 `cv2`，不可颠倒

### Q3：画面卡顿

**A**：按以下顺序尝试：

1. 确认 `cvframe_to_surface` 使用 `pygame.transform.scale` 而非 `smoothscale`
2. 确认主循环帧率为 30 FPS
3. 检查终端日志（`logs/手势控制RGB灯带_YYYYMMDD.log`）是否有异常循环

### Q4：日志文件在哪里查看

**A**：日志文件位于程序同目录下的 `logs/` 文件夹，文件名格式为 `手势控制RGB灯带_YYYYMMDD.log`（如 `手势控制RGB灯带_20260811.log`）。程序崩溃或异常退出后，可在日志文件中查看完整的 `print` 输出和异常堆栈，用于事后排查。同一天多次运行会追加到同一文件，以 `======== 时间 运行开始 ========` 分隔。

### Q5：为什么不再检测雪花噪声帧

**A**：旧版本通过 `frame.std()` 检测雪花噪声帧，但在 ARM 设备（rockchip RK 平台）上 `std()` 计算开销过大，会拖慢采集线程。现改为 `gray.mean()` 仅检测全黑/全白帧（阈值 `< 10` 或 `> 245`），在保证基本有效性的前提下降低 CPU 开销。

### Q6：退出按钮在哪里

**A**：退出按钮位于界面右上角标题栏内（`WIDTH-280, 30, 240×70`），不在底部栏。点击该按钮或按 ESC 键均可退出程序。底部栏仅显示操作提示信息。

## 十一、扩展方向

- 增加更多手势（如石头剪刀布的三种手势完整识别）
- 支持双手同时识别，分别控制两条灯带
- 增加语音播报当前手势名称
- 将手势映射为智能家居控制指令（通过 MQTT 发送）
- 增加手势学习模式，支持用户自定义手�