# 好搭AI派 · 系统环境与非视觉官方库探测报告 v1

> 探测日期：2026-08-14 08:54 ~ 08:55（设备本地时间）
> 关键版本决策追加：2026-08-14 22:00（针对 Python / pygame / OpenCV 三项常见疑问的结论与理由）
> 核心硬件勘误：2026-08-15（实测 SoC 为 RK3588S，非 RK3566；详见 1.1 节）
> 探测脚本：[探测_系统环境.py](file:///d:/笔记本同步/好搭AI派资料/探测_系统环境.py)、[探测_非视觉官方库.py](file:///d:/笔记本同步/好搭AI派资料/探测_非视觉官方库.py)、[探测_硬件规格.py](file:///d:/笔记本同步/好搭AI派资料/探测_硬件规格.py)
> 原始日志：`logs_探测_系统环境_20260814.txt`、`logs_探测_非视觉官方库_20260814.txt`、`logs/探测_硬件规格_20260815.log`
> 适用设备：好搭AI派（RK3588S / haoda-pi / device-tree：rockchip,rk3588s-tablet-f12-v11）
> 文档版本：v1.2

---

## 目录

1. [硬件与系统全景](#1-硬件与系统全景)
2. [软件环境（Python 与三方库）](#2-软件环境python-与三方库)
3. [ESP32 模块完整 API 参考](#3-esp32-模块完整-api-参考)
4. [Line_Sensor 巡线传感器模块](#4-line_sensor-巡线传感器模块)
5. [voice_api 语音 AI 模块](#5-voice_api-语音-ai-模块)
6. [audio_recorder 录音模块](#6-audio_recorder-录音模块)
7. [audio_player 播放模块](#7-audio_player-播放模块)
8. [text_recognition 文字识别模块](#8-text_recognition-文字识别模块)
9. [关键注意事项与风险](#9-关键注意事项与风险)
10. [附录：V4L2 设备 / 音频设备 / 串口设备完整清单](#10-附录设备清单)

---

## 1. 硬件与系统全景

### 1.1 核心硬件

| 项目 | 实测值 | 备注 |
|------|--------|------|
| 处理器 | **8核 ARM Cortex-A76 + Cortex-A55 大小核异构**：processor 0-3 = A55 (CPU part 0xd05)，processor 4-7 = A76 (CPU part 0xd0b)；lscpu 显示最大主频 2.352 GHz | **v1.2 勘误**：v1.1 报告写"8核全 A55（3座×2核）"为误判，原因是 lscpu 在 big.LITTLE 平台仅显示第一个在线核型号。实测 device-tree compatible = `rockchip,rk3588s-tablet-f12-v11`，SoC 是 **RK3588S**（不是 RK3566）。A76 大核专门承担大模型推理、网络、多媒体、AI 计算；A55 小核承担常规负载与 UI |
| GPU | Mali-G610 MP4（Bifrost 驱动栈，dmesg 报告 Kernel DDK version g18p0） | RK3588S 标配。支持 OpenGL ES 3.2、OpenCL 2.2、Vulkan 1.1 |
| NPU | RK3588S 内置，设备节点 `/sys/class/devfreq/fdab0000.npu` 存在，compatible = `rockchip,rk3588-rknpu`，运行频率 1 GHz | **算力 6 TOPS @ INT8**（RK3588S 标配 3 核 NPU），支持 INT4/INT8/INT16/FP16 运算。运行时库 `librknnrt.so` 已装；Python 绑定 `rknnlite2` 未装（详见第9章） |
| 内存 | 7.7 GiB（可用 6.7 GiB） | **无 Swap 分区**，大程序需注意内存上限 |
| 存储 | 227 GB eMMC / NVMe，已用 13 GB，可用 **205 GB** | 空间非常充裕 |
| 显示（面板） | 原生 **8 寸电容触摸屏，1200×1920 竖屏**（DSI-1 通道） | **UI 设计尺寸按 1920×1080 横屏**（用户选择，系统 X11 层做旋转映射，非特殊指定不做全屏，不考虑黑边问题） |
| USB 摄像头 | `/dev/video40`、`/dev/video41`、`/dev/video42`（uvcvideo 驱动） | USB2.0 Camera，外接 ESP32 扩展板 USB 口；实际设备号可能为 40/41/42 之一，**不能只检测 40** |
| 板载摄像头接口 | video0~9（rkcif-mipi-lvds2）、video10~21（rkcif-mipi-lvds3）、video22~39（rkisp ISP节点） | 共 40 个内部 MIPI/ISP 节点，其中 rkcif 是 Camera Interface，rkisp 是 ISP 处理管道 |
| 音频编解码器 | ES8323，card 0 | 播放+录音共用一路硬件 |
| ESP32 连接 | 内部 UART `/dev/ttyS9` | 非 USB 转串口，设备列表中无 ttyUSB* / ttyACM* |
| 网络 | WiFi 5（802.11ac，2.4G+5G 双频）模组 AP6275P / BCM43752，wlan0；蓝牙 5.0（默认 Soft blocked，需 `rfkill unblock` 启用） | v1.2 勘误：v1.1 仅记录"WiFi 2.4G"，实际为 Wi-Fi 5 + 5GHz 可用，外网连通（baidu/aliyun TCP 80 通），无 HTTP(S) 代理 |

### 1.2 操作系统

| 项目 | 实测值 |
|------|--------|
| 发行版 | Ubuntu 20.04.6 LTS (Focal Fossa) |
| 内核 | Linux 5.10.160 SMP aarch64，编译日期 2026-04-23 |
| 架构 | aarch64 / 64-bit ELF（支持 32/64-bit 指令） |
| 主机名 | haoda-pi |
| 工作目录 | `/home/cxdz/jupyter/user`（脚本所在），用户主目录 `/home/cxdz`（5.1 GB） |

---

## 2. 软件环境（Python 与三方库）

### 2.1 Python 运行时（硬约束）

```
Python 3.8.10 (default, Mar 18 2025, 20:04:55)  [GCC 9.4.0]
- 可执行路径: /usr/bin/python3
- 64-bit: True
- 无其他 Python 版本（仅 python2.7 / python3.8）
```

**⚠️ 硬约束：所有新安装的三方库必须支持 Python 3.8。**

### 2.2 关键三方库状态

#### ✅ 已安装且可正常导入（17 个）

| 库 | 版本 | 典型用途 | 备注 |
|----|------|---------|------|
| pygame-ce | **2.5.2**（SDL 2.30.8） | UI / 音频播放 | 是 pygame-ce 社区版，非原版 |
| cv2 (OpenCV) | **5.0.0** | 图像处理 | ⚠️ 大版本 5，非 4.x，API 有变更可能 |
| numpy | **1.24.4** | 数值计算 | 已废弃 `np.bool` / `np.int` 等别名 |
| PIL (Pillow) | 10.4.0 | 图像读写 | |
| mediapipe | **0.10.9** | 手势 / 姿态 / 人脸网格 | 手势+姿态已项目级验证 |
| torch | **2.4.1** | PyTorch 推理 | 可直接 CPU 推理 Torch 模型 |
| scipy | 1.10.1 | 科学计算 | |
| scikit-learn | 1.3.2 | 机器学习 | |
| baidu-aip | 4.16.13 | 百度云人脸/表情/OCR | |
| requests | 2.32.3 | HTTP 客户端 | 天气 API 等 |
| paho.mqtt | 3.5 级 | MQTT 物联网 | 已项目级验证（订阅端） |
| pyserial | 3.5 | 串口通信 | |
| dt-apriltags | **3.1.7** | AprilTag 识别 | ✅ **意外发现**：已预装，无需经 V3 |
| datasets | 3.1.0 | HuggingFace 数据集 | |
| Django | 2.2.12 | Web 框架 | |
| Flask | 1.1.1 | Web 轻框架 | |
| Cython | 3.1.2 | C 扩展编译 | 编译 dlib 等需要 |

#### ❌ 未安装（6 个）

| 库 | 状态 | 需要时的安装方式 | 用途 |
|----|------|-----------------|------|
| paddleocr / paddle | 未装 | `pip install paddleocr paddlepaddle` — ARM 上编译复杂，**text_recognition 模块自带 PPOCR 已够用，不建议重复装** | OCR（独立于官方库） |
| ultralytics | 未装 | `pip install ultralytics==8.0.228`（最后支持 Python 3.8 的版本） | YOLOv8 自定义目标检测 |
| dlib | 未装 | `pip install dlib==19.24.0`（需本地编译，ARM 上耗时 ~30 分钟且需要 Cython + 内存充足） | 人脸对齐/检测（face_recognition 依赖） |
| face_recognition | 未装 | `pip install face_recognition`（依赖 dlib） | 跨设备人脸识别特征向量 |
| rknnlite2 / rknn | 未装 | ⚠️ 必须匹配设备 RKNN 工具链版本；librknnrt.so 已装，找 Rockchip 官方对应 whl | NPU 加速自定义 RKNN 模型推理 |
| onnx / onnxruntime | 未装 | `pip install onnx onnxruntime==1.16.3`（最后支持 Py3.8 的 ort） | ONNX 模型推理 |

#### ⚠️ 有问题（1 个）

| 库 | 问题 | 修复方式 |
|----|------|---------|
| pandas | `import pandas` 报 `AttributeError: module 'numpy' has no attribute 'bool'` | `pip install -U pandas==2.0.3`（兼容 numpy 1.24 的最后版本）。**如果不用 pandas 可不处理** |

### 2.3 环境变量（关键项）

| 变量 | 值 | 说明 |
|------|----|------|
| LIBGL_ALWAYS_SOFTWARE | **未设置** | ⚠️ **必须在每个脚本开头设置** `os.environ['LIBGL_ALWAYS_SOFTWARE']='1'`，否则 rockchip GPU driver 会崩溃 |
| DISPLAY | `:0` | X11 显示正常 |
| PYTHONPATH | `/home/cxdz/jupyter/lib` | **好搭 AI 派官方库（7 个 .so 文件）都放在此目录** — camera_vision_system_v3 / ESP32 / Line_Sensor / voice_api / audio_recorder / audio_player / text_recognition |
| LD_LIBRARY_PATH | cv2 lib64 + `~/manager/_internal` | |

### 2.4 官方库位置（全部是 C 扩展 .so）

```
/home/cxdz/jupyter/lib/
├── camera_vision_system_v3.cpython-38-aarch64-linux-gnu.so
├── ESP32.cpython-38-aarch64-linux-gnu.so
├── Line_Sensor.cpython-38-aarch64-linux-gnu.so
├── voice_api.cpython-38-aarch64-linux-gnu.so
├── audio_recorder.cpython-38-aarch64-linux-gnu.so
├── audio_player.cpython-38-aarch64-linux-gnu.so
└── text_recognition.cpython-38-aarch64-linux-gnu.so
```

> 7 个官方库全部可导入 ✅。注意 text_recognition 导入时会有 `utils.operators` 的警告，不影响实际使用。

### 2.5 🔴 三项关键版本决策（2026-08-14 结论：全部维持现状不变）

> 以下三项是项目开发中最常被问到的"要不要升/装"问题，结论全部为**维持现状，不做任何改动**。理由如下，每条都有三层现实约束 + 替代方案覆盖验证。

#### 2.5.1 Python 版本：**不要升级，维持 3.8.10**

| 维度 | 内容 |
|---|---|
| **结论** | ❌ **不升**。维持 `/usr/bin/python3 = 3.8.10` 不变 |
| **约束 1：操作系统绑定** | 设备是 **Ubuntu 20.04.6 LTS**，`apt` / `dpkg` / `network-manager` / `udev` / `timedatectl` 等大量系统组件全部依赖 python3=3.8。**一旦替换系统 python3 → apt 直接崩，无法装任何软件包**，只能重刷固件 |
| **约束 2：官方库 C 扩展绑定** | 7 个官方库（V3 视觉/ESP32/Line_Sensor/voice_api/audio_recorder/audio_player/text_recognition）的文件名都是 `XXX.cpython-38-aarch64-linux-gnu.so`，**编译时绑定 CPython 3.8 ABI**。升到 3.9+ → 所有官方库 `ImportError: module format not recognized`，视觉系统、语音、OCR、ESP32 硬件控制集体失效 |
| **约束 3：NPU 工具链版本** | RK3588S 的 `librknnrt.so`（7.7MB）与配套 rknnlite2 Python 绑定，与 RKNN 工具链版本 + Python 版本**严格一一对应**。现有工具链只出了 Python 3.8 whl，升版本后装不到对应 NPU 绑定，自编译需整套 Rockchip SDK |
| **因 3.8 而受限的库** | pygame-ce ≤ 2.5.2、ultralytics ≤ 8.0.228、onnxruntime ≤ 1.16.3、pandas ≤ 2.0.3 —— **对当前需求无影响**（详见 2.2 节状态表备注） |
| **如果未来必须升** | 用 **pyenv 本地多版本隔离**（`~/.pyenv` 下单独编译 3.9/3.10，虚拟环境跑新项目），**绝对不要动 `/usr/bin/python3`** |

#### 2.5.2 pygame vs pygame-ce：**已装 pygame-ce 2.5.2，不要额外装 pygame，也不要升 pygame-ce**

| 维度 | 内容 |
|---|---|
| **结论** | ✅ **维持现状**：pygame-ce 2.5.2（SDL 2.30.8）。**不要 `pip install pygame`**，也**不要升级 pygame-ce 到 2.5.4+** |
| **pygame 与 pygame-ce 的关系** | pygame-ce 是**原 pygame 核心开发者**接手的社区维护版（Community Edition），是目前的主线与维护线；**原版 pygame 已停止更新**。两者 import 语句完全相同（都是 `import pygame`），但 **pip 包名互斥，必须二选一，不能同时装**（同时装会互相覆盖文件，导致导入崩溃） |
| **不要额外装 pygame 的原因** | 当前已装 pygame-ce 2.5.2，所有项目正常运行。`pip install pygame` 会与 pygame-ce 冲突，触发包覆盖与段错误。正确的切换命令是 `pip uninstall pygame` 再 `pip install pygame-ce`，但这里不需要切换 |
| **不要升 pygame-ce 到 2.5.4+ 的原因** | pygame-ce **2.5.4 起已放弃 Python 3.8 支持**（pygame-ce 2.5.6 Release Notes 明确：最后一个支持 Python 3.9 的版本，PyPy 3.9/3.10 已剔除）。而设备锁 Python 3.8，装新版 pygame-ce 会提示 `requires-python >=3.9`，强行 `--no-deps` 安装会出现 C 扩展 ABI 不匹配 |
| **2.5.2 vs 2.5.x 新版的能力差距** | 新版主要增加：`draw.flood_fill`、`transform.pixelate`、`Color.from_hex`、`Sound.copy`、厚 aaline、`geometry` API 等。**好搭AI派所有项目只用 display/font/surface 的基础绘制**，这些新增能力完全用不到 |

#### 2.5.3 OpenCV 5.0 缺失的 8 个子模块：**不需要额外装 opencv-contrib-python**

| 维度 | 内容 |
|---|---|
| **结论** | ❌ **不装**。维持 `opencv-python 5.0.0` 现状，缺失的 8 个子模块（tracking / video / videoio / photo / stitching / calib3d / features2d / objdetect）**全部有替代方案** |
| **8 个子模块与替代方案对照表** | **tracking** → V3 色块/人脸/物体追踪 + MediaPipe 物体锁定；**video** → 当前无运动分析需求；**videoio** → `cv2.VideoCapture` / `cv2.imwrite` 不在此模块中，实测全部正常可用；**photo** → 无 HDR/去噪需求；**stitching** → 无图像拼接需求；**calib3d** → `aruco` 子模块可用（含标定） + dt-apriltags 自带位姿估计 + V3 pose_detection；**features2d** → `xfeatures2d` 子模块可用（含 SIFT）；ORB/FAST 通常在 `cv2` 主命名空间下存在；**objdetect** → 人脸用 MediaPipe/V3 face_recognition、行人用 MediaPipe Pose、二维码用 V3 qr_code 检测器 |
| **为什么不能装 opencv-contrib-python** | 1) **版本必须严格一致**：contrib 版本必须与 5.0.0 完全相同，否则 ABI 冲突；2) **arm64 + Python 3.8 + 5.x 的 whl 极可能不存在**：OpenCV 5 仍是预发布状态，contrib 包对 aarch64 + cp38 的预编译 wheel 通常滞后于主包几个月甚至没有，`pip install` 会触发本地编译（30+ 分钟，大概率失败）；3) **必须二选一卸载**：opencv-python 与 opencv-contrib-python 互不兼容，必须 `pip uninstall opencv-python` 再装 contrib，破坏当前所有已验证的 cv2 代码 |
| **正确的决策流程**（真需要某个函数时） | 1) 先测试 `cv2.QRCodeDetector()` / `cv2.solvePnP()` / `cv2.ORB_create()` 等函数是否在**主命名空间**下可用（OpenCV Python 绑定会把核心函数 flat 到顶层，不严格按子模块划分）；2) 若真不可用，用上面的替代方案实现；3) 只有当上述两步都失败且替代方案也没有时，才考虑把整个 opencv-python **降级**到 `opencv-contrib-python==4.10.0.84`（最后一个对 cp38/aarch64 有 whl 的版本），同时注意 4→5 的 API 差异（如 `findContours` 3返回值 vs 2返回值） |

---

## 3. ESP32 模块完整 API 参考

> 导入方式：`from ESP32 import ESP32, GPIO_IO_01, ...`
> 或：`from ESP32 import *`（注入 35 个名称，含所有常量、类、time/threading/math 等工具模块）

### 3.1 引脚常量

| 类别 | 常量 | 值 | 数量 |
|------|------|----|------|
| 数字 IO 口 | `GPIO_IO_01` ~ `GPIO_IO_08` | 1 ~ 8 | 8 个 |
| 模拟输入口 | `ADC_IO_01` ~ `ADC_IO_08` | 1 ~ 8 | 8 个 |
| 板载按键 | ~~`GPIO_BUTTON = 0`~~ | **忽略**（无实体按键） | - |

### 3.2 类实例化与生命周期

```python
board = ESP32()                  # 实例化（自动 init ESP32）
board.start(port=None)           # 打开串口并启动（port=None 时自动检测）
board.safe_operation(op, *args)  # 安全操作：带自动重试(最多3次,超时2s)
board.close()                    # 关闭连接
board.reset_device()             # 重置设备
board.restart_device()           # 重启设备
board.autoDetectPort()           # 自动检测 ESP32 串口
board.listAvailablePorts()       # 列出所有可用串口
board.check_device_response(timeout=1.0)  # 检查设备响应
```

**内置稳定性保障**（无需自己实现）：
- `__max_retry_count = 3`
- `__sync_timeout = 2.0s`
- `safe_operation()` 统一异常恢复

### 3.3 GPIO / PWM / ADC（基础）

| 方法 | 签名 | 说明 |
|------|------|------|
| digitalWrite | `digitalWrite(self, pin, level)` | 设置数字电平。level=1 高，level=0 低 |
| digitalRead | `digitalRead(self, pin, timeout=None)` | 读数字电平，0/1；返回结果 |
| digitalReadCallback | `digitalReadCallback(self, pin, callback)` | **异步版**：callback 收到结果时触发 |
| analogWrite | `analogWrite(self, pin, pwm)` | 输出 PWM（相当于 PWM 功能）。pwm 范围待实际硬件验证（通常 0~1023） |
| analogRead | `analogRead(self, pin, timeout=None)` | 读模拟值（12 位 ADC，预计范围 0~4095） |
| analogReadCallback | `analogReadCallback(self, pin, callback)` | **异步版** |

### 3.4 预置传感器（同步 + 异步回调）

| 传感器 | 同步方法 | 异步 Callback 方法 | 返回值预期 |
|--------|---------|-------------------|-----------|
| DHT11 温湿度 | `dhtReadTemperature(pin, timeout=None)` | `dhtReadTemperatureCallback(pin, callback)` | ℃（float），实际精度 ±1~2℃ |
| | `dhtReadHumidity(pin, timeout=None)` | `dhtReadHumidityCallback(pin, callback)` | %RH（float） |
| DS18B20 防水温度 | `ds18b20Read(pin, timeout=None)` | `ds18b20ReadCallback(pin, callback)` | ℃（float） |
| 超声波测距 | `ultrasonicRead(pin, timeout=None)` | `ultrasonicReadCallback(pin, callback)` | 距离（cm？mm？待实际硬件验证） |
| BMP280 气压 | `bmp280ReadPress(sck_pin=15, sda_pin=16, timeout=None)` | `bmp280ReadPressCallback(sck_pin=15, sda_pin=16, callback=None)` | Pa / hPa？待验证 |
| BMP280 温度 | `bmp280ReadTemperature(sck_pin=15, sda_pin=16, timeout=None)` | `bmp280ReadTemperatureCallback(sck_pin=15, sda_pin=16, callback=None)` | ℃ |

> 🔴 **BMP280 是未文档化的隐藏功能**，通过 I2C 接口（默认 sck=GPIO15, sda=GPIO16）。

### 3.5 执行器

| 方法 | 签名 | 说明 |
|------|------|------|
| servo | `servo(self, pin, angle)` | 舵机控制，angle=0~180°（待验证实际范围） |
| ws2812Init | `ws2812Init(self, pin, num)` | 初始化 WS2812 灯带，num=灯珠数量 |
| ws2812Write | `ws2812Write(self, pin, num, r, g, b)` | 写入颜色，r/g/b = 0~255 |
| motor_MA | `motor_MA(self, speed)` | 电机 A 通道速度，预计 -1023~1023（正负=方向） |
| motor_MB | `motor_MB(self, speed)` | 电机 B 通道速度 |

### 3.6 I2C 通信接口（🔴 重大未文档化功能，6 方法）

```python
board.i2c_init(bus, scl, sda)                     # 初始化 I2C 总线，指定 SCL/SDA 引脚
board.i2c_scan(bus, timeout=None)                 # 扫描总线上的设备地址（返回列表）
board.i2c_writeto(bus, addr, buf)                 # 向 addr 写入字节数据 buf
board.i2c_readfrom(bus, addr, nbytes, timeout=None)  # 从 addr 读 nbytes 字节
board.i2c_writeto_mem(bus, addr, memaddr, buf)        # 向 addr 的 memaddr 寄存器写数据
board.i2c_readfrom_mem(bus, addr, memaddr, nbytes, timeout=None)  # 读 addr 的 memaddr 寄存器 nbytes
```

**可驱动的 I2C 设备举例：** OLED SSD1306/SH1106 显示屏、AT24Cxx EEPROM、BH1750 光照、AHT20 温湿度、MPU6050 六轴、PCF8563 RTC 时钟、VL53L0X 激光测距等。

### 3.7 UART 串口通信接口（🔴 重大未文档化功能，5 方法）

```python
board.uart_init(uart_id, baudrate=115200, bits=8, parity=None, stop=1, tx=None, rx=None, timeout=10, flow=0)
board.uart_deinit(uart_id)                         # 关闭串口
board.uart_any(uart_id, timeout=None)              # 返回缓冲区中可读字节数
board.uart_read(uart_id, nbytes=1, timeout=None)   # 读 nbytes 字节
board.uart_write(uart_id, data)                    # 写数据（bytes/bytearray）
```

**可驱动的 UART 设备举例：** NEO-6M GPS 模块、HC-05/HC-08 蓝牙模块、SIM800C GPRS、PZEM-004T 电能表、其他 MCU。

### 3.8 数据打包工具（自定义协议用）

```python
# 序列化（Python值 → bytes）
board.char2byte(cval)       # 字符 → 1字节
board.short2bytes(sval)     # 16位短整型 → 2字节（大/小端待验证）
board.long2bytes(lval)      # 32位长整型 → 4字节
board.float2bytes(fval)     # 32位浮点数 → 4字节

# 反序列化（从ESP32回复buffer中解析）
board.readBytes(position)   # 读字节数组
board.readShort(position)
board.readLong(position)
board.readFloat(position)
board.readString(position)
```

---

## 4. Line_Sensor 巡线传感器模块

> 独立模块：`import Line_Sensor`（ESP32 模块**内部不存在**此子模块，必须单独 import）
> 底层通过 I2C 通信，固定地址 `Line_Sensor.Line_Sensor_ADDRESS = 82`。

### 4.1 构造

```python
from Line_Sensor import Line_Sensor
ls = Line_Sensor()               # 初始化（需确保已连接 ESP32 并 start）
```

### 4.2 通道常量（4 路传感器）

| 通道 | 寄存器类型 | 左2 | 左1 | 右1 | 右2 | 全部 |
|------|-----------|-----|-----|-----|-----|------|
| AD 原始值 | AD_* | AD_L2=3 | AD_L1=5 | AD_R1=7 | AD_R2=9 | - |
| 比较值（阈值） | Compare_* | Compare_L2=11 | Compare_L1=13 | Compare_R1=15 | Compare_R2=17 | - |
| 推荐阈值 | Rec_* | Rec_L2=19 | Rec_L1=20 | Rec_R1=25 | Rec_R2=22 | Rec_all=26 |
| 线高（Lineheight 索引） | pn | - | 0 | 1 | 2 | 3 |

### 4.3 方法

| 方法 | 签名 | 说明 |
|------|------|------|
| readline | `readline(self, li)` | 读单路巡线结果（0/1）。li=0~3（对应 L2/L1/R1/R2） |
| readline_all | `readline_all(self, li)` | 一次性读 4 路，li 是长度4的数组/列表（by reference 返回），li[0]~li[3] 对应 L2/L1/R1/R2 的 0/1 |
| readlinead | `readlinead(self, li_ad)` | 读 4 路 AD 原始模拟值，li_ad 是长度4数组（用于精细校准） |
| linecalibrate | `linecalibrate(self)` | 一键校准巡线传感器 |
| Lineheight | `Lineheight(self, pn, heights)` | 设置单路高度值，pn=0~3（L2/L1/R1/R2） |

> 内部还有 `_readReg` / `_writeReg` 直接操作 I2C 寄存器的底层接口。

---

## 5. voice_api 语音 AI 模块

> 服务器地址：
> - `API_BASE_URL = http://www.haohaodada.com/project/voiceAI/ApiZNBW.php`（LLM/翻译/TTS/ASR 总入口）
> - `API_VOICE_URL = http://www.haohaodada.com/project/voiceAI/ApiXXKJ.php`（另一个接口）
> - Token 文件：`TOKEN_FILE_PATH = /tmp/.system_cache/config.json`（自动缓存）

### 5.1 VoiceAPI 类

```python
from voice_api import VoiceAPI
api = VoiceAPI()
```

### 5.2 核心方法（全部存在 ✅）

| 方法 | 签名 | 说明 |
|------|------|------|
| get_token | `get_token(self, user, password)` | 用账号密码换取并缓存 token。后续方法依赖它 |
| tts_synthesize | `tts_synthesize(self, text, output_path=None)` | 文本 → 语音合成。若 output_path 非 None，保存为文件；否则返回音频 bytes |
| voice_recognition | `voice_recognition(self, audio_file_path)` | 语音文件 → 文字识别（ASR）。文件路径输入（AudioRecorder 默认格式 16kHz/mono/float32 兼容） |
| translate_english | `translate_english(self, text)` | **中 → 英**翻译。没有 `translate_chinese`（反向翻译不存在） |
| llm_chat | `llm_chat(self, text)` | 通用大模型对话（老模型） |
| llm_chat_znbw_2025 | `llm_chat_znbw_2025(self, text)` | 🔴 智能博物 2025 版大模型（更新，优先推荐） |

### 5.3 Token 管理（内部）

```python
VoiceAPI._load_token()           # 从 config.json 加载
VoiceAPI._save_token()           # 保存到 config.json
VoiceAPI._ensure_token_loaded()  # 确保 token 已加载
VoiceAPI._reload_token()         # 重新加载
```

⚠️ **不存在 `refresh_token` / `check_token_valid`**。token 失效（HTTP 401）时只能重新调用 `get_token(user, password)`。

### 5.4 不存在的高级接口（避免踩坑）

❌ refresh_token / check_token_valid / set_app_key / set_app_secret
❌ get_quota_info / get_qps_limit / set_timeout / set_proxy / get_last_error
❌ tts_synthesize_to_file / tts_get_voices（tts_synthesize 已通过 output_path 参数支持）
❌ voice_recognition_file / stream_asr_start / stream_asr_stop
❌ translate_chinese / translate_to（仅支持中→英）
❌ llm_chat_stream / llm_set_model / llm_set_system_prompt
❌ close()

> **2026-08-14 实测补充**：`VoiceAPI()` 无参实例化成功，但 `llm_chat('你好')` 和 `llm_chat_znbw_2025('你好')` 均返回 **None**（未先 `get_token()` 登录）。使用 LLM 对话前必须先调用 `get_token(user, password)` 获取认证 token。无环境变量密钥（内置或需用户账号）。

---

## 6. audio_recorder 录音模块

> 底层实现：`sounddevice (sd)` + `soundfile (sf)`，不是 pyaudio。

### 6.1 构造与默认值

```python
from audio_recorder import AudioRecorder
rec = AudioRecorder(
    sample_rate=16000,   # ⭐ 与 VoiceAPI ASR 完全匹配，无需重采样
    channels=1,          # 单声道
    dtype='float32'      # 32位浮点
)
```

### 6.2 方法

| 方法 | 签名 | 说明 |
|------|------|------|
| set_output_dir | `set_output_dir(self, output_dir)` | 设置保存目录（绝对/相对） |
| record_fixed_duration | `record_fixed_duration(self, duration, device=None, filename=None, output_dir=None)` | 录指定秒数（duration 秒）。返回音频数据；若指定 filename 则同时落盘。device=None 时自动选默认声卡（card 0 ES8323） |
| start_recording | `start_recording(self, device=None)` | **启动无限录音**（直到 stop_recording） |
| stop_recording | `stop_recording(self)` | 停止录音，返回累积的音频 ndarray |
| save_audio | `save_audio(self, audio_data, filename=None, output_dir=None)` | 把 ndarray 保存为 wav 文件；filename=None 时自动生成时间戳文件名 |
| _list_audio_devices | `_list_audio_devices(self)` | 列出所有可用录音设备（内部方法） |

### 6.3 不存在的接口

❌ set_sample_rate / set_channels / set_format（必须在 `__init__` 时确定，不能动态改）
❌ pause_recording / resume_recording / is_recording（只有 start/stop 两种状态）
❌ get_audio_path / get_audio_data / get_duration
❌ cleanup / close

---

## 7. audio_player 播放模块

> 双引擎：`pygame-ce`（默认，prefer_pygame=True）+ `pyaudio`（备用）。底层二选一自动切换。

### 7.1 AudioPlayer 类

```python
from audio_player import AudioPlayer
player = AudioPlayer()
```

| 方法 | 签名 | 说明 |
|------|------|------|
| play_file | `play_file(self, file_path, prefer_pygame=True)` | 播放本地音频文件（wav/mp3）。同步阻塞直到播放完毕 |
| play_audio | `play_audio(self, audio_bytes, prefer_pygame=True)` | 播放内存中的 bytes（如 tts_synthesize 直接返回的字节流） |
| play_with_pygame | `play_with_pygame(self, audio_bytes)` | 直接走 pygame 引擎 |
| play_with_pyaudio | `play_with_pyaudio(self, audio_bytes)` | 直接走 pyaudio 引擎 |
| cleanup | `cleanup(self)` | 释放资源（关闭 pygame mixer / pyaudio） |

### 7.2 模块级快捷函数

```python
from audio_player import play_audio, play_wav_audio, get_global_player
get_global_player()    # 全局单例 AudioPlayer
play_audio(bytes)      # 用全局 player 播放 bytes
play_wav_audio(...)    # 专用 wav 播放
```

### 7.3 🔴 不存在的播放控制（重要限制 — 2026-08-14 盲点探测实测确认）

❌ pause / resume / stop（**一旦开始播放只能播到底**）
❌ set_volume / get_volume
❌ seek / set_position / get_position / get_duration
❌ is_playing / is_paused
❌ set_speed / set_pitch / set_loop / get_metadata
❌ close（只有 cleanup）

> **如果需要暂停/音量/停止控制 → 直接用 pygame.mixer 或 pyaudio，不要用 AudioPlayer。**
> **实测确认**：AudioPlayer 类级+实例级全反射，仅 5 个公开方法（cleanup/play_audio/play_file/play_with_pyaudio/play_with_pygame），实例属性仅 pygame_initialized 和 pyaudio_instance。无任何隐藏的播放控制方法。

---

## 8. text_recognition 文字识别模块（PPOCR）

> 内置 PaddleOCR 精简版。输入形状固定 `DET_INPUT_SHAPE=[480,480]`，`PPOCR_AVAILABLE=True`。

### 8.1 TextRecognizer 类

```python
from text_recognition import TextRecognizer
ocr = TextRecognizer()
ocr.initialize()    # 显式初始化 PPOCR（首次调用 recognize_text 时也会懒加载）
```

| 方法 | 签名 | 说明 |
|------|------|------|
| recognize_text | `recognize_text(self, image_input, confidence_threshold=0.5)` | 识别图片文字。image_input 可以是 **文件路径** 或 **numpy 数组 (cv2.imread/BGR)**。**返回 dict**（2026-08-14 实测确认）：`{'success': bool, 'text': str (全部文字拼接), 'details': [{'text': str, 'confidence': float, 'bbox': [[x,y]×4]}], 'error': None\|str}` — details 中每项含单行文本、置信度、4 角坐标 bbox，可用于画框 |
| initialize | `initialize(self)` | 初始化 PPOCR（内部懒加载，可不用手动调） |
| cleanup | `cleanup(self)` | 释放资源 |
| _process_input | `_process_input(self, image_input)` | 内部预处理 |

### 8.2 模块级快捷函数

```python
from text_recognition import recognize_image_text, extract_text_from_image
recognize_image_text(...)     # 独立识别函数（不经过类）
extract_text_from_image(...)  # 纯文字提取
```

### 8.3 不存在的接口

❌ recognize_image_text（类内不存在，但模块级存在）
❌ recognize_image / recognize_file（用 recognize_text 传路径即可）
❌ set_language / set_region / enable_angle_classify / set_detection_threshold（阈值已通过 confidence_threshold 参数暴露）
❌ get_last_result / get_inference_time / set_model_path / get_supported_languages
❌ release / close（用 cleanup）

---

## 9. 关键注意事项与风险

| # | 风险 / 注意 | 影响范围 | 应对 |
|---|------------|---------|------|
| 1 | **LIBGL_ALWAYS_SOFTWARE 未在全局设置** | 所有 pygame + cv2 脚本 | 每个脚本第一行代码：`import os; os.environ['LIBGL_ALWAYS_SOFTWARE']='1'`，再 import pygame |
| 2 | cv2 版本是 **5.0.0**（非 4.x） | 图像处理项目 | 使用 cv2 5.0 新 API 时先查文档；已写好的项目如出现未知错误先怀疑 API 变更 |
| 3 | **Swap=0B**（无交换分区） | 大内存模型 | 监控内存使用，避免加载 >6GB 模型导致 OOM 被杀 |
| 4 | pandas 导入失败（numpy 1.24 兼容性） | 数据分析项目 | 升级 pandas 到 2.0.3 或不用 pandas |
| 5 | USB 摄像头设备号是 **video40 / video41 / video42**（非 0/1/2） | cv2.VideoCapture | 纯 cv2 模式下按 40→41→42 顺序尝试 `VideoCapture('/dev/videoN', cv2.CAP_V4L2)` + 帧有效性验证，**不能只检测 video40** |
| 6 | AudioPlayer **无播放控制** | 音乐播放器/长音频项目 | 要暂停/音量 → 直接 `pygame.mixer.music` |
| 7 | AudioRecorder **无暂停** | 录音项目 | 要暂停功能 → 多次 start/stop + 拼接 ndarray |
| 8 | VoiceAPI **无 token 自动刷新** + 无多语言翻译 | 联网项目 | 出现 401 重新登录；只支持中→英，反过来需 LLM 实现 |
| 9 | text_recognition 只有一个阈值可调 | OCR 项目 | 复杂 OCR 需求可考虑 pip 装 paddleocr（不推荐，重复依赖） |
| 10 | NPU Python 绑定缺失 | 自定义 RKNN 模型 | 要么用 PyTorch CPU 跑（慢），要么装 rknnlite2（需匹配工具链） |
| 11 | ESP32 的 analogWrite(pwm) 范围、servo 角度范围、超声波单位 均**待实际硬件验证** | 硬件项目 | 做硬件项目时加打印实测值 |
| 12 | 7 个官方库都是 `.so` 二进制闭源，无法单步调试 | 所有官方库调用 | 遇到 bug 只能通过输入/输出分析 + 对比 V3 探测脚本找模式；必要时回 Python 替代方案 |
| 13 | 🔴 **不要动系统 Python 版本**（见 2.5.1） | 整个系统 | Ubuntu 20.04 系统 + 7 个官方库 cpython-38.so + RKNN 工具链 三层绑定 Python 3.8。升版本 = apt 崩 + 官方库全失效 + NPU 绑定丢失。未来必须用时 → pyenv 本地隔离，别动 `/usr/bin/python3` |
| 14 | 🔴 **不要装 pygame 原版或升 pygame-ce**（见 2.5.2） | UI / 音频播放项目 | 已装 pygame-ce 2.5.2，与原版 pygame **互斥**；2.5.4+ 已放弃 Python 3.8。装错或升错 = import 段错误 / 扩展 ABI 不匹配。**任何时候都只保留 pygame-ce 2.5.2 一个包** |
| 15 | 🔴 **不要装 opencv-contrib-python**（见 2.5.3） | 图像处理项目 | 与 opencv-python 5.0.0 **互斥**；cp38/aarch64/5.x 组合大概率没有 whl，装时触发 30+ 分钟本地编译且必崩；8 个缺失子模块**全部有替代方案**。真缺某函数时，先查 `cv2` 顶层命名空间，再用 V3/MediaPipe/dt-apriltags 替代 |

---

## 10. 附录：设备清单

### 10.1 V4L2 视频设备（共 44 个）速查表

| 范围 | 驱动 | 用途 |
|------|------|------|
| /dev/video0 ~ 9 (10个) | rkcif-mipi-lvds2 | MIPI CSI 摄像头接口 2（内部节点） |
| /dev/video10 ~ 21 (12个) | rkcif-mipi-lvds3 | MIPI CSI 摄像头接口 3（内部节点） |
| /dev/video22 ~ 29 (8个) | rkisp_v6 (rkisp0) | ISP 0 处理管道：mainpath/selfpath/fbcpath/iqtool/rawrd*/statistics/input-params |
| /dev/video30 | rkisp_v6 | ISP input-params (metadata output) |
| /dev/video31 ~ 39 (9个) | rkisp_v6 (rkisp1) | ISP 1 处理管道 |
| **/dev/video40** | **uvcvideo** | **USB 摄像头（可直接 cv2.VideoCapture）** ⭐ |
| /dev/video41 | uvcvideo | 同一 USB 摄像头 metadata capture |
| /dev/video-dec0 | - | 视频解码硬件节点 |
| /dev/video-enc0 | - | 视频编码硬件节点 |

### 10.2 音频设备速查表

| 类型 | 名称 | 格式 | 状态 |
|------|------|------|------|
| 播放 sink 0 | platform-es8388-sound.stereo-fallback | s16le 2ch 44100Hz | SUSPENDED（正常） |
| 播放 sink 1 | combined（组合输出） | s16le 2ch 44100Hz | SUSPENDED |
| 录音 source 0 | es8388-sound.stereo-fallback.monitor | s16le 2ch 44100Hz | SUSPENDED |
| 录音 source 1 | es8388-sound.stereo-fallback（**板载麦克风**） | s16le 2ch 44100Hz | SUSPENDED（选 source 1 录音） |
| 录音 source 2 | combined.monitor | s16le 2ch 44100Hz | SUSPENDED |

> 硬件录音 card 0, device 0: `rockchip,es8388-codec` (ES8323.7-0011-0)
> 硬件播放 card 0, device 0: 同上

### 10.3 串口设备速查表

| 设备 | 类型 | 用途 |
|------|------|------|
| /dev/ttyS9 | 板载 UART | **ESP32 通信串口**（内部连接，用户一般无需手动操作） |
| /dev/ttyUSB* | USB 串口 | 当前为空（无 USB 转串口转换器） |
| /dev/ttyACM* | USB CDC ACM | 当前为空（无 Arduino / ST-Link 等） |
