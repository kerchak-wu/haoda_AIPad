# 文字识别播报器项目说明文档

## 一、项目概述

基于好搭AI派开发的实时文字识别与语音播报系统。摄像头实时画面显示在 Pygame 界面上，点击识别按钮后对当前画面进行 OCR 识别，将识别到的文字显示在结果区，并调用在线语音 AI 合成播报。

**程序文件**：`文字识别播报器.py`

**核心功能**：

- 1920×1080 科技感深色主题界面
- 摄像头实时画面显示（640×480）
- OCR 文字识别（官方 text_recognition.TextRecognizer / PaddleOCR）
- 在线 TTS 语音合成 + 可中断播报
- 线程化异步处理（识别/播报不阻塞界面）

### 项目结构

```markdown
haoda_AIPad/
├── 文字识别播报器.py                # 本程序主入口
├── 文字识别播报器项目说明文档.md    # 本文档
├── images/
│   └── 1.jpg                        # 背景图（1920×1080，可选）
├── logs/                            # 日志目录（运行时自动创建）
│   └── 文字识别播报器_YYYYMMDD.log  # 按日期命名的运行日志（追加模式）
├── recordings/                      # TTS 音频临时目录（运行时自动创建）
│   └── ocr_result.wav               # 最近一次 TTS 合成结果
└── 字体文件/                         # 系统字体源文件（参考）
```

### 资源文件说明

- `images/1.jpg`：背景图片，程序启动时加载。如缺失或加载失败，会自动生成深蓝渐变背景，不影响运行。
- `logs/文字识别播报器_YYYYMMDD.log`：运行日志，记录所有 `print` 输出（含异常堆栈），用于事后排查问题。每次运行追加写入，文件名按当天日期生成。
- `recordings/ocr_result.wav`：TTS 合成的临时音频文件，每次识别会覆盖。

---

## 二、关键技术经验（重要）

### 1. 模块导入顺序陷阱：`text_recognition` 必须最先导入

**问题**：`ppocr_system` 依赖 `utils.operators`。若先导入 `cv2`/`pygame`/`camera_vision_system_v3`，它们会把 `utils` 注册为非包模块，导致 `ppocr_system` 报错 `'utils' is not a package`。

**解决方案**：`text_recognition` 必须在文件最顶部、所有其他库之前导入：

```python
# !!! text_recognition 必须在所有其他库之前导入 !!!
try:
    from text_recognition import TextRecognizer as _TextRecognizer
    _TEXT_RECOGNITION_AVAILABLE = True
except Exception as _e:
    _TEXT_RECOGNITION_AVAILABLE = False

import os
import cv2
import pygame
# ... 其他库
```

### 2. 初始化顺序严格依赖

`TextRecognizer()` 实例化**必须在摄像头打开之前**完成，否则 `ppocr_system` 因 USB 总线/设备资源冲突无法加载。当前采用纯 cv2 模式（不用 SDK 视觉系统），正确顺序：

```markdown
1. TextRecognizer()              ← OCR 必须最先初始化（设备操作前）
2. VoiceAPI() + get_token()
3. AudioPlayer()
4. cv2.VideoCapture(path, CAP_V4L2)  ← 摄像头最后打开，独占 USB 总线
```

### 3. 摄像头调用模式：纯 cv2 模式（CAP_V4L2 后端，绕过 FFMPEG）

本程序**不使用 `camera_vision_system_v3` 视觉系统**（`enable_basic=False`、`enable_advanced=False`，无任何检测功能，SDK 仅被用作帧采集器），故改为纯 cv2 模式直接管理摄像头，彻底解决启动慢问题。

**问题背景（SDK 模式为何慢）**：OpenCV 的 FFMPEG 后端设备列表仅 32 项，`camera_id >= 32`（如好搭AI派扩展板上的 40/41/42）即使 `/dev/videoN` 节点存在也会触发 `Camera index out of range ... nb_devices is 32` 错误，每次失败耗时约 4~6 秒；SDK 内部再回退备用 ID，叠加浪费十几秒。此外 `/dev/video41` 常为元数据节点（`Not a video capture device`），并非真正的视频捕获设备。

**解决方案**：用 `cv2.VideoCapture('/dev/videoN', cv2.CAP_V4L2)` 显式指定 **V4L2 后端**并传入**设备路径字符串**。V4L2 后端无 32 项限制，直连内核驱动，毫秒级完成；且能区分捕获节点与元数据节点。

```python
# 正确（毫秒级，跳过 FFMPEG）：
cap = cv2.VideoCapture('/dev/video40', cv2.CAP_V4L2)

# 错误（触发 FFMPEG 越界，浪费约 6 秒）：
cap = cv2.VideoCapture(40)           # 不传后端，FFMPEG 优先
cap = cv2.VideoCapture(41)           # 传 int，FFMPEG 优先且 ID>=32
```

**候选顺序**（按用户建议）：先检测 40，再 41，最后 42。每步都：
1. `os.path.exists('/dev/videoN')` 检查节点是否存在
2. `cv2.VideoCapture(path, cv2.CAP_V4L2)` 尝试打开
3. 设置 640×480 分辨率
4. 最多读 5 帧，用 `gray.mean()` 验证非全黑/全白（参考范例帧有效性检测）

### 4. 帧采集：`cap.read()` 取代 `capture_frame()` + `refresh_results()`

纯 cv2 模式无需调用 SDK 的 `refresh_results()` / `capture_frame()`，改用标准 `cap.read()`：

```python
ret, frame = cap.read()   # frame 是 BGR 格式 numpy 数组，ndim==3
# ret=False / frame=None 视为采集失败，配合失败计数降频（连续3次失败→3fps）
```

后台线程按项目记忆要求：正常间隔 0.05s（≈20fps），连续失败 3 次后降为 0.33s（≈3fps），恢复后自动回到 20fps。

### 5. 摄像头帧获取必须放后台线程

**问题**：`cap.read()` 在摄像头异常（拔掉/接触不良、扩展板松动）时会阻塞或持续失败，导致主循环卡死、退出按钮无响应。

**解决方案**：用后台守护线程持续抓帧写入共享变量（`threading.Lock` 保护读写），主循环只读最新帧，永不阻塞。同时按项目记忆加入**失败降频**机制：连续失败 3 次后从 20fps 降到 3fps，恢复后自动回到 20fps。

```python
def _frame_capture_worker(self):
    normal_interval = 0.05   # ≈20 fps
    slow_interval = 0.33     # ≈3 fps
    fail_streak = 0
    while self._frame_thread_running:
        if _cv2_cap is None:
            time.sleep(slow_interval); continue
        try:
            ret, f = _cv2_cap.read()
            if ret and f is not None and f.ndim == 3:
                fail_streak = 0
                with self._frame_lock:
                    self._latest_bgr = f
            else:
                fail_streak += 1
        except Exception:
            fail_streak += 1
        interval = slow_interval if fail_streak >= 3 else normal_interval
        time.sleep(interval)
```

### 6. 画面缩放用 `scale` 不用 `smoothscale`

`pygame.transform.smoothscale`（双线性插值）CPU 开销大，每帧缩放会导致明显卡顿。改用 `pygame.transform.scale`（最近邻），速度大幅提升，摄像头画面场景下画质差异不明显。

### 7. 摄像头分辨率影响流畅度

1280×720 数据量大、卡顿明显；降至 640×480 数据量减少约 75%，流畅度显著提升，OCR 识别效果不受影响。

### 8. OCR 使用官方后端

使用官方 `text_recognition.TextRecognizer`（依赖 `ppocr_system` / PaddleOCR），严格参照范例 5.21。注意：`text_recognition` 必须在所有其他库之前导入（见第 1 条），且 `TextRecognizer()` 实例化必须在 `cv2.VideoCapture` 打开摄像头之前（见第 2 条）。

```python
# 文件最顶部导入（避免 utils 模块名冲突）
from text_recognition import TextRecognizer

# open_camera 之前初始化
ocr_recognizer = TextRecognizer()

# 识别（严格参照范例 5.21）
result = ocr_recognizer.recognize_text(frame, confidence_threshold=0.5)
```

### 9. 语音播报可中断设计

参照 `唐诗宋词朗读器.py` 模式，用 `stop_requested` 全局标志 + `pygame.mixer.music` 实现可中断播放：

```python
def play_audio_interruptible(audio_path):
    pygame.mixer.music.load(audio_path)
    pygame.mixer.music.play()
    while pygame.mixer.music.get_busy():
        if stop_requested:          # 检测中断标志
            pygame.mixer.music.stop()
            break
        time.sleep(0.1)
```

### 10. 异步识别播报不阻塞界面

OCR + TTS 耗时较长，放后台线程执行，主循环持续刷新界面：

```python
def recognize_and_play_async(frame):
    def worker():
        ocr_result = ocr_recognize(frame)
        # ... 显示文字
        voice_api.tts_synthesize(text, path)
        play_audio_interruptible(path)
    threading.Thread(target=worker, daemon=True).start()
```

### 11. Rockchip 平台兼容性补丁

为避免 Rockchip 平台（RK3588S / RK3566 / RK3568 系列通用）上 Pygame 与 OpenCV 的底层驱动冲突（GPU 驱动加载失败、音频驱动与 V4L2 死锁），程序开头加入三处补丁（参考《视觉系统摄像头调用参考方案》第 7 章）：

```python
import os
# 1. 强制 libGL 使用软件渲染，避免 rockchip 平台 GPU 驱动加载失败
os.environ.setdefault('LIBGL_ALWAYS_SOFTWARE', '1')

# 2. 导入顺序：先 pygame 再 cv2（pygame 先导入让 SDL 接管视频子系统）
import pygame
import cv2

# 3. 分段初始化（不调用 pygame.init()），避免 pygame.mixer 导致原生崩溃
pygame.display.init()
pygame.font.init()
# mixer 需用于语音播报，单独初始化并容错（Rockchip 平台音频驱动可能崩溃）
try:
    pygame.mixer.init()
except Exception as e:
    print('pygame.mixer 初始化失败，语音播报功能不可用:', e)
```

**特殊说明**：本程序需要 `pygame.mixer` 进行语音播报，与其他程序不同——不能用"跳过 mixer"的方案，而是保留 `pygame.mixer.init()` 但用 try/except 包裹。这样既能在正常平台使用语音播报，又能在 Rockchip 音频驱动异常时避免崩溃（仅语音播报功能不可用，OCR 和界面仍正常）。

### 12. 日志输出机制

程序启动时在 `logs/` 目录下创建按日期命名的日志文件，所有 `print` 输出（含异常堆栈）同时写入控制台和日志文件，便于事后排查问题。

```python
# 日志文件：logs/文字识别播报器_YYYYMMDD.log，追加模式，块缓冲
_LOG_DIR = 'logs'
_LOG_FILE = os.path.join(_LOG_DIR, '文字识别播报器_%s.log' %
                         _datetime.datetime.now().strftime('%Y%m%d'))
_debug_log_fp = open(_LOG_FILE, 'a', encoding='utf-8', buffering=-1)

# TeeStdout 包装 stdout/stderr，print 同时写入控制台和文件
sys.stdout = _TeeStdout(sys.stdout)
sys.stderr = _TeeStdout(sys.stderr)
```

**设计要点**：
- **目录与命名**：`logs/` 目录、`文字识别播报器_YYYYMMDD.log`，符合项目日志规范
- **追加模式（`'a'`）**：同一天多次运行不会覆盖历史日志，每次运行写入 `======== 时间 运行开始 ========` 分隔
- **块缓冲（`buffering=-1`）**：避免高频 `print` 阻塞主循环
- **TeeStdout 包装**：无需修改业务代码中的 `print` 语句，自动双写
- **退出关闭**：`run()` 退出清理末尾关闭文件指针，防止日志丢失

### 13. 退出时显式停止后台检测

退出清理时需在 `cleanup()` 前显式调用 `threaded_system.stop_background_detection()`，确保后台检测线程正确停止（报告 4.1 节：该方法在 `threaded_system` 下，不在 `vision_system` 顶层）：

```python
# 退出清理顺序
stop_playback()                  # 停止语音播报
ocr_cleanup()                    # 清理 OCR 资源
self._frame_thread_running = False   # 停后台抓帧线程
self._frame_thread.join(timeout=2)
vision_system.threaded_system.stop_background_detection()  # 停后台检测线程
vision_system.cleanup()          # 释放视觉系统整体资源
player.cleanup()                 # 清理音频播放器
pygame.quit()
_debug_log_fp.close()            # 关闭日志文件
```

---

## 三、硬件 API 调用规范

**说明**：本程序不使用 `camera_vision_system_v3` 视觉系统（无需检测功能），摄像头改为纯 cv2 模式直接管理。其余硬件 API 严格参照范例代码。

| 功能 | API 调用 | 参照范例 / 说明 |
| ------ | --------- | --------- |
| 摄像头打开 | `_open_cv2_camera(640,480)` → 内部按 40→41→42 顺序 `cv2.VideoCapture('/dev/videoN', cv2.CAP_V4L2)` + `cap.set(FRAME_WIDTH/HEIGHT)` + 帧有效性验证 | 自定义：显式指定 CAP_V4L2 后端，绕过 FFMPEG 32 项限制 |
| 帧捕获 | `ret, frame = cap.read()`（后台线程，含失败降频：正常 20fps，连续失败 3 次→3fps） | OpenCV 标准 API |
| 摄像头释放 | `cap.release()` | OpenCV 标准 API |
| OCR 识别 | `ocr_recognizer.recognize_text(frame, confidence_threshold=0.5)` | 范例 5.21 |
| TTS 语音合成 | `voice_api.tts_synthesize(text, 'recordings/ocr_result.wav')` | 范例 4.1 |
| 语音 AI 认证 | `voice_api.get_token(username, password)` | 范例 4.1 |
| 音频播放 | `AudioPlayer()` / `pygame.mixer.music` | 范例 3.2 |

---

## 四、界面设计要点

### 布局（1920×1080）

```markdown
┌─────────────────────────────────────────────┐
│              标题栏（科技感标题）      [退出] │
├──────────────────────────┬──────────────────┤
│                          │                  │
│    摄像头实时画面         │    识别结果       │
│   （四角瞄准框+扫描线）    │   （状态指示）    │
│                          │                  │
├──────────────────────────┴──────────────────┤
│  [识别]  [停止播报]   状态文字               │
└─────────────────────────────────────────────┘
```

> 退出按钮位于标题栏右上角（`WIDTH-280, 20, 240×70`），底部栏仅保留识别/停止播报按钮与状态信息。

### 配色方案（科技感深色主题）

```python
BG_TOP        = (10, 14, 30)    # 深蓝渐变顶部
BG_BOTTOM     = (4, 6, 18)      # 深蓝渐变底部
PANEL_COLOR   = (16, 22, 44)    # 面板背景
PANEL_BORDER  = (0, 160, 210)   # 面板边框
ACCENT_CYAN   = (0, 229, 255)   # 主强调色（青色）
TITLE_COLOR   = (0, 229, 255)   # 标题
STATUS_READY  = (0, 255, 136)   # 就绪（绿）
STATUS_BUSY   = (255, 200, 60)  # 忙碌（黄）
STATUS_ERROR  = (255, 80, 90)   # 错误（红）
```

### 背景图片

优先加载 `images/1.jpg`，加载失败回退到深蓝渐变背景：

```python
try:
    bg_raw = pygame.image.load(os.path.join('images', '1.jpg'))
    # 使用 scale 而非 smoothscale（项目记忆：smoothscale CPU 开销过大）
    self.bg = pygame.transform.scale(bg_raw, (WIDTH, HEIGHT))
except Exception:
    self.bg = make_gradient_bg(WIDTH, HEIGHT, BG_TOP, BG_BOTTOM)
```

### 科技感装饰元素

- **四角瞄准框**：摄像头画面四角的青色直角线
- **扫描线动画**：识别时画面上滚动的青色发光线
- **圆角面板**：`border_radius=16`
- **按钮悬停效果**：鼠标悬停时颜色变亮

---

## 五、使用前配置

### 1. 语音 AI 认证

替换程序中的账号密码为经过认证的好好搭搭账号：

```python
VOICE_USERNAME = '你的账号'
VOICE_PASSWORD = '你的密码'
```

### 2. 字体路径

程序使用好搭AI派内置字体（绝对路径）：

```python
FONT_BOLD_PATH = '/home/cxdz/jupyter/assets/PingFang_Bold.ttf'
FONT_REG_PATH  = '/home/cxdz/jupyter/assets/PingFang_Regular.ttf'
FONT_KAI_PATH  = '/home/cxdz/jupyter/assets/simkai.ttf'
```

### 3. 录音目录

程序会在 `recordings/` 目录下生成临时音频文件，目录不存在时自动创建。

### 4. 背景图片

将背景图片放在 `images/1.jpg`，缺失时自动回退渐变背景。

---

## 六、交互方式

| 操作 | 效果 |
| ------ | ------ |
| 点击「识别」或按空格键 | OCR 识别当前画面 → 显示文字 → 语音播报 |
| 点击「停止播报」 | 中断当前语音播放 |
| 点击右上角标题栏内「退出」或按 ESC | 退出程序 |

---

## 七、踩坑记录与避坑指南

| 问题 | 原因 | 解决方案 |
| ------ | ------ | --------- |
| `importerror: ppocr_system模块不可用` | `utils` 模块名冲突 | `text_recognition` 必须最先导入 |
| `'utils' is not a package` | 先导入 cv2/pygame 导致冲突 | 同上 |
| 画面卡顿 | `smoothscale` + 高分辨率 | 用 `scale` + 640×480 |
| 启动慢（摄像头检测耗时长） | OpenCV FFMPEG 后端对 ID>=32 越界失败，叠加 SDK 回退，累计十几秒 | 用 `_open_cv2_camera()` 按 40→41→42 顺序 `cv2.VideoCapture(path, cv2.CAP_V4L2)`，毫秒级完成 |
| FFMPEG `Camera index out of range (expected: index < 32)` | FFMPEG 后端设备列表仅 32 项，40/41/42 均越界 | 显式指定 `cv2.CAP_V4L2` 后端 + 字符串路径，绕过 FFMPEG |
| V4L2 `Not a video capture device` | `/dev/video41` 常为元数据节点，非捕获节点 | 打开后验证 `cap.read()` 帧有效性，自动跳过非捕获节点，回退下一个候选 |
| `cap.read()` 持续返回 None/False，画面不动 | USB 连接线松动 / 设备节点异常 | 检查扩展板与摄像头连接；按项目记忆设备优先 /dev/video40 和 /dev/video41；日志会打印三个候选的逐个尝试结果 |
| 采集频率掉到 ~3fps（画面明显慢） | 连续 3 次帧采集失败触发降频 | 检查硬件连接；恢复后会自动回到 ~20fps |
| 摄像头画面不更新、退出按钮无响应 | 帧采集阻塞主循环 | 后台守护线程抓帧 + `threading.Lock` 保护，主循环永不阻塞 |
| 摄像头误判断开 | 自定义状态检测逻辑不准 | 删除状态检测，直接显示最新帧（无帧则显示"摄像头启动中…"） |
| 语音播报无法中断 | 阻塞播放 | `stop_requested` 标志 + 循环检测 |

---

## 八、常见问题

### Q1：程序启动后立即崩溃（段错误）

**A**：通常是 Rockchip 平台兼容性问题，确认以下几点：

1. 程序开头已设置 `LIBGL_ALWAYS_SOFTWARE=1` 环境变量
2. 未调用 `pygame.init()`，改用 `pygame.display.init()` + `pygame.font.init()` 分段初始化
3. import 顺序为先 `pygame` 再 `cv2`，不可颠倒
4. `pygame.mixer.init()` 用 try/except 包裹，避免音频驱动异常导致整体崩溃

### Q2：启动时提示"pygame.mixer 初始化失败，语音播报功能不可用"

**A**：Rockchip 平台音频驱动异常。OCR 识别和界面显示仍可正常使用，仅语音播报功能不可用。如需语音播报，检查音频驱动或更换平台。

### Q3：日志文件在哪里查看

**A**：日志文件位于程序同目录下的 `logs/` 文件夹，文件名格式为 `文字识别播报器_YYYYMMDD.log`（如 `文字识别播报器_20260811.log`）。程序崩溃或异常退出后，可在日志文件中查看完整的 `print` 输出和异常堆栈，用于事后排查。同一天多次运行会追加到同一文件，以 `======== 时间 运行开始 ========` 分隔。

### Q4：退出按钮在哪里

**A**：退出按钮位于界面右上角标题栏内（`WIDTH-280, 20, 240×70`），不在底部栏。点击该按钮或按 ESC 键均可退出程序。底部栏仅保留「识别」「停止播报」按钮与状态信息。

### Q5：退出时摄像头设备占用未释放

**A**：确认退出流程已调用 `vision_system.threaded_system.stop_background_detection()`（在 `threaded_system` 下，非 `vision_system` 顶层），再调用 `vision_system.cleanup()`。完整退出顺序见第二章第 13 节。

---

## 九、参考资源

- **范例代码**：`好搭AI派范例代码.md`
  - 5.AI视觉算法 21.实时文字识别（摄像头+OCR+TTS）
  - 4.语音AI 1.语音合成
  - 3.音频处理 2.音频播放
  - 8.pygame 10.音乐播放-按钮
- **本地参考项目**：
  - `唐诗宋词朗读器.py`（界面风格、线程化播报、可中断播放）
  - `物体学习.py` / `人脸学习.py`（摄像头设备探测逻辑、帧有效性验证）
- **字体参考**：`好搭AI派可用字体列表.txt`
