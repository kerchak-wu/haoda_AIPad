# 文字识别播报器项目说明文档

## 一、项目概述

基于好搭AI派开发的实时文字识别与语音播报系统。摄像头实时画面显示在 Pygame 界面上，点击识别按钮后对当前画面进行 OCR 识别，将识别到的文字显示在结果区，并调用在线语音 AI 合成播报。

**程序文件**：`文字识别播报器.py`

**核心功能**：

- 1920×1080 科技感深色主题界面
- 摄像头实时画面显示（640×480）
- OCR 文字识别（PaddleOCR 官方后端 + Tesseract 备选）
- 在线 TTS 语音合成 + 可中断播报
- 线程化异步处理（识别/播报不阻塞界面）

---

## 二、关键技术经验（重要）

### 1. 模块导入顺序陷阱：`text_recognition` 必须最先导入

**问题**：`ppocr_system` 依赖 `utils.operators`。若先导入 `cv2`/`pygame`/`camera_vision_system_v3`，它们会把 `utils` 注册为非包模块，导致 `ppocr_system` 报错 `'utils' is not a package`。

**解决方案**：`text_recognition` 必须在文件最顶部、所有其他库之前导入：

```python
# !!! text_recognition 必须在所有其他库之前导入 !!!
try:
    from text_recognition import TextRecognizer as _TextRecognizer
    _TEXT_RECOGNITION_AVAILABLE = True
except Exception as _e:
    _TEXT_RECOGNITION_AVAILABLE = False

import os
import cv2
import pygame
# ... 其他库
```

### 2. 初始化顺序严格依赖

`TextRecognizer()` 实例化**必须在 `open_camera()` 之前**，否则 `ppocr_system` 因摄像头资源冲突无法加载。正确顺序：

```markdown
1. create_vision_system_v3()     ← 创建视觉系统
2. TextRecognizer()              ← OCR 必须在此初始化（摄像头启动前）
3. VoiceAPI() + get_token()
4. AudioPlayer()
5. open_camera()                 ← 摄像头打开
6. start_background_detection()  ← 后台检测启动
```

### 3. `capture_frame()` 必须配对 `refresh_results()`

范例 5.21 主循环中，每次 `capture_frame()` 之前都调用了 `vision_system.result_accessor.refresh_results()` 刷新结果缓冲区。**漏掉这一步会导致 `capture_frame()` 返回 None**。

```python
vision_system.result_accessor.refresh_results()
frame = vision_system.capture_frame()
```

### 4. `camera_id` 参数取值限制

| 传参方式 | 结果 | 原因 |
| --------- | ------ | ------ |
| `camera_id=-1` | ✅ 正常 | SDK 自动检测，范例统一用法 |
| `camera_id=41` | ❌ 失败 | V4L2 整数索引越界（设备列表通常只有 32 项） |
| `camera_id="/dev/video41"` | ❌ 失败 | SDK 类型校验只接受 int |

**结论**：严格参照范例用 `camera_id=-1`，让 SDK 内部自动选择设备。

### 5. 摄像头帧获取必须放后台线程

**问题**：`vision_system.capture_frame()` 在摄像头异常（拔掉/接触不良）时会**阻塞**，导致主循环卡死、退出按钮无响应。

**解决方案**：用后台守护线程持续抓帧写入共享变量，主循环只读最新帧，永不阻塞：

```python
def _frame_capture_worker(self):
    while self._frame_thread_running:
        try:
            vision_system.result_accessor.refresh_results()
            frame = vision_system.capture_frame()
            if frame is not None:
                with self._frame_lock:
                    self._latest_bgr = frame
        except Exception:
            pass
        time.sleep(0.01)

def grab_frame(self):
    with self._frame_lock:
        frame = self._latest_bgr
    if frame is None:
        return None
    # BGR -> RGB -> Surface
    ...
```

### 6. 画面缩放用 `scale` 不用 `smoothscale`

`pygame.transform.smoothscale`（双线性插值）CPU 开销大，每帧缩放会导致明显卡顿。改用 `pygame.transform.scale`（最近邻），速度大幅提升，摄像头画面场景下画质差异不明显。

### 7. 摄像头分辨率影响流畅度

1280×720 数据量大、卡顿明显；降至 640×480 数据量减少约 75%，流畅度显著提升，OCR 识别效果不受影响。

### 8. OCR 后端容错机制

官方 `text_recognition.TextRecognizer` 依赖 `ppocr_system`（PaddleOCR），某些环境可能不可用。设计双后端自动切换：

```python
# 优先官方 PaddleOCR
if _TEXT_RECOGNITION_AVAILABLE:
    ocr_recognizer = _TextRecognizer()   # paddleocr 后端

# 回退 Tesseract（纯软件库，需安装 tesseract-ocr）
if ocr_backend is None:
    import pytesseract
    # tesseract 后端
```

Tesseract 安装命令（好搭AI派为 Linux）：

```bash
sudo apt-get install tesseract-ocr tesseract-ocr-chi-sim
pip install pytesseract
```

### 9. 语音播报可中断设计

参照 `唐诗宋词朗读器.py` 模式，用 `stop_requested` 全局标志 + `pygame.mixer.music` 实现可中断播放：

```python
def play_audio_interruptible(audio_path):
    pygame.mixer.music.load(audio_path)
    pygame.mixer.music.play()
    while pygame.mixer.music.get_busy():
        if stop_requested:          # 检测中断标志
            pygame.mixer.music.stop()
            break
        time.sleep(0.1)
```

### 10. 异步识别播报不阻塞界面

OCR + TTS 耗时较长，放后台线程执行，主循环持续刷新界面：

```python
def recognize_and_play_async(frame):
    def worker():
        ocr_result = ocr_recognize(frame)
        # ... 显示文字
        voice_api.tts_synthesize(text, path)
        play_audio_interruptible(path)
    threading.Thread(target=worker, daemon=True).start()
```

---

## 三、硬件 API 调用规范

严格参照范例代码，涉及外接硬件的 API 不可自由发挥：

| 功能 | API 调用 | 参照范例 |
| ------ | --------- | --------- |
| 视觉系统初始化 | `create_vision_system_v3(camera_id=-1, width=640, height=480, enable_basic=False, enable_advanced=False)` | 5.21 |
| 摄像头打开 | `vision_system.open_camera()` | 5.21 |
| 后台检测启动 | `vision_system.threaded_system.start_background_detection(show_preview=False)` | 5.21 |
| 结果刷新 | `vision_system.result_accessor.refresh_results()` | 5.21 |
| 帧捕获 | `vision_system.capture_frame()` | 5.21 |
| OCR 识别 | `ocr_recognizer.recognize_text(frame, confidence_threshold=0.5)` | 5.21 |
| TTS 语音合成 | `voice_api.tts_synthesize(text, 'recordings/ocr_result.wav')` | 4.1 |
| 语音 AI 认证 | `voice_api.get_token(username, password)` | 4.1 |
| 音频播放 | `AudioPlayer()` / `pygame.mixer.music` | 3.2 |

---

## 四、界面设计要点

### 布局（1920×1080）

```markdown
┌─────────────────────────────────────────────┐
│              标题栏（科技感标题）              │
├──────────────────────────┬──────────────────┤
│                          │                  │
│    摄像头实时画面         │    识别结果       │
│   （四角瞄准框+扫描线）    │   （状态指示）    │
│                          │                  │
├──────────────────────────┴──────────────────┤
│  [识别]  [停止播报]   状态文字    [退出]      │
└─────────────────────────────────────────────┘
```

### 配色方案（科技感深色主题）

```python
BG_TOP        = (10, 14, 30)    # 深蓝渐变顶部
BG_BOTTOM     = (4, 6, 18)      # 深蓝渐变底部
PANEL_COLOR   = (16, 22, 44)    # 面板背景
PANEL_BORDER  = (0, 160, 210)   # 面板边框
ACCENT_CYAN   = (0, 229, 255)   # 主强调色（青色）
TITLE_COLOR   = (0, 229, 255)   # 标题
STATUS_READY  = (0, 255, 136)   # 就绪（绿）
STATUS_BUSY   = (255, 200, 60)  # 忙碌（黄）
STATUS_ERROR  = (255, 80, 90)   # 错误（红）
```

### 背景图片

优先加载 `images/1.jpg`，加载失败回退到深蓝渐变背景：

```python
try:
    bg_raw = pygame.image.load(os.path.join('images', '1.jpg'))
    self.bg = pygame.transform.smoothscale(bg_raw, (WIDTH, HEIGHT))
except Exception:
    self.bg = make_gradient_bg(WIDTH, HEIGHT, BG_TOP, BG_BOTTOM)
```

### 科技感装饰元素

- **四角瞄准框**：摄像头画面四角的青色直角线
- **扫描线动画**：识别时画面上滚动的青色发光线
- **圆角面板**：`border_radius=16`
- **按钮悬停效果**：鼠标悬停时颜色变亮

---

## 五、使用前配置

### 1. 语音 AI 认证

替换程序中的账号密码为经过认证的好好搭搭账号：

```python
VOICE_USERNAME = '你的账号'
VOICE_PASSWORD = '你的密码'
```

### 2. 字体路径

程序使用好搭AI派内置字体（绝对路径）：

```python
FONT_BOLD_PATH = '/home/cxdz/jupyter/assets/PingFang_Bold.ttf'
FONT_REG_PATH  = '/home/cxdz/jupyter/assets/PingFang_Regular.ttf'
FONT_KAI_PATH  = '/home/cxdz/jupyter/assets/simkai.ttf'
```

### 3. 录音目录

程序会在 `recordings/` 目录下生成临时音频文件，目录不存在时自动创建。

### 4. 背景图片

将背景图片放在 `images/1.jpg`，缺失时自动回退渐变背景。

---

## 六、交互方式

| 操作 | 效果 |
| ------ | ------ |
| 点击「识别」或按空格键 | OCR 识别当前画面 → 显示文字 → 语音播报 |
| 点击「停止播报」 | 中断当前语音播放 |
| 点击「退出」或按 ESC | 退出程序 |

---

## 七、踩坑记录与避坑指南

| 问题 | 原因 | 解决方案 |
| ------ | ------ | --------- |
| `importerror: ppocr_system模块不可用` | `utils` 模块名冲突 | `text_recognition` 必须最先导入 |
| `'utils' is not a package` | 先导入 cv2/pygame 导致冲突 | 同上 |
| `capture_frame()` 返回 None | 漏调 `refresh_results()` | 每次捕获前先刷新 |
| 程序卡死、退出按钮无反应 | `capture_frame()` 阻塞主循环 | 帧获取放后台线程 |
| 画面卡顿 | `smoothscale` + 高分辨率 | 用 `scale` + 640×480 |
| `camera_id=41` 打不开 | V4L2 索引越界 | 用 `camera_id=-1` |
| `camera_id="/dev/video41"` 报错 | SDK 只接受 int | 同上，用 `-1` |
| 摄像头误判断开 | 自定义状态检测逻辑不准 | 删除状态检测，直接显示帧 |
| 语音播报无法中断 | 阻塞播放 | `stop_requested` 标志 + 循环检测 |

---

## 八、参考资源

- **范例代码**：`好搭AI派范例代码.md`
  - 5.AI视觉算法 21.实时文字识别（摄像头+OCR+TTS）
  - 4.语音AI 1.语音合成
  - 3.音频处理 2.音频播放
  - 8.pygame 10.音乐播放-按钮
- **本地参考项目**：
  - `唐诗宋词朗读器.py`（界面风格、线程化播报、可中断播放）
  - `物体学习.py` / `人脸学习.py`（摄像头设备探测逻辑、帧有效性验证）
- **字体参考**：`好搭AI派可用字体列表.txt`
