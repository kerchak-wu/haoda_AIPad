# 人脸学习与识别项目文档

自包含的桌面人脸学习与识别程序（不依赖好搭 AI 派专有 SDK），固定窗口 1920×1080，
支持人脸学习登记、实时识别、人脸库查看与删除管理，并将摄像头画面直接整合到主窗口中。
参考：`github.com/kerchak-wu/haoda_AIPad`（好搭 AI 派范例 08.人脸学习1 / 09.人脸学习2 / 10.人脸识别）。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 摄像头采集 | OpenCV `VideoCapture`（外接摄像头） |
| 人脸检测 | OpenCV Haar 级联 `haarcascade_frontalface_default.xml` |
| 人脸识别 | OpenCV LBPH 人脸识别器（`cv2.face.LBPHFaceRecognizer_create`，随 `opencv-contrib-python` 提供） |
| 界面 | `pygame`（推荐 `pygame-ce`），1920×1080 |
| 数据持久化 | JSON 数据库 + LBPH 模型文件 + PNG 样本图 |

> 不再依赖 `camera_vision_system_v3` 专有 SDK，任何装有 OpenCV + pygame 的电脑接上 USB 摄像头即可运行。

### 摄像头节点说明（重要）

本程序**固定探测 `/dev/video41`（优先）和 `/dev/video40`**，不再扫描其他节点，启动快、不卡顿。
若你的摄像头节点编号不同，请修改 `人脸学习.py` 中 `open_camera()` 内的 `(41, 40)` 元组。

探测每个节点时会做三层防护：
1. **MJPG 编码**：设置 `CAP_PROP_FOURCC = MJPG`，解决 USB 摄像头默认格式导致的雪花/绿屏
2. **4 秒超时**：用 `signal.SIGALRM` 打断卡在 `select()` 的元数据/损坏节点（避免启动卡死）
3. **雪花检测**：用 `INTER_AREA` 下采样比较标准差比值，过滤随机噪声帧（实测真实画面 r≈0.95，雪花 r≈0.03）

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 人脸学习 | 输入姓名 → 采集多帧 → 自动分配人脸 ID → 保存样本与训练模型 |
| 人脸识别 | 实时检测并识别人脸，画面上直接框出人脸并显示姓名/置信度 |
| 摄像头整合 | 摄像头画面直接嵌入主窗口（1100×520），不再弹出独立预览窗口 |
| 后台处理 | 采集线程与识别线程分离，主循环零阻塞，界面不卡顿 |
| 人脸库查看 | 「查看人脸库详细信息」按钮弹窗分页显示所有人脸的 ID、姓名、登记时间、样本数 |
| 人脸删除 | 列表中每条记录旁有「删除」按钮，带二次确认，删除后自动重训练模型 |
| 识别历史 | 实时记录识别结果（时间、ID、姓名、置信度），最多保留 20 条 |
| 数据可移植 | 程序关闭后，其他程序可加载已保存的模型与数据库独立完成识别 |

## 三、项目结构

```
haoda_AIPad/
├── 人脸学习.py                # 主程序（含 FaceEngine 引擎 + pygame 界面）
├── 人脸学习与识别项目文档.md   # 本文档
├── images/
│   └── 1.jpg                  # 背景图（1920×1080）
├── cascades/                  # Haar 级联模型（自包含，随项目附带）
│   ├── haarcascade_frontalface_default.xml
│   └── haarcascade_frontalface_alt2.xml
└── face_data/                 # 人脸数据目录（运行时自动创建）
    ├── face_db.json           # 人脸库 {id: {name, created_at, samples}}
    ├── face_model.yml         # LBPH 训练好的识别模型
    └── images/                # 学习时保存的人脸样本图（PNG 无损）
        ├── 1_0.png … 1_14.png
        └── 2_0.png …
```

### 人脸数据库格式（face_db.json）

```json
{
  "faces": {
    "1": {
      "name": "张三",
      "created_at": "2026-07-24 15:30:00",
      "samples": ["face_data/images/1_0.png", "...", "face_data/images/1_14.png"]
    },
    "2": {
      "name": "李四",
      "created_at": "2026-07-24 15:35:12",
      "samples": ["face_data/images/2_0.png", "..."]
    }
  }
}
```

- 键：人脸 ID（字符串形式，程序自动按递增分配）
- 值：`name`（姓名）、`created_at`（登记时间）、`samples`（样本图路径列表）

## 四、环境要求

- Python 3.8 及以上
- 依赖：
  - `opencv-contrib-python`（提供 `cv2` 与 `cv2.face` LBPH 识别器；**注意不要与 `opencv-python` 同时安装**）
  - `pygame-ce` 或 `pygame`（界面）
  - `numpy`（数组处理）
  - 外接 USB 摄像头

## 五、安装与运行

```bash
# 安装依赖
pip install opencv-contrib-python pygame-ce numpy

# 运行程序
python3 人脸学习.py
```

> 摄像头固定探测 `/dev/video41`（优先）和 `/dev/video40`。若节点编号不同，请修改 `人脸学习.py` 中 `open_camera()` 内的 `(41, 40)` 元组。运行时终端会打印实际使用的节点编号。

## 六、界面布局

界面采用左右双栏布局（1920×1080）：

```
y= 12   人脸学习与识别系统（标题 48pt 加粗，居中）    退出程序（右上角）
y= 75   [学习人脸]  [人脸识别]（模式切换按钮）

┌─ 左侧面板 (60,145) 1180×850 ──────────────┐  ┌─ 学习控件面板 (1280,145) 580×240 ─┐
│ 学习人脸 — 摄像头画面（副标题）            │  │ 学习控件                          │
│ ┌──────────────────────────────────────┐  │  │ 姓名：[________]                  │
│ │   摄像头画面 1100×520                │  │  │ [开始学习人脸]                    │
│ │   （识别时框出人脸并显示姓名/置信度）  │  │  └──────────────────────────────────┘
│ └──────────────────────────────────────┘  │  y=400 [查看人脸库详细信息] 按钮
│ 操作提示 / 识别历史                       │  ┌─ 已保存人脸库 (1280,470) 580×525 ─┐
└──────────────────────────────────────────┘  │ 已保存人脸库  共 N 人              │
                                               │ ID 1 : 张三  时间 样本N    [删除] │
                                               │ ID 2 : 李四  时间 样本N    [删除] │
                                               └───────────────────────────────────┘
y=1005  [删除操作 toast 提示]
y=1055  ESC 退出 | 摄像头画面已整合到主窗口 | 鼠标滚轮可滚动人脸库列表
```

### 模式切换

- **学习人脸模式**：左侧下方显示操作提示；右侧上方显示姓名输入框与学习按钮
- **人脸识别模式**：左侧下方显示识别历史；右侧上方显示最新识别结果；摄像头画面上实时绘制人脸框与姓名

## 七、工作流程

### 人脸学习流程

```
输入姓名 ──> 点击「开始学习人脸」（或按回车）
   │
   ├─ 姓名为空 ──> 提示"请先输入姓名"
   ├─ 摄像头未打开 ──> 提示"摄像头未打开"
   │
   └─ 启动子线程，在 LEARN_DURATION 秒内采集多帧
           │
           ├─ Haar 检测每帧中最大人脸，灰度化并归一化为 200×200
           ├─ 未检测到人脸 ──> 提示"学习失败，未在画面中检测到人脸"
           │
           └─ 分配新 ID（已有最大 ID + 1）
                   │
                   ├─ 样本图保存为 face_data/images/<id>_<n>.png（PNG 无损）
                   ├─ 写入 face_db.json（name + created_at + samples）
                   ├─ 用全部样本重训练 LBPH 模型并保存 face_model.yml
                   └─ 显示"✅ 学习成功！ID=xx 姓名=xx"
```

### 人脸识别流程

```
后台识别线程持续从最新帧检测并识别人脸（约 10fps）
   │
   ├─ Haar 检测人脸 → 提取归一化人脸 → LBPH predict 得到 (label, distance)
   │
   ├─ distance < 阈值 且 label 在库中 ──> 命中，查姓名，画面框为绿色显示姓名+置信度
   └─ 否则 ──> 未知，画面框为橙色显示"未知"
   │
   主循环：冷却机制（30 帧）避免同一结果刷屏，记录到识别历史
```

### 删除人脸流程

```
点击列表中「删除」按钮 ──> 弹出确认对话框（显示 ID 与姓名）
   │
   ├─ 点击「取消」或按 ESC ──> 关闭对话框
   │
   └─ 点击「确认删除」
           │
           ├─ 删除该 ID 的所有样本图文件
           ├─ 从 face_db.json 移除该 ID
           ├─ 用剩余样本重训练 LBPH 模型并保存
           └─ 底部 toast 提示"✅ 已删除：ID=xx 姓名=xx"
```

## 八、技术说明

### 1. 摄像头后台线程采集
摄像头采集是 I/O 密集操作，同步调用会卡顿。`camera_capture_loop()` 守护线程持续 `cap.read()` 并加锁更新 `latest_frame`，主循环零阻塞读取。采集时同样用 `_is_valid_frame` 校验，过滤偶发雪花/空帧。

### 2. 后台识别线程
`recognition_loop()` 线程在识别模式下对最新帧调用 `engine.recognize()`，结果供主循环绘制人脸框，避免在主循环做耗时检测。

### 3. 人脸学习子线程
`learn_new_face` 采集 + 训练是耗时操作，子线程执行并通过 `learn_lock` 互斥，期间按钮显示"学习中..."并禁用。

### 4. LBPH 识别与置信度
LBPH `predict` 返回 `(label, distance)`，distance 越小越相似（0=完美）。置信度映射为 `1 - distance/阈值`（0~1）。默认阈值 `RECOG_THRESHOLD=75`，真实人脸非匹配距离通常 >80，可正确拒绝；可按需调整。

### 5. PNG 无损样本
样本图使用 PNG 保存，避免 JPEG 压缩丢失人脸细节影响识别精度。

### 6. 删除即重训练
LBPH 不支持单样本删除，因此删除某 ID 后用剩余全部样本重新 `train` 并保存模型，保证识别结果与库一致。

### 7. 中文字体适配
`find_chinese_font()` 按优先级查找系统可用中文字体（SimHei / 微软雅黑 / PingFang / Noto Sans CJK / 文泉驿等）。

### 8. 摄像头雪花/卡顿防护（关键）
USB 摄像头在 Linux 上常见三类问题，本程序均做了防护：

| 问题 | 现象 | 防护机制 |
| --- | --- | --- |
| 默认格式不匹配 | `cap.read()` 返回成功但帧是雪花/绿屏 | 打开后设置 `CAP_PROP_FOURCC = MJPG` |
| 元数据/损坏节点 | `cap.read()` 卡在 `select()` 永不返回 | `signal.SIGALRM` 4 秒超时强制打断 |
| 随机噪声帧 | 帧数据是纯随机像素（std 反而很高） | `INTER_AREA` 下采样后标准差比值 < 0.2 判定为雪花 |

雪花检测原理：真实画面有空间结构（相邻像素相似），下采样后结构保留、std 几乎不变（r≈0.95）；随机噪声相邻像素无关，下采样时相互抵消、std 急剧下降（r≈0.03）。阈值 0.2 可清晰区分。

## 九、可配置项

在 `人脸学习.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `CAMERA_ID` | 保留兼容，已不影响探测（实际探测见 `open_camera`） | -1 |
| 摄像头节点 | 在 `open_camera()` 的 `(41, 40)` 元组中修改 | 41 优先，40 兜底 |
| `CAMERA_W` / `CAMERA_H` | 摄像头采集分辨率 | 1280 × 720 |
| `CAM_DISP_W` / `CAM_DISP_H` | 摄像头画面显示尺寸 | 1100 × 520 |
| `BG_IMAGE` | 背景图路径 | `images/1.jpg` |
| `FACE_DATA_DIR` | 人脸数据目录 | `face_data` |
| `RECOG_THRESHOLD` | LBPH 识别距离阈值 | 75.0 |
| `LEARN_SAMPLES` | 学习采集样本帧数 | 15 |
| `LEARN_DURATION` | 学习采集时长（秒） | 1.8 |
| `FACE_SIZE` | 样本归一化尺寸 | 200 |

## 十、对外 API（供其他程序调用）

本模块提供 `FaceEngine` 类与若干函数，其他程序可直接 import，无需启动界面：

```python
from 人脸学习 import FaceEngine, get_face_name, load_face_database, list_known_faces
```

### FaceEngine 类

| 方法 | 说明 |
| --- | --- |
| `FaceEngine(data_dir, threshold)` | 创建引擎并自动加载人脸库与模型 |
| `engine.load()` | 重新加载 JSON 数据库与模型 |
| `engine.learn_face(frames, name)` | 从多帧学习新人脸，返回分配的 ID |
| `engine.recognize(frame)` | 识别 frame 中所有人脸，返回 `[(box, face_id, name, confidence), ...]` |
| `engine.delete_face(face_id)` | 删除指定 ID 的人脸并重训练 |
| `engine.get_name(face_id)` | 根据 ID 查姓名 |
| `engine.list_faces()` | 返回 `[(id, info), ...]` |
| `engine.count()` | 返回人脸数量 |

### 便捷函数

| 函数 | 返回值 | 说明 |
| --- | --- | --- |
| `load_face_database(path)` | `{id: {name, created_at, samples}}` | 加载整个人脸库 |
| `get_face_name(face_id, path)` | `str` 或 `None` | 根据 ID 查姓名 |
| `list_known_faces(path)` | `[(id, name), ...]` | 所有人脸 ID 与姓名 |

### 调用示例

```python
from 人脸学习 import FaceEngine

# 其他程序：加载已训练好的模型，直接识别
engine = FaceEngine()
# 摄像头取一帧 frame ...
for box, face_id, name, conf in engine.recognize(frame):
    if name:
        print("识别到：{}  置信度 {:.0%}".format(name, conf))
    else:
        print("未知人脸")
```

```python
from 人脸学习 import get_face_name, load_face_database, list_known_faces

# 根据 ID 查姓名
name = get_face_name(1)
# 获取整个人脸库
db = load_face_database()
# 获取 ID-姓名 列表
for face_id, name in list_known_faces():
    print("{}: {}".format(face_id, name))
```

## 十一、快捷键与交互

| 按键/操作 | 功能 |
| --- | --- |
| `ESC` | 关闭弹窗 → 退出程序 |
| `回车` | 学习模式下输入姓名后直接开始学习 |
| `Backspace` | 删除输入框中最后一个字符 |
| 鼠标滚轮 | 在人脸库列表区域上下滚动 |
| 鼠标点击 | 选择输入框、点击按钮、删除人脸等 |
