# 人脸学习与识别项目文档

基于 `pygame-ce` 与好搭AI派 `camera_vision_system_v3` 视觉系统开发的桌面人脸学习与识别程序，固定窗口尺寸 1920×1080，支持人脸学习登记、实时识别、人脸库查看与删除管理，并将摄像头画面直接整合到主窗口中。

## 一、功能特性

| 模块 | 功能 |
| --- | --- |
| 人脸学习 | 输入姓名后点击按钮，调用摄像头自动学习人脸并分配 ID，保存到 `face_data/face_db.json` |
| 人脸识别 | 实时识别摄像头画面中的人脸，按已保存的 ID 匹配姓名并显示置信度 |
| 摄像头整合 | 摄像头画面直接嵌入主窗口（1100×520），不再弹出独立预览窗口 |
| 后台采集 | 独立线程持续采集画面，主循环零阻塞，界面不卡顿 |
| 人脸库查看 | 弹窗分页显示所有人脸的 ID、姓名、登记时间 |
| 人脸删除 | 列表中每条记录旁有「删除」按钮，带二次确认对话框 |
| 识别历史 | 实时记录识别结果（时间、ID、姓名、置信度），最多保留 20 条 |
| 数据库调用 | 提供 `get_face_name` 等函数供其他程序直接 import 调用 |
| 背景 | 使用 `images/1.jpg` 作为全屏背景图 |
| 退出 | 右上角「退出程序」按钮 |

## 二、快捷键与交互

| 按键/操作 | 功能 |
| --- | --- |
| `ESC` | 关闭弹窗 → 退出程序 |
| `回车` | 学习模式下输入姓名后直接开始学习 |
| `Backspace` | 删除输入框中最后一个字符 |
| 鼠标滚轮 | 在人脸库列表区域上下滚动 |
| 鼠标点击 | 选择输入框、点击按钮、删除人脸等 |

## 三、项目结构

```
haoda_AIPad/
├── 人脸学习.py                # 主程序
├── 人脸学习与识别项目文档.md   # 本文档
├── images/
│   └── 1.jpg                  # 背景图（1920×1080）
├── face_data/                 # 人脸数据库目录（自动创建）
│   └── face_db.json           # 人脸库 JSON 文件
└── camera_vision_system_v3.py # 好搭AI派视觉系统模块（SDK 提供）
```

### 人脸数据库格式（face_db.json）

```json
{
  "faces": {
    "1": {
      "name": "张三",
      "created_at": "2026-07-24 15:30:00"
    },
    "2": {
      "name": "李四",
      "created_at": "2026-07-24 15:35:12"
    }
  }
}
```

- 键：人脸 ID（字符串形式，由视觉系统自动分配）
- 值：包含 `name`（姓名）和 `created_at`（登记时间）的对象

## 四、环境要求

- Python 3.8 及以上
- 依赖：
  - `pygame-ce`（提供 `pygame` 模块）
  - `opencv-python`（提供 `cv2`，画面格式转换）
  - `numpy`（数组转置）
  - 好搭AI派 SDK：`camera_vision_system_v3`（视觉系统，含人脸学习与识别算法）
  - 外接摄像头（USB 摄像头）

## 五、安装与运行

```bash
# 安装依赖
pip install pygame-ce opencv-python numpy

# 运行程序
python3 人脸学习.py
```

> 若摄像头无法打开，请修改 `人脸学习.py` 顶部的 `CAMERA_ID`（-1 自动选择，或改为 0/1/2 指定具体摄像头编号）。

## 六、界面布局

界面采用左右双栏布局（1920×1080）：

```
y= 12   人脸学习与识别系统（标题 48pt 加粗，居中）    退出程序（右上角）
y= 75   [学习人脸]  [人脸识别]（模式切换按钮）

┌─ 左侧面板 (60,145) 1180×850 ──────────────┐  ┌─ 学习控件面板 (1280,145) 580×240 ─┐
│ y=165 学习人脸 — 摄像头画面（副标题）      │  │ 学习控件                          │
│ y=250 ┌──────────────────────────────┐    │  │ 姓名：[________]                  │
│       │                              │    │  │ [开始学习人脸]                    │
│       │   摄像头画面 1100×520        │    │  └──────────────────────────────────┘
│       │   （识别时左上角显示检测指示） │    │  y=400 [查看人脸库详细信息] 按钮
│ y=770 └──────────────────────────────┘    │  ┌─ 已保存人脸库 (1280,470) 580×525 ─┐
│ y=790 操作提示 / 识别历史                 │  │ 已保存人脸库  共 N 人              │
│       1. 在右侧输入框输入姓名             │  │ ┌──────────────────────────────┐  │
│       2. 点击「开始学习人脸」...          │  │ │ ID 1 : 张三     2026-... [删除]│  │
└──────────────────────────────────────────┘  │ │ ID 2 : 李四     2026-... [删除]│  │
                                               │ └──────────────────────────────┘  │
                                               └───────────────────────────────────┘
y=1005  [删除操作 toast 提示]
y=1055  ESC 退出 | 摄像头画面已整合到主窗口 | 鼠标滚轮可滚动人脸库列表
```

### 模式切换

- **学习人脸模式**：左侧下方显示操作提示；右侧上方显示姓名输入框与学习按钮
- **人脸识别模式**：左侧下方显示识别历史；右侧上方显示最新识别结果（姓名/ID/置信度/时间）

## 七、工作流程

### 人脸学习流程

```
输入姓名 ──> 点击「开始学习人脸」（或按回车）
   │
   ├─ 姓名为空 ──> 提示"请先输入姓名"
   ├─ 摄像头未打开 ──> 提示"摄像头未打开"
   │
   └─ 启动子线程执行 vision_system.learn_new_face()
           │
           ├─ 学习失败 ──> 显示错误信息
           │
           └─ 学习成功 ──> 获取 face_id
                   │
                   ├─ 写入 faces 字典（name + created_at）
                   ├─ 保存到 face_db.json
                   └─ 显示"✅ 学习成功！ID=xx 姓名=xx"
```

### 人脸识别流程

```
主循环每帧 ──> refresh_results() 刷新检测结果
   │
   ├─ 未检测到人脸 ──> 等待
   │
   └─ 检测到人脸 ──> 获取 face_id 与 confidence
           │
           ├─ 冷却中且 ID 未变 ──> 跳过（避免重复记录）
           │
           └─ 冷却结束或 ID 变化 ──> 查询 get_face_name(face_id)
                   │
                   ├─ 已登记 ──> 显示姓名（绿色）+ 记入历史
                   └─ 未登记 ──> 显示"未知人脸"（橙色）+ 记入历史
```

### 删除人脸流程

```
点击列表中「删除」按钮 ──> 弹出确认对话框（显示 ID 与姓名）
   │
   ├─ 点击「取消」或按 ESC ──> 关闭对话框
   │
   └─ 点击「确认删除」
           │
           ├─ 从 faces 字典移除该 ID
           ├─ 保存到 face_db.json
           └─ 底部显示 toast 提示"✅ 已删除：ID=xx 姓名=xx"（持续 6 秒）
```

## 八、技术说明

### 1. 摄像头后台线程采集（解决卡顿）

摄像头画面采集是 I/O 密集操作，若在主循环中同步调用 `capture_frame()` 会阻塞 UI 渲染导致界面卡顿。

**解决方案**：启动 `camera_capture_loop()` 守护线程，持续采集画面并转为 pygame Surface，通过 `threading.Lock` 保护共享的 `latest_cam_surface`。主循环只需加锁读取已生成的 Surface，零阻塞。采集频率控制在约 25fps（`time.sleep(0.04)`），降低 CPU 占用。

### 2. 人脸学习子线程

`learn_new_face()` 是耗时操作，直接在主线程调用会冻结界面。

**解决方案**：点击学习按钮后启动 `daemon` 子线程执行学习，通过 `learn_lock` 互斥锁防止重复触发。学习期间按钮显示"学习中..."并禁用，完成后更新状态提示。

### 3. 识别冷却机制

连续检测到同一人脸时，若每帧都记录会导致历史列表被同一结果刷屏。

**解决方案**：引入 `recog_cooldown`（30 帧 ≈ 1 秒）冷却计时。仅当冷却结束或检测到的 `face_id` 发生变化时才记录新结果。

### 4. OpenCV 画面转 pygame Surface

摄像头原始画面为 OpenCV 的 BGR NumPy 数组，需转换为 pygame 可用的 Surface。

**解决方案**：`cvframe_to_surface()` 先 `cvtColor` 转 RGB，再 `transpose` 转置轴顺序，用 `surfarray.make_surface` 生成 Surface，最后 `smoothscale` 缩放到显示尺寸。

### 5. 人脸 ID 提取兼容

`learn_new_face()` 的返回值类型在不同 SDK 版本中可能为 `int`、`dict` 或 `str`。

**解决方案**：`extract_face_id()` 按优先级依次尝试解析，兼容多种返回格式。

### 6. 中文字体适配

`find_chinese_font()` 按优先级查找系统可用中文字体（SimHei / 微软雅黑 / PingFang / Noto Sans CJK / 文泉驿等），保证中文界面在 Windows / macOS / Linux 下均能正常显示。

## 九、可配置项

在 `人脸学习.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `CAMERA_ID` | 摄像头编号（-1 自动选择） | -1 |
| `CAMERA_W` / `CAMERA_H` | 摄像头采集分辨率 | 1280 × 720 |
| `BG_IMAGE` | 背景图路径 | `images/1.jpg` |
| `FACE_DB_DIR` | 人脸数据库目录 | `face_data` |
| `FACE_DB_FILE` | 人脸库 JSON 文件路径 | `face_data/face_db.json` |
| `RECOG_COOLDOWN_FRAMES` | 识别冷却帧数 | 30 |
| `DETAIL_PAGE_SIZE` | 查看弹窗每页条数 | 8 |

## 十、对外 API（供其他程序调用）

本模块提供以下函数，其他程序可直接 import 调用，无需启动界面：

```python
from 人脸学习 import get_face_name, load_face_database, list_known_faces
```

| 函数 | 参数 | 返回值 | 说明 |
| --- | --- | --- | --- |
| `load_face_database(path)` | `path`：JSON 文件路径，可选 | `{face_id: {"name":..., "created_at":...}}` | 加载整个人脸库字典 |
| `save_face_database(faces, path)` | `faces`：字典；`path`：路径，可选 | 无 | 保存人脸库到 JSON |
| `get_face_name(face_id, path)` | `face_id`：人脸 ID；`path`：可选 | `str` 或 `None` | 根据 ID 查姓名，未登记返回 None |
| `list_known_faces(path)` | `path`：可选 | `[(face_id, name), ...]` | 获取所有人脸 ID 与姓名列表 |

### 调用示例

```python
from 人脸学习 import get_face_name, load_face_database, list_known_faces

# 1. 根据人脸 ID 查姓名
name = get_face_name(1)
if name:
    print(f"识别到：{name}")
else:
    print("未登记人脸")

# 2. 获取整个人脸库
db = load_face_database()
for fid, info in db.items():
    print(f"ID={fid}  姓名={info['name']}  登记时间={info['created_at']}")

# 3. 获取 ID-姓名 列表
for face_id, name in list_known_faces():
    print(f"{face_id}: {name}")
```

## 十一、模式与状态定义

| 模式常量 | 含义 |
| --- | --- |
| `MODE_LEARN` | 学习人脸模式（输入姓名 + 学习） |
| `MODE_RECOGNIZE` | 人脸识别模式（实时识别 + 历史记录） |

### 学习状态

| 状态 | 含义 | 按钮显示 |
| --- | --- | --- |
| 空闲 | 可输入姓名并开始学习 | 开始学习人脸 |
| 学习中 | 子线程正在调用 `learn_new_face()` | 学习中...（禁用） |

### 识别状态

| 场景 | 显示内容 | 颜色 |
| --- | --- | --- |
| 摄像头未打开 | 摄像头未打开 | 红色 |
| 等待识别 | 等待识别人脸... | 灰色 |
| 识别到已登记 | 识别到：{姓名} | 绿色 |
| 识别到未登记 | 未知人脸 | 橙色 |

## 十二、弹窗说明

### 1. 查看人脸库详细信息弹窗

- 点击「查看人脸库详细信息」按钮打开
- 半透明遮罩 + 1200×820 面板
- 分页显示，每页 8 条，含 ID、姓名、登记时间
- 支持上一页/下一页翻页
- ESC 或「关闭」按钮关闭

### 2. 删除确认对话框

- 点击列表中「删除」按钮触发
- 520×240 对话框，显示待删除的 ID 与姓名
- 「确认删除」执行删除并保存；「取消」关闭对话框
- 删除后底部显示 toast 提示（持续 6 秒）

## 十三、参考范例

本程序基于好搭AI派视觉算法范例开发：

| 范例文件 | 对应功能 |
| --- | --- |
| `5.AI视觉算法/08.人脸学习1.hd` | 人脸学习基础 |
| `5.AI视觉算法/09.人脸学习2.hd` | 人脸学习进阶 |
| `5.AI视觉算法/10.人脸识别.hd` | 人脸识别 |

> 范例使用 `camera_vision_system_v3` 视觉系统模块，本程序在其基础上集成了 GUI 界面、人脸库管理（JSON 持久化）、查看与删除功能。
