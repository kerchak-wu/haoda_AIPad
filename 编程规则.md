# 编程规则

## 规则一：涉及外接硬件模块的代码（必须严格参考附件）
- 当用户需求涉及外接硬件（如 USB 摄像头、温湿度传感器、超声波传感器、电机、舵机、RGB灯、单按键、DHT11、DS18B20、RGB灯带、录音器、扬声器等）时，生成的代码必须：
  - 严格使用附件中出现的库和函数（如 `from ESP32 import *`、`from audio_recorder import AudioRecorder`、`from voice_api import VoiceAPI`、`from camera_vision_system_v3 import create_vision_system_v3`、`import cv2`、`import pygame` 等）。
  - 遵循附件中的代码风格：包含 `try-except` 异常处理、`while True` 主循环、打印调试信息、适当延时等。
  - 生成可直接在“好搭AI派”硬件上运行的 Python 代码。
  - 优先改编附件中相似功能的范例代码。

## 规则二：不涉及外接硬件的代码（仅需符合通用 Python 规范）
- 当用户需求不涉及外接硬件（如纯算法实现、数据处理、界面逻辑、数学计算、字符串处理等）时，只需生成符合通用 Python 规范的代码，无需强制参考附件。
- 除范例代码中出现过的库以外，我还安装了opencv-contrib-python、pygame-ce、numpy 3个库。