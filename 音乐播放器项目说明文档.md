# 音乐播放器项目文档

基于 `pygame-ce` 开发的桌面音乐播放器，固定窗口尺寸 1920×1080，支持播放本地音乐文件、进度拖动、音量调节等功能。

## 一、功能特性

| 模块 | 功能 |
| --- | --- |
| 播放 | 自动扫描 `recordings/` 目录下的音乐文件（支持 mp3 / wav / ogg / flac） |
| 播放控制 | 上一首、播放/暂停、下一首、停止 |
| 进度条 | 可点击拖动跳转播放位置，实时显示已播时长与总时长 |
| 音量 | 音量 +/- 按钮，实时显示音量条与百分比（范围 0–100，步进 10） |
| 播放列表 | 居中显示曲目列表，当前播放项高亮 |
| 自动连播 | 一首播完自动播放下一首 |
| 背景 | 使用 `images/1.jpg` 作为全屏背景图 |
| 退出 | 右上角「退出程序」按钮 |

## 二、快捷键

| 按键 | 功能 |
| --- | --- |
| `空格` | 播放 / 暂停 |
| `←` | 上一首 |
| `→` | 下一首 |
| `↑` | 音量 + |
| `↓` | 音量 - |
| `ESC` | 退出程序 |

## 三、项目结构

```
haoda_AIPad/
├── music_player.py            # 主程序
├── 音乐播放器项目文档.md       # 本文档
├── images/
│   └── 1.jpg                  # 背景图（1920×1080）
└── recordings/                # 音乐文件目录（放入你的 mp3/wav/ogg/flac）
```

## 四、环境要求

- Python 3.8 及以上
- 依赖：`pygame-ce`（提供 `pygame` 模块）

## 五、安装与运行

```bash
# 安装依赖
pip install pygame-ce

# 运行播放器
python3 music_player.py
```

> 将你的音乐文件放入 `recordings/` 文件夹后启动程序即可自动加载。

## 六、界面布局

界面按自上而下垂直布局（1920×1080）：

```
y= 80   ♪ 音乐播放器（标题 64pt）
y=220   正在播放：xxx（40pt）
y=250   播放列表（大标题 50pt 加粗）
y=320   ┌───────────────────────┐
        │  播放列表项...          │
y=825   └───────────────────────┘
y=855   ▬▬▬▬▬▬▬▬▬▬▬▬ 进度条（时间轴）
y=877   00:12                  03:45
y=898   音量  ████████░░░░░░  70%
y=950   [上一首] [播放] [下一首] [停止]    [音量 -] [音量 +]
y=1050  快捷键提示
```

## 七、技术说明

### 1. 播放时长获取

`pygame.mixer.music` 在部分版本（如 pygame-ce 2.5.7）中没有 `get_length()` 方法，直接调用会抛出 `AttributeError`。

**解决方案**：在 `play_index()` 中使用 `pygame.mixer.Sound(path).get_length()` 预读取时长并缓存到 `current_length`，避免每次循环调用。

### 2. 播放位置跟踪

`pygame.mixer.music.get_pos()` 在暂停/拖动场景下不够可靠。

**解决方案**：采用手动计时机制：
- `play_start_tick`：记录每次 play/unpause 时刻的 pygame 时钟刻度
- `paused_pos`：暂停时累计已播放秒数
- `get_position()`：根据播放状态实时计算当前位置

### 3. 切歌即时停止

切换上一首/下一首时，原实现因 `Sound` 预读取耗时，旧曲目会继续发声直到新曲目加载完成。

**解决方案**：在 `play_index()` 开头先调用 `pygame.mixer.music.stop()` 立即停止当前播放，再进行时长检测与新文件加载。

### 4. 中文字体适配

`find_chinese_font()` 按优先级查找系统可用中文字体（SimHei / 微软雅黑 / PingFang / Noto Sans CJK / 文泉驿等），保证中文界面在 Windows / macOS / Linux 下均能正常显示。

## 八、可配置项

在 `music_player.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `MUSIC_DIR` | 音乐文件目录 | `recordings` |
| `BG_IMAGE` | 背景图路径 | `images/1.jpg` |
| `SUPPORTED_FORMATS` | 支持的音频格式 | mp3, wav, ogg, flac |
