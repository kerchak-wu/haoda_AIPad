# 姿态检测项目说明文档

基于好搭AI派（ESP32 + pygame）开发的姿态检测程序：USB外接摄像头实时采集画面，使用好搭AI派视觉系统内置的 `pose_detection` 算法检测人体姿态关键点，并通过几何特征分类自动判断「站立 / 坐着 / 蹲着 / 躺着 / 未知」五类姿态。界面实时显示摄像头画面（叠加关键点、骨架连线和姿态标签）、检测人数统计和每人详细特征数据。提供 `PoseDetector` 类供其他程序导入调用。

> **重要说明**：本项目使用好搭AI派官方视觉系统内置的 `pose_detection` 算法（非 MediaPipe）。官方算法的检测精度和关键点稳定性**不如 MediaPipe**，在光线不足、人体部分遮挡或侧身等场景下容易出现关键点缺失或漂移，进而影响姿态分类准确性。如需更高精度的姿态检测，建议参考「手势控制RGB灯带.py」使用 MediaPipe 的方案。但官方算法无需额外安装依赖，部署更简单。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 姿态检测 | `camera_vision_system_v3` 的 `enable_pose_detection` + `result_accessor`（参照范例 5.18） |
| 姿态分类 | 自研几何特征分类器：5 个归一化特征 + 站立白名单策略 + 二级分类 + 保守回退 |
| 摄像头采集 | **视觉系统统一管理**：`vision_system.open_camera()` + `capture_frame()`，不使用 cv2 VideoCapture |
| **分辨率适配** | **实际分辨率动态检测**：从 `capture_frame()` 返回的 frame.shape 读取真实宽高，叠加坐标基准不再依赖硬编码 CAMERA_W/H（v1.3+） |
| 界面显示 | `pygame`，1920×1080，背景图 `images/1.jpg` + 半透明面板；**Pygame 分段初始化**（参照 Rockchip 兼容补丁第 7 章） |
| 多线程 | 后台采集线程（0.05s 间隔）+ 主循环线程，`threading.Lock` 保护帧读写；采集线程做 3 重帧校验（shape/mean） |
| 日志输出 | `logs/姿态检测_YYYYMMDD.log`，追加模式不覆盖，块缓冲避免卡顿；`_TeeStdout` 双写 stdout/stderr；退出前显式 flush+close |
| 硬件控制 | `ESP32` 扩展板导入与 `board.start()` 均 try/except 容错；失败时进入仅界面调试模式，不提前崩溃 |
| Rockchip 兼容 | `LIBGL_ALWAYS_SOFTWARE=1` + pygame 分段 init + mixer 单次 init/quit，避免 Mali 驱动与 V4L2 死锁 |
| 清理流程 | `PoseDetector.close()`：停采集线程 → `threaded_system.stop_background_detection()` → `vision_system.cleanup()`（参照 V3 API 报告） |

> **关键设计**：摄像头完全由视觉系统管理（`open_camera` + `capture_frame`），不使用 cv2 VideoCapture，避免与视觉系统的 V4L2 设备冲突。姿态检测必须 `open_camera()` + `start_background_detection()`，因为结果获取依赖后台检测线程。

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 实时画面 | 摄像头画面嵌入主窗口（880×660），约 20 FPS 显示 |
| 姿态检测 | 视觉系统后台检测人体姿态，返回检测框、置信度、17 个关键点坐标 |
| 姿态分类 | 自动判断「站立 / 坐着 / 蹲着 / 躺着 / 未知」五类，基于几何特征分析 |
| 画面叠加 | 检测框（按姿态着色）+ 姿态标签 + 置信度 + 关键点圆点 + 骨架连线 |
| 人数统计 | 实时显示检测人数和各类姿态人数（如「站立1 坐着1」） |
| 特征展示 | 右侧面板显示每人置信度条、检测框坐标、关键点可见数、分类特征值 |
| 降频策略 | 连续 3 次无结果自动降频（10fps→3fps），恢复后回升，节省 CPU |
| 底部图例 | 5 类姿态色块标注，便于快速对照画面中框的颜色 |
| 模块调用 | 提供 `PoseDetector` 类，其他程序可 `from 姿态检测 import PoseDetector` 导入 |
| 退出方式 | 界面「退出程序」按钮 / ESC 键 / 窗口关闭按钮 |
| 调试日志 | 所有 print 输出同步写入 `logs/姿态检测_YYYYMMDD.log`，方便导出排查 |

## 三、姿态分类算法

### 1. 分类流程

```
特征不足（<8个可见关键点） ──→ unknown（未知）
        │
        ▼
站立白名单：5 项 ALL 通过？ ──是──→ standing（站立）
        │否（禁止判站立）
        ▼
躯干水平（倾角≥52°）或 身体横向（高/宽≤1.05）？ ──是──→ lying（躺着）
        │否
        ▼
膝盖弯得狠（≤115°）且 身体矮胖（高/宽≤1.45）？ ──是──→ squatting（蹲着）
        │否
        ▼
          sitting（坐着，保守回退）
```

### 2. 五个归一化特征

| 特征 | 含义 | 站立典型值 | 坐着/蹲着典型值 | 躺着典型值 |
| --- | --- | --- | --- | --- |
| `aspect_ratio` | 身体高 / 身体宽（包围盒） | ≥1.55（细长） | ≤1.45（矮胖） | ≤1.05（横向） |
| `knee_angle_min` | 两膝 ∠髋-膝-踝 的较小值（°） | ≥145°（近乎直） | ≤115°（明显弯） | — |
| `torso_tilt_deg` | 肩中点→髋中点 连线偏离竖直的角度 | ≤22°（直立） | — | ≥52°（水平） |
| `leg_straight_ratio` | 小腿垂直跨度 / 整条腿垂直跨度 | ≥0.78（直腿） | <0.7（弯腿） | — |
| `hip_rel_height` | 髋部在「头顶→脚底」区间的相对高度 | ≤0.62（中间） | ≥0.7（靠近脚底） | — |

> 所有特征均归一化（比例或角度），与摄像头距离、分辨率无关，跨设备复用。

### 3. 站立白名单策略（保守判定）

站立判定采用**白名单**而非兜底：只有 5 项强证据**同时满足**才判「站立」，任一项不满足即禁止判站立。这是参考量产保守判定经验的设计——避免把坐着、蹲着误判为站立。

### 4. 保守回退

- 特征不全（可见关键点 <8 个）→ `unknown`，不冒险判站立
- 排除站立和躺着后，膝盖弯曲不明显 → 保守回退到 `sitting`

### 5. 姿态颜色映射

| 姿态键名 | 显示名称 | 颜色 | 说明 |
| --- | --- | --- | --- |
| `standing` | 站立 | 蓝 (40, 160, 255) | 身体直立、腿伸直 |
| `sitting` | 坐着 | 绿 (60, 190, 90) | 保守回退默认类别 |
| `squatting` | 蹲着 | 橙 (255, 165, 0) | 膝盖弯曲大、身体矮胖 |
| `lying` | 躺着 | 紫 (180, 80, 220) | 躯干水平或身体横向 |
| `unknown` | 未知 | 灰 (150, 150, 150) | 关键点不足、无法分类 |

## 四、界面布局

界面按左摄像头右信息面板布局（1920×1080）：

```markdown
y=  0   ┌──────────── 顶部标题区 (高130) ────────────┐
y= 25   │           姿态检测（标题 64pt）     [退出程序]│
y= 90   │ USB摄像头实时采集 · 视觉系统内置姿态检测算法 │
y=130   └────────────────────────────────────────────┘
y=150   ┌──────────────────┐  ┌─────────────────────────┐
        │ 摄像头画面面板     │  │  检测结果                │
        │ ● 已连接           │  │  检测人数  2             │
        │                    │  │  刷新频率：每3帧...      │
        │   实时摄像头画面    │  │  检测到2人（站立1 坐着1） │
        │   + 检测框(着色)    │  │  ───────────────────    │
        │   + 姿态标签        │  │  姿态详情                │
        │   + 关键点圆点      │  │  第1人  [站立]           │
        │   + 骨架连线        │  │  ████████░░ 96%         │
        │   (880 × 660)      │  │  检测框：(12%, 15%)...   │
        │                    │  │  关键点：15/17 可见      │
        │                    │  │  高宽比:1.82 膝角:168°  │
        │  实际分辨率（动态）│  │  ───────────────────    │
        │                    │  │  第2人  [坐着]           │
        │                    │  │  ...                     │
y=970   └──────────────────┘  └─────────────────────────┘
y=970   ┌──────────── 底部栏 (高110) ────────────────┐
y=1025  │ ESC退出 · 红圆点=关节 · 蓝连线=骨架   ■站立 ■坐着 ■蹲着 ■躺着 ■未知 │
y=1080  └────────────────────────────────────────────┘
```

> 退出按钮固定置于标题栏右上角 `(WIDTH-280, 30, 240×70)`，不设在右下角。
> 底部栏右侧排有 5 类姿态色块图例，便于对照画面中检测框的颜色。

## 五、项目结构

```markdown
haoda_AIPad/
├── 姿态检测.py                      # 主程序
├── 姿态检测项目说明文档.md          # 本文档
├── logs/                            # 日志目录（自动生成）
│   └── 姿态检测_YYYYMMDD.log        # 按日期命名的日志文件
├── images/
│   └── 1.jpg                        # 背景图（1920×1080）
├── 好搭AI派学习手册.md              # 硬件 API 参考
├── 好搭AI派范例代码.md              # 范例代码参考
├── 物体学习.py                      # 视觉系统摄像头管理模式参考
├── 人脸识别灯效.py                  # 降频策略参考
└── 手势控制RGB灯带.py               # MediaPipe 姿态检测参考（对比项目）
```

## 六、硬件接线

| 接口 | 设备 | 说明 |
| --- | --- | --- |
| USB | 外接摄像头 | 接在扩展板上，系统识别为 `/dev/video40` 或 `/dev/video41` |
| ESP32 | 好搭AI派扩展板 | `board.start()` 初始化，USB摄像头依赖扩展板 |

> 注意：好搭AI派右下角开关需拨到左侧以启用外设接口。
> 注意：USB摄像头接在扩展板上，扩展板连接异常时摄像头也无法使用，姿态检测无法运行。

## 七、环境要求

- 好搭AI派平台（ESP32 + Linux）
- 依赖（均平台自带）：
  - `pygame` —— 界面显示
  - `opencv-python`（提供 `cv2`）—— 仅用于图像格式转换（BGR→RGB→Surface）和姿态可视化叠加
  - `numpy` —— 数组处理
  - `math` —— 角度计算（标准库）
  - `ESP32` —— 硬件控制
  - `camera_vision_system_v3` —— 姿态检测算法（好搭AI派自带）
- 字体文件（位于 `/home/cxdz/jupyter/assets/`）：
  - `PingFang_Regular.ttf` —— 常规文字
  - `PingFang_Bold.ttf` —— 粗体（标题、按钮）
- 外接 USB 摄像头

> 与「手势控制RGB灯带.py」不同，本项目**不需要安装 mediapipe**，完全基于好搭AI派自带的视觉系统算法。

## 八、运行

```bash
python3 姿态检测.py
```

启动后：

1. 程序自动初始化 ESP32 扩展板和视觉系统（含 `open_camera` + `start_background_detection`）
2. 摄像头画面实时显示在左侧面板，叠加姿态检测可视化
3. 站在摄像头前 1-3 米处，保持身体在画面内
4. 右侧面板实时显示检测人数、每人姿态标签、置信度和特征值
5. 画面中检测框颜色对应姿态类型（蓝=站立、绿=坐着、橙=蹲着、紫=躺着、灰=未知）
6. 底部栏右侧有姿态颜色图例对照
7. 点击界面右上角「退出程序」按钮或按 ESC 退出

## 九、模块调用（PoseDetector 类）

`PoseDetector` 类封装了视觉系统的姿态检测和分类功能，可供其他程序导入调用：

```python
from 姿态检测 import PoseDetector, classify_pose, POSE_LABEL

# 初始化（内部会 open_camera + start_background_detection）
detector = PoseDetector()

# 获取最新姿态检测结果（自动触发刷新）
results = detector.get_pose_results()

# 每个结果 r 包含：
#   r['posture']:           'standing' / 'sitting' / 'squatting' / 'lying' / 'unknown'
#   r['posture_features']:  归一化特征字典（aspect_ratio, knee_angle_min, ...）
#   r['box']:               检测框 (x, y, w, h)，可能为相对坐标或绝对像素
#   r['confidence']:        检测置信度 0-1
#   r['keypoints']:         17 个关键点坐标列表

for r in results:
    key = r['posture']
    name = POSE_LABEL[key]['name']     # '站立' / '坐着' / ...
    conf = r['confidence']
    print('%s (%.0f%%)' % (name, conf * 100))

# 释放资源
detector.close()
```

### PoseDetector API

| 方法 | 说明 |
| --- | --- |
| `PoseDetector(width=1280, height=720)` | 构造函数，初始化视觉系统、打开摄像头、启动后台检测、启动采集线程 |
| `get_pose_results()` | 获取最新姿态检测结果列表（自动触发刷新，含频率控制）。每个元素含 `box`/`confidence`/`keypoints`/`posture`/`posture_features` |
| `get_cached_results()` | 仅获取缓存结果，不触发刷新（用于高频绘制） |
| `get_current_frame()` | 获取当前摄像头帧的副本（线程安全） |
| `get_actual_resolution()` | **v1.3+** 获取实际摄像头分辨率 `(w, h)`；启动初期尚未检测到帧时回退到 `CAMERA_W/H`。用于坐标缩放基准，避免请求分辨率与实际分辨率不一致时叠加层错位 |
| `close()` | 停止采集线程并释放视觉系统资源 |

### 独立调用姿态分类函数

```python
from 姿态检测 import classify_pose, POSE_LABEL

# keypoints 为视觉系统返回的 17 点坐标列表
posture, features = classify_pose(keypoints)
print(POSE_LABEL[posture]['name'])  # '站立' / '坐着' / ...
print(features['knee_angle_min'])   # 膝角
print(features['aspect_ratio'])     # 高宽比
```

## 十、技术说明

### 1. 视觉系统初始化（严格参照范例 5.18）

```python
self.vision_system = create_vision_system_v3(
    camera_id=-1, width=width, height=height,
    enable_basic=False, enable_advanced=False
)
self.vision_system.detection_config.enable_pose_detection = True
self.vision_system._init_detectors()
self.vision_system.open_camera()
self.vision_system.threaded_system.start_background_detection(show_preview=False)
```

### 2. 姿态检测结果获取

严格参照范例代码 5.18：

```python
self.vision_system.result_accessor.refresh_results()
count = self.vision_system.result_accessor.get_pose_detection_count()
for i in range(count):
    box = self.vision_system.result_accessor.get_pose_detection_box(i)
    confidence = self.vision_system.result_accessor.get_pose_detection_confidence(i)
    keypoints = self.vision_system.result_accessor.get_pose_detection_keypoints(i)
```

### 3. 摄像头管理（关键设计）

**不使用 cv2 VideoCapture**，完全由视觉系统管理摄像头：

| 方式 | 是否使用 | 原因 |
| --- | --- | --- |
| `vision_system.open_camera()` | ✓ | 姿态检测必须，打开 V4L2 设备 |
| `vision_system.capture_frame()` | ✓ | 获取当前帧用于界面显示，只读缓存不访问 V4L2 |
| `cv2.VideoCapture()` | ✗ | 会与视觉系统抢 V4L2 设备导致冲突 |
| `start_background_detection()` | ✓ | 后台检测线程持续检测，结果由 result_accessor 获取 |

### 4. 多线程架构

| 线程 | 职责 | 间隔 |
| --- | --- | --- |
| 主循环 | 事件处理 + 界面绘制 + 检测结果刷新 | `clock.tick(30)` 约 33ms |
| 采集线程 | `vision_system.capture_frame()` 获取帧并覆盖 `raw_frame` | 固定 0.05s（≈20fps） |
| 视觉系统后台检测线程 | 持续姿态检测（由 `start_background_detection` 启动） | 视觉系统内部管理 |

### 5. 降频策略（参考人脸识别灯效）

检测结果刷新采用自适应频率控制：

| 状态 | 刷新频率 | 触发条件 |
| --- | --- | --- |
| 正常 | 每 3 帧（≈10fps） | 默认状态 |
| 降频 | 每 10 帧（≈3fps） | 连续 3 次无结果或异常 |
| 恢复 | 回到每 3 帧 | 再次检测到结果 |

> 降频策略避免无人时高频调用 `refresh_results()` 浪费 CPU。

### 6. 帧缩放方式

全程使用 `pygame.transform.scale`（非 `smoothscale`）进行帧和背景缩放，降低 CPU 开销（项目经验：smoothscale 对摄像头帧缩放、1920×1080 大图缩放都有显著 CPU 开销，容易拖慢主循环）。

### 7. Rockchip 平台兼容性补丁（参照《视觉系统摄像头调用参考方案》第 7 章）

在好搭AI派（RK3588 等 Rockchip SoC）上运行时必须启用以下补丁，否则容易出现 pygame/mixer/驱动崩溃：

| 补丁 | 实现 | 作用 |
| --- | --- | --- |
| 环境变量 | 程序开头 `os.environ.setdefault('LIBGL_ALWAYS_SOFTWARE', '1')`，必须在 `import pygame` 之前设置 | 强制 libGL 软件渲染，绕过 Mali GPU 驱动在 pygame+cv2 同进程下的崩溃问题 |
| Pygame 分段初始化 | 不调用 `pygame.init()`，分别 `pygame.display.init()` + `pygame.font.init()`，mixer 单独 try/except 初始化 | 避免一次性 init 时 joystick/mixer/CDROM 等子模块在 ARM 驱动上的段错误 |
| mixer 单次 init/quit | `__init__` 中只初始化一次 mixer，`_cleanup`/退出时统一 quit，不反复 init/quit | 避免 Rockchip 音频驱动反复 init/quit 后出现死锁或无声 |

> 以上补丁在「人脸识别灯效.py」「文字识别触发视频播放wb.py」等项目已验证稳定。

### 8. ESP32 与视觉系统导入容错

| 场景 | 原行为 | 新行为 |
| --- | --- | --- |
| `from ESP32 import *` 失败 | ImportError 崩溃 | 捕获异常、打日志、继续运行（界面可启动但姿态检测不可用，用于调试） |
| `from camera_vision_system_v3 import ...` 失败 | ImportError 崩溃 | 捕获异常、打日志、启动调试模式（界面显示、无检测） |
| `board.start()` 返回 False 或抛异常 | 仅打印警告但后续 PoseDetector 可能崩溃 | 明确记录 `_board_isstarted=False`，PoseDetector 内部再判定摄像头可用性 |
| `create_vision_system_v3` 抛异常 | 程序崩溃 | PoseDetector 捕获异常、记录日志、`vision_system=None`，进入调试模式 |

### 9. 后台检测线程启停位置（参照 camera_vision_system_v3_API 分析报告）

| 操作 | 正确调用位置 | 易错位置 |
| --- | --- | --- |
| 启动后台检测 | `vision_system.threaded_system.start_background_detection(show_preview=False)` | — |
| 停止后台检测 | `vision_system.threaded_system.stop_background_detection()` | ❌ 不要在 `vision_system` 顶层找 stop_*（不存在） |

`PoseDetector.close()` 清理顺序：
1. 停止采集线程（`self._capture_running=False` + `sleep(0.2)`）
2. `threaded_system.stop_background_detection()` 停止视觉系统后台检测
3. `vision_system.cleanup()` 释放视觉系统资源

### 10. 帧有效性校验（采集线程内）

`_capture_loop` 中对 `capture_frame()` 返回的帧做 3 重校验：
1. 非 `None` 且有 `shape` 属性
2. `len(shape) == 3` 且 `size > 0`（RGB/BGR 必须是 3 维数组）
3. `frame.mean()` 在 `[5, 250]` 区间（过滤 V4L2 偶发的全黑/全白脏帧）

> 使用 `frame.mean()` 而非 `gray.std()` 检测全黑/全白帧，因为 ARM 上灰度转换 + 标准差计算开销显著。

### 11. 日志机制

- 日志目录：`logs/`（自动创建）
- 文件命名：`姿态检测_YYYYMMDD.log`（按日期，同一天多次运行追加）
- 打开模式：`'a'`（追加，不覆盖）
- 缓冲方式：`buffering=-1`（块缓冲，避免后台检测线程高频写日志阻塞主循环）
- 运行分隔：每次启动写入 `======== YYYY-MM-DD HH:MM:SS 运行开始 ========` 标记

### 12. 实际分辨率动态适配机制（v1.3+，修复叠加层错位）

**问题背景**：`create_vision_system_v3(camera_id=-1, width=1280, height=720, ...)` 请求的 1280×720 只是"期望分辨率"，视觉系统库会根据摄像头硬件能力、USB 带宽、驱动限制自动回退。实际运行日志显示常见回退值为 **640×480**（如 2026-08-12 运行日志：`分辨率=640x480`）。若 `draw_pose_overlay()` 仍用硬编码 `CAMERA_W/H=1280×720` 作为坐标基准：
- 当库返回**绝对像素坐标**时（基于 640×480 的检测框/关键点），代码按 1280×720 换算 → 检测框和关键点**偏小、偏左上**，出现明显错位。
- 即使库返回相对坐标，右下角显示"1280×720"也会误导用户。

**解决方案（v1.3 起生效）**：

| 环节 | 实现 |
| --- | --- |
| **检测点** | `PoseDetector._capture_loop()` 首次拿到通过 shape/mean 校验的有效帧时，读取 `frame.shape → (H, W, C)`，取 `W=shape[1], H=shape[0]` 作为真实分辨率 |
| **存储** | `PoseDetector._actual_cam_w / _actual_cam_h`，由 `_actual_res_lock`（`threading.Lock`）保护跨线程读写 |
| **对外接口** | `PoseDetector.get_actual_resolution() → (w, h)`。启动初期（首帧未到时）回退到 `CAMERA_W/H`，保证调用方不崩溃 |
| **用于叠加缩放** | `PoseDetectApp.draw_camera()` 中 `draw_pose_overlay(frame, results, actual_cw, actual_ch, ...)` 的 `cam_src_w/cam_src_h` 参数从 `get_actual_resolution()` 获取，不再直接使用全局 `CAMERA_W/H` |
| **用于显示** | 摄像头面板右下角的分辨率标签（原为固定 1280×720）改为显示 `get_actual_resolution()` 的真实值 |
| **日志输出** | 首次检测到分辨率时打印一条对比日志，便于排查：<br>✓ 实际摄像头分辨率：640×480（请求 1280×720，已自动适配坐标缩放基准）<br>✓ 实际摄像头分辨率：1280×720（与请求值一致） |

**关键边界保证**：
- 即使实际值与请求值一致，也打印一条确认日志（便于事后核对）。
- `get_actual_resolution()` 在尚未检测到帧时回退到 `CAMERA_W/H`，避免 `draw_camera()` 在启动瞬间拿到 `(None, None)` 导致 `TypeError`。
- 分辨率只在**首次**检测时赋值并打印一次，不重复检测以节省 CPU。

### 13. 姿态分类阈值集中管理

所有判定阈值集中在两个字典中，便于标定调整：

```python
# 站立白名单阈值（全部要同时满足）
STAND_REQUIRE = {
    'aspect_ratio_min': 1.55,
    'knee_angle_min': 145.0,
    'torso_tilt_max': 22.0,
    'leg_straight_min': 0.78,
    'hip_rel_height_max': 0.62,
}

# 二级分类阈值
POSE_THRESHOLD = {
    'lying_aspect_max': 1.05,
    'lying_torso_tilt_min': 52.0,
    'squat_knee_angle_max': 115.0,
    'squat_aspect_max': 1.45,
    'min_visible_key_pts': 8,
}
```

> 如在实际环境中发现误判，微调对应数字即可。例如把 `knee_angle_min` 从 145 降到 140 可以让站立判定更宽松。

## 十一、官方算法与 MediaPipe 对比

> **重要提醒**：本项目使用好搭AI派官方视觉系统的 `pose_detection` 算法，效果不如 MediaPipe。

| 对比项 | 官方 pose_detection（本项目） | MediaPipe Pose（手势控制RGB灯带.py） |
| --- | --- | --- |
| 安装依赖 | 无需额外安装，平台自带 | 需用户自行安装 `mediapipe` |
| 关键点数量 | 17 点（COCO 格式） | 33 点（更丰富，含面部和脚部细节） |
| 检测精度 | 一般，光线不足或遮挡时易缺失 | 较高，关键点更稳定 |
| 实时性能 | 由视觉系统后台线程管理，CPU 占用可控 | 需自行管理推理，CPU 占用较高 |
| 骨架定义 | 17 点 COCO 标准连接 | 33 点完整人体骨架 |
| 部分遮挡 | 容易丢失关键点，影响分类 | 有遮挡恢复机制，鲁棒性更好 |
| 侧身/非正面 | 关键点漂移明显 | 相对稳定 |
| 部署难度 | 低（开箱即用） | 中（需安装 + 版本兼容） |

**选型建议**：

- 快速验证 / 教学 demo / 不想装额外依赖 → 用官方算法（本项目）
- 需要高精度姿态分类 / 关键点稳定性要求高 / 需要脚部细节 → 用 MediaPipe（参考手势控制RGB灯带.py）

## 十二、可配置项

在 `姿态检测.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `CAMERA_W` / `CAMERA_H` | **请求的**摄像头采集分辨率（传入 `create_vision_system_v3`）；**实际分辨率由 `get_actual_resolution()` 从 frame.shape 动态检测**，视觉系统可能因硬件限制回退到更低值（如 640×480）。叠加可视化坐标基准使用实际值，不使用此硬编码值 | 1280 × 720 |
| `CAM_DISP_W` / `CAM_DISP_H` | 摄像头画面显示尺寸 | 880 × 660 |
| `REFRESH_NORMAL` | 正常刷新频率（帧） | 3（≈10fps） |
| `REFRESH_SLOW` | 降频刷新频率（帧） | 10（≈3fps） |
| `FAIL_THRESHOLD` | 连续失败次数阈值 | 3 |
| `STAND_REQUIRE` | 站立白名单阈值字典 | 见第十章第 8 节 |
| `POSE_THRESHOLD` | 二级分类阈值字典 | 见第十章第 8 节 |
| `POSE_LABEL` | 姿态名称与颜色映射 | 见第三章第 5 节 |
| `FONT_PATH` | 常规字体路径 | `/home/cxdz/jupyter/assets/PingFang_Regular.ttf` |
| `FONT_BOLD_PATH` | 粗体字体路径 | `/home/cxdz/jupyter/assets/PingFang_Bold.ttf` |
| `_LOG_DIR` | 日志目录 | `logs` |

## 十三、常见问题与故障排查

### 1. 摄像头打不开

| 可能原因 | 排查与解决 |
| --- | --- |
| 扩展板未连接 | USB摄像头接在扩展板上，扩展板异常时摄像头不可用 |
| 设备节点不对 | 查看日志 `logs/姿态检测_*.log` 中的摄像头探测信息 |
| 摄像头被占用 | 确保没有其他程序正在使用摄像头 |

### 2. 姿态分类不准确

| 可能原因 | 排查与解决 |
| --- | --- |
| 官方算法精度有限 | 光线不足或遮挡时关键点缺失，参考第十一章对比说明 |
| 关键点不足（<8个） | 显示「未知」，调整站位让更多关节进入画面 |
| 站着被判成坐着 | 降 `STAND_REQUIRE['knee_angle_min']`（如 145→140）使站立更宽松 |
| 坐着被判成站着 | 升 `STAND_REQUIRE['aspect_ratio_min']`（如 1.55→1.65）使站立更严格 |
| 蹲着和坐着混淆 | 调 `POSE_THRESHOLD['squat_knee_angle_max']`（蹲着膝盖更弯） |
| 侧身检测不准 | 官方算法侧身关键点漂移明显，建议正面朝向摄像头 |

### 3. 画面卡顿

| 可能原因 | 排查与解决 |
| --- | --- |
| CPU 占用过高 | 确认降频策略生效（无人时应降到 3fps） |
| 后台检测线程开销 | 官方算法本身有 CPU 开销，属正常现象 |
| 日志写入频繁 | 确认日志为块缓冲 `buffering=-1` |

### 4. 检测不到人

| 可能原因 | 排查与解决 |
| --- | --- |
| 距离太远/太近 | 建议站在摄像头前 1-3 米处 |
| 身体部分出画 | 确保头、肩、髋、膝、踝都在画面内 |
| 光线太暗 | 增加环境光照 |

### 5. 程序启动就崩溃 / pygame 初始化失败

| 可能原因 | 排查与解决 |
| --- | --- |
| Rockchip Mali GPU 驱动与 pygame+cv2 冲突 | 确认 `LIBGL_ALWAYS_SOFTWARE=1` 在 `import pygame` 之前已设置（参照第 10 章第 7 节） |
| pygame.init() 一次性 init mixer/joystick 子模块崩溃 | 确认改为分段初始化：`display.init()` + `font.init()`，mixer 单独 try/except |
| ESP32 扩展板未接或驱动缺失 | 从 v1.2 起导入阶段已容错，不会崩溃；查看日志 `logs/姿态检测_*.log` 中是否有 `[警告] ESP32 ...` |

### 6. 退出程序后再次启动提示摄像头被占用 / 后台僵尸进程

| 可能原因 | 排查与解决 |
| --- | --- |
| 视觉系统后台检测线程未正常停止 | 从 v1.2 起 `PoseDetector.close()` 会先调 `threaded_system.stop_background_detection()`，再 `cleanup()`（参照第 10 章第 9 节） |
| 直接 kill 进程绕过了清理逻辑 | `ps aux | grep 姿态检测` 杀掉僵尸进程，或拔插 USB 摄像头重置设备 |

### 7. 画面显示全黑但采集线程正常运行

| 可能原因 | 排查与解决 |
| --- | --- |
| V4L2 偶发全黑脏帧被采集写入缓存 | 从 v1.2 起 `_capture_loop` 增加 `frame.mean()` 校验，mean<5 的全黑帧与 mean>250 的全白帧均被跳过（参照第 10 章第 10 节） |

### 8. 检测框/关键点与人体实际位置错位（偏小、偏左上）

| 可能原因 | 排查与解决 |
| --- | --- |
| **v1.2 及以下**：请求分辨率（默认 1280×720）与视觉系统实际打开的分辨率不一致（常见 640×480），叠加缩放仍用硬编码 1280×720 作基准 | **升级到 v1.3+**：v1.3 起使用 `get_actual_resolution()` 从 `frame.shape` 动态读取真实宽高，`draw_pose_overlay` 的坐标基准与实际帧保持一致 |
| 视觉系统返回绝对像素坐标，但代码按相对坐标处理 | 查看日志中"实际摄像头分辨率"的记录，确认基准值；`draw_pose_overlay` 内已对 0-1.5 范围值做相对坐标兼容，超出范围按绝对像素，一般无需调整 |
| 多人同时检测时边缘人错位明显 | 官方 pose_detection 对画面边缘目标关键点稳定性较差，尽量让被检测者处于画面中央 1-3 米处 |

## 十四、调试经验总结

### 1. 官方算法关键点格式兼容

视觉系统返回的关键点坐标基准未在文档中明确说明（可能是相对 0-1 或绝对像素）。`_kp()` 函数和 `draw_pose_overlay()` 均做了兼容处理：如果坐标值在 0-1.5 范围内则视为相对坐标并按画面尺寸缩放，否则视为绝对像素。

### 2. 站立白名单策略的必要性

最初尝试用「站立兜底」方式（不满足躺/蹲/坐就判站），导致坐着和半蹲被误判为站立。改为「站立白名单」后：只有 5 项强证据全部满足才判站，否则禁止判站，大幅降低误判率。

### 3. 保守回退而非冒险判定

关键点不足时（<8 个可见），不尝试猜测姿态，直接返回 `unknown`。排除站立后如果也不符合躺/蹲条件，保守回退到 `sitting`（坐着是最常见的非站立姿态）。这比冒险判站立更安全。

### 4. 降频策略避免无效 CPU 开销

无人时 `refresh_results()` 仍被高频调用，浪费 CPU。参考人脸识别灯效的降频策略：连续 3 次无结果自动降频到 3fps，有人时恢复 10fps。

### 5. 扩展板与摄像头的依赖关系

USB摄像头接在扩展板上，扩展板连接异常时摄像头也无法使用。因此扩展板初始化失败时应明确提示「姿态检测无法运行」，而非「仅需摄像头」。

### 6. Rockchip 平台三段补丁必须严格按顺序

好搭AI派（RK3588 等 SoC）同时运行 pygame+cv2+vision_system 时，Mali GPU 驱动、音频驱动、V4L2 驱动容易出现资源冲突。经验证必须同时执行以下三条补丁：
1. `LIBGL_ALWAYS_SOFTWARE=1`（在 import pygame 前设置环境变量）
2. pygame 分段初始化（display/font 分开 init，mixer 单独 try/except）
3. 后台检测线程在 `threaded_system` 启停（不与 cv2 VideoCapture 混用）

### 7. 1920×1080 大图不要用 smoothscale

`pygame.transform.smoothscale` 对 1920×1080 尺寸的图片会做双线性插值，在 ARM 上 CPU 开销显著。摄像头帧缩放、背景图加载一律改用普通 `transform.scale`，节省 50% 以上的 CPU 时间（项目已对比验证）。

### 8. stop_background_detection 不在 vision_system 顶层

多次踩坑确认：`start_background_detection()` 和 `stop_background_detection()` 都挂在 `vision_system.threaded_system` 对象下，而不是 `vision_system` 顶层。V3 API 分析报告 v2.0 已明确：清理时必须先 `threaded_system.stop_background_detection()`，再 `vision_system.cleanup()`。

### 9. CAMERA_W/H 只是请求值，叠加缩放必须用实际分辨率（v1.3 踩坑）

2026-08-12 运行日志暴露了问题：程序请求 `create_vision_system_v3(width=1280, height=720)`，但视觉系统实际打开的摄像头是 `640×480`。v1.2 及以下 `draw_pose_overlay()` 用硬编码 1280×720 作为 `cam_src_w/cam_src_h` 基准，当库返回绝对像素坐标时（基于 640×480），叠加的检测框和关键点会**偏小、偏左上**，与人体实际位置明显错位。

教训：所有依赖「摄像头源尺寸」的坐标换算、缩放比例计算，**一律不要相信构造函数传入的 width/height 参数**，必须从 `capture_frame()` 返回的真实 `frame.shape` 动态检测。这也是模式 3（全托管视觉系统）的必然代价——摄像头完全由库内部控制，应用层只能观察实际结果，不能假设请求值一定生效。

## 十五、版本历史

| 版本 | 日期 | 主要变更 |
| --- | --- | --- |
| 1.0 | 2026-08-07 | 初始版本：PoseDetector 类、1920×1080 界面、官方 pose_detection 算法、关键点骨架可视化 |
| 1.1 | 2026-08-07 | 新增姿态分类功能：5 个归一化特征 + 站立白名单 + 二级分类 + 保守回退，支持站立/坐着/蹲着/躺着/未知五类判定 |
| 1.1 | 2026-08-07 | 检测框按姿态着色、右侧面板增加姿态色块和特征值显示、底部栏增加姿态图例 |
| 1.1 | 2026-08-07 | 修正扩展板异常提示：USB摄像头接在扩展板上，扩展板异常时姿态检测无法运行 |
| 1.2 | 2026-08-11 | 增加 Rockchip 兼容补丁：`LIBGL_ALWAYS_SOFTWARE=1`、pygame 分段 init（display/font/mixer 分开）、mixer 单次 init/quit |
| 1.2 | 2026-08-11 | ESP32/camera_vision_system_v3 导入 try/except 容错，扩展板启动失败不崩溃，进入调试模式 |
| 1.2 | 2026-08-11 | 采集线程增加 frame.mean() 校验（过滤全黑/全白脏帧），使用 mean 而非 std 避免 ARM 上高开销 |
| 1.2 | 2026-08-11 | `PoseDetector.close()` 增加 `threaded_system.stop_background_detection()` 正确调用（V3 API 报告） |
| 1.2 | 2026-08-11 | 背景图 `smoothscale` → `scale`（节省 CPU）；退出前显式 flush+close 日志文件，避免日志丢失 |
| 1.2 | 2026-08-11 | 同步更新项目说明文档：新增 5 个技术章节 + 3 个故障排查条目 + 3 条调试经验 |
| **1.3** | **2026-08-12** | **修复坐标缩放基准不一致问题**：PoseDetector 新增 `_actual_cam_w/_actual_cam_h`，在 `_capture_loop` 首次拿到有效帧时从 `frame.shape` 读取真实分辨率，并用 `_actual_res_lock` 保护跨线程读写 |
| **1.3** | **2026-08-12** | 新增 `PoseDetector.get_actual_resolution()` API：启动初期回退到 `CAMERA_W/H`，拿到首帧后返回实际宽高，供绘制层使用 |
| **1.3** | **2026-08-12** | `draw_camera()` 中 `draw_pose_overlay()` 的 `cam_src_w/cam_src_h` 参数改为使用 `get_actual_resolution()` 返回值，不再直接引用全局 `CAMERA_W/H`；右下角分辨率标签同步改为显示实际值 |
| **1.3** | **2026-08-12** | 首次检测到实际分辨率时打印对比日志（请求值 vs 实际值，是否一致），便于事后排查分辨率回退 |
| **1.3** | **2026-08-12** | 同步更新项目说明文档：技术方案表新增「分辨率适配」行、PoseDetector API 新增 `get_actual_resolution`、可配置项更新 `CAMERA_W/H` 说明、新增第十章第 12 节「实际分辨率动态适配机制」、第十三章第 8 节「叠加层错位」故障排查、第十四章第 9 条踩坑经验、版本历史 v1.3 全量记录 |

## 十六、参考文档对照

本次及历次复核优化主要依据以下文档和约束：

| 文档 / 依据 | 对应优化点 |
| --- | --- |
| 《视觉系统摄像头调用参考方案》第 7 章 Rockchip 兼容性补丁 | `LIBGL_ALWAYS_SOFTWARE=1`、pygame 分段初始化、mixer 单次 init/quit |
| 《视觉系统摄像头调用参考方案》模式 3（全托管） | 使用 vision_system.open_camera + capture_frame，不混用 cv2 VideoCapture（原项目已符合，历次仅做核对） |
| 《camera_vision_system_v3_API 分析报告》2.3.1 threaded_system 启停 | stop_background_detection 的位置在 threaded_system 下，已修正 close 流程 |
| 《camera_vision_system_v3_API 分析报告》ResultAccessor 的 API | 程序使用了 get_pose_detection_count/box/confidence/keypoints，与 result_accessor 刷新频率策略一致 |
| 项目工程约定（logs/ 目录、追加模式、帧校验 mean 代替 std） | 日志目录、采集线程 mean 过滤全黑帧、frame.mean() 代替 gray.std()（已踩坑教训） |
| **2026-08-12 现场运行日志分析（v1.3 依据）** | **请求分辨率 1280×720 vs 实际 640×480 不一致 → 新增 `get_actual_resolution()` 动态适配，消除 `draw_pose_overlay` 坐标缩放基准错误** |
| **叠加层可视化设计约束（v1.3 依据）** | **`cam_src_w/cam_src_h` 必须与 `capture_frame()` 返回的 frame.shape 宽高严格一致，否则库返回的绝对像素坐标换算必然错位** |

## 十七、扩展方向

- 其他程序通过 `from 姿态检测 import PoseDetector` 导入，实现姿态检测 + 自定义业务逻辑
- 配合 RGB 灯带实现「不同姿态触发不同灯效」（参考人脸识别灯效.py 的灯效架构）
- 配合语音播报实现「姿态识别 + 语音提醒」（参考物体识别播报.py 的 TTS 方案）
- 增加姿态变化计数（如坐→站转换次数统计，适用于康复训练场景）
- 增加异常姿态报警（如跌倒检测：突然从站立变为躺着）
- 如需更高精度，可参考手势控制RGB灯带.py 改用 MediaPipe Pose（33 关键点）
- 姿态分类阈值可通过配置文件外部化，支持运行时动态标定
