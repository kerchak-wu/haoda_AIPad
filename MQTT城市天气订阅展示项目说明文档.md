# MQTT 城市天气订阅展示项目文档

基于 `pygame-ce`、Open-Meteo 免费 API 与 `paho-mqtt` 开发的城市实时天气订阅展示程序。固定窗口尺寸 1920×1080，展示国内 12 个主要城市的实时天气、详细环境数据及未来 5 日预报。除支持触摸点击/按键切换城市外，程序还通过 MQTT 订阅 `topic/city` 主题，接收数字即可远程切换到对应城市，非常适合物联网教学与远程交互演示。界面在静态背景图基础上，动态叠加天气色彩滤镜与雨/雪/雾粒子特效，视觉效果美观且信息丰富。

## 一、项目简介

本项目是一个专为好搭AI派（及其他支持 Python/Pygame 的设备）设计的天气信息展示应用，核心亮点是**通过 MQTT 订阅远程控制展示城市**：程序连接到 MQTT 服务器 `192.168.2.251`，订阅主题 `topic/city`，当收到数字 `1` 时显示列表中第一个城市（北京），收到 `2` 显示上海，以此类推，共支持 1~12 号城市。同时保留触摸点击与键盘操作，三种交互方式互不冲突。

程序通过调用免费的 Open-Meteo API 获取实时及预报数据，并根据获取到的真实天气类型，在背景图上动态叠加对应的渐变色调和粒子动效（如雨天有斜线雨滴、雪天有飘落雪花、雾天有移动雾团），让用户一目了然地感受到天气的氛围。

## 二、功能特性

| 模块 | 功能说明 |
| --- | --- |
| **MQTT 订阅切换** | 订阅 `topic/city` 主题，收到数字 1~12 自动切换到对应城市（核心特性） |
| **实时天气** | 自动获取并展示所选城市的当前温度、天气状况、最高/最低/体感温度 |
| **详细数据** | 以横向卡片网格展示湿度、风力风向、气压、能见度、紫外线、空气质量、日出日落 |
| **未来预报** | 获取并展示未来 5 日的日期、星期、天气图标、天气描述及高低温 |
| **城市切换** | 左侧提供 12 个主要城市列表，支持触摸点击或键盘上下键切换（与 MQTT 并存） |
| **背景叠加** | 以 `images/1.jpg` 为底图，叠加对应天气的半透明渐变滤镜和暗化遮罩 |
| **动态粒子** | 根据天气类型实时渲染动态粒子（雨=斜线下落，雪=飘落，雾=移动云团） |
| **多线程防卡顿** | 天气数据获取与 MQTT 网络循环均运行在后台线程，主界面 60 FPS 流畅不阻塞 |
| **字体自适应** | 优先寻找好搭AI派自带中文字体，自动适配不同运行环境 |
| **手动刷新** | 右上角提供刷新按钮，遇到网络波动时可手动重新拉取当前城市数据 |
| **MQTT 容错** | MQTT 连接失败时程序仍可正常运行，可继续使用点击/按键切换城市 |

## 三、项目目录结构

```text
haoda_AIPad/
├── weather_mqtt.py                        # 主程序（含 MQTT 订阅 + 天气展示）
├── MQTT城市天气订阅展示项目说明文档.md      # 本文档
└── images/
    └── 1.jpg                              # 背景底图（1920×1080）
```

## 四、环境要求与依赖

- **系统环境**：Python 3.8 及以上
- **核心依赖**：
  - `pygame-ce`（提供 `pygame` 模块，界面渲染）
  - `paho-mqtt`（MQTT 通信，好搭AI派平台已预装）
- **内置库依赖**：`os`, `sys`, `math`, `random`, `threading`, `urllib.request`, `json`, `datetime`（均无需额外安装）
- **网络环境**：
  - 需连接互联网以访问 Open-Meteo 免费 API
  - 需能访问 MQTT 服务器 `192.168.2.251:1883`
- **硬件要求**：好搭AI派 或 任何支持 Pygame 的桌面电脑

## 五、安装与运行

1. **安装依赖**

   ```bash
   pip install pygame-ce paho-mqtt
   ```
  
   > 好搭AI派平台通常已预装 `paho-mqtt`，可直接运行。
2. **准备资源**：
   确保在项目同目录下存在 `images` 文件夹，并在其中放入一张名为 `1.jpg` 的背景图（建议分辨率 1920×1080）。若无此图，程序将回退为纯色渐变背景。
  
3. **运行程序**：

   ```bash
   python3 weather_mqtt.py
   ```

## 六、MQTT 订阅说明（核心）

### 1. 连接配置

| 配置项 | 值 |
| --- | --- |
| MQTT 服务器地址 | `192.168.2.251` |
| 端口 | `1883` |
| 订阅主题 | `topic/city` |
| Client ID | `haoda_weather` |

### 2. 消息格式与城市映射

向 `topic/city` 发布**纯数字**消息即可切换城市，数字与城市对应关系如下：

| 消息 | 城市 | 消息 | 城市 |
| --- | --- | --- | --- |
| `1` | 北京 | `7` | 武汉 |
| `2` | 上海 | `8` | 西安 |
| `3` | 广州 | `9` | 哈尔滨 |
| `4` | 深圳 | `10` | 三亚 |
| `5` | 杭州 | `11` | 拉萨 |
| `6` | 成都 | `12` | 乌鲁木齐 |

- 收到非数字消息：终端打印提示，不切换城市。
- 收到越界数字（如 `0`、`13`）：终端打印提示，不切换城市。

### 3. 测试 MQTT

使用 `mosquitto_pub` 测试：

```bash
mosquitto_pub -h 192.168.2.251 -t topic/city -m "1"   # 显示北京
mosquitto_pub -h 192.168.2.251 -t topic/city -m "5"   # 显示杭州
mosquitto_pub -h 192.168.2.251 -t topic/city -m "12"  # 显示乌鲁木齐
```

### 4. 工作机制

- 程序启动时调用 `connect_mqtt()` 连接服务器，连接成功后自动订阅 `topic/city`。
- MQTT 网络事件由 `loop_start()` 启动的后台线程处理，不阻塞 pygame 主循环。
- `on_message` 回调将解析出的城市下标写入全局变量 `mqtt_pending_idx`。
- 主循环每帧检查 `mqtt_pending_idx`，有值则切换 `selected_idx` 并清空，触发天气数据重新加载与界面刷新。
- 程序退出时调用 `loop_stop()` 与 `disconnect()` 清理 MQTT 连接。

## 七、界面布局设计

界面按自上而下、左中右结构布局，固定尺寸 1920×1080：

```text
┌─────────────────────────────────────────────────────────────────┐
│ [刷新]                      MQTT 城市天气订阅展示          [退出] │
│                        2026年01月01日 12:00:00                  │
├──────────────────┬──────────────────────────────────────────────┤
│ 城市列表         │  北京                                更新于.. │
│ ┌──────────────┐ │     18°C                                        │
│ │ 北京       ◄ │ │     晴                          ☀ (天气图标)  │
│ │ 上海         │ │   最高22° 最低10° 体感16°                      │
│ │ 广州         │ │  ┌──┬──┬──┬──┬──┬──┬──┬──┐                  │
│ │ ...          │ │  │湿度│风力│气压│能见│紫外│空气│日出│日落│     │
│ │              │ │  └──┴──┴──┴──┴──┴──┴──┴──┘                  │
│ └──────────────┘ │ ─── 未来5日预报 ───                            │
│                  │ ┌────┬────┬────┬────┬────┐                    │
│                  │ │今日│明日│周三│周四│周五│                    │
│                  │ │ ☀  │ ☁  │ 🌧 │ ❄  │ ☀  │                    │
│                  │ └────┴────┴────┴────┴────┘                    │
└──────────────────┴──────────────────────────────────────────────┘
  点击城市切换 | ↑↓ 切换 | MQTT topic/city 收数字切换 | ESC 退出
```

## 八、数据来源与 API 交互

本项目使用完全免费且无需注册的 Open-Meteo API。

### 1. API 地址与参数

- **天气基础数据**：`https://api.open-meteo.com/v1/forecast`
  - 请求参数：`latitude`, `longitude`, `current_weather=true`, `hourly=relative_humidity_2m,pressure_msl,...`, `daily=weathercode,temperature_2m_max,...`, `timezone=Asia/Shanghai`
- **空气质量数据**：`https://air-quality-api.open-meteo.com/v1/air-quality`
  - 请求参数：`latitude`, `longitude`, `current=us_aqi`, `timezone=Asia/Shanghai`

### 2. 数据本地化解析逻辑

获取到的原始数据为英文及代码，程序内进行了自动本地化转换：

- **天气代码解析** (`parse_weather_code`)：将 WMO 标准代码转换为“晴、多云、阴、小雨、雷阵雨”等中文描述。
- **风力风向转换** (`parse_wind_direction`, `kmh_to_beaufort_str`)：将角度转换为“东北风”，将风速转为国内习惯的“3级”。
- **紫外线与空气质量**：根据 UV 指数和 US AQI 数值，自动判定为“强/中/弱”和“优/良/轻度污染”等。

## 九、核心技术实现

### 1. MQTT 订阅与主循环解耦

MQTT 回调运行在 `paho-mqtt` 的网络线程中，不能直接操作 pygame 界面。程序采用“回调写标志、主循环消费”的解耦模式：

- `on_message` 只解析消息并把目标城市下标存入全局变量 `mqtt_pending_idx`。
- pygame 主循环每帧检查该变量，非空时切换城市并清空，确保所有界面更新都在主线程完成，避免线程安全问题。

### 2. 背景层级叠加设计

为保证在任意背景图下文字清晰可读，同时凸显天气氛围，背景采用 4 层叠加渲染：

1. **底层**：绘制 `images/1.jpg`。若图片缺失则绘制深灰色底。
2. **天气滤镜层**：根据当前天气（如晴天暖橙色、雨天深蓝色），绘制半透明（Alpha=160）的垂直渐变层。
3. **暗化遮罩**：覆盖一层 45 透明度的黑色，进一步降低背景亮度，突出前景面板。
4. **粒子层**：最顶层绘制动态天气粒子，让动态效果直接呈现在屏幕上。

### 3. 多线程异步加载机制

- **天气数据**：采用 Python `threading` 模块，每次启动或切换城市时开启守护线程 `fetch_and_update` 进行 API 调用，获取后写入全局字典 `CITIES_DATA`，主线程每帧读取最新状态，实现非阻塞 UI。
- **MQTT 网络**：`paho-mqtt` 自带后台网络线程（`loop_start`），独立处理订阅与消息接收。

### 4. 粒子系统动效设计

`ParticleSystem` 类根据当前天气动态生成和管理粒子：

- **雨**：生成 150~280 个斜向移动的线段，速度极快，模拟大雨倾盆。
- **雪**：生成 80~150 个圆点，下落速度慢并带有正弦波左右摆动效果，模拟雪花飘落。
- **雾**：生成 25 个半径较大的半透明圆，缓慢横向移动，模拟雾气流转。

切换城市且天气类型发生变化时，粒子系统自动清空并按新天气重新初始化。

### 5. 天气图标纯代码绘制

为不依赖外部图片资源，所有天气图标（太阳、云朵、雨伞、雪花等）均使用 `pygame.draw` 模块的基础图形（圆、线、多边形）通过数学坐标计算绘制，在不同位置可自适应展示为大图标（当天信息）或小图标（未来预报）。

### 6. 中文字体适配策略

`find_chinese_font()` 按严格优先级寻找可用中文字体，确保好搭AI派与各系统正常显示中文：

1. 优先在系统字体列表寻找 `simhei`、`microsoftyahei` 等。
2. 检测好搭AI派路径：`/home/cxdz/jupyter/assets/simhei.ttf`。
3. 检测系统通用路径：`/usr/share/fonts/...`、`C:/Windows/Fonts/...`。

## 十、交互与快捷键

| 按键/动作 | 功能说明 |
| --- | --- |
| `MQTT 发布数字 1~12` | 远程切换到对应城市（核心交互） |
| `鼠标左键点击` | 点击左侧城市列表切换城市；点击右上角按钮执行对应功能 |
| `↑` | 切换到上一个城市 |
| `↓` | 切换到下一个城市 |
| `ESC` | 退出程序 |

> MQTT、点击、按键三种方式可同时使用，最后触发的操作生效。

## 十一、可配置项说明

在 `weather_mqtt.py` 顶部的配置区可调整以下核心常量：

| 常量名 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `BG_IMAGE` | 背景图路径 | `images/1.jpg` |
| `MQTT_BROKER` | MQTT 服务器地址 | `192.168.2.251` |
| `MQTT_PORT` | MQTT 端口 | `1883` |
| `MQTT_TOPIC` | 订阅主题 | `topic/city` |
| `WEATHER_COLORS` | 各天气对应的背景渐变颜色元组 (顶部色, 底部色) | 见源码字典 |
| `CITIES_CONFIG` | 城市列表配置（名称、纬度、经度） | 包含国内 12 个主要城市 |
| `CITIES_DATA` | 运行时动态获取的天气数据缓存 | `{}` |

## 十二、常见问题排查

**Q1：MQTT 切换城市无反应？**

A：请检查：① 设备与 MQTT 服务器 `192.168.2.251` 网络是否互通；② 服务器上 MQTT 服务是否运行；③ 发布的消息是否为纯数字（`1`~`12`）；④ 终端是否打印 `MQTT 已连接...`，若打印 `MQTT 连接出错` 则连接失败。MQTT 连接失败不影响点击/按键切换。

**Q2：启动后右侧信息显示“获取失败”或一直处于“获取中”状态？**

A：请检查设备网络是否连接正常，是否能访问 `api.open-meteo.com`。可点击右上角“刷新”按钮重试。

**Q3：背景图片没有显示？**

A：请检查 `images/1.jpg` 文件是否存在，路径是否正确。程序允许无此图，此时会回退为纯色渐变底，不影响功能。

**Q4：界面文字出现方块或乱码？**

A：中文字体加载失败。程序会尝试自动寻找系统字体，若失败可手动将中文字体（如 `simhei.ttf`）放置于 `/home/cxdz/jupyter/assets/` 目录下。

**Q5：未来5日预报区不显示？**

A：预报区位于面板底部，若数据未加载完成会显示“预报数据获取中...”，等待后台线程获取完毕后会自动渲染卡片。
