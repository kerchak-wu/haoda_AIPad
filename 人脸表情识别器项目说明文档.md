# 人脸表情识别器项目说明文档

基于 camera_vision_system_v3 视觉系统的人脸表情实时识别程序：通过 RKNN 本地离线模型识别 7 类表情（开心/悲伤/愤怒/惊讶/厌恶/恐惧/中性）+ 投入度等级，1920×1080 界面同步显示摄像头画面、当前表情、全表情置信度分布、投入度等级和表情变化记录，并根据表情驱动 IO1 WS2812 RGB 灯带（11 颗灯珠）显示对应颜色。

> 本项目经过五轮迭代修复：表情全中性 bug（别名映射）、界面拉伸超屏（FULLSCREEN 去除）、RGB 灯带不亮（Init 缺失+参数错位）、界面↔灯带颜色错位（color/led_rgb 统一）、日志粘行（行缓冲+补换行）。完整修复过程见第八章。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 表情识别 | camera_vision_system_v3 全托管模式（V3 管理摄像头），RKNN 本地离线推理，RK3566 NPU 加速 |
| 摄像头采集 | V3 `capture_frame()` + 独立帧采集线程，V4L2 设备节点预检测（41→40→42） |
| 界面显示 | `pygame`，1920×1080 普通窗口（非 FULLSCREEN），背景图 `images/1.jpg` + 半透明面板 |
| RGB 灯带控制 | `ESP32.ws2812Init` + `ws2812Write`（5 参数正确签名，参照手势控制RGB灯带.py） |
| 多线程 | 帧采集线程 + 主循环线程（检测+绘制+灯效），线程锁保护帧数据 |
| 日志系统 | `_TeeStdout` 双写（控制台+文件），行缓冲，自动补换行，V3 刷屏日志过滤 |

> 关键设计：V3 全托管模式下，`open_camera()` + `start_background_detection()` 由 V3 内部管理摄像头；表情识别通过 `result_accessor` 的 `get_facial_expression_*` 系列接口获取结果，主循环每 15 帧刷新一次检测结果。

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 实时表情识别 | 7 类表情（开心/悲伤/愤怒/惊讶/厌恶/恐惧/中性）+ Contempt 轻蔑归入厌恶 |
| 全表情置信度 | 7 条水平条形图实时显示各表情置信度分布，当前表情高亮 |
| 投入度等级 | 高/中/低投入度 + 子项置信度展示 |
| 表情变化记录 | 最多 5 条历史记录，含时间戳、表情切换路径、置信度，支持 R 键/按钮重置 |
| RGB 灯带联动 | 检测到表情时 11 颗灯珠同步显示对应颜色，未检测到人脸时自动熄灭 |
| 摄像头预检测 | 启动时探测 /dev/video41 → 40 → 42，MJPG 编码，全黑/全白帧过滤 |
| 加载画面 | 模型加载、摄像头初始化等阶段显示加载提示，ESC 可紧急退出 |
| 退出方式 | 界面右上角「退出程序」按钮 / ESC 键 / 窗口关闭按钮 / SIGINT/SIGTERM 信号 |
| 资源清理 | 退出时停止后台检测、释放 V3 资源、熄灭灯带、关闭 ESP32、关闭日志文件 |

## 三、表情-灯带颜色映射

> v5.2 核心约束：`color`（界面条/标签色）与 `led_rgb`（灯带色）**必须完全相同**，避免「界面是X色但灯是Y色」的感知错位。

| 表情 | 界面色 = 灯带色 | RGB 值 | 说明 |
| --- | --- | --- | --- |
| 开心 | 金黄 | (255, 193, 7) | 笑脸黄 |
| 悲伤 | 矢车菊蓝 | (100, 149, 237) | 忧郁蓝 |
| 愤怒 | 正红 | (220, 50, 50) | 情绪红 |
| 惊讶 | 紫 | (180, 80, 220) | 诧异紫 |
| 厌恶 | 海绿 | (85, 160, 85) | 不快绿（含 Contempt 轻蔑） |
| 恐惧 | 警示橙 | (230, 110, 0) | 警惕橙（原灰改为橙，提升白底可读性） |
| 中性 | 冷灰蓝 | (120, 130, 150) | 无情绪冷色 |
| 未检测 | 全灭 | (0, 0, 0) | 灯带熄灭 |

映射表在 `人脸表情识别器.py` 顶部 `EMOTION_LIST` 中配置，修改时需同时更新 `color` 和 `led_rgb` 保持一致。

## 四、项目结构

```markdown
好搭AI派实验/
├── 人脸表情识别器.py                # 本程序主入口
├── 人脸表情识别器项目说明文档.md    # 本文档
├── camera_vision_system_v3/         # V3 视觉系统库（好搭AI派自带）
├── ESP32.py                         # ESP32 硬件控制库（好搭AI派自带）
├── images/
│   └── 1.jpg                        # 背景图（1920×1080，缺失时自动渐变背景）
├── logs/                            # 日志目录（运行时自动创建）
│   └── 人脸表情识别器_YYYYMMDD.log  # 按日期命名的运行日志（追加模式，行缓冲）
└── 好搭AI派学习手册.md              # 硬件 API 参考
```

### 资源文件说明

- `images/1.jpg`：背景图片，程序启动时加载。如缺失或加载失败，自动生成浅蓝渐变背景（`BG_TOP` → `BG_BOTTOM`），不影响运行。
- `logs/人脸表情识别器_YYYYMMDD.log`：运行日志，记录所有 `print` 输出（含异常堆栈和 V3 原始返回值调试信息）。行缓冲（`buffering=1`），实时可见，崩溃不丢日志。每次运行追加写入，文件名按当天日期生成。

## 五、硬件接线

| 接口 | 设备 | 说明 |
| --- | --- | --- |
| IO1 (GPIO_IO_01) | WS2812 RGB 灯带 | 11 颗灯珠，需接上拉扩展模块 |
| ESP32 扩展板 USB 口 | 外接 USB 摄像头 | 系统识别为 `/dev/video40` 或 `/dev/video41` |

> 注意：好搭AI派右下角开关需拨到左侧以启用外设接口。

## 六、环境要求

- Python 3.8 及以上
- 依赖：
  - `pygame`（界面，好搭AI派自带）
  - `opencv-python`（提供 `cv2`，好搭AI派自带）
  - `numpy`（数组处理，好搭AI派自带）
  - `camera_vision_system_v3`（视觉系统，好搭AI派自带，含 RKNN 表情识别模型）
  - `ESP32`（硬件控制，好搭AI派自带）
- 外接 USB 摄像头
- WS2812 RGB 灯带（11 颗灯珠）+ 上拉扩展模块

## 七、运行

```bash
python3 人脸表情识别器.py
```

启动后：

1. 程序显示加载画面，依次探测摄像头（41→40→42）、加载 RKNN 模型、初始化检测器、打开摄像头、启动后台检测
2. 对准摄像头做出表情，界面实时显示当前表情、置信度分布、投入度等级
3. 表情变化时灯带同步变色，变化记录显示在右下面板
4. 点击界面右上角「退出程序」按钮或按 ESC 退出

## 八、关键实现说明

### 1. V3 全托管模式初始化

V3 视觉系统采用全托管模式（V3 管理摄像头生命周期），初始化流程严格按顺序执行：

```python
# 1. 创建实例（不自动检测）
vision_system = create_vision_system_v3(
    camera_id=cam_id, width=1280, height=720,
    enable_basic=False, enable_advanced=False, auto_detect=False,
)

# 2. 启用表情识别模块
vision_system.detection_config.enable_facial_expression = True

# 3. 初始化检测器（加载 RKNN 模型）
vision_system._init_detectors()

# 4. 打开摄像头
vision_system.open_camera()

# 5. 启动后台检测线程
vision_system.threaded_system.start_background_detection(show_preview=False)
```

> 注意：步骤 3 `_init_detectors()` 必须在 `open_camera()` 之前调用，否则检测器未加载模型。步骤 5 启动后 V3 内部线程持续推理，主循环通过 `result_accessor.refresh_results()` 获取最新结果。

### 2. 表情识别核心逻辑（全中性 bug 修复）

#### 2.1 问题根因

V3 的 `get_facial_expression_emotions_confidence()` 返回的字典 key 是**首字母大写的名词**（如 `Happiness`、`Sadness`、`Contempt`），而非小写形容词（如 `happy`、`sad`）。原 `emotion_idx()` 直接用原始 key 查 `EMOTION_ALIAS_MAP`，由于大小写不匹配导致全部查不到，兜底返回 6（中性），表现为**所有表情都被识别为中性**。

#### 2.2 修复方案

```python
def emotion_idx(value):
    """任意返回值 → EMOTION_LIST 索引（找不到返回 6=中性）
    关键：V3 返回首字母大写名词（Happiness/Sadness/Anger/Contempt...），
    这里统一 strip + lower，确保无论大小写、名词形容词变体都能命中。"""
    if value is None:
        return 6
    _key = str(value).strip()
    _key_lower = _key.lower()          # ← 关键：统一转小写再查
    if _key_lower in EMOTION_ALIAS_MAP:
        return EMOTION_ALIAS_MAP[_key_lower]
    return EMOTION_ALIAS_MAP.get(_key, 6)
```

别名映射表补充名词变体（全部存小写 key）：

```python
_EXTRA_ALIASES = {
    'happiness': 0, 'happy.': 0, 'pleased': 0, 'glad': 0,
    'sadness': 1, 'unhappy': 1, 'sorrow': 1,
    'anger': 2, 'rage': 2, 'mad': 2,
    'surprise': 3, 'astonished': 3, 'amazed': 3,
    'disgust': 4, 'revulsion': 4, 'contempt': 4,  # Contempt轻蔑→归入厌恶
    'fear': 5, 'scared': 5, 'afraid': 5, 'frightened': 5,
    'calm': 6, 'normal': 6, 'none': 6, 'expressionless': 6,
}
```

#### 2.3 主表情选取策略

不直接使用 `get_facial_expression_emotion()` 的单一返回值，而是**从全置信度字典取最大值**作为主表情，再用 `emotion()` 做 80% 阈值交叉参考：

```python
# 1. 从全置信度字典取最大值
conf_en = {}  # 规范化后的英文key→置信度
for _k, _v in conf_dict_raw.items():
    _idx = emotion_idx(_k)
    _key = EMOTION_LIST[_idx]['key']
    conf_en[_key] = max(conf_en.get(_key, 0), float(_v))

best_idx = 6  # 中性兜底
best_conf = 0.0
for _key, _v in conf_en.items():
    if _v > best_conf:
        best_conf = _v
        best_idx = emotion_idx(_key)

# 2. emotion() 返回值作为交叉参考
emo_raw = get_facial_expression_emotion()
emo_idx2 = emotion_idx(emo_raw)
emo_conf2 = conf_en.get(EMOTION_LIST[emo_idx2]['key'], 0.0)
if emo_conf2 >= best_conf * 0.8 and emo_conf2 > 0:
    best_idx = emo_idx2    # emotion() 结果置信度接近最大值时尊重它
    best_conf = emo_conf2
```

#### 2.4 调试日志

每 5 次成功检测打印一次原始返回值，用于观察 V3 返回格式变化：

{% raw %}
```python
print('[检测-调试 #%d] emotion()=[%s] type=%s | 主表情=%s(%.3f) | conf_dict: {%s}' % (
    count, emo_raw, type(emo_raw).__name__,
    EMOTION_LIST[best_idx]['name'], best_conf,
    ', '.join('%s=%.3f' % (k, v) for k, v in conf_dict_raw.items())
))
```
{% endraw %}

未识别 key 告警：当 `conf_dict` 中出现不在已知别名表的 key 时打印警告，便于及时发现 V3 新增的表情类别。

### 3. RGB 灯带控制（不亮 bug 修复）

#### 3.1 问题根因（双 Bug 叠加）

- **根因 A**：缺少 `ws2812Init(pin, count)` 初始化。WS2812 驱动必须先 Init 才能写，否则所有 Write 被静默丢弃。
- **根因 B**：`ws2812Write` 参数错位。原传 6 参数 `(1, 8, 0, r, g, b)`，API 实际为 5 参数 `ws2812Write(pin, led_idx, r, g, b)`，导致 R 分量位置 = 常量 0 = 永远黑灯。

#### 3.2 修复实现

初始化（ESP32 连接成功后立即执行）：

```python
self.board.ws2812Init(LED_PIN, LED_COUNT)        # 必须！先初始化
self.board.ws2812Write(LED_PIN, 255, 0, 0, 0)    # 全灭验证通信
self._led_ok = True
```

写入函数（5 参数正确签名 + 批量/逐颗降级）：

```python
def _set_led(self, r, g, b):
    """点亮全部 11 颗灯珠为同一颜色；r/g/b 范围 0-255"""
    if self.board is None or not self._led_ok:
        return
    r = int(max(0, min(255, r)))  # 钳制 0-255
    g = int(max(0, min(255, g)))
    b = int(max(0, min(255, b)))
    try:
        self.board.ws2812Write(LED_PIN, 255, r, g, b)  # 255=批量写入全部
    except Exception:
        for _i in range(LED_COUNT):                     # 降级：逐颗 0..10
            self.board.ws2812Write(LED_PIN, _i, r, g, b)
```

#### 3.3 WS2812 API 速查

| 方法 | 参数 | 说明 |
| --- | --- | --- |
| `ws2812Init(pin, count)` | pin: GPIO 编号, count: 灯珠数 | 初始化 WS2812 驱动，**必须先调用** |
| `ws2812Write(pin, idx, r, g, b)` | idx: 0..count-1 单颗 / 255 全部 | 写入颜色，5 参数 |

### 4. 界面布局（1920×1080 严格约束）

#### 4.1 窗口模式

```python
_os.environ.setdefault('SDL_VIDEO_CENTERED', '1')  # 窗口居中
self.screen = pygame.display.set_mode((WIDTH, HEIGHT))  # 无 FULLSCREEN
```

不使用 `pygame.FULLSCREEN`：好搭AI派物理 >1920 像素，FULLSCREEN 会拉伸模糊。普通窗口 1920×1080 在桌面模式下视觉效果等同全屏且像素 1:1 不缩放。

#### 4.2 布局分区

```markdown
┌──────────────────────────── 1920 ────────────────────────────┐
│ 标题栏 (130px)                                   [退出程序]   │
├──────────────────┬───────────────────────────┬───────────────┤
│                  │                           │               │
│  摄像头面板       │     表情分析面板           │               │
│  920×800         │     940×800               │               │
│                  │  ┌─ 当前表情（大字）        │               │
│  摄像头画面       │  ├─ 投入度等级              │               │
│  880×660         │  ├─ 7表情置信度条形图       │               │
│                  │  └─ 表情变化记录（5条）     │               │
│                  │                           │               │
├──────────────────┴───────────────────────────┴───────────────┤
│ 底部状态栏 (110px)                                            │
│ 检测状态 | 推理耗时 | 采集帧率 | 检测频率                       │
│ ESP32灯带状态 | 视觉系统V3状态 | 操作提示                       │
└──────────────────────────────────────────────────────────────┘
```

面板高度计算：`PANEL_H = 1080 - 130(标题) - 20(上间隙) - 20(下间隙) - 110(底栏) = 800`

#### 4.3 文字裁剪

所有文本渲染均通过 `clamp_text()` 或 `_render_colored()` 做最大宽度裁剪（二分法截断+省略号），确保文字不超出面板边界：

```python
def clamp_text(font, text, max_width, ellipsis='…'):
    """渲染文本并在超过 max_width 时截断+加省略号"""
    surf = font.render(text, True, TEXT_COLOR)
    if surf.get_width() <= max_width:
        return surf, surf.get_width()
    # 二分法找最大可显示长度
    lo, hi = 0, len(text)
    best = 0
    while lo <= hi:
        mid = (lo + hi) // 2
        if font.render(text[:mid] + ellipsis, True, TEXT_COLOR).get_width() <= max_width:
            best = mid
            lo = mid + 1
        else:
            hi = mid - 1
    return font.render(text[:best] + ellipsis, True, TEXT_COLOR), 0
```

### 5. 日志系统（粘行 bug 修复）

#### 5.1 问题根因

- **全缓冲**：`open(buffering=-1)` 使用 8KB 全缓冲，日志积攒到 8KB 才落盘，运行时看文件缺最新内容，崩溃丢日志。
- **粘行**：ESP32/V3 底层部分代码直接 `sys.stdout.write(xxx)` 不带 `\n`，原 `_TeeStdout` 原样写入文件导致多条粘成一行。

#### 5.2 修复实现

```python
# 行缓冲：写完一行立即落盘
_debug_log_fp = open(_LOG_FILE, 'a', encoding='utf-8', buffering=1)

class _TeeStdout:
    def write(self, msg):
        self.original.write(msg)
        if not msg or msg.isspace():
            return
        try:
            # 过滤 V3 刷屏日志
            for _pat in _V3_SPAM_PATTERNS:
                if _pat.match(msg.strip()):
                    return
            _debug_log_fp.write(msg)
            if not msg.endswith('\n'):       # 自动补换行
                _debug_log_fp.write('\n')
        except Exception:
            pass
```

### 6. 摄像头预检测

复用手势控制RGB灯带.py 的探测方案：

- **设备节点路径方式打开**：`cv2.VideoCapture('/dev/video41')`，避免整数索引越界
- **V4L2 标志**：`cv2.VideoCapture(dev_path, cv2.CAP_V4L2)`
- **帧有效性检测**：通过 `gray.mean()` 检测全黑/全白帧（`5 < mean < 250`）
- **探测顺序**：41 → 40 → 42，第一个可用的即返回

### 7. Rockchip 平台兼容性补丁

```python
import os
# 1. 强制 libGL 使用软件渲染，避免 rockchip 平台 GPU 驱动加载失败
os.environ.setdefault('LIBGL_ALWAYS_SOFTWARE', '1')

# 2. 导入顺序：先 pygame 再 cv2
import pygame
import cv2

# 3. 分段初始化（不调用 pygame.init()），避免 mixer 与 V4L2 死锁
pygame.display.init()
pygame.font.init()
```

### 8. 帧缩放方式

使用 `pygame.transform.scale`（非 `smoothscale`）进行摄像头帧缩放，降低 CPU 开销。

## 九、开发提示词（可复用经验）

### 原始提示词

```markdown
调用camera_vision_system_v3的人脸表情识别算法写一个人脸表情识别的程序，其他要求按惯例。
```

### 迭代优化提示词

本项目经过五轮迭代修复，每轮提示词体现不同的调试技巧。

#### 第一轮：功能完善

```md
表情描述要用中文；界面参考人数实时统计程序；日志内容如下，参考并进行分析、优化。
```

**优化要点**：指定中文化要求 + 参考已有项目风格 + 提供日志供分析。

#### 第二轮：Bug 精准描述

```md
表情识别出来全是中性，不对；界面大小1920*1080；很多地方文字重叠、超出框，检查优化。
```

**优化要点**：多个问题一次提出，每个问题精确定位（「全是中性」「文字重叠超出框」），引导 AI 分别分析根因。

#### 第三轮：回归问题 + 参考引导

```md
我用了开心、惊讶、悲伤3个表情，检测出来仍然全是中性。界面不对，把整个屏幕占满了，
要控制为1920*1080，参考手势控制RGB灯带程序。日志内容如下：[日志内容]
```

**优化要点**：
- **提供测试用例**：「开心、惊讶、悲伤3个表情」让 AI 知道测试覆盖范围
- **强调问题未解决**：「仍然全是中性」提示上一轮修复无效，需重新分析根因
- **参考已有项目**：「参考手势控制RGB灯带程序」引导 AI 检索正确的窗口创建方式
- **附上日志**：提供原始日志供 AI 分析 V3 返回值格式

#### 第四轮：硬件反馈

```md
RGB灯带没有亮，io1,11灯；其他功能没什么问题。
```

**优化要点**：
- **先肯定再提问题**：「其他功能没什么问题」让 AI 聚焦灯带问题
- **精确硬件参数**：「io1,11灯」直接给出引脚和灯珠数，避免 AI 猜测
- **简洁描述现象**：「没有亮」三字足矣，AI 自行分析根因

#### 第五轮：颜色一致性 + 日志可读性

```md
检测到开心，界面上显示是黄色的色条，灯是绿色的。不对应。
日志内容没有适当的分行，看起来很不方便。
```

**优化要点**：
- **具体场景描述**：「检测到开心」+「界面黄/灯绿」精确定位颜色错位
- **对比式描述**：「黄色的色条，灯是绿色的」通过对比让问题一目了然
- **日志问题一句话**：「没有适当的分行」简洁描述，AI 自行分析缓冲机制

### 后续项目可复用的提示词模板

```md
调用camera_vision_system_v3的[算法模块]写一个[功能描述]的程序。
表情/手势/姿态描述要用中文；界面参考[已有项目]；
硬件接线：[IO口编号]接[外设规格]；
好搭AI派上要有界面1920*1080，显示[界面元素]，界面上要有退出程序按钮。
程序涉及硬件部分严格参照本地文件夹中的好搭AI派学习手册和好搭AI派范例代码。
```

## 十、常见问题

### Q1：表情识别全部显示「中性」

**A**：检查 `emotion_idx()` 函数是否将输入 `.strip().lower()` 后再查 `EMOTION_ALIAS_MAP`。V3 返回首字母大写名词（如 `Happiness`），不转小写会查不到。查看日志中 `[检测-调试]` 行的 `emotion()` 原始值和 `conf_dict` key 类型，确认 V3 返回格式。如有未识别 key，日志会打印 `[检测-警告] conf_dict 中存在未识别 key`，需补充到 `_EXTRA_ALIASES`。

### Q2：界面占满整个屏幕且模糊

**A**：确认 `pygame.display.set_mode((WIDTH, HEIGHT))` 未使用 `pygame.FULLSCREEN` 标志。好搭AI派物理屏 >1920 像素，FULLSCREEN 会拉伸模糊。普通窗口 1920×1080 像素 1:1 不缩放。

### Q3：RGB 灯带不亮

**A**：按以下顺序排查：
1. 查看日志是否出现 `[灯带] WS2812 初始化成功`，若无则 `ws2812Init` 失败
2. 确认 `ws2812Write` 传 5 参数 `(pin, idx, r, g, b)`，不是 6 参数
3. 确认 ESP32 扩展板已连接（底部状态栏显示绿色「灯带就绪」）
4. 确认右下角开关拨到左侧启用外设接口
5. 查看 `[灯带] 写入失败` 日志的具体错误信息

### Q4：界面色条颜色与灯带颜色不一致

**A**：检查 `EMOTION_LIST` 中每个表情的 `color` 和 `led_rgb` 是否完全相同（RGB 三元组一模一样）。v5.2 强制 `color == led_rgb`，修改颜色时两者必须同步更新。

### Q5：日志文件内容粘在一行

**A**：确认日志文件 `open()` 使用 `buffering=1`（行缓冲），且 `_TeeStdout.write()` 在消息结尾无 `\n` 时自动补 `\n`。

### Q6：界面文字重叠或超出面板

**A**：所有文本渲染应通过 `clamp_text()` 或 `_render_colored()` 做最大宽度裁剪。检查右侧面板高度预算（800px）是否被各区块超出，必要时压缩字号或减少显示条数（如表情变化记录限制 5 条）。

### Q7：程序启动后立即崩溃（段错误）

**A**：确认 Rockchip 平台兼容性补丁已生效：`LIBGL_ALWAYS_SOFTWARE=1`、import 顺序先 pygame 再 cv2、不调用 `pygame.init()` 改用分段初始化。

### Q8：退出按钮在哪里

**A**：退出按钮位于界面右上角标题栏内（`WIDTH-280, 30, 240×70`）。底部栏仅显示状态信息和操作提示。

## 十一、扩展方向

- 增加更多表情类别（V3 支持 8 类，当前 Contempt 归入厌恶，可独立显示）
- 表情触发事件联动（如检测到「开心」拍照留念、检测到「悲伤」播放音乐）
- 表情统计报表（按时间段统计各表情占比，导出 CSV）
- 多人脸表情识别（V3 支持多人脸，当前仅取第一个）
- 表情驱动的智能家居控制（通过 MQTT 发送表情事件）
- 表情识别精度优化（调整检测频率、增加帧间平滑/防抖）
