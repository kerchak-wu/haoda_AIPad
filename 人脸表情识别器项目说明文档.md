# 人脸表情识别器项目说明文档

## 一、项目概述

本项目运行于好搭AI派（ESP32 + rockchip 平台），通过 USB 外接摄像头实时采集人脸画面，调用百度智能云人脸识别 API，在线识别表情、情绪、年龄、性别、是否佩戴眼镜等信息，并通过 Pygame 在 1920×1080 屏幕上展示实时画面与识别结果。

### 核心功能

1. 调用百度智能云人脸识别 API，识别 USB 摄像头画面中的人脸表情
2. 界面 1920×1080，科技感深色主题
3. 实时显示摄像头画面（后台抓帧 + scale 缩放，降低延迟）
4. OpenCV Haar 级联分类器实时绘制人脸框（本地推理，无延迟）
5. 百度 AI 每隔约 5 秒检测一次表情/情绪/年龄/性别/眼镜
6. 右侧面板展示识别结果（表情类型、概率进度条、附加信息）
7. 退出按钮 + ESC 快捷键

---

## 二、硬件依赖

| 硬件 | 说明 |
| ------ | ------ |
| 好搭AI派 | ESP32 + rockchip RK 平台 |
| USB 外接摄像头 | 接好搭AI派 USB 接口，分辨率 640×480，30fps |
| 网络 | 需联网（百度智能云在线 API） |
| 显示屏 | 1920×1080 |

### 摄像头连接说明

- 摄像头插入好搭AI派的 USB 接口
- 右下角开关需拨到左侧（USB 摄像头模式）
- 程序使用 `camera_vision_system_v3` SDK，`camera_id=-1` 自动检测设备节点

---

## 三、软件依赖

### 第三方库

```md
baidu-aip          # 百度智能云 AI SDK
opencv-python      # OpenCV 图像处理
pygame             # 界面显示（实际安装为 pygame-ce）
numpy              # 数值计算
camera_vision_system_v3  # 好搭AI派视觉 SDK（系统内置）
```

### 百度智能云配置

在 [人脸表情识别器.py](file:///d:/笔记本同步/好搭AI派资料/人脸表情识别器.py) 中配置以下认证信息：

```python
BAIDU_APP_ID = ''
BAIDU_API_KEY = ''
BAIDU_SECRET_KEY = ''
```

> 如需使用自己的百度智能云账号，请在 [百度智能云控制台](https://cloud.baidu.com/product/face.html) 创建人脸识别应用，替换上述三个值。

---

## 四、文件结构

```md
好搭AI派资料/
├── 人脸表情识别器.py              # 主程序
├── 人脸表情识别器-项目说明.md     # 本说明文档
├── images/
│   └── 1.jpg                     # 背景图片（可选，缺失时自动生成渐变背景）
├── camera_vision_system_v3.py    # 好搭AI派视觉 SDK（系统内置）
└── 其他范例程序与学习手册
```

### 资源文件说明

- `images/1.jpg`：背景图片，程序启动时加载。如缺失或加载失败，会自动生成科技感渐变背景，不影响运行。

---

## 五、技术架构

### 5.1 整体架构

程序采用**三线程模型**，避免界面卡顿：

```md
┌─────────────────────────────────────────────────┐
│                  主线程（Pygame）                 │
│  - 事件处理（鼠标、键盘）                         │
│  - 界面绘制（背景、面板、摄像头画面、结果）         │
│  - 30 FPS 刷新                                   │
└─────────────────────────────────────────────────┘
          ▲ 读取 _latest_bgr          ▲ 读取检测结果
          │                           │
┌─────────┴───────────┐  ┌────────────┴──────────────┐
│  帧抓取线程（后台）   │  │  百度检测线程（后台）       │
│  - vision_system     │  │  - 每 5 秒调用百度 API     │
│    .capture_frame()  │  │  - 编码 JPEG → base64      │
│  - 写入 _latest_bgr  │  │  - 解析表情/情绪/年龄等    │
│  - 间隔 0.01 秒      │  │  - 写入全局检测结果变量     │
└──────────────────────┘  └───────────────────────────┘
```

### 5.2 摄像头方案

使用好搭AI派 SDK 的 `camera_vision_system_v3`，而非 `cv2.VideoCapture` 直接操作 V4L2 设备：

```python
vision_system = create_vision_system_v3(
    camera_id=-1, width=640, height=480,
    enable_basic=False, enable_advanced=False
)
vision_system.open_camera()
vision_system.threaded_system.start_background_detection(show_preview=False)
```

**不使用 `cv2.VideoCapture` 的原因**：在 rockchip 平台上，`cv2.VideoCapture` 直接操作 V4L2 设备与 SDL2 视频子系统冲突，会导致 `ioctl(VIDIOC_QBUF): Bad file descriptor` 错误。

### 5.3 百度 AI 调用流程

```md
摄像头帧 (BGR)
    │
    ├── 缩小到 160×120（减少编码时间）
    │
    ├── cv2.imencode('.jpg', frame, quality=60)
    │
    ├── base64.b64encode() → 字符串
    │
    ├── baidu_client.detect(image, 'BASE64', options)
    │       options:
    │         face_field: expression,emotion,gender,age,glasses
    │         max_face_num: 1
    │         face_type: LIVE
    │
    └── 解析返回的 JSON → 更新全局变量
```

### 5.4 Haar 级联人脸检测

使用 OpenCV 内置的 Haar 级联分类器，在本地进行人脸检测并绘制人脸框：

- 模型：`haarcascade_frontalface_default.xml`
- 检测频率：每 10 帧检测一次（降低 CPU 占用）
- 用途：在百度 API 检测间隔期间，仍能实时显示人脸框位置

---

## 六、关键配置参数

| 参数 | 值 | 说明 |
| ------ | ----- | ------ |
| `WIDTH, HEIGHT` | 1920, 1080 | 界面分辨率 |
| `DETECT_INTERVAL` | 5.0 秒 | 百度 API 检测间隔 |
| `CAMERA_W, CAMERA_H` | 640, 480 | 摄像头分辨率 |
| Haar 检测间隔 | 每 10 帧 | 本地人脸框检测频率 |
| 主循环帧率 | 30 FPS | Pygame 界面刷新率 |
| JPEG 编码质量 | 60 | 百度 API 上传图片质量 |
| 上传图片尺寸 | 160×120 | 缩小后编码，减少 CPU 占用 |

---

## 七、运行方式

### 7.1 在好搭AI派上运行

1. 将 `人脸表情识别器.py` 上传到好搭AI派
2. 确保 `images/1.jpg` 存在（可选）
3. 确保 USB 摄像头已连接，右下角开关拨到左侧
4. 运行程序：

```bash
python3 人脸表情识别器.py
```

### 7.2 操作方式

- **退出程序**：点击右下角"退出"按钮，或按 `ESC` 键
- **表情识别**：自动进行，每 5 秒识别一次，无需手动操作

---

## 八、界面说明

### 界面布局

```md
┌─────────────────────────────────────────────────────────────┐
│                    人脸表情识别系统                          │
│        FACE EXPRESSION RECOGNITION | 百度智能云              │
├──────────────────────────────────────┬──────────────────────┤
│                                      │    识别结果          │
│                                      │  ● 状态指示灯        │
│         摄像头实时画面                 │ ─────────────────── │
│         640 × 480                     │  表 情              │
│                                      │  微笑  85.2%        │
│         [人脸框 + 表情标签]            │  ████████░░         │
│                                      │                     │
│                                      │  情 绪              │
│                                      │  开心  72.1%        │
│                                      │  ███████░░░         │
│                                      │ ─────────────────── │
│                                      │  年龄    28 岁      │
│                                      │  性别    男          │
│                                      │  眼镜    无眼镜      │
│                                      │  人脸置信度 99.5%   │
│                                      │ ─────────────────── │
│                                      │  上次检测: 2.3s 前  │
├──────────────────────────────────────┴──────────────────────┤
│ 状态: 识别成功：微笑          检测到 1 张人脸    [ 退出 ]   │
└─────────────────────────────────────────────────────────────┘
```

### 识别结果说明

- **表情**：无表情 / 微笑 / 大笑（附置信度进度条）
- **情绪**：愤怒 / 厌恶 / 恐惧 / 开心 / 悲伤 / 惊讶 / 中性（附置信度进度条）
- **附加信息**：年龄、性别、眼镜类型、人脸置信度
- **状态指示灯**：绿色=就绪，黄色=识别中，红色=错误

---

## 九、rockchip 平台适配要点

本项目在开发过程中解决了多个 rockchip 平台兼容性问题，以下是关键适配要点：

### 9.1 libGL 驱动加载失败

**现象**：`libGL error: failed to load driver: rockchip`

**解决**：在导入 cv2/pygame 之前设置环境变量

```python
os.environ.setdefault('LIBGL_ALWAYS_SOFTWARE', '1')
```

### 9.2 import 顺序

**现象**：`FATAL: exception not rethrown`

**解决**：必须先 `import pygame`，再 `import cv2`。pygame 先导入让 SDL 接管视频子系统，cv2 不会再重复初始化 OpenGL。

### 9.3 不使用 pygame.init()

**现象**：`FATAL: exception not rethrown`

**解决**：只初始化需要的子模块，避免 `pygame.mixer` 初始化触发 rockchip 原生崩溃

```python
pygame.display.init()
pygame.font.init()
# 不调用 pygame.init()
```

### 9.4 摄像头初始化顺序

**现象**：`ioctl(VIDIOC_QBUF): Bad file descriptor`

**解决**：摄像头必须在 `pygame.display.set_mode()` 之后打开

### 9.5 GIL 争用导致摄像头掉线

**现象**：运行一段时间后"摄像头读取失败，尝试重新连接"

**解决**：

1. 百度 API 检测间隔设为 5 秒
2. 图片缩小到 160×120 再编码
3. 编码操作之间插入 `time.sleep(0.05)` 释放 GIL

### 9.6 性能优化

- `make_gradient_bg` 用 numpy 向量化代替逐行绘制
- 预创建面板 Surface，避免每帧重复创建
- 背景图片 `convert()` 加速 blit
- Haar 检测每 10 帧一次，降低 CPU 占用

---

## 十、参考资源

| 资源 | 用途 |
| ------ | ------ |
| [好搭AI派学习手册.md](file:///d:/笔记本同步/好搭AI派资料/好搭AI派学习手册.md) | 硬件接口规范 |
| [好搭AI派范例代码.md](file:///d:/笔记本同步/好搭AI派资料/好搭AI派范例代码.md) | 摄像头、Pygame 范例 |
| [文字识别播报器.py](file:///d:/笔记本同步/好搭AI派资料/文字识别播报器.py) | camera_vision_system_v3 + 后台线程抓帧参考 |
| [人脸识别播放视频.py](file:///d:/笔记本同步/好搭AI派资料/人脸识别播放视频.py) | import 顺序、pygame 初始化参考 |
| [百度人脸识别 API 文档](https://cloud.baidu.com/doc/FACE/index.html) | API 参数说明 |

---

## 十一、常见问题

### Q1：程序启动后界面黑屏，几秒后退出

**A**：检查 `app.run()` 是否被调用。入口代码应为：

```python
if __name__ == '__main__':
    app = ExpressionApp()
    app.run()
```

### Q2：libGL 错误

**A**：确认文件开头有 `os.environ.setdefault('LIBGL_ALWAYS_SOFTWARE', '1')`，且在 `import cv2` 之前。

### Q3：摄像头打开失败

**A**：

1. 检查 USB 摄像头是否插入
2. 右下角开关是否拨到左侧
3. 查看终端是否有 `摄像头已打开` 的日志

### Q4：运行一段时间后摄像头读取失败

**A**：这是 GIL 争用导致的 SDK 超时。确认 `DETECT_INTERVAL` 为 5.0 秒，且检测线程中有 `time.sleep(0.05)` 释放 GIL。偶发卡顿属正常现象，程序会自动恢复。

### Q5：百度 API 返回错误

**A**：查看终端日志中的 `error_code`：

- `222202` / `222203`：未检测到人脸（正常现象）
- `110`：access_token 失效，检查 APP_ID/API_KEY/SECRET_KEY
- `17`：Open api daily request limit reached（配额用尽）
