# 智能风扇控制项目文档

基于好搭AI派（ESP32 + pygame）开发的智能风扇控制系统，通过触摸屏幕按钮切换风扇档位，ma 口驱动电机带动风扇转动，p1 口舵机指示当前档位，界面固定窗口 1920×1080。

## 一、功能特性

| 模块 | 功能 |
| --- | --- |
| 档位控制 | 5 档可调（0档关闭 / 1档低速 / 2档中速 / 3档高速 / 4档最高） |
| 电机驱动 | ma 口（motor_MA）输出 PWM（0–1023）带动风扇转动 |
| 舵机指示 | p1 口（GPIO_IO_01）舵机随档位转动 0°–180° 指示当前档位 |
| 信息显示 | 屏幕实时显示当前档位、电机转速值、舵机角度、运行状态 |
| 风扇动画 | 右侧动画区绘制 4 叶片旋转风扇，旋转速度随档位变化 |
| 触摸交互 | 底部 5 个档位按钮，触摸切换档位，当前档位高亮 |
| 背景 | 使用 `images/1.jpg` 作为全屏背景图 |
| 半透明 UI | 所有面板与按钮采用半透明效果，透出背景图 |
| 退出 | 右上角「退出」按钮，退出时自动关闭电机并复位舵机 |

## 二、快捷键

| 按键 | 功能 |
| --- | --- |
| `0`–`4` | 快捷切换对应档位 |
| `ESC` / 关闭窗口 | 退出程序 |

## 三、硬件接线

| 外设 | 扩展板接口 | SDK 调用 | 说明 |
| --- | --- | --- | --- |
| 电机（风扇） | ma 口 | `board.motor_MA(speed)` | speed 范围 0–1023 |
| 舵机（档位指示） | p1 口（GPIO_IO_01） | `board.servo(GPIO_IO_01, angle)` | angle 范围 0–180° |

## 四、档位配置

| 档位 | 名称 | 电机转速值 | 舵机角度 | 显示颜色 |
| --- | --- | --- | --- | --- |
| 0档 | 关闭 | 0 | 0° | 灰色 |
| 1档 | 低速 | 256 | 45° | 绿色 |
| 2档 | 中速 | 512 | 90° | 青色 |
| 3档 | 高速 | 768 | 135° | 橙色 |
| 4档 | 最高 | 1023 | 180° | 红色 |

## 五、项目结构

```md
haoda_AIPad/
├── fan_control.py              # 主程序
├── 风扇控制项目说明文档.md      # 本文档
└── images/
    └── 1.jpg                   # 背景图（1920×1080）
```

## 六、环境要求

- 好搭AI派平台（ESP32 + Linux）
- 依赖：`ESP32` SDK（平台自带）、`pygame`（平台自带）

## 七、安装与运行

```bash
# 在好搭AI派平台直接运行
python3 fan_control.py
```

> 运行前请确保扩展板已连接、电机接 ma 口、舵机接 p1 口，并在 `images/` 目录放入 `1.jpg` 背景图。

## 八、界面布局

界面按左信息右动画布局（1920×1080）：

```markdown
y= 40   智能风扇控制系统（标题 80pt）              [退出]
y=150   ───────────── 分隔线 ─────────────
y=200   ┌─────────────┐  ┌──────────────────┐
        │ 左侧信息面板  │  │  右侧风扇动画区    │
        │ 当前档位      │  │   ◯ 旋转风扇      │
        │ 电机转速值    │  │  （叶片随档位旋转）│
        │ 舵机角度      │  │                  │
        │ 运行状态      │  └──────────────────┘
y=760   └─────────────┘
y=820   [0档关闭] [1档低速] [2档中速] [3档高速] [4档最高]
y=1030  触摸下方按钮切换风扇档位 ...
```

## 九、技术说明

### 1. 档位控制

`apply_gear(gear_index)` 根据 `GEAR_CONFIG` 配置表同时设置电机转速和舵机角度，统一管理档位参数，便于扩展。

### 2. 半透明 UI

新增辅助函数 `draw_rounded_rect_alpha()`，使用带 `SRCALPHA` 标志的 Surface 绘制带 alpha 通道的圆角矩形，实现面板与按钮的半透明效果。函数内部通过 `pygame.Rect(rect)` 统一转换入参，兼容 tuple 和 Rect 对象。

### 3. 风扇动画

风扇叶片旋转角度 `fan_angle` 每帧增量与档位电机值成正比（`motor / 1023.0 * 20.0`），档位越高叶片旋转越快，0 档停止旋转。叶片用多边形近似绘制。

### 4. 安全退出

主循环包裹在 `try/finally` 中，无论正常退出还是异常，`finally` 块都会关闭电机（`motor_MA(0)`）并复位舵机（`servo(SERVO_PIN, 0)`），再清理 pygame 资源。

## 十、可配置项

在 `fan_control.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `GEAR_CONFIG` | 各档位的电机值/舵机角度/名称/颜色 | 5 档配置 |
| `SERVO_PIN` | 舵机引脚 | `GPIO_IO_01`（p1 口） |
| `BUTTON_W` / `BUTTON_H` | 档位按钮尺寸 | 320 × 180 |
| `fill_alpha` | 面板/按钮半透明度 | 140–200 |
