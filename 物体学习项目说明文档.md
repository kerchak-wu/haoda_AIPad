# 物体学习项目说明文档

基于好搭AI派（ESP32 + pygame）开发的物体学习程序：USB外接摄像头实时采集画面，输入物体名称后将当前画面学习为该物体类别/样本。同一物体支持多次学习（多角度/多实例）增强识别鲁棒性。物体记录持久化到 JSON 文件，重启后自动加载并继续学习。提供 `ObjectLearner` 类供其他程序导入调用。

> 本项目基于人脸学习.py 的结构演化而来，但因物体学习与人脸学习的 API 差异（必须 `open_camera` + `start_background_detection`），摄像头管理方式完全不同。完整技术差异见第九章。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 物体学习 | `camera_vision_system_v3` 的 `add_object_recognition_class` / `add_object_recognition_sample`（参照范例 5.11） |
| 摄像头采集 | **视觉系统统一管理**：`vision_system.open_camera()` + `capture_frame()`，不使用 cv2 VideoCapture |
| 界面显示 | `pygame`，1920×1080，背景图 `images/1.jpg` + 半透明面板 |
| 多线程 | 后台采集线程（0.05s 间隔）+ 主循环线程，`threading.Lock` 保护帧读写 |
| 数据持久化 | JSON 文件（`object_records.json`），学习/删除时自动保存，启动时自动加载 |
| 日志输出 | `logs/物体学习_YYYYMMDD.log`，追加模式不覆盖，块缓冲避免卡顿 |
| 硬件控制 | `ESP32` 扩展板初始化（`board.start()`） |
| 平台兼容 | Rockchip 兼容性补丁（`LIBGL_ALWAYS_SOFTWARE=1` + pygame 分段初始化），避免 GPU/音频驱动冲突 |

> **关键设计**：与人脸学习不同，物体学习必须调用 `vision_system.open_camera()` + `start_background_detection()`，因为 `add_object_recognition_sample` 使用视觉系统当前捕获的帧。因此**不能使用 cv2 VideoCapture**，否则会与视觉系统抢 V4L2 设备导致冲突。

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 实时画面 | 摄像头画面嵌入主窗口（880×660），约 20 FPS 显示 |
| 物体学习 | 输入名称 → 点击「学习物体」→ 当前帧注册为物体样本 |
| 多次学习 | 同一物体可多次学习，样本数累计显示，增强识别鲁棒性 |
| 名称回填 | 点击已学习物体列表中的名称，自动回填到输入框，方便多次学习 |
| 数据持久化 | 学习记录保存到 JSON 文件，重启后自动加载 |
| 删除记录 | 列表项右侧删除按钮，仅删除应用层 JSON 记录（**不调用视觉系统 delete**，避免破坏识别模型） |
| 模块调用 | 提供 `ObjectLearner` 类，其他程序可 `from 物体学习 import ObjectLearner` 导入 |
| 退出方式 | 界面「退出程序」按钮 / ESC 键 / 窗口关闭按钮 |

## 三、界面布局

界面按左摄像头右信息面板布局（1920×1080）：

```markdown
y=  0   ┌──────────── 顶部标题区 (高130) ────────────┐
y= 25   │           物体学习（标题 64pt）     [退出程序]│
y= 90   │ USB摄像头采集画面 · 输入名称后点击学习 · 同一物体可多次学习 │
y=130   └────────────────────────────────────────────┘
y=150   ┌──────────────────┐  ┌─────────────────────────┐
        │ 摄像头画面面板     │  │  右侧信息面板             │
        │ ● 已连接           │  │  物体名称                │
        │                    │  │  [请输入物体名称...]     │
        │                    │  │  [    学习物体    ]      │
        │   实时摄像头画面    │  │  学习成功：xxx（样本 N）  │
        │   (880 × 660)      │  │  ───────────────────    │
        │                    │  │  已学习物体（N 个类别）   │
        │                    │  │  1. lifangti  样本 17  [删除]│
        │           1280 ×720│  │  2. cup       样本 5   [删除]│
y=950   └──────────────────┘  └─────────────────────────┘
y=970   ┌──────────── 底部栏 (高110) ────────────────┐
y=1020  │ ESC 退出 · 输入物体名称后回车或点击「学习物体」│
y=1080  └────────────────────────────────────────────┘
```

> 退出按钮固定置于标题栏右上角 `(WIDTH-280, 30, 240×70)`，不设在右下角。
> 列表项名称可点击回填到输入框，hover 时变蓝并显示下划线提示。

## 四、项目结构

```markdown
haoda_AIPad/
├── 物体学习.py                      # 主程序
├── 物体学习项目说明文档.md          # 本文档
├── object_records.json              # 物体记录持久化文件（自动生成）
├── logs/                            # 日志目录（自动生成）
│   └── 物体学习_YYYYMMDD.log        # 按日期命名的日志文件
├── images/
│   └── 1.jpg                        # 背景图（1920×1080）
├── 好搭AI派学习手册.md              # 硬件 API 参考
├── 好搭AI派范例代码.md              # 范例代码参考
├── 人脸学习.py                      # 结构参考项目
├── 人脸识别灯效.py                  # 视觉系统摄像头管理模式参考
└── 清空物体数据库.py                # 数据库清理工具
```

## 五、硬件接线

| 接口 | 设备 | 说明 |
| --- | --- | --- |
| USB | 外接摄像头 | 系统识别为 `/dev/video40` 或 `/dev/video41` |
| ESP32 | 好搭AI派扩展板 | `board.start()` 初始化 |

> 注意：好搭AI派右下角开关需拨到左侧以启用外设接口。

## 六、环境要求

- 好搭AI派平台（ESP32 + Linux）
- 依赖（均平台自带）：
  - `pygame` —— 界面显示
  - `opencv-python`（提供 `cv2`）—— 仅用于图像格式转换（BGR→RGB→Surface）
  - `numpy` —— 数组处理
  - `ESP32` —— 硬件控制
  - `camera_vision_system_v3` —— 物体学习算法（好搭AI派自带）
- 字体文件（位于 `/home/cxdz/jupyter/assets/`）：
  - `PingFang_Regular.ttf` —— 常规文字
  - `PingFang_Bold.ttf` —— 粗体（标题、按钮）
- 外接 USB 摄像头
- **USB 外接键盘**（用于输入物体名称，好搭AI派不支持中文输入法，名称只能用英文/拼音）

## 七、运行

```bash
python3 物体学习.py
```

启动后：

1. 程序自动初始化 ESP32 扩展板和视觉系统（含 `open_camera` + `start_background_detection`）
2. 摄像头画面实时显示在左侧面板
3. 在右侧输入框用 USB 键盘输入物体名称（如 `cup`、`shuibei`、`lifangti`）
4. 对准物体后点击「学习物体」或按回车，学习成功后名称出现在列表中
5. 同一物体多次学习时，直接点击列表中的名称回填到输入框，再点「学习物体」即可追加样本
6. 点击列表项右侧「删除」可移除记录
7. 点击界面右上角「退出程序」按钮或按 ESC 退出

## 八、模块调用（ObjectLearner 类）

`ObjectLearner` 类封装了视觉系统的物体学习功能，可供其他程序导入调用：

```python
from 物体学习 import ObjectLearner

# 初始化（内部会 open_camera + start_background_detection）
learner = ObjectLearner()

# 用当前摄像头帧学习/添加样本
learner.learn_current_frame('shuibei')   # 第一次学 shuibei：创建类别
learner.learn_current_frame('shuibei')   # 第二次学 shuibei：追加样本

# 查询已学习物体
objects = learner.get_learned_objects()  # [{name, sample_count, first_learned, last_learned}, ...]
count = learner.get_object_count()
obj = learner.find_by_name('shuibei')

# 删除记录（仅删应用层，不破坏视觉系统模型）
learner.delete_object(0)  # 按索引删除

# 释放资源
learner.close()
```

### ObjectLearner API

| 方法 | 说明 |
| --- | --- |
| `ObjectLearner(width=1280, height=720)` | 构造函数，初始化视觉系统、打开摄像头、启动后台检测、加载持久化记录 |
| `learn_current_frame(name)` | 用当前 USB 摄像头帧学习/添加样本。新类别调用 `add_object_recognition_class`，已有类别调用 `add_object_recognition_sample` |
| `get_learned_objects()` | 获取已学习物体列表 `[{name, sample_count, first_learned, last_learned}, ...]` |
| `get_object_count()` | 获取已学习物体类别数量 |
| `find_by_name(name)` | 根据类别名查找物体记录 |
| `delete_object(index)` | 按索引删除物体记录（仅删除应用层 JSON，不调用视觉系统删除接口） |
| `get_current_frame()` | 获取当前摄像头帧的副本（线程安全） |
| `close()` | 停止采集线程并释放视觉系统资源 |

## 九、技术说明

### 1. 视觉系统初始化（严格参照范例 5.11）

```python
self.vision_system = create_vision_system_v3(
    camera_id=-1, width=width, height=height,
    enable_basic=False, enable_advanced=False
)
self.vision_system.detection_config.enable_object_recognition = True
self.vision_system._init_detectors()
self.vision_system.open_camera()  # 必须！add_object_recognition_sample 需要当前帧
self.vision_system.threaded_system.start_background_detection(show_preview=False)
```

**与人脸学习的关键差异**：
- 人脸学习用 `learn_new_face(frame=...)` 传帧方式，**不需要** `open_camera()`
- 物体学习用 `add_object_recognition_sample(class_name=...)`，**必须** `open_camera()` + `start_background_detection()`，因为该方法使用视觉系统当前捕获的帧

### 2. 物体学习 API

严格参照范例代码 5.11：

```python
# 新类别：用当前帧创建类别
vision_system.add_object_recognition_class(frame=current_frame, class_name='shuibei')

# 已有类别：添加样本（视觉系统使用当前捕获的帧）
vision_system.add_object_recognition_sample(class_name='shuibei')
```

**学习策略**：
- `ObjectLearner` 维护 `_classes_created` 集合记录已创建的类别名
- 新类别（不在集合中）→ 调用 `add_object_recognition_class` 创建类别，`sample_count=1`
- 已有类别（在集合中）→ 调用 `add_object_recognition_sample` 追加样本，`sample_count+=1`

### 3. 摄像头管理（关键设计）

**不使用 cv2 VideoCapture**，完全由视觉系统管理摄像头：

| 方式 | 是否使用 | 原因 |
| --- | --- | --- |
| `vision_system.open_camera()` | ✓ | 物体学习必须，打开 V4L2 设备 |
| `vision_system.capture_frame()` | ✓ | 获取当前帧用于界面显示，只读缓存不访问 V4L2 |
| `cv2.VideoCapture()` | ✗ | 会与视觉系统抢 V4L2 设备导致冲突 |
| `start_background_detection()` | ✓ | 后台检测线程持续识别（虽然本程序不取结果，但学习 API 需要） |

### 4. 多线程架构

| 线程 | 职责 | 间隔 |
| --- | --- | --- |
| 主循环 | 事件处理 + 界面绘制 + 学习操作 | `clock.tick(30)` 约 33ms |
| 采集线程 | `vision_system.capture_frame()` 获取帧并覆盖 `raw_frame` | 固定 0.05s（≈20fps） |
| 视觉系统后台检测线程 | 持续检测（由 `start_background_detection` 启动） | 视觉系统内部管理 |

采集线程关键设计（参考人脸识别灯效.py 已验证模式）：
- **固定 0.05s 睡眠**：保证画面流畅，不紧循环调用
- **帧有效性验证**：`hasattr(frame, 'shape') and len(frame.shape) == 3` 跳过损坏帧
- **capture_frame() 只读缓存**：不访问 V4L2，与后台检测线程不冲突
- **threading.Lock 保护**：`raw_frame` 读写加锁

### 5. Rockchip 平台兼容性补丁

为避免 RK3566/RK3568 平台上 Pygame 与 OpenCV 的底层驱动冲突（段错误、摄像头 V4L2 死锁），程序顶部加入两处补丁（参考《视觉系统摄像头调用参考方案》第 7 章）：

```python
# 1. 强制 libGL 软件渲染，绕过 Mali GPU 驱动缺陷
#    必须在 import pygame 之前设置
import os
os.environ.setdefault('LIBGL_ALWAYS_SOFTWARE', '1')

# 2. pygame 分段初始化，跳过 mixer 模块（本程序无音频需求）
#    避免 pygame.mixer 与摄像头 V4L2 驱动产生死锁
pygame.display.init()
pygame.font.init()
# 不调用 pygame.init()
```

### 6. 帧缩放方式

使用 `pygame.transform.scale`（非 `smoothscale`）进行帧缩放，降低 CPU 开销（项目经验：smoothscale 对摄像头帧缩放有显著 CPU 开销）。

### 7. 数据持久化

- 文件格式：JSON（`object_records.json`）
- 存储内容：`[{"name": "shuibei", "sample_count": 3, "first_learned": "2026-08-05 17:20:30", "last_learned": "2026-08-05 17:25:12"}, ...]`
- 保存时机：学习成功、删除时自动保存
- 加载时机：程序启动时自动加载，已记录的类别名会加入 `_classes_created` 集合（视觉系统内部类别跨重启保留）
- 线程安全：`threading.RLock` 保护读写

### 8. 日志机制

- 日志目录：`logs/`（自动创建）
- 文件命名：`物体学习_YYYYMMDD.log`（按日期，同一天多次运行追加）
- 打开模式：`'a'`（追加，不覆盖）
- 缓冲方式：`buffering=-1`（块缓冲，避免后台检测线程高频写日志阻塞主循环）
- 运行分隔：每次启动写入 `======== YYYY-MM-DD HH:MM:SS 运行开始 ========` 标记

## 十、调试经验总结

### 1. 必须用视觉系统管理摄像头（重要差异）

最初参考人脸学习.py 用 cv2 VideoCapture 采集摄像头，导致与视觉系统的 `open_camera()` 冲突。

**根因**：物体学习 API `add_object_recognition_sample` 使用视觉系统当前捕获的帧，必须先 `open_camera()`。而 cv2 VideoCapture 会占用同一个 V4L2 设备，两者无法共存。

**解决方案**：完全由视觉系统管理摄像头，用 `capture_frame()` 获取帧用于界面显示（参考人脸识别灯效.py 已验证模式）。

### 2. 画面卡顿优化

初始版本画面卡顿，主要原因：
1. **日志行缓冲**：`buffering=1` 导致后台检测线程每次输出都写盘，阻塞主循环
2. **采集间隔过长**：`time.sleep(0.15)` ≈ 6.6fps，画面更新慢

**解决方案**：
- 日志改为块缓冲 `buffering=-1`
- 采集间隔改为 `time.sleep(0.05)` ≈ 20fps

### 3. Rockchip 平台段错误

未加补丁时，启动后偶发段错误崩溃，集中在 Pygame 初始化和摄像头启动阶段。

**根因**：RK3566/RK3568 平台 Mali GPU 驱动存在缺陷，`pygame.init()` 加载 mixer 模块时与 V4L2 摄像头驱动产生死锁。

**解决方案**：参考《视觉系统摄像头调用参考方案》第 7 章加入兼容性补丁（`LIBGL_ALWAYS_SOFTWARE=1` + pygame 分段初始化，详见第九章第 5 节）。

### 4. 中文输入不支持

尝试增加 `TEXTINPUT` 事件处理支持中文 IME 输入，但好搭AI派系统不支持中文输入法，最终回退到仅 `KEYDOWN` + `event.unicode` 的英文/数字输入版本。

**教训**：好搭AI派无中文输入法环境，物体名称只能用英文/拼音命名。下游程序（如物体识别播报）可通过映射表将英文/拼音转为中文用于 TTS 播报。

### 5. delete 破坏识别模型（与人脸学习一致）

`delete_object_recognition_class` 虽然按 API 规范设计支持删除单个类别，但为保险起见，`delete_object` 方法仅删除应用层 JSON 记录，不调用视觉系统删除接口（与人脸学习 delete_face 的教训一致）。

如需彻底清理视觉系统数据库，使用独立的 `清空物体数据库.py` 脚本。

## 十一、可配置项

在 `物体学习.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `CAMERA_W` / `CAMERA_H` | 摄像头采集分辨率（视觉系统内部） | 1280 × 720 |
| `CAM_DISP_W` / `CAM_DISP_H` | 摄像头画面显示尺寸 | 880 × 660 |
| `OBJECT_DATA_FILE` | 物体记录持久化文件 | `object_records.json` |
| `FONT_PATH` | 常规字体路径 | `/home/cxdz/jupyter/assets/PingFang_Regular.ttf` |
| `FONT_BOLD_PATH` | 粗体字体路径 | `/home/cxdz/jupyter/assets/PingFang_Bold.ttf` |
| `_LOG_DIR` | 日志目录 | `logs` |

## 十二、常见问题与故障排查

### 1. 摄像头打不开

| 可能原因 | 排查与解决 |
| --- | --- |
| 设备节点不对 | 查看日志 `logs/物体学习_*.log` 中的摄像头探测信息 |
| 摄像头被占用 | 确保没有其他程序正在使用摄像头 |
| 视觉系统初始化失败 | 查看是否输出 `object_recognition 算法已启用` |

### 2. 物体学习失败

| 可能原因 | 排查与解决 |
| --- | --- |
| 画面中无清晰物体 | 确保物体在摄像头视野中央，光线充足 |
| 物体名称为空 | 输入框不能为空，会提示「请先输入物体名称」 |
| 摄像头未就绪 | 等待状态栏显示摄像头已连接 |

### 3. 数据库残留旧数据

现象：只学了 2 个物体，但日志显示「已加载物体数据库: 4 个类别」。

**解决方案**：运行 `python3 清空物体数据库.py` 清空视觉系统数据库，再重新学习。

### 4. 重启后看不到已学习的物体

| 可能原因 | 排查与解决 |
| --- | --- |
| JSON 文件丢失 | 检查 `object_records.json` 是否存在 |
| 视觉系统数据丢失 | 视觉系统内部数据通常持久化存储，重启不丢失；若丢失需重新学习 |

## 十三、版本历史

| 版本 | 日期 | 主要变更 |
| --- | --- | --- |
| 1.0 | 2026-08 | 初始版本：ObjectLearner 类、1920×1080 界面、USB 摄像头学习、持久化 |
| 1.1 | 2026-08 | 移除 `learn_from_image` 方法，完全基于 USB 摄像头实时画面学习 |
| 1.2 | 2026-08 | 增加列表名称点击回填功能，支持多次学习免重复输入名称 |
| 1.3 | 2026-08 | 中文输入尝试与回退（好搭AI派不支持中文输入法） |
| 1.4 | 2026-08 | 画面卡顿优化：日志改块缓冲，采集间隔 0.15s → 0.05s |
| 1.5 | 2026-08 | 日志改为 `logs/` 目录 + 追加模式，不再覆盖 |
| 1.6 | 2026-08 | 按 API 分析报告 v2.1 复核：加入 Rockchip 兼容性补丁；pygame 分段初始化；移除启动时调试代码（dir() 列举方法） |

## 十四、扩展方向

- 其他程序通过 `from 物体学习 import ObjectLearner` 导入，实现物体学习 + 自定义业务逻辑
- 配合 `物体识别播报.py` 实现学习→识别→语音播报完整流程
- 支持学习质量检测（物体清晰度、光照评估）
- 增加样本管理（查看/删除单个样本，而非整个类别）
- 支持物体分组管理（按类别/用途分类）
