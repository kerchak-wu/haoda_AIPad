# 物体识别播报项目说明文档

基于好搭AI派（ESP32 + pygame）开发的物体识别播报程序：USB外接摄像头实时采集画面，点击「播报」按钮识别当前画面中的物体，并通过语音合成播报结果。识别到已学习物体播报"这是{中文名}"，未识别到或未学习过的物体播报"物体未学习"。语音合成使用好搭AI派语音 AI（VoiceAPI + TTS），英文/拼音物体名通过映射表自动转为汉语普通话播报。

> 本程序依赖 `物体学习.py` 生成的 `object_records.json` 记录文件，需先运行物体学习程序学习物体后再使用本程序识别播报。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 物体识别 | `camera_vision_system_v3` 的 `result_accessor` 获取识别结果（参照范例 5.12） |
| 摄像头采集 | **视觉系统统一管理**：`vision_system.open_camera()` + `capture_frame()`，不使用 cv2 VideoCapture |
| 语音合成 | `VoiceAPI` + `tts_synthesize`（参照范例 4.1 / 唐诗宋词朗读器） |
| 音频播放 | `pygame.mixer.music` 非阻塞播放 |
| 名称翻译 | `OBJECT_NAME_CN_MAP` 映射表，英文/拼音 → 中文 |
| 界面显示 | `pygame`，1920×1080，背景图 `images/1.jpg` + 半透明面板 |
| 多线程 | 后台采集线程（0.15s 间隔）+ 语音合成播放线程 + 主循环线程 |
| 日志输出 | `logs/物体识别播报_YYYYMMDD.log`，追加模式不覆盖 |
| 硬件控制 | `ESP32` 扩展板初始化（`board.start()`） |

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 实时画面 | 摄像头画面嵌入主窗口（880×660），约 6.6 FPS 显示 |
| 物体识别 | 点击「播报识别结果」按钮或按空格键，识别当前画面中的物体 |
| 语音播报 | 识别到已学习物体 → 播报"这是{中文名}"；未学习 → 播报"物体未学习" |
| 名称翻译 | 英文/拼音物体名自动映射为中文播报（映射表可扩展） |
| 识别结果展示 | 大字显示识别结果、置信度、英文标识 |
| 已学习列表 | 显示已学习物体列表（从 `object_records.json` 加载），当前识别项高亮 |
| 语音缓存 | 相同文本只合成一次，后续直接播放缓存音频 |
| 退出方式 | 界面「退出程序」按钮 / ESC 键 / 窗口关闭按钮 |

## 三、界面布局

界面按左摄像头右信息面板布局（1920×1080）：

```markdown
y=  0   ┌──────────── 顶部标题区 (高130) ────────────┐
y= 25   │         物体识别播报（标题 64pt）  [退出程序]│
y= 90   │ USB摄像头实时识别 · 点击播报按钮语音播报      │
y=130   └────────────────────────────────────────────┘
y=150   ┌──────────────────┐  ┌─────────────────────────┐
        │ 摄像头画面面板     │  │  右侧信息面板             │
        │ ● 已连接           │  │  语音播报                │
        │                    │  │  [🔊 播报识别结果 ]     │
        │                    │  │  识别到：xxx（置信度）   │
        │   实时摄像头画面    │  │  ───────────────────    │
        │   (880 × 660)      │  │  识别结果                │
        │                    │  │  这是 立方体（大字绿色）  │
        │                    │  │  标识：lifangti         │
        │                    │  │  置信度：0.92           │
        │           1280 ×720│  │  ───────────────────    │
        │                    │  │  已学习物体（2 个类别）   │
        │                    │  │  1. lifangti → 立方体 ← 当前│
        │                    │  │  2. cup      → 水杯      │
y=950   └──────────────────┘  └─────────────────────────┘
y=970   ┌──────────── 底部栏 (高110) ────────────────┐
y=1020  │ ESC 退出 · 点击播报按钮或按空格键识别并播报    │
y=1080  └────────────────────────────────────────────┘
```

> 退出按钮固定置于标题栏右上角 `(WIDTH-280, 30, 240×70)`，不设在右下角。

## 四、项目结构

```markdown
haoda_AIPad/
├── 物体识别播报.py                  # 主程序
├── 物体识别播报项目说明文档.md      # 本文档
├── object_records.json              # 物体记录文件（由 物体学习.py 生成）
├── recordings/                      # 语音音频缓存目录（自动生成）
│   └── 1940359792375253841.wav      # 按文本哈希命名的缓存音频
├── logs/                            # 日志目录（自动生成）
│   └── 物体识别播报_YYYYMMDD.log    # 按日期命名的日志文件
├── images/
│   └── 1.jpg                        # 背景图（1920×1080）
├── 好搭AI派学习手册.md              # 硬件 API 参考
├── 好搭AI派范例代码.md              # 范例代码参考
├── 唐诗宋词朗读器.py                # 语音合成参考项目
├── 人脸识别灯效.py                  # 视觉系统摄像头管理模式参考
├── 物体学习.py                      # 上游程序（生成 object_records.json）
└── 清空物体数据库.py                # 数据库清理工具
```

## 五、硬件接线

| 接口 | 设备 | 说明 |
| --- | --- | --- |
| USB | 外接摄像头 | 系统识别为 `/dev/video40` 或 `/dev/video41` |
| USB | 外接扬声器/耳机 | 语音播报输出（系统默认音频设备） |
| ESP32 | 好搭AI派扩展板 | `board.start()` 初始化 |

> 注意：好搭AI派右下角开关需拨到左侧以启用外设接口。

## 六、环境要求

- 好搭AI派平台（ESP32 + Linux）
- 依赖（均平台自带）：
  - `pygame` —— 界面显示 + 音频播放（`pygame.mixer`）
  - `opencv-python`（提供 `cv2`）—— 仅用于图像格式转换（BGR→RGB→Surface）
  - `numpy` —— 数组处理
  - `ESP32` —— 硬件控制
  - `camera_vision_system_v3` —— 物体识别算法（好搭AI派自带）
  - `audio_player` —— 音频播放器（好搭AI派自带）
  - `voice_api` —— 语音 AI 接口（好搭AI派自带）
- 字体文件（位于 `/home/cxdz/jupyter/assets/`）：
  - `PingFang_Regular.ttf` —— 常规文字
  - `PingFang_Bold.ttf` —— 粗体（标题、按钮、识别结果）
- 外接 USB 摄像头
- 外接扬声器或耳机
- **语音 AI 账号**：需在程序顶部配置 `VOICE_USERNAME` / `VOICE_PASSWORD`（好好搭搭认证账号）

## 七、运行

### 前置条件

1. 已运行 `物体学习.py` 学习至少 1 个物体（生成 `object_records.json`）
2. 已在程序顶部配置语音 AI 账号：
   ```python
   VOICE_USERNAME = 'your_username'
   VOICE_PASSWORD = 'your_password'
   ```

### 启动

```bash
python3 物体识别播报.py
```

启动后：

1. 程序自动初始化 ESP32 扩展板、视觉系统（含 `open_camera` + `start_background_detection`）
2. 进行语音 AI 认证（`get_token`），日志输出「语音 AI 认证成功」
3. 摄像头画面实时显示在左侧面板
4. 对准已学习的物体，点击「播报识别结果」按钮或按空格键
5. 识别成功后语音播报"这是{中文名}"，界面显示识别结果、英文标识、置信度
6. 未识别到或未学习过的物体播报"物体未学习"
7. 点击界面右上角「退出程序」按钮或按 ESC 退出

## 八、技术说明

### 1. 视觉系统初始化与识别（严格参照范例 5.12）

```python
# 初始化
vision_system = create_vision_system_v3(
    camera_id=-1, width=1280, height=720,
    enable_basic=False, enable_advanced=False
)
vision_system.detection_config.enable_object_recognition = True
vision_system._init_detectors()
vision_system.open_camera()
vision_system.threaded_system.start_background_detection(show_preview=False)

# 识别（参照范例 5.12）
vision_system.result_accessor.refresh_results()
class_name = vision_system.result_accessor.get_object_recognition_class_name()
confidence = vision_system.result_accessor.get_object_recognition_confidence()
```

**重要修正**：`CompleteDetectionResultAccessor` **没有** `get_object_recognition_count` 方法（api-reference.md 文档有误），必须以范例代码 5.12 为准，直接调用 `get_object_recognition_class_name()` 和 `get_object_recognition_confidence()`。

### 2. 识别结果处理流程

```python
refresh_results()
class_name = get_object_recognition_class_name()
confidence = get_object_recognition_confidence()

# class_name 无效 → 未识别到物体
if not class_name or class_name in ('None', '未知', 'unknown', ''):
    → 播报"物体未学习"

# 校验是否为已学习的物体（双重确认）
if class_name not in learned_object_names:
    → 播报"物体未学习"

# 识别成功
→ 播报"这是{to_chinese(class_name)}"
```

### 3. 语音合成与播放（参照唐诗宋词朗读器）

```python
# 语音 AI 认证
voice_api = VoiceAPI('http://www.haohaodada.com/project/voiceAI/ApiZNBW.php')
voice_api.get_token(VOICE_USERNAME, VOICE_PASSWORD)

# TTS 合成到文件
audio_data = voice_api.tts_synthesize(text, audio_path)

# pygame.mixer 非阻塞播放
pygame.mixer.music.load(audio_path)
pygame.mixer.music.play()
while pygame.mixer.music.get_busy():
    time.sleep(0.1)
```

**音频缓存**：相同文本（如"这是水杯"）只合成一次，以文本哈希作为文件名存到 `recordings/` 目录，后续直接播放缓存。

### 4. 名称翻译映射表

好搭AI派不支持中文输入，物体学习时只能用英文/拼音命名。本程序通过 `OBJECT_NAME_CN_MAP` 映射表将英文/拼音转为中文用于 TTS 合成：

```python
OBJECT_NAME_CN_MAP = {
    'shuibei': '水杯', 'lifangti': '立方体', 'pingguo': '苹果',
    'cup': '水杯', 'book': '书本', 'cube': '立方体',
    # ... 约 80 项，可在程序顶部自行补充
}

def to_chinese(name):
    return OBJECT_NAME_CN_MAP.get(name.strip().lower(), name)
```

**查找策略**：
- 优先精确匹配（不区分大小写）
- 未找到时原样返回（TTS 也能朗读英文，只是按字母/单词发音）

**补充映射**：在 `OBJECT_NAME_CN_MAP` 字典中加一行 `'英文或拼音': '中文名'` 即可。

### 5. 摄像头管理（与物体学习.py 一致）

**不使用 cv2 VideoCapture**，完全由视觉系统管理摄像头：

| 方式 | 是否使用 | 原因 |
| --- | --- | --- |
| `vision_system.open_camera()` | ✓ | 物体识别必须，打开 V4L2 设备 |
| `vision_system.capture_frame()` | ✓ | 获取当前帧用于界面显示，只读缓存不访问 V4L2 |
| `cv2.VideoCapture()` | ✗ | 会与视觉系统抢 V4L2 设备导致冲突 |
| `start_background_detection()` | ✓ | 后台检测线程持续识别，`refresh_results()` 轮询结果 |

### 6. 多线程架构

| 线程 | 职责 | 间隔 |
| --- | --- | --- |
| 主循环 | 事件处理 + 界面绘制 + 识别触发 | `clock.tick(30)` 约 33ms |
| 采集线程 | `vision_system.capture_frame()` 获取帧并覆盖 `raw_frame` | 固定 0.15s（≈6.6fps） |
| 视觉系统后台检测线程 | 持续识别（由 `start_background_detection` 启动） | 视觉系统内部管理 |
| 语音合成播放线程 | TTS 合成 + `pygame.mixer` 播放（避免阻塞主循环） | 事件驱动 |

### 7. 日志机制

- 日志目录：`logs/`（自动创建）
- 文件命名：`物体识别播报_YYYYMMDD.log`（按日期，同一天多次运行追加）
- 打开模式：`'a'`（追加，不覆盖）
- 缓冲方式：`buffering=-1`（块缓冲，避免高频写日志阻塞主循环）
- 运行分隔：每次启动写入 `======== YYYY-MM-DD HH:MM:SS 运行开始 ========` 标记

## 九、调试经验总结

### 1. get_object_recognition_count 方法不存在（重要修正）

最初参考 api-reference.md 调用 `get_object_recognition_count()`，运行报错：
```
'CompleteDetectionResultAccessor' object has no attribute 'get_object_recognition_count'
```

**根因**：api-reference.md 文档有误，`CompleteDetectionResultAccessor` 类没有 `get_object_recognition_count` 方法。

**解决方案**：以范例代码 5.12 为权威，直接调用 `get_object_recognition_class_name()` 和 `get_object_recognition_confidence()`，不调用 count。

**教训**：好搭AI派的 API 文档可能存在错误，**范例代码才是权威**。遇到 API 问题时优先查看范例代码的调用方式。

### 2. 英文/拼音转中文播报

好搭AI派不支持中文输入法，物体学习时只能用英文/拼音命名。直接播报英文名体验差（TTS 按字母发音）。

**解决方案**：在程序顶部维护 `OBJECT_NAME_CN_MAP` 映射表（约 80 项常用物体），播报时自动转换为中文名进行 TTS 合成。用户可自行补充映射。

### 3. 日志覆盖问题

初始版本用 `open(DEBUG_LOG_FILE, 'w', ...)` 模式，每次运行覆盖上次日志，无法追溯历史问题。

**解决方案**：
- 日志统一存到 `logs/` 目录
- 文件名含程序名+日期：`物体识别播报_YYYYMMDD.log`
- 用追加模式 `'a'`，同一天多次运行追加到同一文件
- 每次启动写入时间分隔标记

### 4. 画面流畅度

物体识别播报程序对画面流畅度要求低于物体学习程序（无需精确对准学习），采集间隔 0.15s（≈6.6fps）可接受。如需更流畅，可改为 0.05s（参考物体学习.py 的优化）。

## 十十、可配置项

在 `物体识别播报.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `CAMERA_W` / `CAMERA_H` | 摄像头采集分辨率（视觉系统内部） | 1280 × 720 |
| `CAM_DISP_W` / `CAM_DISP_H` | 摄像头画面显示尺寸 | 880 × 660 |
| `OBJECT_DATA_FILE` | 物体记录文件（由 物体学习.py 生成） | `object_records.json` |
| `AUDIO_CACHE_DIR` | 语音音频缓存目录 | `recordings` |
| `VOICE_USERNAME` | 语音 AI 用户名 | `'username'`（需替换） |
| `VOICE_PASSWORD` | 语音 AI 密码 | `'password'`（需替换） |
| `OBJECT_NAME_CN_MAP` | 英文/拼音 → 中文映射表 | 约 80 项内置 |
| `FONT_PATH` | 常规字体路径 | `/home/cxdz/jupyter/assets/PingFang_Regular.ttf` |
| `FONT_BOLD_PATH` | 粗体字体路径 | `/home/cxdz/jupyter/assets/PingFang_Bold.ttf` |
| `_LOG_DIR` | 日志目录 | `logs` |

## 十一、常见问题与故障排查

### 1. 语音 AI 认证失败

| 可能原因 | 排查与解决 |
| --- | --- |
| 账号密码未配置 | 在程序顶部设置 `VOICE_USERNAME` / `VOICE_PASSWORD` |
| 账号未认证 | 需使用在好好搭搭平台完成认证的账号 |
| 网络问题 | 确保好搭AI派网络连接正常 |

### 2. 识别不到物体（一直播报"物体未学习"）

| 可能原因 | 排查与解决 |
| --- | --- |
| 未学习物体 | 先运行 `物体学习.py` 学习物体 |
| `object_records.json` 不存在 | 由 物体学习.py 生成，确认文件存在 |
| 摄像头未对准物体 | 调整物体位置和角度，确保在摄像头视野中央 |
| 光线不足 | 增加环境光线 |
| `get_object_recognition_count` 报错 | 已修复，确认使用 `get_object_recognition_class_name` 方式 |

### 3. 语音播报无声

| 可能原因 | 排查与解决 |
| --- | --- |
| 未接扬声器 | 接 USB 扬声器或耳机到好搭AI派 |
| 音量过低 | 调整系统音量 |
| TTS 合成失败 | 查看日志 `[语音] 合成失败` 信息，检查网络和账号 |
| 音频缓存损坏 | 删除 `recordings/` 目录下的缓存文件，重新合成 |

### 4. 物体名未转中文（播报英文）

| 可能原因 | 排查与解决 |
| --- | --- |
| 映射表未收录 | 在 `OBJECT_NAME_CN_MAP` 中补充 `'英文名': '中文名'` |
| 大小写不匹配 | 查找时自动转小写，无需担心大小写 |

## 十二、版本历史

| 版本 | 日期 | 主要变更 |
| --- | --- | --- |
| 1.0 | 2026-08 | 初始版本：视觉系统识别 + 语音播报 + 1920×1080 界面 |
| 1.1 | 2026-08 | 修复 `get_object_recognition_count` AttributeError（改为范例 5.12 调用方式） |
| 1.2 | 2026-08 | 增加英文/拼音 → 中文映射表，播报汉语普通话 |
| 1.3 | 2026-08 | 日志改为 `logs/` 目录 + 追加模式，不再覆盖 |

## 十三、扩展方向

- 增加连续识别模式（每隔几秒自动识别并播报）
- 增加识别结果统计（识别次数、成功率）
- 支持多语言播报（英语、方言等）
- 增加识别置信度阈值过滤（低于阈值不播报）
- 与物联网模块结合，识别到特定物体触发智能家居动作
- 支持物体计数（如流水线产品计数）
