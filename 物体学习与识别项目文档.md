# 物体学习与识别项目文档

自包含的桌面物体学习与识别程序（不依赖好搭 AI 派专有 SDK），固定窗口 1920×1080，
支持物体学习登记、实时识别、物体库查看与删除管理，并将摄像头画面直接整合到主窗口中。
参考：`github.com/kerchak-wu/haoda_AIPad`（好搭 AI 派范例 11.物体识别学习 / 12.物体识别）。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 摄像头采集 | OpenCV `VideoCapture`（外接摄像头） |
| 物体特征提取 | OpenCV ORB（Oriented FAST and Rotated BRIEF）特征点检测 + 二进制描述子 |
| 物体识别 | BFMatcher 汉明距离匹配 + 绝对距离阈值筛选 + RANSAC 单应性矩阵几何验证 |
| 界面 | `pygame`（推荐 `pygame-ce`），1920×1080 |
| 数据持久化 | JSON 数据库 + 描述子/特征点 npy 文件 + PNG 样本图 |

> 不再依赖 `camera_vision_system_v3` 专有 SDK，任何装有 OpenCV + pygame 的电脑接上 USB 摄像头即可运行。

### ORB 物体识别原理

ORB 是 OpenCV 内置的快速特征检测/描述算法，对物体的纹理细节具有**旋转不变性**与**一定的尺度不变性**。

**学习阶段**：从画面中心 ROI（60% 区域）提取 ORB 特征点和描述子，采集多帧后去重保存。

**识别阶段**：从整帧提取 ORB 特征，与库中各类物体的描述子做 BFMatcher 汉明距离匹配，满足三重判据即识别成功：
1. **优质匹配数** ≥ `GOOD_MATCH_THRESHOLD`（40）
2. **RANSAC 内点数** ≥ `MIN_INLIERS`（20）
3. **内点比例** ≥ `MIN_INLIER_RATIO`（0.08）

> 匹配采用**绝对距离阈值**而非 Lowe 比率测试：物体学习通常采集多帧静止物体，特征高度相似，比率测试会因次近邻距离≈最近邻距离而失效；绝对距离阈值对多样本更鲁棒，误匹配由 RANSAC 几何一致性进一步过滤。

### 适用场景

适用于具有**丰富纹理**的物体（如带图案的卡片、标签、包装盒、玩具、书本封面等）；
纯色无纹理物体特征点稀少，识别效果会下降。

### 摄像头节点说明（重要）

本程序**固定探测 `/dev/video41`（优先）和 `/dev/video40`**，不再扫描其他节点，启动快、不卡顿。
若你的摄像头节点编号不同，请修改 `物体学习.py` 中 `open_camera()` 内的 `(41, 40)` 元组。

探测每个节点时会做三层防护：
1. **MJPG 编码**：设置 `CAP_PROP_FOURCC = MJPG`，解决 USB 摄像头默认格式导致的雪花/绿屏
2. **4 秒超时**：用 `signal.SIGALRM` 打断卡在 `select()` 的元数据/损坏节点（避免启动卡死）
3. **雪花检测**：用 `INTER_AREA` 下采样比较标准差比值，过滤随机噪声帧（实测真实画面 r≈0.95，雪花 r≈0.03）

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 物体学习 | 输入名称 → 采集多帧 → 自动分配物体 ID → 保存特征与样本图 |
| 物体识别 | 实时提取 ORB 特征并匹配，画面上直接框出物体并显示名称/置信度 |
| 摄像头整合 | 摄像头画面直接嵌入主窗口（1100×520），不再弹出独立预览窗口 |
| 后台处理 | 采集线程与识别线程分离，主循环零阻塞，界面不卡顿 |
| 物体库查看 | 「查看物体库详细信息」按钮弹窗分页显示所有物体的 ID、名称、登记时间、样本数 |
| 物体删除 | 列表中每条记录旁有「删除」按钮，带二次确认，删除后自动重建索引 |
| 识别历史 | 实时记录识别结果（时间、ID、名称、置信度），最多保留 20 条 |
| 数据可移植 | 程序关闭后，其他程序可加载已保存的数据库独立完成识别 |
| 特征去重 | 多帧学习静止物体时自动去除高度相似的描述子，减少冗余并降低误匹配率 |

## 三、项目结构

```
haoda_AIPad/
├── 物体学习.py                  # 主程序（含 ObjectEngine 引擎 + pygame 界面）
├── 物体学习与识别项目文档.md     # 本文档
├── 物体识别播放视频.py           # 物体识别触发视频播放（见配套文档）
├── 物体识别触发视频播放项目说明文档.md
├── images/
│   └── 1.jpg                    # 背景图（1920×1080）
└── object_data/                 # 物体数据目录（运行时自动创建）
    ├── object_db.json           # 物体库 {id: {name, created_at, samples, roi_size, ...}}
    ├── descriptors/             # ORB 描述子（uint8, N×32）
    │   ├── 1_0.npy … 1_14.npy
    │   └── 2_0.npy …
    ├── keypoints/               # 特征点坐标（float32, N×2，roi_size 坐标系）
    │   ├── 1_0.npy … 1_14.npy
    │   └── 2_0.npy …
    └── images/                  # 学习时保存的 ROI 样本图（PNG 无损）
        ├── 1_0.png … 1_14.png
        └── 2_0.png …
```

### 物体数据库格式（object_db.json）

```json
{
  "objects": {
    "1": {
      "name": "卡片",
      "created_at": "2026-07-24 15:30:00",
      "samples": 15,
      "roi_size": [360, 360],
      "desc_files": ["object_data/descriptors/1_0.npy", "..."],
      "kp_files":   ["object_data/keypoints/1_0.npy",   "..."],
      "sample_images": ["object_data/images/1_0.png", "..."]
    },
    "2": {
      "name": "书本",
      "created_at": "2026-07-24 15:35:12",
      "samples": 15,
      "roi_size": [360, 270],
      "desc_files": ["object_data/descriptors/2_0.npy", "..."],
      "kp_files":   ["object_data/keypoints/2_0.npy",   "..."],
      "sample_images": ["object_data/images/2_0.png", "..."]
    }
  }
}
```

- 键：物体 ID（字符串形式，程序自动按递增分配）
- 值：
  - `name`：物体名称
  - `created_at`：登记时间
  - `samples`：样本帧数
  - `roi_size`：学习 ROI 缩放后的尺寸 [宽, 高]（保持纵横比，较长边为 `OBJ_SIZE`）
  - `desc_files`：描述子文件路径列表
  - `kp_files`：特征点文件路径列表
  - `sample_images`：样本图路径列表

## 四、环境要求

- Python 3.8 及以上
- 依赖：
  - `opencv-python` 或 `opencv-contrib-python`（提供 `cv2` 与 ORB；无需 face 模块）
  - `pygame-ce` 或 `pygame`（界面）
  - `numpy`（数组处理）
  - 外接 USB 摄像头

## 五、安装与运行

```bash
# 安装依赖
pip install opencv-python pygame-ce numpy

# 运行程序
python3 物体学习.py
```

> 摄像头固定探测 `/dev/video41`（优先）和 `/dev/video40`。若节点编号不同，请修改 `物体学习.py` 中 `open_camera()` 内的 `(41, 40)` 元组。运行时终端会打印实际使用的节点编号。

## 六、界面布局

界面采用左右双栏布局（1920×1080）：

```
y= 12   物体学习与识别系统（标题 48pt 加粗，居中）    退出程序（右上角）
y= 75   [学习物体]  [物体识别]（模式切换按钮）

┌─ 左侧面板 (60,145) 1180×850 ──────────────┐  ┌─ 学习控件面板 (1280,145) 580×240 ─┐
│ 学习物体 — 摄像头画面（副标题）            │  │ 学习控件                          │
│ ┌──────────────────────────────────────┐  │  │ 名称：[________]                  │
│ │   摄像头画面 1100×520                │  │  │ [开始学习物体]                    │
│ │   （识别时框出物体并显示名称/置信度）  │  │  └──────────────────────────────────┘
│ └──────────────────────────────────────┘  │  y=400 [查看物体库详细信息] 按钮
│ 操作提示 / 识别历史                       │  ┌─ 已保存物体库 (1280,470) 580×525 ─┐
└──────────────────────────────────────────┘  │ 已保存物体库  共 N 个              │
                                               │ ID 1 : 卡片  时间 样本N    [删除] │
                                               │ ID 2 : 书本  时间 样本N    [删除] │
                                               └───────────────────────────────────┘
y=1005  [删除操作 toast 提示]
y=1055  ESC 退出 | 摄像头画面已整合到主窗口 | 鼠标滚轮可滚动物体库列表
```

### 模式切换

- **学习物体模式**：左侧下方显示操作提示；右侧上方显示名称输入框与学习按钮
- **物体识别模式**：左侧下方显示识别历史；右侧上方显示最新识别结果；摄像头画面上实时绘制物体框与名称

## 七、工作流程

### 物体学习流程

```
输入名称 ──> 点击「开始学习物体」（或按回车）
   │
   ├─ 姓名为空 ──> 提示"请先输入物体名称"
   ├─ 摄像头未打开 ──> 提示"摄像头未打开"
   │
   └─ 启动子线程，在 LEARN_DURATION 秒内采集多帧
           │
           ├─ 从画面中心 ROI 提取 ORB 特征（保持纵横比缩放）
           ├─ 有效特征数为 0 ──> 提示"学习失败，未检测到足够的物体特征"
           │
           └─ 分配新 ID（已有最大 ID + 1）
                   │
                   ├─ 描述子保存为 object_data/descriptors/<id>_<n>.npy
                   ├─ 特征点保存为 object_data/keypoints/<id>_<n>.npy
                   ├─ 样本图保存为 object_data/images/<id>_<n>.png（PNG 无损）
                   ├─ 写入 object_db.json（name + created_at + samples + roi_size + 文件列表）
                   ├─ 重建内存索引（合并描述子 + 去重）
                   └─ 显示"✅ 学习成功！ID=xx 名称=xx"
```

### 物体识别流程

```
后台识别线程以约 4fps 从最新帧提取 ORB 特征并匹配
   │
   ├─ 提取 ORB 特征 → BFMatcher 汉明距离匹配 → 绝对距离阈值筛选优质匹配
   │
   ├─ 三重判据验证：
   │    ├─ 优质匹配数 ≥ 40
   │    ├─ RANSAC 内点数 ≥ 20
   │    └─ 内点比例 ≥ 8%
   │
   ├─ 全部通过 ──> 命中，查名称，RANSAC 投影边角得到物体框，画面绿色框显示名称+置信度
   └─ 任一不通过 ──> 未识别，不绘制框
   │
   主循环：冷却机制避免同一结果刷屏，记录到识别历史
```

### 删除物体流程

```
点击列表中「删除」按钮 ──> 弹出确认对话框（显示 ID 与名称）
   │
   ├─ 点击「取消」或按 ESC ──> 关闭对话框
   │
   └─ 点击「确认删除」
           │
           ├─ 删除该 ID 的所有描述子文件、特征点文件、样本图文件
           ├─ 从 object_db.json 移除该 ID
           ├─ 重建内存索引
           └─ 底部 toast 提示"✅ 已删除：ID=xx 名称=xx"
```

## 八、技术说明

### 1. 摄像头后台线程采集

摄像头采集是 I/O 密集操作，同步调用会卡顿。`camera_capture_loop()` 守护线程持续 `cap.read()` 并加锁更新 `latest_frame`，主循环零阻塞读取。采集后立即降采样到 640px 宽，减小后续处理数据量。采集时用 `_is_valid_frame` 校验，过滤偶发雪花/空帧。

### 2. 后台识别线程

`recognition_loop()` 线程在识别模式下对最新帧调用 `engine.recognize()`，结果供主循环绘制物体框。识别频率限制为约 4fps（`MIN_INTERVAL=0.25s`），避免 CPU 占满导致渲染卡顿。帧缓存机制确保同一帧不重复识别。

### 3. 物体学习子线程

`learn_new_object` 采集 + 特征提取是耗时操作，子线程执行并通过 `learn_lock` 互斥，期间按钮显示"学习中..."并禁用。

### 4. ORB 特征与置信度

ORB 检测返回特征点及其 256 位二进制描述子。匹配使用 BFMatcher 汉明距离，距离越小越相似。置信度映射为 `good_count / (threshold * 2.5)`（封顶 1.0）。

### 5. 特征去重

多帧学习静止物体时，各帧特征高度相似，直接拼接会导致库内大量冗余描述子，既拖慢匹配又增加与无关画面的随机匹配概率。去重逻辑：用 BFMatcher 对合并后的描述子做 knnMatch(k=2)，每个描述子的次近邻（最近邻是自身）距离若小于 `DEDUP_DISTANCE`，则视为重复并删除其一。

### 6. RANSAC 几何验证

优质匹配后用 RANSAC 算法计算单应性矩阵（homography），将物体库中的特征点坐标投影到查询帧中。投影误差在阈值内的匹配点称为"内点"（inlier）。内点数和内点比例过低说明匹配虽然数量多但几何上不一致，大概率是误匹配。三重判据（数量 + 绝对内点数 + 内点比例）有效降低误识别率。

### 7. ROI 纵横比保持

学习时中心 ROI 缩放到 `OBJ_SIZE` 时保持原始纵横比（较长边为 `OBJ_SIZE`，较短边按比例），与识别时整帧按宽度等比缩放的逻辑一致，避免特征点相对位置形变导致匹配失败。

### 8. PNG 无损样本

样本图使用 PNG 保存，避免 JPEG 压缩丢失物体纹理细节影响识别精度。

### 9. 中文字体适配

`find_chinese_font()` 按优先级查找系统可用中文字体（SimHei / 微软雅黑 / PingFang / Noto Sans CJK / 文泉驿等）。

### 10. 摄像头雪花/卡顿防护（关键）

USB 摄像头在 Linux 上常见三类问题，本程序均做了防护：

| 问题 | 现象 | 防护机制 |
| --- | --- | --- |
| 默认格式不匹配 | `cap.read()` 返回成功但帧是雪花/绿屏 | 打开后设置 `CAP_PROP_FOURCC = MJPG` |
| 元数据/损坏节点 | `cap.read()` 卡在 `select()` 永不返回 | `signal.SIGALRM` 4 秒超时强制打断 |
| 随机噪声帧 | 帧数据是纯随机像素（std 反而很高） | `INTER_AREA` 下采样后标准差比值 < 0.2 判定为雪花 |

雪花检测原理：真实画面有空间结构（相邻像素相似），下采样后结构保留、std 几乎不变（r≈0.95）；随机噪声相邻像素无关，下采样时相互抵消、std 急剧下降（r≈0.03）。阈值 0.2 可清晰区分。

## 九、可配置项

在 `物体学习.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| 摄像头节点 | 在 `open_camera()` 的 `(41, 40)` 元组中修改 | 41 优先，40 兜底 |
| `CAMERA_W` / `CAMERA_H` | 摄像头采集分辨率 | 1280 × 720 |
| `CAM_DISP_W` / `CAM_DISP_H` | 摄像头画面显示尺寸 | 1100 × 520 |
| `CAP_SMALL_W` | 采集后降采样宽度（越小越流畅，识别率略降） | 640 |
| `BG_IMAGE` | 背景图路径 | `images/1.jpg` |
| `OBJECT_DATA_DIR` | 物体数据目录 | `object_data` |
| `GOOD_MATCH_THRESHOLD` | 优质匹配数阈值（越大越严格） | 40 |
| `MATCH_DIST_THRESHOLD` | ORB 汉明距离阈值（越小越严格） | 50 |
| `MIN_INLIERS` | RANSAC 最小内点数 | 20 |
| `MIN_INLIER_RATIO` | RANSAC 最小内点比例 | 0.08 |
| `DEDUP_DISTANCE` | 描述子去重距离 | 24 |
| `ORB_FEATURES` | 每帧提取的 ORB 特征点上限 | 800 |
| `LEARN_SAMPLES` | 学习采集样本帧数 | 15 |
| `LEARN_DURATION` | 学习采集时长（秒） | 1.8 |
| `LEARN_ROI_FRAC` | 学习 ROI 占画面比例 | 0.60 |
| `OBJ_SIZE` | ROI 归一化尺寸（较长边像素数） | 360 |
| `RECOG_PROC_W` | 识别处理帧宽度 | 640 |

## 十、对外 API（供其他程序调用）

本模块提供 `ObjectEngine` 类与若干函数，其他程序可直接 import，无需启动界面：

```python
from 物体学习 import ObjectEngine, get_object_name, load_object_database, list_known_objects
```

### ObjectEngine 类

| 方法 | 说明 |
| --- | --- |
| `ObjectEngine(data_dir, threshold)` | 创建引擎并自动加载物体库与索引 |
| `engine.load()` | 重新加载 JSON 数据库并重建索引 |
| `engine.learn_object(frames, name)` | 从多帧学习新物体，返回分配的 ID |
| `engine.recognize(frame)` | 识别 frame 中的物体，返回 `[(box, object_id, name, confidence), ...]` |
| `engine.delete_object(object_id)` | 删除指定 ID 的物体并重建索引 |
| `engine.get_name(object_id)` | 根据 ID 查名称 |
| `engine.list_objects()` | 返回 `[(id, info), ...]` |
| `engine.count()` | 返回物体数量 |

### 便捷函数

| 函数 | 返回值 | 说明 |
| --- | --- | --- |
| `load_object_database(path)` | `{id: {name, created_at, samples, ...}}` | 加载整个物体库 |
| `get_object_name(object_id, path)` | `str` 或 `None` | 根据 ID 查名称 |
| `list_known_objects(path)` | `[(id, name), ...]` | 所有物体 ID 与名称 |

### 调用示例

```python
from 物体学习 import ObjectEngine

# 其他程序：加载已学习的物体库，直接识别
engine = ObjectEngine()
# 摄像头取一帧 frame ...
for box, obj_id, name, conf in engine.recognize(frame):
    print("识别到：{}  置信度 {:.0%}".format(name, conf))
```

```python
from 物体学习 import get_object_name, load_object_database, list_known_objects

# 根据 ID 查名称
name = get_object_name(1)
# 获取整个物体库
db = load_object_database()
# 获取 ID-名称 列表
for obj_id, name in list_known_objects():
    print("{}: {}".format(obj_id, name))
```

## 十一、快捷键与交互

| 按键/操作 | 功能 |
| --- | --- |
| `ESC` | 关闭弹窗 → 退出程序 |
| `回车` | 学习模式下输入名称后直接开始学习 |
| `Backspace` | 删除输入框中最后一个字符 |
| 鼠标滚轮 | 在物体库列表区域上下滚动 |
| 鼠标点击 | 选择输入框、点击按钮、删除物体等 |
