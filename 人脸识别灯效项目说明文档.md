# 人脸识别灯效项目说明文档

基于好搭AI派（ESP32 + pygame）开发的人脸识别灯效程序：调用「人脸学习」程序保存的人脸记录，视觉系统实时识别摄像头画面中的人脸，根据不同人脸驱动 IO1 WS2812 RGB 灯带（11 灯珠）显示不同炫酷灯效。支持自定义人脸-灯效映射，界面显示摄像头画面、识别结果和灯效信息。

> 本项目经过多轮迭代优化：摄像头采集线程架构调整、face_id=None 识别失败处理、delete_face 破坏识别模型的故障修复、灯效选择弹窗布局优化。完整调试过程见第八章。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 人脸识别 | `camera_vision_system_v3` 的 `open_camera` + `start_background_detection` + `result_accessor`（严格参照范例 5.10） |
| 摄像头采集 | 视觉系统统一管理（`capture_frame`），不使用 cv2 `VideoCapture`，避免设备冲突 |
| 界面显示 | `pygame`，1920×1080，背景图 `images/1.jpg` + 半透明面板 |
| RGB 灯带控制 | `ESP32.ws2812Init` / `ws2812Write`（严格参照范例 2.4） |
| 多线程 | 后台采集线程（固定 0.15s 间隔）+ 主循环线程（识别检查 + 界面绘制 + 灯效驱动） |
| 数据持久化 | JSON 文件（`face_effect_map.json`），自定义灯效映射自动保存和加载 |

> 关键设计：**摄像头完全由视觉系统管理**，不使用 cv2 `VideoCapture`。这与「人脸学习」程序不同——人脸学习用 `learn_new_face(frame=...)` 传帧方式不需要 `open_camera()`，而人脸识别必须用 `open_camera()` + `start_background_detection()`，因此 cv2 不能再访问摄像头设备。

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 实时人脸识别 | 视觉系统后台检测，每 15 帧轮询一次识别结果（约 2 次/秒） |
| 多人脸支持 | 加载人脸学习程序保存的全部人脸记录，按 face_id 匹配 |
| 灯效驱动 | 10 种动态灯效 + 待机 + 未知 + 关闭，由主循环按帧驱动 |
| 自定义灯效映射 | 点击右侧列表项打开灯效选择弹窗，可为每个人脸指定灯效 |
| 灯效映射持久化 | 自定义映射保存到 `face_effect_map.json`，重启后自动加载 |
| 默认灯效分配 | 未自定义映射的人脸按 `face_id % len(EFFECT_LIST)` 自动分配灯效 |
| 识别状态显示 | 实时显示识别到的姓名、ID、置信度、当前灯效 |
| 退出方式 | 界面右上角「退出程序」按钮 / ESC 键 / 窗口关闭按钮 |
| 资源清理 | 退出时关闭摄像头、清理视觉系统资源、熄灭灯带 |
| 调试日志 | 所有 print 输出同步写入 `logs/人脸识别灯效_YYYYMMDD.log`，方便导出排查 |

## 三、灯效列表

| 灯效键名 | 显示名称 | 主题色 | 说明 |
| --- | --- | --- | --- |
| `rainbow` | 彩虹流光 | (255, 100, 200) | 颜色沿灯带流动 |
| `fire` | 火焰跳动 | (255, 60, 30) | 红橙黄随机跳动 |
| `ocean` | 海洋波涛 | (30, 100, 255) | 蓝色波纹追逐 |
| `breathing` | 呼吸渐变 | (60, 200, 100) | 绿色缓慢呼吸 |
| `twinkle` | 星光闪烁 | (255, 220, 100) | 金色随机闪烁 |
| `chase` | 色彩追逐 | (100, 200, 255) | 三色光点循环 |
| `heartbeat` | 心跳节奏 | (255, 130, 180) | 粉色双跳 |
| `aurora` | 极光幻彩 | (100, 255, 200) | 青绿紫渐变流动 |
| `meteor` | 流星飞逝 | (200, 100, 255) | 紫色彗星拖尾 |
| `lightning` | 闪电脉冲 | (255, 255, 100) | 黄色快速闪烁 |
| `standby` | 待机呼吸 | (80, 80, 120) | 冷蓝色缓慢呼吸（无人脸时） |
| `unknown` | 未知闪烁 | (150, 150, 150) | 灰色闪烁（未识别人脸时） |
| `none` | 灯光关闭 | (60, 60, 60) | 熄灭所有灯珠 |

灯效定义在 `人脸识别灯效.py` 顶部 `EFFECT_LIST` 和 `EFFECT_NAMES` 中配置。

## 四、界面布局

界面按左摄像头右信息面板布局（1920×1080）：

```markdown
y=  0   ┌──────────── 顶部标题区 (高130) ────────────┐
y= 18   │           人脸识别灯效（标题 64pt）   [退出程序]│
y= 90   │  视觉系统实时识别 · 不同人脸触发不同灯效 · IO1 11灯珠 │
y=130   └────────────────────────────────────────────┘
y=150   ┌──────────────────┐  ┌─────────────────────────┐
        │ 摄像头画面        │  │  识别结果                │
        │ ● 已连接          │  │  张三（大字 56pt）        │
        │                   │  │  ID:1  置信度:0.974      │
        │   实时摄像头画面   │  │  灯效：彩虹流光           │
        │   (880 × 660)     │  │  识别到：张三（ID:1...）  │
        │                   │  │  ───────────────────    │
        │           640 × 480│  │  已学习人脸（4）·点击修改 │
        │                   │  │  1. zhao      [彩虹流光] │
        │                   │  │  2. qian       火焰跳动  │
        │                   │  │  3. sun        海洋波涛  │
        │                   │  │  4. lee        呼吸渐变  │
y=970   └──────────────────┘  └─────────────────────────┘
y=970   ┌──────────── 底部栏 (高110) ────────────────┐
y=1025  │ ESC 退出 · 对准摄像头即可自动识别 · 点击右侧列表可修改人脸灯效 │
y=1080  └────────────────────────────────────────────┘
```

> 退出按钮固定置于标题栏右上角，不设在右下角。

### 灯效选择弹窗

点击右侧列表项后弹出居中弹窗（780×1010）：

- 标题：「选择灯效」+ 人脸姓名和 ID
- 当前灯效：显示当前分配的灯效
- 灯效列表：10 种灯效可选，行高 68px，背景框与文字不重叠
- 当前选中项有 ✓ 标记和绿色边框
- 重置默认按钮：清除自定义映射，恢复系统默认分配
- 关闭按钮：关闭弹窗

### 自定义灯效标记

右侧人脸列表中，灯效名称的显示方式：

| 显示 | 含义 |
| --- | --- |
| `[彩虹流光]` | 用户通过弹窗手动指定的灯效（保存在 `face_effect_map.json`） |
| `彩虹流光` | 系统默认分配的灯效（按 `face_id % len(EFFECT_LIST)`） |

## 五、项目结构

```markdown
haoda_AIPad/
├── 人脸识别灯效.py                  # 本程序主入口
├── 人脸识别灯效项目说明文档.md      # 本文档
├── 人脸学习.py                      # 人脸学习程序（生成 face_database/face_records.json）
├── 人脸学习项目说明文档.md          # 人脸学习程序文档
├── 清空人脸数据库.py                # 故障恢复工具（清空视觉系统人脸数据）
├── face_database/                   # V3 SDK 人脸特征库 + 应用层记录
│   └── face_records.json            # 人脸记录文件（由人脸学习程序生成）
├── face_effect_map.json             # 人脸-灯效自定义映射（本程序生成）
├── debug_log.txt                    # 旧版调试日志（已弃用，改用 logs/ 目录）
├── logs/                            # 日志文件夹（自动创建）
│   └── 人脸识别灯效_YYYYMMDD.log     # 调试日志（按程序名+日期命名，追加模式）
├── images/
│   └── 1.jpg                        # 背景图（1920×1080）
├── 好搭AI派学习手册.md               # 硬件 API 参考
├── 好搭AI派范例代码.md               # 范例代码参考
└── 手势控制RGB灯带.py                # 灯效实现与多线程参考项目
```

## 六、硬件接线

| 接口 | 设备 | 说明 |
| --- | --- | --- |
| IO1 (GPIO_IO_01) | WS2812 RGB 灯带 | 11 颗灯珠，需接上拉扩展模块 |
| USB | 外接摄像头 | 由视觉系统管理（不用 cv2 VideoCapture） |
| ESP32 | 好搭AI派扩展板 | `board.start()` 初始化 |

> 注意：好搭AI派右下角开关需拨到左侧以启用外设接口。

## 七、环境要求

- 好搭AI派平台（ESP32 + Linux）
- 依赖（均平台自带）：
  - `pygame` —— 界面显示
  - `opencv-python`（提供 `cv2`）—— 仅用于图像格式转换（BGR→RGB→Surface）
  - `numpy` —— 数组处理
  - `ESP32` —— 硬件控制
  - `camera_vision_system_v3` —— 人脸识别算法（好搭AI派自带）
- 字体文件（位于 `/home/cxdz/jupyter/assets/`）：
  - `PingFang_Regular.ttf` —— 常规文字
  - `PingFang_Bold.ttf` —— 粗体（标题、按钮）
- 外接 USB 摄像头
- WS2812 RGB 灯带（11 灯珠）+ 上拉扩展模块
- 已通过「人脸学习」程序学习至少 1 个人脸

## 八、运行

```bash
python3 人脸识别灯效.py
```

启动后：

1. 程序自动初始化 ESP32 扩展板和视觉系统
2. 加载 `face_database/face_records.json` 人脸记录和 `face_effect_map.json` 灯效映射
3. 视觉系统打开摄像头并启动后台检测
4. 对准摄像头，识别到已学习的人脸后自动切换对应灯效
5. 点击右侧列表项可打开灯效选择弹窗，修改人脸-灯效映射
6. 点击界面右上角「退出程序」按钮或按 ESC 退出

> 前置条件：需先运行「人脸学习」程序学习人脸，确保 `face_database/face_records.json` 存在且非空。

## 九、技术说明

### 1. 视觉系统初始化（严格参照范例 5.10）

```python
self.vision_system = create_vision_system_v3(
    camera_id=-1, width=1280, height=720,
    enable_basic=False, enable_advanced=False
)
self.vision_system.detection_config.enable_face_recognition = True
self.vision_system._init_detectors()
self.vision_system.open_camera()
self.vision_system.threaded_system.start_background_detection(show_preview=False)
```

**关键点**：

- `show_preview=False`：不弹出 OpenCV 窗口，画面由 pygame 界面显示
- 摄像头完全由视觉系统管理，不使用 cv2 `VideoCapture`
- 初始化顺序：视觉系统初始化 → 打开摄像头 → 启动后台检测

### 2. 识别结果轮询

在主循环中每 15 帧调用一次 `check_recognition()`：

```python
self.vision_system.result_accessor.refresh_results()
count = self.vision_system.result_accessor.get_face_count()
face_id = self.vision_system.result_accessor.get_face_id()
confidence = self.vision_system.result_accessor.get_face_confidence()
```

**`refresh_results()` 只读取后台检测线程的缓存结果**，不直接访问 V4L2 设备，因此不会与采集线程冲突。

### 3. face_id=None 处理

当 `get_face_count() > 0` 但 `get_face_id()` 返回 `None` 时，表示检测到人脸但识别失败（视觉系统数据库中无匹配项）。程序会：

- 显示「检测到人脸（未识别）」
- 切换到 `unknown` 灯效
- 提示「可能需要清空数据库重新学习」

### 4. 多线程架构

| 线程 | 职责 | 阻塞点 |
| --- | --- | --- |
| 主循环 | 事件处理 + 识别检查 + 界面绘制 + 灯效驱动 | 无阻塞，30 FPS |
| 采集线程 | `vision_system.capture_frame()` 固定 0.15s 间隔 | V4L2 阻塞读帧 |

**关键设计**：

- 采集线程固定 0.15s 间隔（约 6-7 FPS），给 V4L2 后台检测线程留出缓冲区访问时间
- 采集线程只负责界面显示用的帧，识别由后台检测线程独立完成
- `threading.Lock` 保护帧数据的读写
- 帧有效性验证：`hasattr(frame, 'shape') and len(frame.shape) == 3`

### 5. RGB 灯效实现

参照范例代码 `2.扩展模块使用 4.RGB灯` 的 `wheel` 函数和 `ws2812Write` 调用方式：

- 灯效由主循环按帧驱动（`update_led_frame()`），单线程避免并发问题
- 每种灯效用 `led_frame` 全局计数器作为时间轴，通过 `math.sin` 产生呼吸/跳动效果
- 颜色计算后通过 `board.ws2812Write((LED_PIN), i, r, g, b)` 写入单颗灯珠
- 仅当 `face_id` 变化时切换灯效，避免每帧重复切换

### 6. 人脸-灯效映射

```python
def get_effect_for_face_id(face_id, custom_map=None):
    if face_id is None:
        return 'standby'
    if custom_map and face_id in custom_map:
        return custom_map[face_id]           # 用户自定义
    return EFFECT_LIST[face_id % len(EFFECT_LIST)]  # 系统默认
```

- 自定义映射存储在 `face_effect_map.json`，键为 face_id（字符串），值为灯效键名
- 修改映射时自动保存，程序启动时自动加载
- 重置默认：从映射中删除该 face_id 的条目

### 7. 调试日志

程序启动时在 `logs/` 目录下创建日志文件（文件名含程序名+日期，如 `人脸识别灯效_20260805.log`），通过 `_TeeStdout` 类将所有 `print` 输出同时写入控制台和文件：

```python
_LOG_DIR = 'logs'
_LOG_FILE = _os.path.join(_LOG_DIR, '人脸识别灯效_%s.log' % _datetime.datetime.now().strftime('%Y%m%d'))
_debug_log_fp = open(_LOG_FILE, 'a', encoding='utf-8', buffering=-1)
```

日志输出特点（参考物体学习项目）：

1. 日志统一存到 `logs/` 文件夹，避免散落在项目根目录
2. 文件名含程序名+日期，不会覆盖上次的日志
3. 用追加模式 `'a'`，同一程序多次运行追加到当天日志
4. 用块缓冲 `buffering=-1`，避免后台检测线程高频写日志阻塞主循环
5. 每次运行写入分隔标记 `======== 时间 运行开始 ========`，区分不同次运行

调试日志包含：

- 视觉系统初始化信息
- `vision_system` 和 `result_accessor` 的所有方法列表
- 人脸数据库信息（`get_face_database_info()`）
- 每秒一次的识别状态（`get_face_count`、`get_face_id`、`get_face_confidence`）
- 灯效切换记录

### 8. 帧缩放方式

使用 `pygame.transform.scale`（非 `smoothscale`）进行帧缩放，降低 CPU 开销（项目经验：smoothscale 对摄像头帧缩放有显著 CPU 开销）。

### 9. 界面半透明面板

```python
panel = pygame.Surface(self.cam_rect.size, pygame.SRCALPHA)
pygame.draw.rect(panel, (*PANEL_COLOR, 170), panel.get_rect(), border_radius=18)
```

- 使用 `pygame.SRCALPHA` 创建带 alpha 通道的 Surface
- 面板颜色 alpha 设为 170（0-255），背景图片可透过面板显示

## 十、调试经验总结

### 1. 摄像头设备冲突

**问题**：视觉系统的 `open_camera()` 与 cv2 `VideoCapture` 抢同一个 V4L2 设备，导致摄像头读取失败（`VIDIOC_QUERYBUF: errno=25`）。

**解决**：摄像头完全由视觉系统管理，不使用 cv2 `VideoCapture`。采集线程调用 `vision_system.capture_frame()` 获取帧。

**与人脸学习程序的区别**：人脸学习用 `learn_new_face(frame=...)` 传帧方式，不需要 `open_camera()`，所以可以用 cv2 采集；人脸识别必须用 `open_camera()` + `start_background_detection()`，所以不能用 cv2。

### 2. 采集线程间隔优化

**问题**：采集线程紧循环调用 `capture_frame()` 导致 V4L2 缓冲区冲突，摄像头掉线。

**解决**：固定 0.15s 间隔（约 6-7 FPS），给后台检测线程足够的缓冲区访问时间。画面流畅度足够，摄像头稳定不掉线。

### 3. face_id=None 识别失败

**问题**：`get_face_count() > 0` 但 `get_face_id()` 返回 `None`，程序状态不更新，一直显示「正在启动识别」。

**根因**：之前在人脸学习程序中调用 `vision_system.delete_face()` 破坏了识别模型，导致检测正常但识别失效。

**解决**：

1. 程序层面：`check_recognition` 处理 `face_id=None` 的情况，显示「检测到人脸（未识别）」
2. 数据层面：运行 `清空人脸数据库.py` 脚本清空视觉系统数据库，重新学习人脸

### 4. delete_face 破坏识别模型

**问题**：在人脸学习程序中调用 `vision_system.delete_face(face_id)` 后，识别功能完全失效。

**现象**：

- `get_face_count=1`（检测到人脸）
- `get_face_id=None`（识别不出来）
- 重启设备无法恢复
- `get_face_database_info()` 显示注册数与实际不符

**根因**：好搭AI派范例代码中没有删除人脸的功能，`delete_face` 不被官方支持，调用后会破坏内部数据库结构。

**解决**：

1. 创建 `清空人脸数据库.py` 脚本，调用 `vision_system.clear_face_database()` 并删除磁盘上的特征文件
2. 人脸学习程序的 `delete_face` 方法改为只删除应用层 JSON 记录

**教训**：不在范例代码中的 API 不要随意调用，可能破坏内部状态。

### 5. 灯效选择弹窗布局优化

**问题**：弹窗中文字与背景框重叠，点击容易误触相邻项。

**解决**：

- 弹窗尺寸从 560×680 逐步加大到 780×1010
- 行高从 42 加大到 68，背景框之间留 8px 间隙
- 文字垂直居中（根据背景框高度自动计算）
- 重置/关闭按钮完全在弹窗面板内

### 6. 调试提示词经验

| 轮次 | 提示词要点 | 效果 |
| --- | --- | --- |
| 初始开发 | 「调用学习过的人脸，识别到不同的人脸，RGB灯带显示不同的效果」 | AI 正确参考了范例 5.10 的识别流程 |
| 摄像头掉线 | 「识别几次后就会出问题。摄像头40无法读取图像」 | AI 分析出 V4L2 缓冲区冲突根因 |
| 画面卡顿 | 「摄像头没有掉，但画面一卡一卡效果很不好」 | AI 优化采集线程间隔 |
| 自定义灯效 | 「加载出已学习人脸后，可以通过点击选择人脸和灯效的对应关系」 | AI 增加灯效选择弹窗 |
| 识别失败排查 | 「一直显示正在启动识别，好搭AI派提示检测到2个人脸」 | AI 定位到 face_id=None 问题 |
| 日志导出 | 「输出内容很多，我没办法复制。能不能把输出写入文档」 | AI 增加 logs/ 目录日志文件（程序名+日期命名） |
| 弹窗优化 | 「上下宽度增大一点，文字、背景框不会重叠」 | AI 加大弹窗尺寸和行高 |

**通用经验**：

1. 摄像头问题直接贴错误日志，AI 能快速定位
2. 界面布局问题用「文字重叠」「点击误触」等具体描述
3. 排查复杂问题时要求 AI 把日志写入文件，方便导出分析

## 十一、可配置项

在 `人脸识别灯效.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `CAMERA_W` / `CAMERA_H` | 摄像头采集分辨率 | 640 × 480 |
| `CAM_DISP_W` / `CAM_DISP_H` | 摄像头画面显示尺寸 | 880 × 660 |
| `LED_PIN` | RGB 灯带引脚 | `GPIO_IO_01` |
| `LED_COUNT` | 灯珠数量 | 11 |
| `FACE_DATA_FILE` | 人脸记录文件 | `face_database/face_records.json` |
| `FACE_EFFECT_MAP_FILE` | 灯效映射文件 | `face_effect_map.json` |
| `_LOG_DIR` | 日志文件夹 | `logs` |
| `_LOG_FILE` | 日志文件名（含程序名+日期） | `logs/人脸识别灯效_YYYYMMDD.log` |
| `FONT_PATH` | 常规字体路径 | `/home/cxdz/jupyter/assets/PingFang_Regular.ttf` |
| `FONT_BOLD_PATH` | 粗体字体路径 | `/home/cxdz/jupyter/assets/PingFang_Bold.ttf` |

## 十二、常见问题与故障排查

### 1. 一直显示「正在启动识别」

| 可能原因 | 排查与解决 |
| --- | --- |
| 视觉系统数据库损坏 | 查看 `logs/人脸识别灯效_YYYYMMDD.log` 中 `get_face_id` 是否为 None，若是则运行 `清空人脸数据库.py` |
| 人脸记录文件为空 | 检查 `face_database/face_records.json` 是否存在且非空，先运行人脸学习程序 |
| 摄像头未打开 | 查看 `logs/人脸识别灯效_YYYYMMDD.log` 中 `视觉系统摄像头已打开` 是否输出 |
| 后台检测未启动 | 查看 `logs/人脸识别灯效_YYYYMMDD.log` 中 `人脸识别后台检测已启动` 是否输出 |

### 2. 摄像头掉线

| 可能原因 | 排查与解决 |
| --- | --- |
| V4L2 缓冲区冲突 | 采集线程间隔已设为 0.15s，若仍掉线可增大间隔 |
| 摄像头被占用 | 确保没有其他程序正在使用摄像头 |
| USB 连接松动 | 检查 USB 摄像头连接 |

### 3. 识别不到已学习的人脸

| 可能原因 | 排查与解决 |
| --- | --- |
| 光线不足 | 增加环境光线，避免逆光 |
| 人脸角度过大 | 正对摄像头，避免侧脸 |
| 置信度过低 | 重新运行人脸学习程序，多角度学习几次 |
| 数据库脏数据 | 运行 `清空人脸数据库.py`，重新学习 |

### 4. 灯效不亮

| 可能原因 | 排查与解决 |
| --- | --- |
| 扩展板未启动 | 检查好搭AI派右下角开关是否拨到左侧 |
| 灯带接线错误 | 确认 WS2812 灯带接在 IO1，且上拉扩展模块已连接 |
| 灯珠数量不匹配 | 确认 `LED_COUNT` 配置与实际灯珠数一致 |

## 十三、版本历史

| 版本 | 日期 | 主要变更 |
| --- | --- | --- |
| 1.0 | 2026-08 | 初始版本：人脸识别、10种灯效、界面显示 |
| 1.1 | 2026-08 | 采集线程固定 0.15s 间隔，解决摄像头掉线 |
| 1.2 | 2026-08 | 帧有效性验证，跳过损坏帧 |
| 1.3 | 2026-08 | 增加自定义灯效映射（face_effect_map.json + 选择弹窗） |
| 1.4 | 2026-08 | 界面改为 1920×1080，布局优化 |
| 1.5 | 2026-08 | 退出按钮移至右上角，列表行高加大 |
| 1.6 | 2026-08 | face_id=None 处理，增加调试日志 |
| 1.7 | 2026-08 | 灯效选择弹窗加大（780×1010），文字垂直居中 |
| 1.8 | 2026-08 | 日志输出改为 logs/ 目录+程序名+日期命名，追加模式，块缓冲（参考物体学习项目） |

## 十四、扩展方向

- 增加语音播报识别到的人脸姓名
- 支持多人脸同时识别，分别触发不同灯效
- 增加 MQTT 上报识别结果，接入智能家居
- 增加识别记录日志，统计人脸出现频次
- 支持动态添加新人脸（运行中调用 FaceLearner 学习）
- 增加灯效自定义编辑器（调整颜色、速度、亮度）
