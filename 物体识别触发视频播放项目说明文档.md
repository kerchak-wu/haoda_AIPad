# 物体识别触发视频播放项目说明文档

基于已登记物体库的实时识别程序：摄像头持续识别物体，识别到指定物体后自动触发播放对应视频，播放结束或手动停止后返回识别界面继续工作。
复用 `物体学习.py` 的 `ObjectEngine` 引擎与 UI 工具，无需重复实现物体检测/识别逻辑。
参考：`物体学习与识别项目文档.md`（物体学习与识别程序）。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 物体识别 | 复用 `物体学习.py` 的 `ObjectEngine`（OpenCV ORB 特征 + BFMatcher 匹配 + RANSAC 验证） |
| 摄像头采集 | OpenCV `VideoCapture`（外接摄像头，设备节点路径方式打开） |
| 视频画面 | OpenCV `VideoCapture` 解码 + pygame Surface 显示（主窗口内集成） |
| 视频音频 | `ffplay`（ffmpeg）后台无窗口播放，未安装则静音 |
| 界面 | `pygame`，1920×1080 |

> 不调用 `pygame.init()`，仅初始化 `display` + `font`，避免 `pygame.mixer` 占用音频设备导致 ffplay 无法播放声音。

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 实时识别 | 摄像头画面嵌入主窗口（1100×520），实时框出物体并标注名称/对应视频 |
| 触发播放 | 识别到 card/book/cup（或 卡片/书本/杯子）后自动播放 videos/ 下对应视频 |
| 视频播放 | 视频画面在主窗口等比缩放显示，音频由 ffplay 后台同步播放 |
| 手动停止 | 视频播放界面底部「停止播放 返回识别」大按钮，点击返回识别 |
| 防误触发 | 连续 3 帧命中才触发；切回识别后 1.2 秒冷却期不触发 |
| 防重复触发 | 同一物体播放后需离开画面再出现才会再次触发 |
| 缓冲区清空 | 视频播放暂停采集，切回时清空摄像头驱动缓冲区积压旧帧，回到实时画面 |
| 后台处理 | 采集线程与识别线程分离，主循环零阻塞 |

## 三、物体→视频映射

| 识别名称（拼音/中文） | 视频文件 |
| --- | --- |
| card / 卡片 | videos/1.mp4 |
| book / 书本 | videos/2.mp4 |
| cup / 杯子 | videos/3.mp4 |

匹配时不区分大小写。映射表在 `物体识别播放视频.py` 顶部 `OBJECT_VIDEO_MAP` 中配置，可按需增删。

## 四、项目结构

```
haoda_AIPad/
├── 物体学习.py                          # 物体引擎（被本程序 import 复用）
├── 物体识别播放视频.py                   # 本程序主入口
├── 物体识别触发视频播放项目说明文档.md    # 本文档
├── 物体学习与识别项目文档.md             # 物体学习与识别程序文档
├── images/
│   └── 1.jpg                            # 背景图（1920×1080）
├── object_data/                         # 物体数据（物体学习.py 生成）
│   ├── object_db.json
│   ├── descriptors/
│   ├── keypoints/
│   └── images/
└── videos/                              # 视频文件目录
    ├── 1.mp4
    ├── 2.mp4
    └── 3.mp4
```

## 五、环境要求

- Python 3.8 及以上
- 依赖：
  - `opencv-python` 或 `opencv-contrib-python`（提供 `cv2` 与 ORB；无需 face 模块）
  - `pygame-ce` 或 `pygame`（界面）
  - `numpy`（数组处理）
  - `ffmpeg`（含 `ffplay`，用于视频音频播放；未安装则视频静音播放）
- 外接 USB 摄像头

## 六、前置准备与运行

### 1. 登记物体

先运行 `物体学习.py`，分别登记名称为 `card`、`book`、`cup`（或中文 卡片/书本/杯子）的三个物体：

```bash
python3 物体学习.py
```

> 学习时将物体放在画面中央引导框内，确保物体有丰富的纹理细节（图案、文字、标签等），纯色无纹理物体识别效果会下降。

### 2. 准备视频文件

将 `1.mp4`、`2.mp4`、`3.mp4` 放入 `videos/` 目录：

```bash
mkdir -p videos
# 把视频文件放入 videos/
```

### 3. 安装依赖

```bash
pip install opencv-python pygame-ce numpy
# 如需视频声音，安装 ffmpeg（含 ffplay）
```

### 4. 运行

```bash
python3 物体识别播放视频.py
```

启动时终端会打印：已登记物体列表、视频文件存在/缺失状态、ffplay 是否可用，便于排查。

> 摄像头固定探测 `/dev/video41`（优先）和 `/dev/video40`。若节点编号不同，请修改 `物体识别播放视频.py` 中 `open_camera()` 内的 `(41, 40)` 元组。

## 七、界面布局

界面采用 1920×1080，分两种模式：

### 识别模式

```
y= 12   物体识别触发视频播放（标题，居中）          退出程序（右上角 240×90 大按钮）
y= 70   实时识别中 — 将已登记物体对准摄像头，自动播放对应视频
y=110   ┌──────────────────────────────────────┐
        │   摄像头画面 1100×520                │
        │   （实时框出物体并标注 名称->视频）    │
        └──────────────────────────────────────┘
y=660   状态显示（等待识别 / 确认中 / 刚播放过...）
y=730   触发规则：card -> 1.mp4    book -> 2.mp4    cup -> 3.mp4
y=1050  ESC 退出  |  视频播放中按 ESC 返回识别
```

### 视频播放模式

```
y=  0   顶部状态栏（正在播放：xxx.mp4 （识别到 xxx））
y= 12   物体识别触发视频播放（标题）               退出程序（右上角大按钮）
y=110   ┌──────────────────────────────────────┐
        │                                      │
        │   视频画面（等比缩放居中显示）         │
        │                                      │
        └──────────────────────────────────────┘
y=960   ┌──────────────────────────────────────┐
y=1060  │   停止播放 返回识别（440×100 大按钮） │
        └──────────────────────────────────────┘
```

## 八、工作流程

### 触发播放流程

```
后台识别线程持续识别最新帧（约 4fps，ORB 特征匹配）
   │
   ├─ 识别到 card/book/cup 且非刚播放过的物体
   │       │
   │       ├─ 连续命中 CONFIRM_FRAMES(3) 帧 ──> 触发播放
   │       └─ 否则显示「识别到 xxx，确认中 (n/3)...」
   │
   ├─ 切回识别后的 1.2 秒冷却期内 ──> 不触发，显示「返回识别模式，稳定中...」
   │
   └─ 刚播放过的物体仍在画面 ──> 不触发，显示「xxx 刚播放过，离开画面后可重新触发」
```

### 视频播放流程

```
触发播放 ──> 打开视频文件（OpenCV）+ 启动 ffplay 播放音频
   │         切换到视频模式，暂停摄像头采集
   │
   ├─ 主循环逐帧读取视频帧并显示
   │
   ├─ 视频播放结束 ──> 自动 stop_video()
   ├─ 点击「停止播放 返回识别」──> stop_video()
   └─ 按 ESC ──> stop_video()

stop_video():
   ├─ 停止 ffplay 音频进程
   ├─ 释放视频 VideoCapture
   ├─ 切回识别模式，设置 1.2 秒冷却
   └─ 摄像头采集线程恢复时清空驱动缓冲区积压旧帧
```

## 九、技术说明

### 1. 不初始化 pygame.mixer
`pygame.init()` 会初始化音频模块并占用音频设备，导致 ffplay 无法获取音频输出。本程序只调用 `pygame.display.init()` + `pygame.font.init()`，把音频设备留给 ffplay。

### 2. ffplay 命令
ffplay 输入文件用位置参数（非 `-i`，那是 ffmpeg 语法）：
```
ffplay -nodisp -autoexit -loglevel quiet <视频文件>
```

### 3. 摄像头节点路径方式打开
直接传设备节点路径字符串 `"/dev/video41"` 给 `cv2.VideoCapture`，而非整数索引。整数会被当作 V4L2 设备列表索引，系统设备列表通常只有 32 项，索引 41 会越界报错。路径方式精确打开对应节点，不受设备列表大小限制。

### 4. 视频播放时暂停采集
视频播放期间摄像头采集线程暂停，避免无意义的 V4L2 ioctl 调用触发 `bad file descriptor` 警告，同时节省 CPU。

### 5. 缓冲区清空（关键）
USB 摄像头有驱动级缓冲区。视频播放暂停采集期间，摄像头仍持续往内核缓冲区推帧。若不清空，恢复采集后 `cap.read()` 会先读出积压的旧帧（暂停几秒可能积压几百帧），表现为「画面接着暂停前的旧画面继续播放」。

恢复采集时用时间窗口持续 `cap.grab()`（只取不解码，速度快）排空 0.4 秒，确保无论暂停多久都能清空所有积压旧帧，回到实时画面。

### 6. 采集循环不 sleep
`cap.read()` 在 V4L2 阻塞模式下本身会等待下一帧到来才返回，不需要额外 sleep。加 sleep 反而让采集跟不上摄像头推帧速度，导致内核 buffer 积压、画面滞后。

### 7. 识别频率限制
ORB 物体识别是 CPU 密集操作（单次约 120ms），识别频率限制为约 4fps（`MIN_INTERVAL=0.25s`），避免占满 CPU 导致画面卡顿。对物体识别来说 4fps 已足够流畅（物体移动不会太快）。

### 8. 帧 surface 缓存
主循环每帧调用 `cvframe_to_surface`，但摄像头是 30fps 而主循环可能跑 30+fps，很多次调用是同一帧。用帧对象 id 做缓存，同一帧不重复做 cvtColor + transpose + make_surface + scale，减少 CPU 开销。

### 9. 防误触发三重机制
| 机制 | 说明 |
| --- | --- |
| 连续帧确认 | 需连续 3 帧命中同一物体才触发，避免单帧误识别 |
| 冷却期 | 切回识别后 1.2 秒不触发，等缓冲区清空 + 画面稳定 |
| 抑制重复 | 同一物体播放后需离开画面再出现才再次触发 |

### 10. 按钮绘制顺序
按钮统一在最后绘制，确保不被视频画面、状态栏等遮挡。按钮尺寸放大（退出 240×90，停止 440×100）便于触摸/点击触发。

## 十、可配置项

在 `物体识别播放视频.py` 顶部配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `VIDEO_DIR` | 视频文件目录 | `videos` |
| `OBJECT_VIDEO_MAP` | 物体名称→视频文件映射 | card→1.mp4 等 |
| 摄像头节点 | 在 `open_camera()` 的 `(41, 40)` 元组中修改 | 41 优先，40 兜底 |
| `CAMERA_W` / `CAMERA_H` | 摄像头采集分辨率 | 1280 × 720 |
| `CAM_DISP_W` / `CAM_DISP_H` | 摄像头画面显示尺寸 | 1100 × 520 |
| `CONFIRM_FRAMES` | 连续命中多少帧才触发 | 3 |

> 识别相关的参数（`GOOD_MATCH_THRESHOLD`、`MIN_INLIERS` 等）在 `物体学习.py` 中配置，详见 `物体学习与识别项目文档.md`。

## 十一、快捷键与交互

| 按键/操作 | 功能 |
| --- | --- |
| `ESC` | 视频模式：返回识别；识别模式：退出程序 |
| 鼠标点击「停止播放 返回识别」 | 视频模式返回识别 |
| 鼠标点击「退出程序」 | 退出程序 |
| 将物体对准摄像头 | 自动识别并触发对应视频播放 |
