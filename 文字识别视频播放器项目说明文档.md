# 文字识别视频播放器项目说明文档

## 一、项目概述

基于好搭AI派开发的外接USB摄像头文字识别视频播放系统。通过 USB 摄像头实时进行 OCR 文字识别，识别到指定关键词后自动播放对应视频，支持暂停/继续/停止控制。采用纯 cv2 独占模式 + 双线程分离架构，实现高帧率摄像头画面采集和流畅的 UI 交互。

**程序文件**：`文字识别视频播放器.py`

**核心功能**：

- 文字识别：USB 摄像头实时采集画面，每 2 秒 OCR 识别一次画面中的文字
- 关键词匹配：识别到"好搭智眼"播放 videos/1.mp4，识别到"芦丁鸡孵化箱"播放 videos/2.mp4，识别到"信息科技实验板"播放 videos/3.mp4
- 视频播放控制：暂停/继续按钮（暂停键变继续键）、停止按钮（返回识别界面）
- 音频同步播放：ffmpeg 提取视频音频轨道为临时 WAV 文件，通过 pygame.mixer 播放
- 科技感 UI：深空蓝黑底色 + 青色主色调 + 发光按钮 + 面板式布局 + 状态指示灯

---

## 二、硬件接线

| 硬件 | 接口 | 说明 |
| ---- | ---- | ---- |
| USB 摄像头 | ESP32 扩展板 USB 口 | 实时画面采集 + OCR 文字识别 |

**依赖文件**：`videos/1.mp4`、`videos/2.mp4`、`videos/3.mp4`（含音频轨的视频文件）

**好搭AI派开关**：右下角开关拨到左侧（外设模式）

---

## 三、关键技术经验（重要）

本项目开发过程中踩坑并解决了多个稳定性问题，以下经验对未来类似项目开发具有参考价值。

### 1. 摄像头卡顿：纯 cv2 独占模式 + 双线程分离架构

**问题现象**：摄像头画面帧率低（6-7fps），画面明显卡顿。

**根因**：采用好搭AI派视觉系统全托管模式，该模式帧率较低，不适合实时预览场景。

**解决方案**：重构为纯 cv2 独占模式 + 双线程分离架构：

```python
# 采集线程：独立循环 cap.read()，30fps
class CameraCapture:
    def _capture_loop(self):
        cap = cv2.VideoCapture()
        cap.open(self.device)
        cap.set(cv2.CAP_PROP_FOURCC, cv2.VideoWriter.fourcc('M', 'J', 'P', 'G'))
        cap.set(cv2.CAP_PROP_FPS, 30)
        while self.running:
            ret, f = cap.read()
            if ret:
                with self._lock:
                    self.frame = f

# 主线程：取最新帧，不阻塞采集
def get_frame(self):
    with self._lock:
        return self.frame.copy() if self.frame is not None else None
```

**关键要点**：

- 视觉系统全托管模式帧率过低（6-7fps），不适用于实时预览场景
- 纯 cv2 独占模式可达到 30fps，且能避免与视频播放的 `cv2.VideoCapture` 资源冲突
- 使用 `threading.Lock` 保护共享帧，采集线程只写、主线程只读

---

### 2. 视频无声：ffmpeg 提取音频到临时 WAV

**问题现象**：播放视频时没有声音。

**解决过程**：

第一次尝试：直接用 `pygame.mixer.music.load(video_path)` 加载 mp4 文件——失败，`pygame.mixer` 不支持直接加载 mp4 容器中的音频。

第二次尝试：使用 `ffmpeg` 提取音频到临时 WAV 文件，再用 `pygame.mixer.music.load()` 播放 WAV——成功。

```python
def _play_video(self, video_path):
    # ... 打开视频 ...

    # 用 ffmpeg 提取音频到临时 WAV 文件
    tmp = tempfile.NamedTemporaryFile(suffix='.wav', delete=False)
    self.audio_temp_path = tmp.name
    tmp.close()
    subprocess.run(
        ['ffmpeg', '-i', video_path, '-vn',
         '-acodec', 'pcm_s16le', '-ar', '44100', '-ac', '2',
         '-y', self.audio_temp_path],
        capture_output=True, timeout=15
    )
    pygame.mixer.init()
    pygame.mixer.music.load(self.audio_temp_path)
    pygame.mixer.music.play()
```

**关键要点**：

- `pygame.mixer.music.load()` 不支持 mp4 容器中的音频，但原生支持 WAV 格式
- ffmpeg 兼容所有视频格式，`-acodec pcm_s16le -ar 44100 -ac 2` 解码为标准 CD 音质 WAV
- 播放结束后用 `os.unlink()` 删除临时文件，避免 /tmp 目录堆积
- 暂停/继续/停止用 `pygame.mixer.music.pause()` / `unpause()` / `stop()` 同步控制

---

### 3. 视频切换失败：纯 cv2 模式避免资源冲突

**问题现象**：识别到文字后无法切换到视频播放界面。

**根因**：视觉系统 API 与 `cv2.VideoCapture` 同时使用摄像头设备，存在资源冲突。

**解决方案**：采用纯 cv2 独占模式，使用独立的 `cv2.VideoCapture` 播放视频，避免资源冲突：

```python
# 视频播放使用独立的 VideoCapture 实例
def _play_video(self, video_path):
    self._stop_video()  # 先停止上一轮播放，释放旧资源
    cap = cv2.VideoCapture()
    cap.open(video_path)
    self.video_cap = cap
```

**关键要点**：

- 摄像头采集和视频播放使用完全独立的 `cv2.VideoCapture` 实例
- 每次播放前先调用 `_stop_video()` 释放上一轮资源

---

### 4. 暂停后视频画面变黑

**问题现象**：点击暂停按钮后，视频画面变成黑色。

**根因**：暂停时绘制 UI 传入 `frame=None`，跳过帧绘制逻辑。

**解决方案**：保存最后一帧到 `self.last_video_frame`，暂停时读取并显示该帧：

```python
def _update_video(self):
    if self.is_paused:
        self._draw_video_ui()  # 不带参数，内部使用 last_video_frame
        return
    ret, frame = self.video_cap.read()
    if not ret:
        self._stop_video()
        return
    self.last_video_frame = frame  # 保存最后一帧
    self._draw_video_ui(frame)

def _draw_video_ui(self, frame=None):
    if frame is None:
        frame = self.last_video_frame  # 暂停时显示缓存的最后一帧
    # ... 绘制帧 ...
```

---

### 5. 暂停后视频仍在播放（音频继续）

**问题现象**：点击暂停后，画面虽然停住了，但视频流仍在前进（音频继续播放）。

**根因**：`_update_video` 中未判断暂停状态，仍调用 `cap.read()` 推进视频流。

**解决方案**：暂停时跳过 `cap.read()`，同时 `pygame.mixer.music.pause()` 停止音频输出：

```python
def _toggle_pause(self):
    self.is_paused = not self.is_paused
    if self.is_paused:
        pygame.mixer.music.pause()
    else:
        pygame.mixer.music.unpause()
```

---

### 6. 摄像头设备检测失败

**问题现象**：程序启动后显示"摄像头未就绪"。

**原因**：摄像头设备路径硬编码，与实际设备路径不匹配；启动时采集线程未产出第一帧。

**解决方案**：

```python
CAMERA_DEVICES = ["/dev/video41", "/dev/video40", "/dev/video0", "/dev/video1", "/dev/video2"]

def _detect_camera():
    for dev in CAMERA_DEVICES:
        cap = cv2.VideoCapture()
        try:
            if cap.open(dev):
                ret, _ = cap.read()
                if ret:
                    cap.release()
                    return dev
        except Exception:
            pass
        cap.release()
    return None
```

---

### 7. 退出按钮与状态文字重叠

**问题现象**：右上角退出按钮遮挡了"CAMERA OK"状态文字。

**原因**：状态文字绘制位置与退出按钮区域重叠。

**解决方案**：将状态指示点从 `SCREEN_WIDTH - 190` 左移到 `SCREEN_WIDTH - 350`，文字从 `SCREEN_WIDTH - 170` 左移到 `SCREEN_WIDTH - 330`，完全避开退出按钮区域。

---

## 四、线程架构总览

本项目共 2 个线程，职责明确：

| 线程 | 职责 | 关键锁 | 注意事项 |
| ---- | ---- | ---- | ---- |
| 主线程 | Pygame 事件循环 + UI 渲染 + OCR 识别 + 视频播放 | 无 | 30fps 循环，OCR 每 2 秒执行一次 |
| 摄像头采集 | 独立循环 `cap.read()` 刷新帧（30fps） | `_lock`（帧数据） | 主线程只读 `get_frame()` 缓存 |

**锁的使用**：

- `_lock`：保护摄像头帧数据（采集线程写，主线程读）
- 采集线程锁内只做帧赋值，持锁时间极短（`self.frame = f`）
- 主线程锁内只做 `frame.copy()`，持锁时间极短

---

## 五、状态机与界面流转

```md
识别模式（OCR + 摄像头预览 + 关键词匹配）
  │ 识别到关键词
  ▼
视频播放模式（播放视频 + 音频同步）
  │
  ├─ 点暂停 ──▶ 暂停状态（画面定格 + 音频暂停）
  │                │ 点继续 ──▶ 视频播放模式
  │
  └─ 点停止 ──▶ 识别模式
```

---

## 六、开发提示词参考

本项目开发过程中使用的关键提示词（按实际开发顺序整理），供未来类似项目开发参考。每条提示词附带了实际效果与对应的文档章节，便于复现。

### 提示词 1：项目初始需求（整体功能定义）

> 好搭AI派外接摄像头（接在ESP32扩展板）文字识别，识别到"好搭智眼"播放视频1.mp4，识别到"芦丁鸡孵化箱"播放视频2.mp4，识别到"信息科技实验板"播放视频3.mp4。视频播放的时候要有暂停按钮和停止按钮，点暂停后视频播放暂停，暂停键变成继续键，点继续键继续播放，点停止按钮返回识别界面。

**实际效果**：完成了项目整体框架与全部基础功能，包括文字识别、关键词匹配、视频播放控制。

**对应文档**：第一章「项目概述」

---

### 提示词 2：添加摄像头预览

> 文字识别界面要看到摄像头画面，界面右上角放一个退出程序按钮。

**实际效果**：添加了摄像头实时画面预览区域和右上角退出按钮。

**对应文档**：第一章「核心功能」

---

### 提示词 3：性能优化与切换修复

> 程序启动慢；摄像头画面卡，识别到文字后切不到播放界面；实时画面不用充满整个界面。

**实际效果**：添加启动加载界面（分阶段提示）；重构为纯 cv2 独占模式 + 双线程分离架构解决卡顿；使用独立 VideoCapture 播放视频避免资源冲突；缩小预览区域避免全屏。

**对应文档**：第三章第 1 条「摄像头卡顿」、第三章第 3 条「视频切换失败」

---

### 提示词 4：音频支持与科技感界面

> 1.视频没有声音；2.视频文件名称中没有video；3.界面要科技感一点。

**实际效果**：添加了音频播放功能；视频文件名改为 1.mp4/2.mp4/3.mp4；界面改为深空蓝黑 + 青色科技感风格。

**对应文档**：第三章第 2 条「视频无声」

---

### 提示词 5：音频方案调整

> 只有3个视频文件，文件名分别为1.mp4，2.mp4，3.mp4，不要改文件名，没有音频文件。

**实际效果**：视频文件名保持 1.mp4/2.mp4/3.mp4，移除外部音频文件方案。

**对应文档**：第三章第 2 条「视频无声」

---

### 提示词 6：恢复音频需求

> 视频文件是有声音的，播放视频文件的时候要把声音也放出来。

**实际效果**：恢复音频播放功能。

**对应文档**：第三章第 2 条「视频无声」

---

### 提示词 7：音频方案升级（ffmpeg 提取）

> 视频的声音没有播出来，换别的办法，要把声音播出来。

**实际效果**：从 `pygame.mixer.music.load(video_path)` 改为 ffmpeg 提取音频到临时 WAV 再播放，彻底解决音频播放问题。

**对应文档**：第三章第 2 条「视频无声」

---

### 提示词 8：UI 细节修复

> 文字识别界面的退出按钮和下面的字重叠了，看不到下面的字；视频播出界面，暂停后画面要停在视频的画面不要黑的，视频播出界面不要退出按钮。

**实际效果**：状态文字左移避开退出按钮；保存最后一帧解决暂停黑屏；视频播放界面移除退出按钮。

**对应文档**：第三章第 4 条「暂停后画面变黑」、第三章第 7 条「退出按钮与状态文字重叠」

---

### 提示词 9：最终修复

> 识别界面，camera……和退出按钮还是有重叠，还要往左移一点；视频播出界面，点暂停后视频还在播。

**实际效果**：状态文字再左移 70px；暂停时跳过 `cap.read()` 且 `pygame.mixer.music.pause()` 暂停音频。

**对应文档**：第三章第 5 条「暂停后视频仍在播放」

---

## 七、参考项目

| 参考项目 | 借鉴内容 |
| ---- | ---- |
| 《视觉系统摄像头调用参考方案.md》 | 摄像头三种架构模式对比（纯cv2独占/混合/全托管）、Rockchip 兼容性补丁（LIBGL_ALWAYS_SOFTWARE） |
| 《好搭AI派范例代码.md》 | 文字识别 API 调用方式、pygame 界面代码结构、视频播放实现参考 |
| 《好搭AI派学习手册.md》 | 好搭AI派基础操作、外设接口说明、扩展模块使用方法 |

---

## 八、配置参数

```python
SCREEN_WIDTH, SCREEN_HEIGHT = 1920, 1080      # 屏幕分辨率
OCR_INTERVAL = 2.0                              # OCR 识别间隔(秒)
CAMERA_DEVICES = ["/dev/video41", "/dev/video40",
                  "/dev/video0", "/dev/video1", "/dev/video2"]  # 摄像头设备路径优先级
PREVIEW_W, PREVIEW_H = 960, 700                 # 摄像头预览区域尺寸
PREVIEW_X, PREVIEW_Y = 60, 140                  # 预览区域位置

# 关键词 → 视频文件映射
VIDEO_MAP = {
    "好搭智眼":     "videos/1.mp4",
    "芦丁鸡孵化箱": "videos/2.mp4",
    "信息科技实验板": "videos/3.mp4",
}
```

---

## 九、常见问题排查

| 问题现象 | 可能原因 | 解决方法 |
| ---- | ---- | ---- |
| 摄像头画面卡顿 | 使用视觉系统全托管模式帧率低 | 改用纯 cv2 独占模式 + 双线程分离架构 |
| 识别到文字后无法切到视频播放 | 视觉系统与 cv2.VideoCapture 资源冲突 | 全部使用纯 cv2 模式，避免资源冲突 |
| 播放视频时没有声音 | pygame.mixer 不支持直接加载 mp4 音频 | 用 ffmpeg 提取音频到临时 WAV 再播放 |
| 暂停后画面变黑 | 暂停时传入 frame=None 跳过帧绘制 | 保存最后一帧，暂停时显示缓存帧 |
| 暂停后视频仍在播 | 未跳过 cap.read() 推进视频流 | 暂停时跳过 cap.read() + 同步暂停音频 |
| 程序启动显示"摄像头未就绪" | 摄像头设备路径不匹配 / 启动时未产出第一帧 | 自动检测设备列表 + wait_ready() 等待第一帧 |
| 退出按钮遮挡状态文字 | 两者绘制位置重叠 | 将状态文字左移到 SCREEN_WIDTH - 330 |

---

*文档版本：1.0*
*最后更新：2026年8月8日*
*适用环境：好搭AI派 / Ubuntu 20.04+ / Python 3.8+*