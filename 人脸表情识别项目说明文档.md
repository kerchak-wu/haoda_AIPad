# 人脸表情识别项目说明文档

基于好搭AI派（ESP32 + pygame）开发的人脸表情识别程序：USB 外接摄像头实时采集画面，后台线程定期调用百度智能云 `AipFace` 人脸识别 API，识别画面中人脸的情绪（emotion 字段），界面实时显示当前主情绪、概率、人脸数量和历史统计。支持七种基本情绪：愤怒、厌恶、恐惧、高兴、悲伤、惊讶、中性。

> 本项目用 **百度智能云在线 API** 做表情识别，与本地项目「人脸识别灯效」（用 `camera_vision_system_v3` 本地视觉系统做人脸身份识别）是两套不同的识别体系。本项目不需要预先学习人脸，只需要把画面送进百度 API 即可。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 表情识别 | 百度智能云 `AipFace.detect(image, 'BASE64', {face_field:'emotion'})` |
| 摄像头采集 | cv2 `VideoCapture`（严格参照范例 7.opencv 与 人脸学习.py 的探测逻辑） |
| 图像编码 | cv2 `imencode('.jpg')` + `base64.b64encode`（API 入参格式） |
| 界面显示 | `pygame`，1920×1080，背景图 `images/1.jpg` + 半透明面板 |
| 多线程 | 摄像头采集线程（快速 `read()` 覆盖旧帧）+ 表情识别线程（固定间隔调用 API）+ 主循环线程（30 FPS 绘制） |
| 调用频率控制 | `API_INTERVAL = 1.5s`，避免超过百度 QPS 限制（默认 2QPS） |
| 调试日志 | `_TeeStdout` 类把所有 print 输出同步写入 `debug_log.txt` |

> 关键设计：**表情识别在线进行，画面采集在本地进行**。摄像头线程只负责 `cap.read()` 覆盖最新帧，识别线程定期取最新帧送进百度 API。两条线程完全独立，通过 `threading.Lock` 保护的 `raw_frame` 共享数据。

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 实时画面采集 | 后台线程快速读帧，主循环 30 FPS 显示 |
| 七种情绪识别 | angry / disgust / fear / happy / sad / surprise / neutral |
| 多人脸支持 | 单次调用可识别最多 5 张人脸，分别返回各自的表情 |
| 概率显示 | 主情绪显示概率百分比，人脸列表显示每张人脸的表情概率 |
| 历史统计 | 七种情绪 + 无人脸 + 异常，共 9 项累计统计，带进度条 |
| API 状态显示 | 调用次数、调用间隔、上次调用时间、当前状态（识别中/空闲/异常/已暂停） |
| 错误提示 | API 返回错误码时在界面右下角显示具体错误信息 |
| 暂停/继续 | 空格键切换自动识别开关，不退出程序 |
| 退出方式 | 界面右上角「退出程序」按钮 / ESC 键 / 窗口关闭按钮 |
| 资源清理 | 退出时关闭摄像头、关闭日志文件、释放 pygame |
| 调试日志 | 所有 print 输出同步写入 `debug_log.txt`，方便导出排查 |

## 三、百度智能云配置

### 1. 凭证配置

在 [人脸表情识别.py](file:///d:/笔记本同步/好搭AI派实验/人脸表情识别.py) 顶部配置区填写：

```python
# 请在此处填写您自己的百度智能云凭证
# 申请地址：https://console.bce.baidu.com/ai/#/ai/face/index
APP_ID = '在此填写 APP_ID'
API_KEY = '在此填写 API_KEY'
SECRET_KEY = '在此填写 SECRET_KEY'
```

### 2. API 调用

```python
from aip import AipFace
client = AipFace(APP_ID, API_KEY, SECRET_KEY)
result = client.detect(image_base64, 'BASE64', {
    'face_field': 'emotion',
    'max_face_num': 5,
})
```

### 3. 返回示例

```json
{
  "error_code": 0,
  "error_msg": "SUCCESS",
  "result": {
    "face_num": 1,
    "face_list": [
      {"emotion": {"type": "happy", "probability": 0.97}}
    ]
  }
}
```

### 4. 免费调用额度

> **重要**：百度智能云人脸识别 API 的免费调用额度为**每月 1000 次**（按调用次数计费，非按人脸数）。

- 默认 QPS 限制：**2 次/秒**
- 本程序默认调用间隔 `API_INTERVAL = 1.5s`，约 40 次/分钟，理论上一小时可消耗 2400 次额度
- **建议**：连续长时间运行时务必关注额度消耗，可在程序运行中按空格暂停识别以节省额度
- 额度用完后 API 会返回错误码（如 `QPS request limit reached` 或额度耗尽相关错误），界面右下角会显示具体错误

### 5. emotion vs expression 字段说明

百度智能云 `face_field` 支持两种表情相关字段，本项目选用 `emotion`：

| 字段 | 支持类型 | 适用场景 |
| --- | --- | --- |
| `expression` | none(不笑) / smile(微笑) / laugh(大笑) | 仅判断笑的程度 |
| `emotion` | angry / disgust / fear / happy / sad / surprise / neutral | 完整七种基本情绪识别 |

`emotion` 基于 FACS（面部动作编码系统），可识别悲伤、惊恐等更丰富的情绪，更适合做表情分析。

## 四、情绪映射表

| type | 中文 | 颜色 RGB | emoji |
| --- | --- | --- | --- |
| `angry` | 愤怒 | (220, 60, 60) 红 | 눈_눈 |
| `disgust` | 厌恶 | (120, 80, 40) 棕绿 | ×_× |
| `fear` | 恐惧 | (140, 80, 180) 紫 | (°△°) |
| `happy` | 高兴 | (255, 200, 50) 金黄 | ◕‿◕ |
| `sad` | 悲伤 | (70, 130, 220) 蓝 | (╥_╥) |
| `surprise` | 惊讶 | (255, 140, 0) 橙 | (O_O)! |
| `neutral` | 中性 | (120, 130, 150) 灰蓝 | •́ ₒ •̀ |
| `unknown` | 未知 | (160, 160, 160) 灰 | ?_? |

映射定义在 [人脸表情识别.py](file:///d:/笔记本同步/好搭AI派实验/人脸表情识别.py) 顶部 `EXPRESSION_NAMES` / `EXPRESSION_COLORS` / `EXPRESSION_EMOJI` 中。

## 五、界面布局

界面按左摄像头右信息面板布局（1920×1080）：

```markdown
y=  0   ┌──────────── 顶部标题区 (高130) ────────────┐
y= 20   │           人脸表情识别（标题 64pt）  [退出程序]│
y= 85   │  USB摄像头采集 · 百度智能云AipFace · emotion  │
y=130   └────────────────────────────────────────────┘
y=150   ┌──────────────────┐  ┌─────────────────────────┐
        │ 摄像头画面        │  │  当前表情                │
        │ ● 已连接          │  │  ┌──────────────────┐  │
        │                   │  │  │   高兴 (96pt)     │  │
        │   实时摄像头画面   │  │  │   ◕‿◕            │  │
        │   (880 × 660)     │  │  │      概率 97%     │  │
        │                   │  │  └──────────────────┘  │
        │                   │  │  检测到人脸：1 张       │
        │                   │  │  人脸1：高兴 (97%)     │
        │ ● 识别中...       │  │  ─────────────────    │
        │           640 × 480│  │  历史统计              │
        │                   │  │  ■ 愤怒    0  □□□□    │
        │                   │  │  ■ 高兴   12  ■■■■    │
        │                   │  │  ■ 悲伤    1  □□□□    │
        │                   │  │  ...                   │
y=970   └──────────────────┘  │  ─────────────────    │
y=970   ┌──────────── 底部栏 (高110) ────────────────┐  │  API 调用              │
y=1025  │ ESC 退出 · 空格暂停 · 自动识别中           │  │  调用：20 次 · 1.5s · │
y=1080  └────────────────────────────────────────────┘  └─────────────────────────┘
```

> 退出按钮固定置于标题栏右上角（`WIDTH-280, 30, 240×70`），不设在右下角。

## 六、项目结构

```markdown
haoda_AIPad/
├── 人脸表情识别.py                  # 本程序主入口
├── 人脸表情识别项目说明文档.md      # 本文档
├── debug_log.txt                    # 调试日志（本程序生成）
├── images/
│   └── 1.jpg                        # 背景图（1920×1080，可选）
├── 好搭AI派学习手册.md               # 硬件 API 参考
├── 好搭AI派范例代码.md               # 范例代码参考
├── 人脸学习.py                      # 摄像头探测逻辑参考项目
└── 手势控制RGB灯带.py                # 摄像头探测逻辑参考项目
```

## 七、硬件接线

| 接口 | 设备 | 说明 |
| --- | --- | --- |
| USB | 外接摄像头 | `/dev/video41` 或 `/dev/video40`，由 cv2 `VideoCapture` 管理 |
| ESP32 | 好搭AI派扩展板 | `board.start()` 初始化（程序仅初始化不实际驱动外设） |

> 注意：好搭AI派右下角开关需拨到左侧以启用外设接口。

## 八、环境要求

- 好搭AI派平台（ESP32 + Linux）
- 依赖：
  - `pygame` —— 界面显示（平台自带）
  - `opencv-python`（提供 `cv2`）—— 摄像头采集 + 图像编码（**需用户自行安装**）
  - `numpy` —— 数组处理（平台自带）
  - `baidu-aip` —— 百度智能云 SDK（**需用户自行安装**）
  - `ESP32` —— 硬件控制（平台自带）
- 字体文件（位于 `/home/cxdz/jupyter/assets/`）：
  - `PingFang_Regular.ttf` —— 常规文字
  - `PingFang_Bold.ttf` —— 粗体（标题、按钮、大字情绪）
- 外接 USB 摄像头
- 百度智能云账号（已开通人脸识别服务）

### 安装第三方库

```bash
pip install baidu-aip opencv-python
```

## 九、运行

```bash
python3 人脸表情识别.py
```

启动后：

1. 程序初始化 ESP32 扩展板和百度智能云 AipFace 客户端
2. 探测 USB 摄像头（`/dev/video41` → `/dev/video40`）
3. 启动摄像头采集线程和表情识别线程
4. 每隔 1.5s 自动调用一次百度 API 识别画面表情
5. 界面实时显示当前表情、概率、人脸数量和历史统计
6. 按 ESC 或右上角按钮退出，按空格暂停/继续识别

> 前置条件：网络连接正常，百度智能云账号已开通人脸识别服务且免费额度未耗尽。

## 十、技术说明

### 1. 摄像头探测与采集（严格参照范例与本地项目）

摄像头探测复用 [人脸学习.py](file:///d:/笔记本同步/好搭AI派实验/人脸学习.py) 的 `_try_open_camera` 函数：

- 探测顺序：`/dev/video41` → `/dev/video40`
- MJPG 编码 + `CAP_PROP_BUFFERSIZE=1`（避免缓冲区积压旧帧）
- SIGALRM 超时保护（4 秒）
- 雪花帧检测（标准差 + 缩放后比值）

采集线程只负责 `cap.read()` 覆盖最新帧，不处理：

```python
def camera_capture_loop(self):
    while self.cam_thread_running:
        ok, frame = self.cap.read()
        if ok and frame is not None:
            with self.frame_lock:
                self.raw_frame = frame
```

### 2. 百度 API 调用线程

独立线程定期取最新帧调用百度 API：

```python
def expression_detect_loop(self):
    while self.detect_thread_running:
        frame = self.get_current_frame()
        self.detect_busy = True
        self.detector.detect(frame)
        self.detect_busy = False
        wait = API_INTERVAL - dt
        if wait > 0:
            time.sleep(wait)
```

**关键设计**：
- 识别线程与采集线程完全独立，互不阻塞
- `API_INTERVAL = 1.5s` 保证不超过 2QPS 限制
- `detect_busy` 标志位避免并发调用 API
- 网络异常或 API 错误不会导致线程退出，只记录错误并继续下次调用

### 3. 图像编码

百度 API 接受 BASE64 编码的图片，cv2 帧需先转 jpg 再 base64：

```python
ok, encoded = cv2.imencode('.jpg', frame, [cv2.IMWRITE_JPEG_QUALITY, 85])
image_base64 = base64.b64encode(encoded).decode()
```

JPEG 质量设为 85（平衡识别准确率和上传大小）。

### 4. 多线程架构

| 线程 | 职责 | 阻塞点 |
| --- | --- | --- |
| 主循环 | 事件处理 + 界面绘制（30 FPS） | 无阻塞 |
| 采集线程 | `cap.read()` 快速覆盖最新帧 | V4L2 阻塞读帧（通常 < 33ms） |
| 识别线程 | 调用百度 API（每 1.5s 一次） | 网络请求阻塞（通常 200-1000ms） |

`threading.Lock` 保护 `raw_frame` 读写。

### 5. 错误处理

百度 API 可能返回的错误：

| 错误码 | 含义 | 处理 |
| --- | --- | --- |
| 0 | 成功 | 正常显示 |
| 222202 | 图片中没有人脸 | 显示「未识别到人脸」 |
| 222210 | 图片格式错误 | 计入异常统计 |
| 17 / 19 | QPS 超限 | 自动等待下次调用 |
| 14 / 18 | 额度不足 | 界面显示错误，建议充值或暂停 |

错误信息显示在界面右下角和 `debug_log.txt` 中。

### 6. 调试日志

程序启动时打开 `debug_log.txt`，通过 `_TeeStdout` 类把所有 `print` 输出同时写入控制台和文件：

```python
DEBUG_LOG_FILE = 'debug_log.txt'
_debug_log_fp = open(DEBUG_LOG_FILE, 'w', encoding='utf-8', buffering=1)
```

调试日志包含：
- ESP32 初始化信息
- 百度客户端创建信息
- 摄像头探测过程（每个设备编号的尝试结果）
- 每次识别的结果或错误
- 异常堆栈

### 7. 帧缩放方式

使用 `pygame.transform.scale`（非 `smoothscale`）进行帧缩放，降低 CPU 开销（项目经验：smoothscale 对摄像头帧缩放有显著 CPU 开销）。

### 8. 界面半透明面板

```python
panel = pygame.Surface(self.info_rect.size, pygame.SRCALPHA)
pygame.draw.rect(panel, (*PANEL_COLOR, 170), panel.get_rect(), border_radius=18)
```

## 十一、可配置项

在 [人脸表情识别.py](file:///d:/笔记本同步/好搭AI派实验/人脸表情识别.py) 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `APP_ID` | 百度智能云 APP_ID | `在此填写 APP_ID` |
| `API_KEY` | 百度智能云 API Key | `在此填写 API_KEY` |
| `SECRET_KEY` | 百度智能云 Secret Key | `在此填写 SECRET_KEY` |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `CAMERA_W` / `CAMERA_H` | 摄像头采集分辨率 | 640 × 480 |
| `CAM_DISP_W` / `CAM_DISP_H` | 摄像头画面显示尺寸 | 880 × 660 |
| `API_INTERVAL` | 百度 API 调用间隔（秒） | 1.5 |
| `DEBUG_LOG_FILE` | 调试日志文件 | `debug_log.txt` |
| `FONT_PATH` | 常规字体路径 | `/home/cxdz/jupyter/assets/PingFang_Regular.ttf` |
| `FONT_BOLD_PATH` | 粗体字体路径 | `/home/cxdz/jupyter/assets/PingFang_Bold.ttf` |

## 十二、常见问题与故障排查

### 1. 摄像头打开失败

| 可能原因 | 排查与解决 |
| --- | --- |
| 摄像头被占用 | 确保没有其他程序正在使用摄像头（包括人脸学习、人脸识别灯效程序） |
| 设备编号不对 | 查看 `debug_log.txt` 中探测日志，确认 `/dev/video40` 和 `/dev/video41` 是否存在 |
| USB 连接松动 | 重新插拔 USB 摄像头 |
| 与视觉系统冲突 | 本项目不使用 `camera_vision_system_v3`，不会冲突 |

### 2. API 调用失败

| 可能原因 | 排查与解决 |
| --- | --- |
| 网络异常 | 检查好搭AI派网络连接 |
| 凭证错误 | 检查 `APP_ID` / `API_KEY` / `SECRET_KEY` 是否正确 |
| QPS 超限 | 增大 `API_INTERVAL`（如改为 2.0s） |
| 额度耗尽 | 查看百度智能云控制台，免费额度每月 1000 次 |
| 服务未开通 | 在百度智能云控制台开通「人脸识别」服务 |

### 3. 表情识别不准

| 可能原因 | 排查与解决 |
| --- | --- |
| 光线不足 | 增加环境光线，避免逆光 |
| 人脸角度过大 | 正对摄像头，避免侧脸 |
| 人脸过小 | 靠近摄像头，让人脸占画面较大比例 |
| 表情不明显 | 百度 emotion 对中性、高兴识别较好，对厌恶等识别相对弱 |

### 4. 界面卡顿

| 可能原因 | 排查与解决 |
| --- | --- |
| 网络请求阻塞主线程 | 识别线程已独立，检查 `detect_busy` 是否长时间为 True |
| 摄像头帧率低 | 检查 USB 摄像头连接，降低 `CAMERA_W` / `CAMERA_H` |
| CPU 占用高 | 已使用 `pygame.transform.scale`（非 smoothscale） |

## 十三、调试经验总结

### 1. emotion vs expression 字段选择

**问题**：初版使用 `face_field='expression'` 只能识别「不笑/微笑/大笑」三种结果，无法识别悲伤、惊恐等情绪。

**根因**：百度 `expression` 字段只判断笑的程度，不是完整情绪识别。

**解决**：改用 `face_field='emotion'`，支持七种基本情绪（angry/disgust/fear/happy/sad/surprise/neutral）。`emotion` 基于 FACS 面部动作编码系统，覆盖范围更广。

### 2. 统计区布局溢出

**问题**：从 5 项统计（不笑/微笑/大笑/无人脸/异常）扩展到 9 项统计（7 种情绪 + 无人脸 + 异常）后，右侧信息面板内容溢出底部。

**解决**：
- 表情卡片高度 180→150
- 统计行高 38→30
- 颜色方块 22→18
- API 调用信息合并为一行，仅异常时显示错误行

### 3. QPS 限制与额度控制

**经验**：
- 百度默认 QPS = 2，间隔 1.5s 比较安全（留 0.5s 余量）
- 免费额度每月 1000 次，按 1.5s 间隔连续运行约 25 分钟即可耗尽
- 长时间运行建议按空格暂停，仅在需要时识别
- 额度耗尽 API 会返回错误码，界面右下角显示具体错误

### 4. 多线程架构稳定性

**经验**：
- 采集线程只读帧不处理，避免 V4L2 缓冲区积压
- 识别线程独立，网络阻塞不影响画面流畅度
- `threading.Lock` 只保护 `raw_frame` 读写，不跨线程调用 pygame
- 异常不抛出，记录后继续下次循环

## 十四、版本历史

| 版本 | 日期 | 主要变更 |
| --- | --- | --- |
| 1.0 | 2026-08-05 | 初始版本：百度 AipFace + cv2 摄像头 + Pygame 界面 |
| 1.1 | 2026-08-05 | 改用 emotion 字段，支持七种基本情绪识别 |
| 1.1 | 2026-08-05 | 统计区紧凑化布局（9 项统计），避免溢出 |

## 十五、扩展方向

- 增加语音播报识别到的情绪名称
- 增加情绪变化曲线图，可视化情绪随时间变化
- 接入 RGB 灯带：不同情绪显示不同灯效（参考人脸识别灯效项目）
- 增加 MQTT 上报情绪数据，接入数据看板
- 增加情绪触发条件：如检测到「悲伤」时播放安慰语音
- 支持截图保存：识别到特定情绪时自动保存画面
- 增加本地缓存：网络断开时显示上次识别结果，不显示空白
