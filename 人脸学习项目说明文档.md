# 人脸学习项目说明文档

基于好搭AI派（ESP32 + pygame）开发的人脸学习程序：USB外接摄像头实时采集画面，输入姓名标签后将当前画面注册为人脸样本，界面显示摄像头画面、学习状态和已学习人脸列表。人脸记录持久化到 JSON 文件，重启后自动加载，支持删除记录。提供 `FaceLearner` 类供其他程序导入调用。

> 本项目经过多轮调试迭代：API 方法名纠正、重复注册补录机制、识别测试功能探索与移除、软键盘尝试与回退。完整调试过程见第七章。

## 一、技术方案

| 模块 | 实现 |
| --- | --- |
| 人脸学习 | `camera_vision_system_v3` 的 `learn_new_face(frame=...)` 传帧方式（参照范例 5.9） |
| 摄像头采集 | OpenCV `VideoCapture`（设备节点路径方式打开，MJPG 编码） |
| 界面显示 | `pygame`，1920×1080，背景图 `images/1.jpg` + 半透明面板 |
| 多线程 | 后台采集线程 + 主循环线程，采集线程只做 `cap.read()` 覆盖最新帧 |
| 数据持久化 | JSON 文件（`face_database/face_records.json`），学习/删除时自动保存，启动时自动加载 |
| 硬件控制 | `ESP32` 扩展板初始化（`board.start()`） |

> 关键设计：**不调用 `vision_system.open_camera()`**，因为 `learn_new_face(frame=...)` 接受外部传入的帧，不需要视觉系统自行打开摄像头。cv2 摄像头用于界面显示和采集帧，视觉系统仅用于人脸学习算法。

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 实时画面 | 摄像头画面嵌入主窗口（800×600），30 FPS 实时显示 |
| 人脸学习 | 输入姓名 → 点击「学习人脸」→ 当前帧注册为人脸样本 |
| 姓名标签 | 应用层维护姓名与 face_id 的映射（视觉系统不存储姓名） |
| 数据持久化 | 学习记录保存到 JSON 文件，重启后自动加载 |
| 重复注册处理 | 视觉系统返回「已注册」时自动补录到本地列表 |
| 删除记录 | 列表项右侧删除按钮，仅删除应用层 JSON 记录（**不调用视觉系统 delete_face**，避免破坏识别模型） |
| 模块调用 | 提供 `FaceLearner` 类，其他程序可 `from 人脸学习 import FaceLearner` 导入 |
| 退出方式 | 界面右上角「退出程序」按钮 / ESC 键 / 窗口关闭按钮 |
| 调试日志 | 所有 print 输出同步写入 `logs/人脸学习_YYYYMMDD.log`，方便导出排查 |

## 三、界面布局

界面按左摄像头右信息面板布局（1920×1080）：

```markdown
y=  0   ┌──────────── 顶部标题区 (高120) ────────────┐
y= 20   │           人脸学习（标题 56pt）       [退出程序]│
y= 80   │  USB摄像头采集画面 · 输入姓名后点击学习 · 可删除记录 │
y=120   └────────────────────────────────────────────┘
y=140   ┌──────────────────┐  ┌─────────────────────────┐
        │ 摄像头画面面板     │  │  右侧信息面板             │
        │ ● 已连接           │  │  姓名标签                │
        │                    │  │  [请输入姓名...]         │
        │                    │  │  [    学习人脸    ]      │
        │   实时摄像头画面    │  │  学习成功：xxx（ID:1）   │
        │   (800 × 600)      │  │  ───────────────────    │
        │                    │  │  已学习人脸（4）         │
        │           640 × 480│  │  1. zhao      ID:1 [删除]│
        │                    │  │  2. qian      ID:2 [删除]│
        │                    │  │  3. sun       ID:3 [删除]│
        │                    │  │  4. lee       ID:4 [删除]│
y=980   └──────────────────┘  └─────────────────────────┘
y=980   ┌──────────── 底部栏 (高100) ────────────────┐
y=1035  │ ESC 退出 · 输入姓名后回车或点击「学习人脸」  │
y=1080  └────────────────────────────────────────────┘
```

> 退出按钮固定置于标题栏右上角，不设在右下角。

## 四、项目结构

```markdown
haoda_AIPad/
├── 人脸学习.py                      # 主程序
├── 人脸学习项目说明文档.md          # 本文档
├── face_database/                   # V3 SDK 人脸特征库 + 应用层记录（自动生成）
│   └── face_records.json            # 人脸记录持久化文件（JSON 格式）
├── logs/                            # 日志文件夹（自动创建）
│   └── 人脸学习_YYYYMMDD.log        # 调试日志（按程序名+日期命名，追加模式）
├── images/
│   └── 1.jpg                        # 背景图（1920×1080）
├── 好搭AI派学习手册.md               # 硬件 API 参考
├── 好搭AI派范例代码.md               # 范例代码参考
├── 手势控制RGB灯带.py                # 摄像头探测与多线程参考项目
└── 手势控制RGB灯带项目说明文档.md     # 参考项目文档
```

## 五、硬件接线

| 接口 | 设备 | 说明 |
| --- | --- | --- |
| USB | 外接摄像头 | 系统识别为 `/dev/video40` 或 `/dev/video41` |
| ESP32 | 好搭AI派扩展板 | `board.start()` 初始化 |

> 注意：好搭AI派右下角开关需拨到左侧以启用外设接口。

## 六、环境要求

- 好搭AI派平台（ESP32 + Linux）
- 依赖（均平台自带）：
  - `pygame` —— 界面显示
  - `opencv-python`（提供 `cv2`）—— 摄像头采集与图像格式转换
  - `numpy` —— 数组处理
  - `ESP32` —— 硬件控制
  - `camera_vision_system_v3` —— 人脸学习算法（好搭AI派自带）
- 字体文件（位于 `/home/cxdz/jupyter/assets/`）：
  - `PingFang_Regular.ttf` —— 常规文字
  - `PingFang_Bold.ttf` —— 粗体（标题、按钮）
- 外接 USB 摄像头

## 七、运行

```bash
python3 人脸学习.py
```

启动后：

1. 程序自动初始化 ESP32 扩展板和视觉系统
2. 自动探测 `/dev/video41` 和 `/dev/video40`，打开第一个可用的摄像头
3. 在右侧输入框输入姓名，对准摄像头后点击「学习人脸」
4. 学习成功后姓名出现在列表中，可点击「删除」移除记录
5. 点击界面右上角「退出程序」按钮或按 ESC 退出

## 八、模块调用（FaceLearner 类）

`FaceLearner` 类封装了视觉系统的人脸学习功能，可供其他程序导入调用：

```python
from 人脸学习 import FaceLearner

# 初始化
learner = FaceLearner()

# 从摄像头帧学习
frame = cap.read()  # cv2 摄像头读取的 BGR 帧
learner.learn_from_frame(frame, '张三')

# 从图片文件学习
learner.learn_from_image('images/face.jpg', '李四')

# 查询已学习人脸
faces = learner.get_learned_faces()  # [(name, face_info), ...]
count = learner.get_face_count()
name = learner.find_name_by_id(1)  # 根据 face_id 查姓名

# 删除记录
learner.delete_face(0)  # 按索引删除

# 释放资源
learner.close()
```

### FaceLearner API

| 方法 | 说明 |
| --- | --- |
| `FaceLearner(width=1280, height=720)` | 构造函数，初始化视觉系统并加载持久化记录 |
| `learn_from_frame(frame, name)` | 从 BGR 帧学习人脸，返回 face_info 字典 |
| `learn_from_image(image_path, name)` | 从图片文件学习人脸 |
| `get_learned_faces()` | 获取已学习人脸列表 `[(name, face_info), ...]` |
| `get_face_count()` | 获取已学习人脸数量 |
| `find_name_by_id(face_id)` | 根据 face_id 查找姓名 |
| `delete_face(index)` | 按索引删除人脸记录（仅删除应用层 JSON，不调用视觉系统 delete_face） |
| `close()` | 释放视觉系统资源 |

## 九、技术说明

### 1. 视觉系统初始化（严格参照范例 5.9）

```python
self.vision_system = create_vision_system_v3(
    camera_id=-1, width=width, height=height,
    enable_basic=False, enable_advanced=False
)
self.vision_system.detection_config.enable_face_recognition = True
self.vision_system._init_detectors()
```

**关键点**：不调用 `open_camera()`。`learn_new_face(frame=...)` 接受外部传入的帧，不需要视觉系统自行打开摄像头。初始化顺序：视觉系统初始化 → 摄像头打开（OCR 初始化在摄像头之前的经验同样适用）。

### 2. 人脸学习 API

严格参照范例代码 5.9 的 `learn_new_face(frame=...)` 调用方式：

```python
face_info = self.vision_system.learn_new_face(frame=frame)
```

返回值 `face_info` 为字典，包含：
- `success`：是否新增成功（True=新增，False=已注册过）
- `face_id`：系统分配的人脸 ID（数字）
- `message`：描述信息

**注意**：`learn_new_face` 不接受 `class_name` 参数，系统自动分配 face_id（数字），姓名与 ID 的映射由 `FaceLearner` 类在应用层维护。

### 3. 重复注册补录机制

视觉系统内部持久化存储已学习的人脸数据（重启不丢失），但应用层 JSON 文件可能丢失。`learn_from_frame` 处理三种情况：

| `learn_new_face` 返回 | 本地列表状态 | 处理 |
| --- | --- | --- |
| `success:True`（新增） | — | 入库 + 保存 JSON |
| `success:False`（已注册），本地无此 face_id | 缺失 | 补录到本地 + 保存 JSON |
| `success:False`（已注册），本地已有此 face_id | 已存在 | 更新姓名（如有变化） |

### 4. 摄像头打开逻辑

复用 `手势控制RGB灯带.py` 的探测方案：

- **设备节点路径方式打开**：`cv2.VideoCapture('/dev/video41')`，避免整数索引越界
- **固定探测顺序**：先 `/dev/video41` 再 `/dev/video40`
- **MJPG 编码**：`cap.set(cv2.CAP_PROP_FOURCC, cv2.VideoWriter_fourcc(*'MJPG'))`
- **SIGALRM 超时**：探测时设置 4 秒超时，打断卡在 `select()` 的 V4L2 设备
- **雪花检测**：通过帧的 std 和缩放后 std 的比值，过滤噪声帧

### 5. 多线程架构

| 线程 | 职责 | 阻塞点 |
| --- | --- | --- |
| 主循环 | 事件处理 + 界面绘制 + 学习操作 | 学习时短暂阻塞（可接受） |
| 采集线程 | `cap.read()` 持续覆盖最新帧 | V4L2 阻塞读帧 |

采集线程总是覆盖 `self.raw_frame`，保证显示的永远是最新画面。`threading.Lock` 保护帧数据的读写。

### 6. 帧缩放方式

使用 `pygame.transform.scale`（非 `smoothscale`）进行帧缩放，降低 CPU 开销（项目经验：smoothscale 对摄像头帧缩放有显著 CPU 开销）。

### 7. 数据持久化

- 文件格式：JSON（`face_database/face_records.json`）
- 存储内容：`[{"name": "张三", "face_info": {"success": true, "face_id": 1, "message": "..."}}, ...]`
- 保存时机：学习成功、补录、删除时自动保存
- 加载时机：程序启动时自动加载
- 线程安全：`threading.RLock` 保护读写

### 8. 调试日志

程序启动时在 `logs/` 目录下创建日志文件（文件名含程序名+日期，如 `人脸学习_20260805.log`），通过 `_TeeStdout` 类将所有 `print` 输出同时写入控制台和文件：

```python
_LOG_DIR = 'logs'
_LOG_FILE = _os.path.join(_LOG_DIR, '人脸学习_%s.log' % _datetime.datetime.now().strftime('%Y%m%d'))
_debug_log_fp = open(_LOG_FILE, 'a', encoding='utf-8', buffering=-1)
```

日志输出特点（参考物体学习项目）：
1. 日志统一存到 `logs/` 文件夹，避免散落在项目根目录
2. 文件名含程序名+日期，不会覆盖上次的日志
3. 用追加模式 `'a'`，同一程序多次运行追加到当天日志
4. 用块缓冲 `buffering=-1`，避免后台线程高频写日志阻塞主循环
5. 每次运行写入分隔标记 `======== 时间 运行开始 ========`，区分不同次运行

调试日志包含：
- 视觉系统初始化信息
- 摄像头探测和打开过程
- 人脸学习结果（成功/失败/已注册补录）
- 人脸记录加载和保存
- 删除记录操作

## 十、调试经验总结

### 1. API 方法名纠正

| 错误方法名 | 正确方法名 | 说明 |
| --- | --- | --- |
| `add_face_recognition_class(frame=..., class_name=...)` | `learn_new_face(frame=...)` | `CameraVisionSystemV3` 无 `add_face_recognition_class` 方法 |
| `add_face_recognition_sample()` | `learn_new_face()` | 无参版本从摄像头采集 |

**教训**：好搭AI派的视觉系统 API 名称与直觉不同，必须严格参照范例代码确认方法名和参数。

### 2. 识别测试功能探索

尝试实现识别测试功能（检测已学习人脸能否被识别），遇到以下问题：

| 尝试方案 | 问题 |
| --- | --- |
| `open_camera()` + `start_background_detection(show_preview=True)` | 弹出 OpenCV 窗口无画面 |
| `show_preview=False` + pygame 界面显示状态 | `open_camera()` 与 cv2 摄像头抢设备 |
| 视觉系统统一管理摄像头（不用 cv2） | `open_camera()` 打不开 `/dev/video40` |

**结论**：视觉系统的 `open_camera()` 在好搭AI派上无法正常打开摄像头设备。识别测试功能需要 `open_camera()` + `start_background_detection()`，与 cv2 摄像头冲突，最终移除该功能。

**教训**：`learn_new_face(frame=...)` 传帧方式不需要 `open_camera()`，是更可靠的学习方式。识别功能依赖 `open_camera()`，在当前设备环境下不可用。

### 3. 软键盘尝试与回退

尝试通过 `subprocess` 调用 Linux 系统软键盘（onboard / matchbox-keyboard），但好搭AI派系统未安装这些软件，软键盘无法调出。最终回退删除所有软键盘相关代码，仅使用 USB 外接键盘输入。

### 4. 界面尺寸调试策略

> **重要经验**：调试阶段使用 1280×800 分辨率，方便在开发环境中查看和调试；功能全部完成后再改为 1920×1080，展示效果更好。

| 阶段 | 分辨率 | 说明 |
| --- | --- | --- |
| 调试阶段 | 1280×800 | 窗口较小，方便在桌面环境中同时查看代码和运行界面 |
| 功能完成 | 1920×1080 | 全屏展示，画面更大，列表可显示更多记录 |

修改时只需调整程序顶部 `WIDTH, HEIGHT` 常量和 `CAM_DISP_W, CAM_DISP_H` 摄像头显示尺寸，布局会自动适配。

### 5. delete_face 破坏识别模型（重要教训）

曾尝试在删除记录时调用 `vision_system.delete_face(face_id)` 同步删除视觉系统数据，结果**破坏了整个识别模型**：

| 现象 | 说明 |
| --- | --- |
| 删除后重新学习，ID 不从 0 开始 | 视觉系统数据持久化，delete_face 破坏了内部数据库结构 |
| 识别程序检测到人脸但 `get_face_id()` 返回 None | 检测正常，识别失效 |
| `get_face_database_info()` 显示注册数与实际不符 | 数据库残留脏数据 |
| 重启设备无法恢复 | 模型损坏持久化，重启不修复 |

**解决方案**：运行 `清空人脸数据库.py` 脚本，调用 `vision_system.clear_face_database()` 并删除磁盘上的 `face_features.npy`、`face_ids.npy` 特征文件，然后重新学习。

**最终修改**：`delete_face` 方法只删除应用层 JSON 记录，不再调用视觉系统的删除接口。视觉系统内部数据保留不影响使用——重新学习同一人会返回旧 face_id 并触发补录机制。

**教训**：
1. 好搭AI派范例代码中**没有删除人脸的功能**，说明 `delete_face` 不被官方支持
2. 不在范例代码中的 API 不要随意调用，可能破坏内部状态
3. 应用层删除 + 视觉系统保留是更安全的设计

### 6. 调试提示词经验

| 轮次 | 提示词要点 | 效果 |
| --- | --- | --- |
| 初始开发 | 「涉及硬件部分严格参考范例代码，摄像头相关部分严格参考范例代码」 | AI 正确参考了范例 5.9 的 `learn_new_face` |
| API 纠错 | 直接贴出错误信息 `AttributeError: 'CameraVisionSystemV3' object has no attribute 'add_face_recognition_class'` | AI 快速定位到正确 API |
| 识别测试 | 描述具体错误日志（V4L2 ioctl 错误） | AI 分析出设备冲突根因 |
| 功能移除 | 「不要识别测试这个功能了。只保留人脸学习和数据持久化、删除功能」 | 明确告知保留什么、移除什么 |
| 软键盘回退 | 「软键盘没有出来。回退一个版本，删除调出软键盘相关程序」 | 果断回退，避免浪费时间 |

**通用经验**：
1. 硬件 API 问题直接贴错误日志，AI 能快速定位
2. 功能不可行时果断移除，不要反复尝试
3. 每次只增删一个功能，便于定位问题

## 十一、可配置项

在 `人脸学习.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `CAMERA_W` / `CAMERA_H` | 摄像头采集分辨率 | 640 × 480 |
| `CAM_DISP_W` / `CAM_DISP_H` | 摄像头画面显示尺寸 | 800 × 600 |
| `FACE_DATA_FILE` | 人脸记录持久化文件 | `face_database/face_records.json` |
| `_LOG_DIR` | 日志文件夹 | `logs` |
| `_LOG_FILE` | 日志文件名（含程序名+日期） | `logs/人脸学习_YYYYMMDD.log` |
| `FONT_PATH` | 常规字体路径 | `/home/cxdz/jupyter/assets/PingFang_Regular.ttf` |
| `FONT_BOLD_PATH` | 粗体字体路径 | `/home/cxdz/jupyter/assets/PingFang_Bold.ttf` |

## 十二、常见问题与故障排查

### 1. 摄像头打不开

| 可能原因 | 排查与解决 |
| --- | --- |
| 设备节点不对 | 查看控制台输出 `探测 /dev/video41` 和 `/dev/video40`，确认设备存在 |
| 摄像头被占用 | 确保没有其他程序正在使用摄像头 |
| MJPG 不支持 | 部分摄像头不支持 MJPG，程序会回退到默认格式 |
| SIGALRM 不可用 | 非主线程环境下无法使用 SIGALRM 超时，程序会跳过超时保护 |

### 2. 人脸学习失败

| 可能原因 | 排查与解决 |
| --- | --- |
| 画面中无人脸 | 确保摄像头画面中有清晰的人脸 |
| 光线不足 | 增加环境光线，避免逆光 |
| API 方法不存在 | 确认 `learn_new_face(frame=...)` 方法名正确（不是 `add_face_recognition_class`） |
| 视觉系统未初始化 | 查看控制台是否输出 `face_recognition 算法已启用` |

### 3. 重启后看不到已学习的人脸

| 可能原因 | 排查与解决 |
| --- | --- |
| JSON 文件丢失 | 检查 `face_database/face_records.json` 是否存在 |
| JSON 文件损坏 | 删除 JSON 文件，重新学习时会自动补录视觉系统中已有的数据 |
| 视觉系统数据丢失 | 视觉系统内部数据通常持久化存储，重启不丢失 |

### 4. 重复学习同一人脸提示「已注册」

这是正常行为。视觉系统检测到已注册的人脸会返回 `success:False`，程序会自动补录到本地列表（如果本地缺失）或更新姓名（如果姓名不同）。

## 十三、版本历史

| 版本 | 日期 | 主要变更 |
| --- | --- | --- |
| 1.0 | 2026-08 | 初始版本：人脸学习、界面显示、FaceLearner 类 |
| 1.1 | 2026-08 | 修正 API 方法名（`add_face_recognition_class` → `learn_new_face`） |
| 1.2 | 2026-08 | 增加数据持久化（JSON）、删除功能、识别测试 |
| 1.3 | 2026-08 | 增加重复注册补录机制 |
| 1.4 | 2026-08 | 识别测试功能探索（open_camera 冲突），最终移除 |
| 1.5 | 2026-08 | 软键盘尝试与回退，界面改为 1920×1080 |
| 1.6 | 2026-08 | 退出按钮从右下角移至右上角标题栏 |
| 1.7 | 2026-08 | delete_face 不再调用视觉系统删除接口（避免破坏识别模型），仅删除应用层 JSON |
| 1.8 | 2026-08 | 日志输出改为 logs/ 目录+程序名+日期命名，追加模式，块缓冲（参考物体学习项目） |

## 十四、扩展方向

- 其他程序通过 `from 人脸学习 import FaceLearner` 导入，实现人脸学习 + 自定义业务逻辑
- 增加人脸识别功能（需解决 `open_camera()` 设备冲突问题）
- 支持从图片批量导入人脸
- 增加学习质量检测（人脸清晰度、角度、光照评估）
- 支持人脸分组管理（按部门/班级等分类）
