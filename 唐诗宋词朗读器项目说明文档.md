# 唐诗宋词朗读器项目文档

基于好搭AI派（ESP32 + pygame）开发的唐诗宋词交互朗读系统，列表展示 5 首经典唐诗与 5 首经典宋词，支持鼠标点击查看全文、TTS 语音朗读、按键切换、RGB 灯带意境灯效、舵机角度指示，并可通过「换一批」按钮切换不同批次的诗词，界面固定窗口 1920×1080。

## 一、功能特性

| 模块 | 功能 |
| --- | --- |
| 诗词列表 | 5 首唐诗 + 5 首宋词，按类型分组显示，带「唐/宋」彩色徽标 |
| 全文显示 | 鼠标点击列表名称，右侧面板显示该诗词全文（楷体显示） |
| 语音朗读 | 点击「朗读」按钮或按下 IO2 按键，TTS 合成并播放全文 |
| 朗读停止 | 点击「停止」按钮即时中断朗读与灯效（未朗读时按钮自动禁用） |
| 批次换一批 | 点击「换一批」按钮切换到下一批 5 唐 5 宋，循环遍历（朗读中禁用） |
| 按键切换 | IO1 按键依次切换下一首诗词，循环遍历 |
| RGB 灯效 | IO3 接 4 颗 WS2812 灯带，朗读时根据诗词意境显示 10 种炫酷灯效 |
| 舵机指示 | IO8 接灰色舵机，不同诗词对应不同角度（0–180° 均分 10 首） |
| 音频缓存 | TTS 合成结果缓存到 `recordings/poem_X.wav`，二次朗读免合成 |
| 界面美化 | `images/1.jpg` 全屏背景，半透明面板，文字自动防溢出 |
| 安全退出 | 「退出」按钮或 ESC 键，自动关闭灯效、熄灭灯珠、清理资源 |

## 二、快捷键与操作

### 鼠标操作

| 操作 | 功能 |
| --- | --- |
| 点击列表项 | 选中该诗词，舵机转到对应角度 |
| 点击「朗读」按钮 | 朗读当前选中诗词 |
| 点击「停止」按钮 | 中断当前朗读与灯效 |
| 点击「换一批」按钮 | 切换到下一批 5 唐 5 宋诗词（朗读中禁用） |
| 点击「退出」按钮 | 退出程序 |

### 硬件按键

| 按键 | 功能 |
| --- | --- |
| IO1 按键 | 切换到下一首诗词（上升沿触发） |
| IO2 按键 | 朗读当前选中诗词（上升沿触发） |

### 键盘快捷键

| 按键 | 功能 |
| --- | --- |
| `↑` / `←` | 切换到上一首 |
| `↓` / `→` | 切换到下一首 |
| `空格` | 朗读当前诗词 |
| `ESC` | 退出程序 |

## 三、硬件接线

| 外设 | 接口 | SDK 调用 | 说明 |
| --- | --- | --- | --- |
| 单按键模块（切换） | IO1（GPIO_IO_01） | `board.digitalRead(GPIO_IO_01)` | 读取数字值 0/1 |
| 单按键模块（朗读） | IO2（GPIO_IO_02） | `board.digitalRead(GPIO_IO_02)` | 读取数字值 0/1 |
| WS2812 RGB 灯带 | IO3（GPIO_IO_03） | `board.ws2812Init` / `board.ws2812Write` | 4 颗灯珠 |
| 灰色舵机 | IO8（GPIO_IO_08） | `board.servo(GPIO_IO_08, angle)` | angle 范围 0–180° |

> **注意**：好搭AI派右下角开关需拨到左侧以启用外设接口。

## 四、诗词数据与灯效映射

程序内置多批诗词（每批 5 唐 + 5 宋），通过「换一批」按钮在批次间循环切换。下表列出默认的第 1 批：

### 第 1 批（默认）

| 序号 | 类型 | 标题 | 作者 | 灯效名称 | 灯效描述 |
| --- | --- | --- | --- | --- | --- |
| 0 | 唐 | 静夜思 | 李白 | moonlight | 冷月白蓝呼吸 |
| 1 | 唐 | 春晓 | 孟浩然 | spring | 粉绿春色流动 |
| 2 | 唐 | 登鹳雀楼 | 王之涣 | sunset | 夕阳红橙追逐 |
| 3 | 唐 | 望庐山瀑布 | 李白 | waterfall | 白蓝流光 |
| 4 | 唐 | 江雪 | 柳宗元 | snow | 冷白闪烁 |
| 5 | 宋 | 水调歌头 | 苏轼 | moonFull | 银月呼吸 |
| 6 | 宋 | 念奴娇·赤壁怀古 | 苏轼 | heroic | 红金火焰跳 |
| 7 | 宋 | 苏幕遮 | 范仲淹 | autumn | 秋色黄粉绿轮转 |
| 8 | 宋 | 声声慢 | 李清照 | sorrow | 暗紫低吟 |
| 9 | 宋 | 满江红 | 岳飞 | passion | 血红金光涌 |

### 第 2 批（点击「换一批」后切换）

| 序号 | 类型 | 标题 | 作者 | 灯效名称 | 灯效描述 |
| --- | --- | --- | --- | --- | --- |
| 0 | 唐 | 咏鹅 | 骆宾王 | spring | 粉绿春色流动 |
| 1 | 唐 | 悯农 | 李绅 | sunset | 夕阳红橙追逐 |
| 2 | 唐 | 枫桥夜泊 | 张继 | moonlight | 冷月白蓝呼吸 |
| 3 | 唐 | 出塞 | 王昌龄 | heroic | 红金火焰跳 |
| 4 | 唐 | 早发白帝城 | 李白 | waterfall | 白蓝流光 |
| 5 | 宋 | 如梦令 | 李清照 | autumn | 秋色黄粉绿轮转 |
| 6 | 宋 | 雨霖铃 | 柳永 | sorrow | 暗紫低吟 |
| 7 | 宋 | 青玉案·元夕 | 辛弃疾 | passion | 血红金光涌 |
| 8 | 宋 | 卜算子·咏梅 | 陆游 | snow | 冷白闪烁 |
| 9 | 宋 | 虞美人 | 李煜 | moonFull | 银月呼吸 |

> 如需扩充更多批次，在 `唐诗宋词朗读器.py` 顶部的 `POEM_BATCHES` 列表中追加新的批次即可，换一批按钮会自动循环到。

## 五、舵机角度映射

10 首诗词均分 0–180° 范围，每首间隔 20°：

| 序号 | 诗词 | 舵机角度 |
| --- | --- | --- |
| 0 | 静夜思 | 0° |
| 1 | 春晓 | 20° |
| 2 | 登鹳雀楼 | 40° |
| 3 | 望庐山瀑布 | 60° |
| 4 | 江雪 | 80° |
| 5 | 水调歌头 | 100° |
| 6 | 念奴娇·赤壁怀古 | 120° |
| 7 | 苏幕遮 | 140° |
| 8 | 声声慢 | 160° |
| 9 | 满江红 | 180° |

## 六、项目结构

```md
haoda_AIPad/
├── 唐诗宋词朗读器.py            # 主程序
├── 唐诗宋词朗读器项目说明文档.md  # 本文档
├── images/
│   └── 1.jpg                    # 背景图（1920×1080）
└── recordings/
    └── poem_0.wav ~ poem_9.wav  # TTS 合成音频缓存（自动生成）
```

## 七、环境要求

- 好搭AI派平台（ESP32 + Linux）
- 依赖（均平台自带）：
  - `ESP32` SDK —— 硬件控制
  - `pygame` —— 界面与音频
  - `voice_api` —— TTS 语音合成
  - `audio_player` —— 音频播放
- 字体文件（位于 `/home/cxdz/jupyter/assets/`）：
  - `PingFang_Regular.ttf` —— 常规文字
  - `PingFang_Bold.ttf` —— 粗体（标题、按钮）
  - `simkai.ttf` —— 楷体（诗词正文）
- 语音 AI 账号：需提供经过认证的好好搭搭账号用户名和密码

## 八、安装与运行

1. 将 `唐诗宋词朗读器.py` 上传至好搭AI派
2. 确保 `images/1.jpg` 背景图存在
3. 修改程序第 56-57 行的 `VOICE_USERNAME` / `VOICE_PASSWORD` 为你的好好搭搭账号
4. 运行程序：

```bash
python3 唐诗宋词朗读器.py
```

> 运行前请确保扩展板已连接，按键接 IO1/IO2，RGB 灯带接 IO3，舵机接 IO8，右下角开关拨到左侧。

## 九、界面布局

界面按左列表右详情布局（1920×1080）：

```markdown
y=  0   ┌──────────── 顶部标题区 (高140) ────────────┐
y= 28   │           唐诗宋词朗读器（标题 64pt）         │
y=100   │  IO1 切换  IO2 朗读  点击名称  朗读/停止/换一批/退出 │
y=140   └────────────────────────────────────────────┘
y=160   ┌──────────────┐  ┌─────────────────────────┐
        │ 左侧列表面板   │  │  右侧详情面板             │
        │ 唐诗 · 五首    │  │  【唐】静夜思             │
        │ [唐] 静夜思    │  │   李白                    │
        │ [唐] 春晓      │  │  ───────────────────     │
        │ [唐] 登鹳雀楼  │  │  床前明月光，疑是地上霜。  │
        │ [唐] 望庐山瀑布│  │  举头望明月，低头思故乡。  │
        │ [唐] 江雪      │  │                          │
        │ 宋词 · 五首    │  │  舵机角度: 0°  灯效: moonlight │
        │ [宋] 水调歌头  │  └─────────────────────────┘
        │ [宋] 念奴娇    │
        │ [宋] 苏幕遮    │
        │ [宋] 声声慢    │
        │ [宋] 满江红    │
y=940   └──────────────┘
y=940   ┌──────────── 底部按钮区 (高140) ────────────┐
y=980   │ 当前: 静夜思·李白  [朗读] [停止] [换一批]  就绪   [退出] │
y=1080  └────────────────────────────────────────────┘
```

## 十、技术说明

### 1. 硬件初始化（严格参照范例）

严格参照范例代码的初始化方式：

- `board = ESP32()` + `board.start()` + 异常抛出
- RGB 灯带：`board.ws2812Init((GPIO_IO_03), 4)` 初始化，`board.ws2812Write` 控制灯珠颜色
- 舵机：`board.servo((GPIO_IO_08), angle)` 控制角度
- 按键：`board.digitalRead` 读取数字值，上升沿触发

RGB 灯带与舵机初始化拆分为独立 `try-except` 块，避免一个外设异常导致另一个被连带跳过。

### 2. RGB 灯效单线程帧更新

灯效采用单线程按帧更新模式（非子线程），避免与主线程的 Pygame/按键扫描并发访问 ESP32 板冲突：

- `update_led_frame()`：每帧根据 `current_effect` 计算灯色并写入
- `start_led_effect(name)`：设置当前灯效名
- `stop_led_effect()`：关闭灯效并熄灭
- 主循环每次 tick 调用 `update_led_frame()`

复用范例 4.RGB灯 中的 `wheel()` 函数生成彩虹颜色。

### 3. 语音朗读与可中断播放

- `read_poem_async(idx)`：异步朗读，先检查本地缓存，无缓存则 TTS 合成
- `play_audio_interruptible(path)`：使用 `pygame.mixer.music` 非阻塞播放，主循环轮询 `stop_requested` 标志实现即时中断
- `stop_reading()`：设置停止标志、停止音乐、关闭灯效
- TTS 合成结果缓存到 `recordings/poem_X.wav`，二次朗读免合成

### 4. 按键边沿检测

参照范例 7.按键控制灯光 的边沿检测写法，记录上次状态，仅在上升沿（0→1）触发动作，避免长按重复触发。每次检测间隔由主循环 `clock.tick(30)` 控制。

### 5. 文字防溢出机制

- 列表项标题超宽时自动等比缩放
- 详情正文逐行检查宽度并缩放
- 正文超出面板底部时停止绘制并显示「……(正文过长，已截断)」
- 顶部标题区与底部按钮区加半透明黑色遮罩，保证文字在图片背景上可读

### 6. 字体方案

参考《好搭AI派可用字体列表.txt》，使用三种字体：

- `PingFang_Regular.ttf` —— 副标题、作者、状态文字
- `PingFang_Bold.ttf` —— 主标题、列表项标题、按钮、徽标
- `simkai.ttf` —— 诗词正文（楷体，契合古典韵味）

### 7. 批次换一批机制

- 诗词数据组织为 `POEM_BATCHES` 列表，每个元素是一批 5 唐 + 5 宋
- 全局变量 `POEMS` 指向当前批次的引用，`current_batch` 记录序号
- 点击「换一批」按钮触发 `on_refresh()`：
  1. 先调用 `stop_reading()` 停止当前朗读与灯效
  2. `current_batch` 取模自增，切换 `POEMS` 指向新批次
  3. 重建列表项矩形 `_build_item_rects()`
  4. 选中复位到第 0 首，舵机归零到 0°
- 朗读进行中禁用换一批按钮，避免音频状态混乱

## 十一、可配置项

在 `唐诗宋词朗读器.py` 顶部的配置区可调整：

| 常量 | 说明 | 默认值 |
| --- | --- | --- |
| `WIDTH` / `HEIGHT` | 窗口尺寸 | 1920 × 1080 |
| `BG_IMAGE` | 背景图片路径 | `images/1.jpg` |
| `BTN_NEXT_PIN` | 切换按键引脚 | `GPIO_IO_01`（IO1） |
| `BTN_READ_PIN` | 朗读按键引脚 | `GPIO_IO_02`（IO2） |
| `LED_PIN` | RGB 灯带引脚 | `GPIO_IO_03`（IO3） |
| `SERVO_PIN` | 舵机引脚 | `GPIO_IO_08`（IO8） |
| `LED_COUNT` | RGB 灯珠数量 | 4 |
| `VOICE_USERNAME` | 语音 AI 账号 | `username`（需替换） |
| `VOICE_PASSWORD` | 语音 AI 密码 | `password`（需替换） |
| `FONT_PATH` | 常规字体 | `PingFang_Regular.ttf` |
| `FONT_BOLD_PATH` | 粗体字体 | `PingFang_Bold.ttf` |
| `FONT_KAI_PATH` | 楷体字体 | `simkai.ttf` |
| `POEM_BATCHES` | 诗词数据池（多批次） | 2 批，每批 5 唐 + 5 宋 |
| `POEMS` | 当前显示的诗词列表 | 初始为 `POEM_BATCHES[0]` |

## 十二、运行诊断

运行时控制台输出关键信息，便于排查问题：

```markdown
RGB灯带初始化完成：IO3
舵机初始化完成：IO8，已归零到 0°
硬件初始化完成：按键(IO1/IO2) + RGB灯带(IO3) + 舵机(IO8)
语音 AI 认证成功
[舵机] 切换到第 0 首 -> IO8 角度 0°
[舵机] 切换到第 1 首 -> IO8 角度 20°
[换一批] 切换到第 2/2 批诗词
[舵机] 切换到第 0 首 -> IO8 角度 0°
```

若出现 `[舵机] 控制异常: ...`，异常信息会指明具体原因，常见排查方向：

- 扩展板开关是否拨到左侧
- 舵机线序是否正确（信号线接 IO8）
- 舵机供电是否正常

## 十三、常见问题与故障排查

### 1. 程序启动后界面不显示 / 闪退

| 可能原因 | 排查与解决 |
| --- | --- |
| 字体文件不存在 | 确认 `/home/cxdz/jupyter/assets/` 下有 `PingFang_Regular.ttf`、`PingFang_Bold.ttf`、`simkai.ttf`；若路径不同，修改程序顶部 `FONT_PATH` / `FONT_BOLD_PATH` / `FONT_KAI_PATH` |
| 背景图缺失 | 确认 `images/1.jpg` 存在；缺失时会自动回退到渐变背景，不影响运行 |
| pygame 初始化失败 | 终端运行查看报错；常见是未安装 pygame 或无显示环境 |

### 2. RGB 灯不亮

| 可能原因 | 排查与解决 |
| --- | --- |
| 引脚接错 | 确认灯带信号线接 **IO3**（不是 IO4 或其他） |
| 扩展板开关未拨 | 好搭AI派右下角开关拨到**左侧**启用外设接口 |
| 初始化失败 | 查看控制台是否输出 `RGB灯带初始化异常: ...` |
| 未触发朗读 | 灯效在朗读时才启动，点击「朗读」按钮或按 IO2 按键 |
| 灯珠数量不符 | 修改程序顶部 `LED_COUNT = 4` 为实际灯珠数 |

### 3. 舵机不动

| 可能原因 | 排查与解决 |
| --- | --- |
| 引脚接错 | 确认舵机信号线接 **IO8** |
| 初始化被跳过 | 之前 RGB 初始化异常会连带跳过舵机初始化，新版已拆分为独立 try-except，查看控制台是否输出 `舵机初始化完成：IO8` |
| 角度无变化 | 查看控制台 `[舵机] 切换到第 X 首 -> IO8 角度 Y°`，无输出则代码未执行到 |
| 供电不足 | 舵机需外接 5V 供电，仅靠 IO 口电流不足 |
| 线序错误 | 灰色舵机线序：信号(黄/白)、VCC(红)、GND(黑/棕) |

### 4. 朗读无声音

| 可能原因 | 排查与解决 |
| --- | --- |
| 账号未配置 | 修改程序顶部 `VOICE_USERNAME` / `VOICE_PASSWORD` 为你的好好搭搭账号 |
| 账号未认证 | 好好搭搭账号需经过认证才能使用语音 AI |
| 网络问题 | TTS 合成需联网，确认设备网络正常 |
| 音量过低 | 检查系统音量与好搭AI派扬声器 |
| 合成失败 | 查看控制台是否输出 `语音合成失败` 或 `朗读异常: ...` |
| 音频缓存损坏 | 删除 `recordings/` 目录下的 wav 文件重新合成 |

### 5. 按键无响应

| 可能原因 | 排查与解决 |
| --- | --- |
| 引脚接错 | IO1=切换按键，IO2=朗读按键，确认对应关系 |
| 开关未拨 | 扩展板右下角开关拨到左侧 |
| 边沿检测失败 | 查看控制台是否有 `digitalRead` 异常输出 |
| 长按无效 | 程序为上升沿触发（松开变按下瞬间），需松开后再按 |

### 6. 「换一批」按钮点不动

| 可能原因 | 排查与解决 |
| --- | --- |
| 朗读进行中 | 朗读时按钮自动禁用（变灰），先点「停止」结束朗读 |
| 按钮被遮挡 | 确认窗口尺寸为 1920×1080，未缩小导致按钮被裁切 |
| 鼠标坐标偏移 | 全屏模式下若系统缩放非 100%，可能坐标偏移，建议窗口模式运行 |

### 7. 文字显示乱码或方块

| 可能原因 | 排查与解决 |
| --- | --- |
| 字体不支持中文 | 确认使用的是 `PingFang` 和 `simkai`，不要换成英文字体 |
| 字体路径错误 | 检查 `FONT_PATH` 等常量指向的路径实际存在 |

### 8. 切换诗词后舵机角度错误

| 可能原因 | 排查与解决 |
| --- | --- |
| 批次切换后未复位 | 正常现象，换批后会自动归零到第 0 首的 0° |
| 诗词数量不是 10 | 若修改了每批诗词数量，角度计算公式 `idx * 180 / (len(POEMS) - 1)` 会自动适配 |

## 十四、版本历史

| 版本 | 日期 | 主要变更 |
| --- | --- | --- |
| 1.0 | 2026-07 | 初始版本：10 首诗词、朗读、RGB 灯效、舵机指示 |
| 1.1 | 2026-07 | 1920×1080 布局 + 背景图 + 文字防溢出 |
| 1.2 | 2026-07 | 增加停止按钮、字体适配可用字体列表、作者移到名称后 |
| 1.3 | 2026-07 | 修正引脚映射（IO3 灯带、IO8 舵机）、灯效改单线程帧更新 |
| 1.4 | 2026-07 | 新增「换一批」按钮，支持多批次诗词切换 |
